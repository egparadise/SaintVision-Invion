---
doc_id: "STATUS-CLAUDE-VERIFICATION-MAP-001"
title: "Claude 영역 검증 상태 지도 (2026-09-19)"
version: "1.3.0"
status: "review"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-22T17:20:00+09:00"
source_of_truth: "Git"
tags: ["saintvision", "status-map", "verification", "unverified", "vf-cl", "vf-cx"]
---

# Claude 영역 검증 상태 지도 (2026-09-19)

이번 세션에서 반복적으로 드러난 것은 **"통과로 기록됐으나 실은 도달조차 못 한" 항목**이었다(migration DSN, channel CAS fixture, image lane 보안 단언). 그래서 지금 시점의 정확한 상태를 한 곳에 남긴다. 세 범주로 구분한다: (1) 실제 검증 완료, (2) 미검증이나 미검증으로 정확히 기록됨, (3) 외부 조치 대기. 각 항목의 **검증 방식·증거·SHA**와, 미검증 항목의 **재실행 조건·명령**을 명시한다.

정본은 Git이다. "검증 완료"는 실제로 실행·도달·통과했고 증거가 있는 것만이며, "reviewed sound"(코드 경로 독립 검토)와 "ran and passed"(실행 통과)를 구분해 표기한다.

---

## 1. 실제 검증 완료 (도달·통과, 증거 있음)

| 항목 | 검증 방식 / 증거 | SHA |
|---|---|---|
| docker_diag 분류기·재시도·타입 일관성·마스킹 | **실행 통과**: 나 24 passed(실 docker) + 사용자 독립 24 passed + Codex 15/22. NTSTATUS 분류, timeout 분류, loader-only allowlist 재시도 안전, 모드 일치 반환 타입, URL+libpq+인용 마스킹 | 71fc9f5·56aa7cb·816346c |
| R2-01 동시성 안전 prune | **실행 통과**(실 docker): exited-only·age gate·비강제 rm, running 동시 컨테이너 생존 2종 | 07bae29·911aed8 |
| R5-01 결과기반 cleanup_owned | **실행 통과**: 본문 실패 생존·전 자원 시도·BaseException 미삼킴 등 6종 | d59b8a6 |
| R5-02 격리 source manifest | **실행 통과**: docker_diag.py 매니페스트 포함, AST 회귀(non-vacuous, 의존 탐지 음성 확인) | d59b8a6 |
| docker 부재 동작 | **실행 통과**: PATH에서 docker 제거→22 passed/2 skipped/0 failed(0.16s), 실 docker 시험 깔끔 skip, 가드 불필요 | c9e6ddf |
| e89a415 channel CAS (실 PG) | **실행 통과는 사용자/Codex**(사용자 75 passed / Codex 79, 실 PG16), **Claude는 reviewed sound**(코드+계약+`channel_monotonic` SQL 대조). disable/만료+version 증가 시나리오가 실제 CAS에 도달 | e89a415 (검토 doc: 원격권한결속과CAS_fixture독립검토) |
| model_remote / model_source·model_runtime 결속 | **Claude reviewed sound**(코드 경로·계약, finding 없음); 실행은 Codex PG 증거 | 5e4d6ae·8c347b7 (검토 doc 2건) |
| e2908a5 registry 권한 결속 | **Claude reviewed sound**(코드 경로·계약·lifecycle, finding 없음); 실행은 Codex PG 증거 | e2908a5 (검토 doc: registry권한결속_독립검토) |
| **Image lane 보안 단언 3건**(writable·business·unreadable) | **실 image lane 통과**(4차 postod-quiet-determination). writable·business는 이전 **host-init 계층**에서 미도달이던 것, unreadable은 이전 **timeout 계층**에서 미도달이던 별도 건(케이스별 범주 상이). 이번 실행 host-init 0건 관측. **단, harness가 이전과 다르고 변수가 여럿이라 엄밀한 통제 실험 아님**(§2.1) | postod-quiet-determination |
| **물리 PITR 리허설 + 엄격 게이트** | **실행 통과**: probe postgres에서 물리 base backup→목표시각 복원→A존재·B부재 확인(정상 PASS/exit0); **음성-회귀 5종**(before/after-insert·basebackup·restore-start·promotion) 각기 다른 게이트로 FAIL/exit1; R2-02 owner 라벨 cleanup으로 6회 잔재 0. 증거 `Evidence/pitr-rehearsal/` | a681da3·09db057·0da140e |
| **VB-FIX-01/02 fixture 실패 경계** | **stub/주입-실패 시험 11 passed**(나 실측 + 사용자 독립 일치). **실 PG 아님** — `test_db_provision`은 주입 step 콜러블, `test_db_login_teardown`은 fake engine/owner를 쓰는 대역 시험이라 `INV_TEST_ADMIN_DSN` 유무와 무관. 부분 할당 후 소유권 추적 정리(5 대조군)·dispose와 DROP 동시 실패 시 ExceptionGroup로 둘 다 보존. VB-FIX-01 Codex 해소 판정 | a5401a7·c754933 |
| 11e9f44 registry policy config / bb4f4cb 검증도구 false-green 방지 | **Claude reviewed sound**(코드 경로, finding 없음); operator-only·엄격 검증·fail-closed·무유출 / 깨진 factory·forged-Bearer·빈 측정이 green 미통과 | 7e3de2a(11e9f44 doc), bb4f4cb(verdict) |
| 착지 통합 회귀 | **사용자 실행·보고**: tip 1eaf285에서 1179 passed/489 skipped/2 deselected/0 failed(75초). R2~R5 착지(b378785) | b378785·3afe227·1eaf285 |

주: 표의 "reviewed sound"는 내가 **코드 경로로 독립 검토**해 결함을 못 찾았다는 뜻이며, 해당 스위트의 **런타임 통과는 사용자/Codex가 실 PG에서** 수행했다. 나는 postgres+무거운 의존 부재로 그 스위트를 직접 실행하지 않았다(정직 기록).

---

## 2. 미검증 — 그러나 미검증으로 정확히 기록됨 (제품 결함 아님, 도달 못 함)

### 2.1 Image lane 보안 단언 — 4차 판별(2026-09-19, postod-quiet-determination)로 갱신

**환경 조치 후 재판별**(harness f4b3f73, digest b93b5ef1f944, 조용한 조건, 증거 `.work/postod-quiet-determination-tests.{xml,json}`): **4 passed / 2 failed / 2 skipped**(사용자 실측; Codex 증거 대조로 아래 정정 반영).

확정적으로 말할 수 있는 것(과잉 해석 배제):
- **이번 실행 NTSTATUS 0xC0000142 = 0건**(증거 XML grep: 3221225794·C0000142 모두 0회). 이전 실행들의 host-init 실패가 이번 실행에는 관측되지 않았다.
- **핸들 실측**: 641,442 → 2,421(OneDrive 재시작 후). 
- **인과 미확정 — 엄밀한 통제 실험 아님**: 이번 4차는 harness(f4b3f73)가 이전 실행과 다르고, 변경된 변수가 최소 셋(OneDrive 재시작·잔재 컨테이너 2개 제거·조용한 구간 확보)이라 **한 변수만 바꾼 통제 실험이 아니다.** 따라서 host-init 소거를 OneDrive 단일 원인으로 귀속할 수 없다 — 확정은 "이번 실행 host-init 0건"과 "핸들 641,442→2,421" 두 관측뿐이다.
- **R5-01에 대해 이번 실행이 보여준 범위**: timeout 케이스가 `host condition prevented verification (unverified, not failed)` + `docker operation timed out 90s`로 **skip**된다는 것 — 즉 timeout이 실패가 아니라 이유 있는 미검증으로 기록됨. **본문 실패+cleanup 실패가 겹치는 보존 시나리오까지 이번 실행이 검증한 것은 아니다**(그 근거는 내 기존 회귀 `tests/test_vf_docker.py`이지 이번 실행이 아니다).

| 케이스 | 검증하려던 단언 | b5f770a(3차) 미도달 원인 | postod(4차) |
|---|---|---|---|
| healthy | 정상 config → HTTP·nonroot·401/403 | (통과) | passed |
| writable | api.json group-writable → 거부 | **host-init** (NTSTATUS) | **passed ✅ 신규(host-init 계층)** |
| business | business config → storage/replica/project 401·200 계약 | **host-init** | **passed ✅ 신규(host-init 계층)** |
| unreadable | api.json 소유자-불가독 → 기동 거부 | **TimeoutExpired** | **passed ✅ 신규(timeout 계층·별도)** |
| workspace | nonroot·workspaceAdmission·재시작 지속 | (통과) | skipped(timeout, 미검증·변동) |
| public-signing-key | signer.pem world-readable → 거부 | host-init | **failed: daemon i/o timeout**(환경/부하, 제품 아님) |
| business-workspace | business + persistent workspace 재시작 지속 | host-init | **failed: daemon i/o timeout**(환경/부하, 제품 아님) |
| **business-kernel-role** | **kernel DB role을 INV_BUSINESS_DSN으로 쓸 때 거부(DB role 거부 단언)** | host-init | **skipped(timeout) — 미검증 유지** |

**판정 갱신(케이스별)**: **신규 검증 3건** — writable·business(이전 **host-init 계층** 미도달), unreadable(이전 **timeout 계층** 미도달·별도) → §1 승격. **범주 변경 2건** — public-signing-key·business-workspace = daemon i/o timeout(CLI 실행됨, daemon 응답; 환경/부하 조건이지 제품 보안 단언 실패 아님 — 제품 결함 여전히 0). **미검증 유지 1건** — business-kernel-role. workspace는 3차 pass→4차 skip(부하 변동). (b5f770a host-init 5건 = writable·public-signing-key·business·business-workspace·business-kernel-role; unreadable만 timeout — 사용자 재파싱 정정.)

- **daemon i/o timeout 범주(관측)**: host-init(프로세스 미실행)과 달리 CLI가 실행돼 daemon이 i/o timeout으로 응답 — 여전히 **환경/부하 조건이지 제품 단언 실패가 아니다**(증거 stderr에 falsifiable). 현재 `is_infrastructure_failure`는 host-init+TimeoutExpired만 미검증(skip)으로 보고 daemon-error는 fail 처리하므로 이 2건이 **failed로 기록**된다. **제안(미구현)**: daemon i/o-timeout 계열(`i/o timeout`/`context deadline exceeded` 등 timeout류만 좁게)을 인프라(미검증)로 분류해 skip 처리하면 "환경이 실패로 위장" 잔여를 닫는다 — 실 docker 오류를 가리지 않도록 좁게. 사용자/Codex 판단 대기(분류기는 Codex 검토 접점).

### 2.2 잔여 판별 조건 (business-kernel-role)

- **관측 사실(확정)**: 핸들 641,442→2,421(265배 감소, 재시작 후), 가용 RAM 896→938MB(거의 불변), 이번 실행 host-init 0건. business-kernel-role은 여전히 timeout으로 미도달.
- **해석(hedged, 미확정)**: host-init 0건과 핸들 감소가 같은 실행에서 관측됐으나 변수가 여럿이라 인과는 미확정. business-kernel-role이 여전히 timeout인 것은 **RAM이 거의 불변인 점과 정합적**이라 잔여 요인이 메모리일 **가능성**을 시사(확정 아님).
- **선행 조건(갱신)**: 잔여 판별에는 **메모리 여유** 확보가 관건으로 보임 — 동시 컨테이너/프로세스 메모리 압박 완화. 사용자 환경 몫.
- **재실행 대상/명령**: 3차와 동일(`run_vf_security_tests.py` 또는 `docker_host` lane의 `test_server_container.py`). timeout이면 R5-01이 미검증(skip)으로 정확히 기록.
- **판별 기준**: business-kernel-role이 보안 단언(잘못된 kernel DB role이 INV_BUSINESS_DSN으로 쓰일 때 DB가 거부)에 **도달**해 pass/fail이 나오면 판별. 도달 못 하면 여전히 미검증.
- **착지와의 관계**: 미검증은 **호스트 조건 문제이지 브랜치 착지 blocker가 아니다**(착지 완료, 이 판별은 운영 인수 트랙).

### 2.3 이번 세션에서 해소된 "도달 못 했던" 선례 (패턴 기록)

지금은 미검증이 아니지만, 같은 패턴이었고 해소됐다:
- **channel CAS fixture** (e89a415): fixture가 version 증가 없이 channel을 UPDATE→`channel_monotonic`(ERRCODE 23514) 위반으로 setup 에러→제품 단언 미도달(+DSN 부재 시 skip). 수정으로 disable/만료+version 증가를 주입해 CAS에 실제 도달. **해소**(사용자 75 passed/Codex 79, 실 PG).
- **migration DSN** (Codex 영역): 환경 부재가 실패로 위장하던 건. Codex 조사·해소.
- **docker_diag timeout 반환 타입 불일치**(816346c 이전): timeout 경로가 str 반환→bytes 호출자 `.decode()` AttributeError→best-effort 래퍼 은폐. **해소**(타입 일관성 고정 + 래퍼를 OSError만 관용).

---

## 3. 외부 조치 대기

| 대기 항목 | 조건 / 담당 |
|---|---|
| Image lane 6건 + business-kernel-role 판별 | 프로세스 생성 여유 있는 호스트(§2.2). 담당 사용자/CI 환경 |
| 3파일 배치/CI 경계 (해소됨) | Codex 3afe227에서 `docker_host` marker로 격리 lane 정의 완료. 유지 |
| Codex 후속 산출물 독립 검토 | Codex의 추가 산출물이 나오면 Claude 독립 검토(현재 미검토 코드 대상 없음) |
| **내 브랜치 착지 backlog** | `git cherry` 기준(§4): 등가 착지 3(VB-FIX·11e9f44 리뷰 = 내용 착지 완료), 내용 차이 8(PITR + 상태 docs). 남은 착지 blocker는 **PITR-R2-02**(본 커밋 해소). Codex 재검토·병합 몫 |
| 운영 인수(별도 트랙) | AC-12 RPO: `archive_mode=off`(prod·workspace compose 실측)라 PITR 불가, RPO=백업 주기 → **미달성**(CL-07). **준비 완료**: opt-in `docker-compose.pitr.yml` + 물리 PITR 리허설 실증(엄격 게이트·음성회귀) + 영향([[2026-09-19_Claude_PITR_변경안과영향]] v1.4.0). 활성·아카이브 대상(별도 장애 도메인)·복구 drill은 사용자/운영. object 저장소·2대 실장비 PTY/Git·GPU는 원격 설치/운영 결정 선행 |

---

## 4. 착지 준비 상태 (Codex 판단 근거) — git cherry 기준으로 정정

**정정(2026-09-19)**: 앞서 "미착지 10커밋"이라 적었는데 이는 **ancestry(선형 계보) 기준**이라 이미 내용 착지된 것을 혼동했다. `git cherry origin/integration/all-agents-unified HEAD` 기준으로 구분해야 한다(integration `120a8a0` vs HEAD `2ed3d65`):
- **ancestry 11** (계보상 integration에 없는 커밋 수).
- **등가 patch 이미 착지 3(`-`)**: `7e3de2a`(11e9f44 sound 판정), `a5401a7`(VB-FIX-01/02), `c754933`(VB-FIX-02 잔여) — **내용이 이미 integration에 착지**했다(Codex도 VB-FIX·11e9f44 착지 완료로 통보). 계보만 남았을 뿐 재검토 대상 아님.
- **내용 차이 8(`+`)**: 나머지(PITR 문서·override·수정 + 상태 지도 docs) — 실질 대기분.

| 항목 | 커밋 | 상태 |
|---|---|---|
| VB-FIX-01/02 + 잔여 | a5401a7·c754933 (`-`) | **내용 착지 완료** (Codex 해소, 재검토 목록에서 제외) |
| 11e9f44 registry policy sound 판정 | 7e3de2a (`-`) | **문서 착지 완료** |
| PITR R1-01/02/03 | a681da3 (`+`) | **Codex 해소 판정** (내용 차이는 남았으나 finding 없음) |
| PITR-R2-01 엄격 게이트·음성회귀 | 09db057·0da140e (`+`) | Codex 선행조건 보강·물리 실행 보고 **수신** |
| **PITR-R2-02 owner/retry docker** | 이번 커밋 (`+`) | **본 커밋으로 해소 제출** (nonce·조회/삭제/부재 분리·확정 전 미재시도·13 음성대조군) → Codex 재검토 |
| PITR 문서·override·상태 지도 | dd6a95d·d052792·8c72fbf·1ff4324·2ed3d65 (`+`) | 위 PITR와 함께 재검토 |

- **남은 착지 blocker**: **PITR-R2-02**(본 커밋으로 해소 제출) — 이것이 전체 PITR 착지를 막던 마지막 항목. Tier-A single-writer/fsync/same-host 및 R1-04 문구 잔여는 준비안 review 범위(아래 §영향/override에 반영).
- **착지 blocker 아님(운영 인수 트랙)**: business-kernel-role 미검증(호스트 메모리), Tier A/B 활성(운영 결정).

---

## 요약

- **검증 완료**(실행 통과): docker_diag 전반·R2-01·R5-01/02·docker 부재(나 실측); **물리 PITR 리허설+엄격 게이트+음성회귀 5종**(나 실측); **VB-FIX-01/02 fixture**(나+사용자 stub 시험 11 passed, **실 PG 아님**); image lane 신규 3건(실 image lane); e89a415 CAS(사용자/Codex **실 PG16**); 착지 회귀(사용자 보고).
- **reviewed sound**(코드 경로): model_remote·model_source/runtime·CAS·e2908a5·**11e9f44·bb4f4cb** — finding 0.
- **미검증(정확히 기록)**: image lane 중 **business-kernel-role 1건**만 미검증 유지(호스트 메모리 압박으로 미도달, 제품 결함 0); public-signing-key·business-workspace는 daemon i/o timeout(환경). 신규 3건은 §1로 승격. 인과는 통제 실험 아님(§2.1).
- **착지 backlog**(git cherry, §4): 등가 착지 3(VB-FIX·11e9f44 리뷰 내용 착지 완료), 내용 차이 8(PITR·상태 docs). 남은 blocker는 **PITR-R2-02**(본 커밋 해소 제출).
- **외부 대기**: business-kernel-role 재판별(메모리 여유 호스트), Codex 후속 산출물, 운영 인수(RPO/Tier 활성).

---

## 5. 새 PC 실 PostgreSQL 전수 실행 (2026-09-22, SHA d01c931a) — not_run이 ran으로 바뀐 자리

측정: Claude 전용 detached 워크트리 `D:\Project\sv-measure-claude`, working_tree_clean=YES, CI backend.yml 스코프(node-dependent 24파일 + 브라우저/컨테이너 6파일 ignore), 실 PG 16.15(`saintvision-invion-dev-pg`, disposable DB만). 전문·명령·exit·소요: [[2026-09-22_17-02-56_KST_REALPG-FULLRUN_Claude_실측]]. provenance: `Evidence/claude-newpc-realpg-d01c931a/provenance.json`.

| 항목 | 새 PC 기준선(13:00, DSN 없음) | 실 PG 전수(16:24~16:56) | 해석 |
|---|---|---|---|
| collected | 2667 | 2667 | 시험 집합 동일 |
| passed | 1656 | **2523** | PG 게이트가 깨어나 통과 |
| skipped | 1011 (전부 DSN 부재) | **142** | 남은 142는 전부 환경 게이트(아래) |
| failed | 0 | **2** | F1 환경(동시 실행 경합, 단독 통과) · F2 시험 결함(`.work` 미생성, origin `51d53b7f`로 수정됨) — 둘 다 귀속 Codex |

남은 skip 142 사유: Linux 파일 백엔드 48 · Linux 자격증명 20 · CX01_CONTAINER 19 · Linux private-dir 15 · Linux publication 13 · 명시 PG 이미지 13 · 후보 이미지+컨테이너 DB 8 · pinned 이미지 2 · 측정트리 파생물 부재 1 · antigravity 1 · symlink 권한 1 · 브라우저 smoke 1. 이전 절차서 §4 미설정(CX01·Linux 스토리지·이미지/브라우저 opt-in)과 전부 일치, 늘어난 skip 없음.

같은 SHA를 다른 Claude 세션이 core.yml 스코프로 독립 측정(2549/2/408, `c5042322`)해 **실패 2건의 이름·판정이 일치** — 답 아는 자리 대조로 확정.

**§1 승격(실행 통과, 실 PG, 나 실측)**: 이전에 "reviewed sound / 실행은 사용자·Codex"였던 PG 스위트(channel CAS·registry 권한 결속·model_remote·discovery 자격증명 integration 등 postgres-마커 전부)가 이 전수에서 **내 손으로 도달·통과**했다(위 2건 제외 0 실패). 개별 카드 증거(S02/S03/S09/S10/S04-05/S07)는 별도 재실행으로 §6에 기록한다.

## 6. 새 PC 실 PG 인수 증거 재실행 (2026-09-22, SHA d01c931a) — 카드별 재사용 증거

같은 측정 트리·같은 SHA에서 S02·S03·S09·S10·S04/05 경합·S07 복구 6세트를 순차 재실행: **762 passed / 88 skipped / 0 failed, 6세트 전부 exit 0**(9m21s). skip 88 = Linux 파일 백엔드 48 · Linux 사설 스토리지 21 · CX01 19 (옛 PC 증거와 동일, 늘어난 skip 0). S03만 192→201(+9, 그 사이 착지한 시험). provenance `Evidence/claude-newpc-realpg-d01c931a/evidence-provenance.json`, junit `.work/evidence-claude-d01c931a/evidence/`. 전문: [[2026-09-22_17-12-01_KST_REALPG-EVIDENCE_Claude_인수증거]]. 카드 판정은 Codex(self-close 아님).

**PITR readiness 실측(dev-pg, disposable DB `pitr_probe_*` 생성→읽기→삭제, 컨테이너 무변경)**: verdict **absent**(archive_mode=off, archive_command disabled, wal_level=replica), `--require-pitr` exit 1 — runbook §4 판정 그대로. 물리 리허설 probe 결과는 별도 History(작업 3).

**물리 PITR 리허설 새 PC 재현(owned probe, dev-pg 미접촉)**: 정상 PASS exit 0 · 결함주입 5종(before/after-insert·basebackup·restore-start·promotion) 각기 다른 게이트로 FAIL exit 1 · 6회 잔재 probe 0 — 옛 PC 증거와 같은 매트릭스·같은 판정(§1 항목 재확인). 보관 주기 결정 7일(파일럿, 운영 재결정). 증거 `Evidence/pitr-rehearsal/2026-09-22_newpc_physical-pitr-rehearsal.txt`, 전문 [[2026-09-22_17-14-47_KST_PITR-RUNBOOK_Claude_실측]].

## 7. 카드 4 — node-dependent 24파일 not_run 확정 + Docker-only skip 23건 실행 전환 (2026-09-22, SHA 3d1892c0)

- **24파일(`tools/node_dependent_tests.py`)은 이 Windows 호스트에서 정직한 not_run**: `inv-node`가 Unix Docker 소켓만 허용(`runtime/docker.go` NewDocker, NODE-0021)하고 호스트 엔드포인트는 npipe뿐·사용자 WSL 배포판 없음. 검증 자리 = hosted Core CI(이미 0 실패 레인 다수). 이 PC에서 돌리려면 WSL2 Ubuntu + Docker 통합(사용자 결정). 전문 [[2026-09-22_17-52-00_KST_NODE-DEPENDENT-NOTRUN_Claude_실측]].
- **skip 142 중 Docker만으로 가능한 23건 → 전부 passed**: role guard 13(`INV_TEST_ROLE_GUARD_IMAGE=postgres:16`) · 후보 서버 컨테이너 8(`deploy/Dockerfile.backend` 빌드 `sha256:0886bab9…`, DB 주소 172.17.0.2) · config volume 2. 잔재 컨테이너·볼륨 0, 이미지는 기록 후 제거. provenance `Evidence/claude-newpc-realpg-d01c931a/card4-docker-lanes-provenance.json`.
- **§2.1/§2.2/§3 갱신 — business-kernel-role 미검증 해소**: 옛 PC에서 4차례 미도달(host-init·timeout 계층)이던 "kernel DB role을 INV_BUSINESS_DSN으로 쓰면 DB가 거부" 단언이 새 PC 실 이미지 레인에서 **도달·통과**. public-signing-key·business-workspace(옛 daemon i/o timeout)도 통과. 이미지 레인 8/8, 제품 보안 단언 실패 0. §3 "Image lane 6건 + business-kernel-role 판별" 대기 항목은 **닫힘**(메모리 여유 호스트가 조건이었음이 확인됨: 2.3GB 여유·동시 컨테이너 0).
- 남는 skip은 Linux 전용(파일 백엔드 48·자격증명 20·private-dir 15·publication 13)·CX01 19·브라우저 1·symlink 권한 1·도구 부재 2 — 이 PC에서 더 전환할 Docker-only 항목 없음.

## 8. 새 PC 첫날 종합 (2026-09-22 18:40 KST, tip 9b6b967b) — hosted CI 첫 완주와 남은 not_run 한 표

- **hosted Core Build 첫 완주 `3d1892c0`(run 35706465645, 30 step 전부 success)**: Node/Python/LAN/API 이미지 빌드 → workspace 복구·containment·business handoff·**두 실 Node shard replacement**·LAN installer 인수 → **full pytest 2948 passed / 58 skipped / 0 failed**, skip ratchet 일치 → `go test ./...`·`-race`·contracts-ts tsc. 이 PC에서 not_run인 node-dependent 24파일이 **hosted에서 실행·통과**했다(검증 자리 확정).
- **hosted Backend 완주 3회**(`7dce1737`·`3d1892c0`·`9cfd8a8e`): 3.12·3.14 각 **2668 / 45 / 0**, declared ratchet 일치. 취소 다수는 concurrency pending 대체(코드 실패 0). Browser Acceptance는 전 SHA 5/1 failure — 케이스 `test_desktop_browser::test_browser_real_catalogue_owner_scope_and_revocation`(Codex F-B 해소로 특정, Gemini 수정 진행).
- **전환 표(옛 PC 기준선 → 오늘)**: CI-스코프 1650/1039skip → 로컬 실 PG **2523/142/2** + hosted **2668/45/0** · 인수 6세트 **762/88/0** · Docker-only **23 passed** · Go 0 → 로컬 build/vet 0 + hosted test success · PITR 리허설 재현 · Codex 착지 10건 sound · 자원 사용량 라우트 서빙 · S02-DB/S03-DB 인계 패키지.
- **남은 not_run**: 24파일 로컬(WSL2 사용자 결정) · Linux 전용 skip(hosted에서 실행됨) · CX01 19 · 물리 노드·실 IdP(외부) · 브라우저 1(Gemini) · symlink·CLI 도구. 전문: [[2026-09-22_18-40-00_KST_HOSTED-CI-TRIAGE_Claude_새PC종합]].

## 9. VF-CL 재고 + 아카이브 보관 정리 도구 (2026-09-22 19:00 KST, 카드 7)

VF-CL-01~05 재고표(착지 SHA·검증 수준·미완·외부 대기)는 [[2026-09-22_19-00-00_KST_VF-CL_재고_및_아카이브보관정리_Claude]]. 무-블록 항목 구현: `tools/pitr_archive_retention.py`(7일 보관 정리 계획, 기본 dry-run·`--apply` 명시, 불변식: 최신 backup 보존·보존 backup 최소 START 이후 WAL 절대 보존·history/partial/타 timeline 보존·backup 없으면 무삭제) + `tests/test_pitr_archive_retention.py` **9 passed**(무작위 300 아카이브 속성 시험 포함; 첫 구현의 경계 결함을 시험이 잡아 수정). runbook §4 정합. 남은 VF-CL 미완은 Codex 계약 결속 확인(model-kind URI·licensePolicy)과 물리/운영 게이트.

## 10. VF-CL-02 model-kind URI → ModelManifest shards 확장 (2026-09-22 19:40 KST, 카드 8)

계약 무변경. `services/resolver.py::resolve_model`(join key models.name+model_versions.version, 주입식 `manifest_reader`, shard→replica.locationId→`public.data_locations`(RLS·reader scope 동일)→`data_replicas` ready 대조, 실패 모드 reason 정직 보고, reader 없으면 `shards=None`·`unavailable`). `tests/test_model_uri_resolver.py` **7 passed** + 기존 resolver 25 passed(실 PG). 운영 reader 결속(a) grant/(b) 커널 HTTP/(c) 커널 라우트는 Codex 결정 대기. 전문 [[2026-09-22_19-20-00_KST_VF-CL-02_model-kind_URI_shards_설계_Claude]].
