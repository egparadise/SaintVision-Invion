---
doc_id: "HIST-VF-CL-CX01-DONE-001"
title: "VF-CL CX-01 병합 후 (a)(b)(c) 완료 (Claude)"
version: "1.0.0"
status: "review"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-18T12:30:00+09:00"
base_sha: "fe4c04c"
branch: "agent/claude/vf-cl-cx01"
task: "VF-CL"
source_of_truth: "Git"
tags: ["saintvision", "vf-cl", "cx-01", "post-merge", "done"]
---

# VF-CL CX-01 병합 후 (a)(b)(c) 완료 (Claude)

CX-01(`fe4c04c` "Merge CX-01 canonical control plane")이 커널 model_manifest·migration 0039/0043·진화 deps.py(project_id)·`/storage/resolve`를 integration에 착지시켰다. 예약한 (a)(b)(c)를 병합 base에서 자동 실행했다. 별도 worktree `agent/claude/vf-cl-cx01`(integration에서 분기), throwaway `postgres:16`(`INV_TEST_ADMIN_DSN`).

## 실행 결과 (커밋 `9e5350f`)

### (a) 병합 base 전체 재검증 — 통과
- 내 VF-CL 스위트가 병합 base에서 **74 passed**(storage_catalog·uri_resolver·replica_repair·model_registry·storage_api·pitr_readiness).
- 병합 base는 conftest를 `INV_TEST_ADMIN_DSN`(per-run DB 생성)으로 바꿨다. 내 시험은 Codex의 진화 conftest·deps.py·api·커널과 **호환**.

### (b) 미채택 반영 — 완료
- `/storage/resolve` owner-scoping: Codex가 `tests/integration/test_storage_catalog_api.py`로 **이미 더 철저히** 시험(실 JWT: owner resolve·owner-scoped list·비소유자 미공개·revocation·forged 거부·read 라우트 mutation 금지). 내가 추가할 필요 없음.
- pin된-node-loss: migration 0043이 `only_ready_replicas_pin`→`retained_replicas_pin(state in ready,stale)`로 교체해 이제 DB에 제약이 있다. 복원한 시험이 `mark_node_replicas_unavailable`가 pin된 ready replica를 stale로 표시하며 **pin 보존**함을 확인. replica_repair **9 passed**.

### (c) 커널 license read-through E2E (옵션 A) — 통과
- 커밋된 커널 매니페스트가 `licensePolicy`·`classification`을 담고 `ModelManifestStore.get`으로 **(model_id, version) 키로 서빙**됨을 커널 commit fixture 재사용으로 검증. app 끝(model_version이 같은 키를 지니고 license 컬럼 없음)은 `test_model_registry`의 join-key 시험이 고정. `tests/integration/test_model_license_readthrough.py` **1 passed**.

### 최종 통합
- (a)+(b)+(c) 합산 **76 passed** on 병합 base.

## VF-CL 트랙 상태 (CX-01 후)

- `implemented`·`locally_verified`·`independently_reviewed`(상호): VF-CL-01~05 완결. finding #1 철회·finding #2 옵션 A 확정·license read-through 검증까지.
- 병합 base에서 내 작업 + 커널이 정합 확인. 내 서비스/모델은 Codex와 byte 동일(하드닝 채택 동기).

## 남은 것 — 외부 gate뿐 (내 영역 아님)

- **VF-CX-05 5-node acceptance** — 운영자 실장비.
- **CI 결제** — 사용자. (모든 검증은 로컬 clean-room, CI-equivalent 아님.)
- **backup 매체/오프사이트** — 운영자. (PITR readiness 도구·runbook은 완료.)

## 다음 첫 행동/담당

- Claude: 새 Codex 산출물이 내 영역을 건드리면 독립 검토·채택(리듬 유지). 5-node·CI·backup 매체가 열리면 해당 인수 검증.
- Codex: `agent/claude/vf-cl-cx01`의 (a)(b)(c) 추가분 검토, integration 반영.
