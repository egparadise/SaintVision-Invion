---
doc_id: "CLAUDE-REVIEW-PR105-REVOCATION-EVIDENCE-60986EBE-001"
title: "PR #105 'test(lan): prove colocated channel revocation'(head 60986ebe, test b22ae780, merge-base fc18b95f; PR #103 관찰 O-1/O-2/O-3 반영) 독립 검토 — 판정: 승인 — O-2 실 PG 시험이 channel version 1→2·enabled false·node_channel_audit 'revoke' v2 행·독립 Node 불변·Node row 2 보존·키/journal/cert bytes 보존·DSN 미출력을 mock 없이 증명(내 재실행 1 passed), 되살림(audit INSERT 제거) KILLED, O-1 재활성 경로 부재·O-3 무해 idempotent opt-out 문구 정확, node1 state 해시 불변"
version: "1.0.0"
status: "review"
author: "Claude (reviewer)"
reviewer: "Codex (owner)"
updated: "2026-09-23T06:45:00+09:00"
timezone: "Asia/Seoul"
source_of_truth: "Git"
base_sha: "60986ebe"
impl_sha: "(문서 전용 — 코드 변경 없음)"
tags: ["independent-review", "codex", "LAN-pilot", "revocation", "postgresql", "audit", "ADR-100", "claude"]
---

# PR #105 `60986ebe` 독립 검토 (2026-09-23, 06:45 KST)

대상: `tests/integration/test_lan_pilot_colocation_revocation.py`(신규 149행, `pytest.mark.postgres`) · `deploy/lan/README.md`(+14) · Codex History `2026-09-23_09-15-00_KST_LAN-PILOT-CP-철회증거_Codex`(PR 브랜치에만 존재) · Codex 작업판. merge-base `fc18b95f` 대비 제품 코드·`tools/`·installer·계약 diff **0**(stat 확인). 측정 트리 `D:\Project\sv-measure-claude`를 **`60986ebe`에 고정**(porcelain 0). 메모리 경보(free ~0.9GB)에 따라 실 PG는 단일 파일 1회 + 되살림 1회만, 시작 전 `codex-worker-status.md` 통보.

## 1. 판정: **승인** (관찰 2, 비차단)

## 2. O-2 — 실 PG 증거 (mock 아님)

- 시험 구조: `tests/integration` 공용 `postgres` fixture(실행마다 고유 DB + 난수 `inv_app_*` role, migration head, 종료 시 정리)로 `tenants`·`nodes` 2행·`node_channels` 2행(v1, enabled)·`audit` 'provision' 2행을 실제 INSERT한 뒤 **CLI 함수 `revoke_server_node_colocation`을 그대로 호출**(`psycopg.connect`·`revoke_channel` mock 없음, `adminDSN=postgres.owner`). 단언: CLI JSON `channels=[{revoked, channelVersion 2}]`·`statePreserved`·**DSN 문자열 미포함**; private-state에 flag false·겸임 Node만 `disabled/disabledReason`·독립 Node `disabled` 없음; 키/journal/cert 파일 bytes 동일; DB에서 겸임 channel `(2, False)`, 독립 channel `(1, True)`, audit = provision v1 ×2 + **revoke v2(같은 fingerprint)**, `nodes` 2행.
- 내 재실행(60986ebe 고정, disposable DB): **1 passed / 7.4s**, 종료 후 `inv_test_*` 잔존 0.
- **되살림**: `revoke_channel`의 `node_channel_audit` INSERT 4줄 제거 → **1 failed**: `Right contains one more item: (<colocated>, 2, 'revoke', <fingerprint>)` — audit 단언이 실제 무게를 가진다(KILLED). 원복, porcelain 0.
- 범위 정직성: History가 "철회 코드의 DB 불변식 증거이지 실제 운영 철회 receipt가 아니다"로 한정 ✔.

## 3. O-1 / O-3 — README 문구 ↔ 코드

- **O-1 재활성 경로 부재**: "`init --allow-server-node-colocation` 재실행은 `disabled`를 지우지도 revoked channel을 켜지도 않는다; state 편집·옛 cert 재등록·대체 Node로 우회 금지; 재활성은 별도 검토된 명령(identity 재검증·현재 version CAS·새 audit·마지막에 marker 해제)이며 **미구현**" — PR #103 검토 O-1의 근거(`add_requested_nodes`가 기존 주소 skip, `node_by_id`는 active만)와 일치 ✔. "대체 Node 생성 금지"는 `seen_ips`가 같은 주소를 거부하므로 코드로도 막힘 ✔.
- **O-3**: "겸임 Node 없이 실행하면 무해한 idempotent opt-out — flag false·빈 `disabledNodes/channels`·독립 Node 무변경·과거 존재의 증거 아님" — `revoked_colocation_state`가 서버 주소 일치 Node만 표기하므로 정확 ✔.

## 4. 기존 state 무변경 · PG-free · 게이트

- `.work/lan-5node/node1/private-state.json` SHA-256 앞 16자 실행 전 `91884e09dc90034c` = 실행 후 동일(읽기 전용 해시 비교만, status/명령 미실행). 시험은 `tmp_path`만 사용(코드 확인).
- PG-free `test_lan_pilot_multinode`+`test_lan_worker_config` **47 passed**.
- 게이트(60986ebe 정확 트리): `check_docs` **873** · `check_contract_bindings` 54/19/14 · `check_response_freshness` · `check_doc_single_source --ratchet` 18 · `check_ontology` · `check_frontend_integrity` 9/0 · `export_schemas --check` 58/58 · `git diff --check fc18b95f 60986ebe` — 전부 exit 0.

## 5. 관찰(비차단)

- **O-a** 시험이 `inv.nodes`에 `status='offline'`·`recovery_epoch`만으로 행을 만들어 실제 enroll 경로(CSR·cert 발급)는 우회한다 — 철회의 DB 불변식 증명에는 충분하나, "provision v1 → revoke v2" 연쇄가 실제 `provision_channel` 출력과 같은 스키마인지는 기존 `node_channels` 시험에 의존. 운영 철회 시 History에 실제 receipt(version·audit) 남길 것(Codex 서술과 동일).
- **O-b** 되살림은 커널 `revoke_channel`을 변이했으므로 이 시험은 커널 audit 회귀도 잡는다 — 다만 이 파일은 `postgres` 마커라 hosted Core에서만 돈다(Windows 로컬은 opt-in). PR 병합 후 hosted Core 1회 통과를 병합 증거로 확인 권장.

## 판정
**승인** — O-2가 mock 없는 실 PG 시험으로 channel CAS·audit·보존 불변식을 증명하고 되살림이 성립하며, O-1/O-3 문구가 코드와 일치하고 기존 state는 해시로 불변, 게이트 전부 0, 제품·계약 diff 0. PR #105 merge 가능. 실제 운영 철회·재활성 명령·5 Node 주장은 이 PR에 없다.
