---
doc_id: "HIST-VF-CX-020305-PREPARATION-001"
title: "VF-CX-02 03 05 인수 준비와 원격 패키지 관측"
version: "1.0.0"
status: "in_progress"
author: "Codex"
reviewer: "Claude"
updated: "2026-09-18T11:28:20+09:00"
source_of_truth: "Git"
---

# VF-CX-02 03 05 인수 준비와 원격 패키지 관측

- owner Codex/reviewer Claude, 사용자 후속 승인; base95c605f, branch agent/codex/vf-acceptance-preparation. 공통 INDEX-PROGRESS-001·개인/VF판·CONTRACT-CX01-CANONICAL-001·ModelManifest/locality 계약 확인, agent-delivery1.1.0/core-reliability1.0.0 적용.
- 현재 공유 정본의 모델 commit/corruption/locality/원자 예약/관측/사전 점검104 passed/0 skipped, 격리PostgreSQL16, exit0. 기존 완료율이나 물리5대 인수를 승격하지 않는다.
- 사용자께서 Claude(a)(b)(c) 재개를 확인했다. Codex는 해당 서비스 수정을 중복 작성하지 않고 커널 무결성 검증과원격 설치 준비에 집중한다.
- 실제 readonly metadata: 운영DB0023→목표0043,kill switch/Node관측/profile/storage권한/bundle선행 미충족. 운영schema·grant·credential·원격설치 변경은 하지 않는다.
- 다음 구현: 기존 공개workspace-worker.zip의 checksum, 중복member, 현재installer, identity/certificate/peer policy만 오프라인 관측한다. image실행·원격설치·fresh mTLS는 검증하지 않았다고항상표시한다. 암호/키/DSN·명령원문을 보고서에반사하지 않는다.

## Claude 후속 변경 독립 검토 및 공유 착지

- 사용자 요청 branch origin/agent/claude/vf-cl-cx01, tip8bafb60(시험9e5350f), basefe4c04c. 3파일125줄 추가/삭제0, 제품 코드 변경0. Codex가 diff와 fixture chain·0043 retained pin 계약을 검토하고 dc5b1e3으로 병합했다.
- 독립 실행: `VF_EVIDENCE_PREFIX=claude-cx01-review python tools/run_vf_security_tests.py tests/test_replica_repair.py tests/integration/test_model_license_readthrough.py tests/integration/test_storage_catalog_api.py tests/test_model_registry.py`: 격리 PostgreSQL16, **36 passed/0 skipped, exit0**, 시험 컨테이너 제거 확인. Claude 보고76은 작성자 결과이며 독립36과 중복 합산하지 않는다.
- 채택 판정: 두 회귀 시험은 유효하며 integration 반영한다. license 시험은 커널의 committed manifest 조회와 필드 존재·재검증 요구만 검증한다. 실제 서비스 간 end-to-end 실행 허가, tenant/project/manifestHash/registryVersionId/content identity의 불변 결속을 입증하지 않는다. pin 시험은 stale 전환 후 pin이 non-null임을 검증하며 정확한 시각 동일성까지 assert하지 않는다.
- Claude History의 '외부 gate뿐'은 작성자 영역 보고다. VF-CX-02/03/05 전체 완료 선언으로 채택하지 않는다. GPU·분산 runtime·대용량 원격 prefetch 등 구현 및 실측 범위는 여전히 별도다.

## VF-CX-05 오프라인 준비 산출물

- `tools/check_workspace_package.py`: 기존 zip checksum/중복 member/identity/certificate/peer policy/현재 installer/image metadata를 읽기 전용 검사. 입력 원문·비밀·예외를 출력하지 않는다. 원격 접속·설치·키 갱신을 수행하지 않는다.
- `python -m pytest -q tests/test_workspace_package.py`: **14 passed, exit0**. 정상 metadata도 imageContentsVerified/installationVerified/mtlsVerified/executionAuthorized는 false이며 만료·미래 인증서/정책 만료/installer drift/중복/손상 입력을 검증한다.
- 실제 `python tools/check_workspace_package.py --state C:/Project/SaintVision-Invion/.work/lan-pilot --output .work/vf-cx05-package.json`: **exit1**, certificateFresh와 peerPolicyFresh 실패. policy 만료2026-09-16T14:37:03.023562+00:00(23:37KST). 나머지8 metadata 항목 일치가 설치 승인이나 mTLS 합격은 아니다.
- `check_lan_storage_readiness.py` 및 `plan_lan_migration.py` 읽기 전용 exit1: 운영DB0023→목표0043,26 migration 대기; kill switch·관측·profile·storage relation/권한·bundle 미충족. 운영 DB 변경 없음.

## 원격 운영자 인계 순서

1. 원격 권한 보유 운영자가 .225와 node identity/epoch/journal 보존 상태를 확인한다. 만료된 certificate와 peer policy는 승인된 enrollment 절차로 갱신하고 최신 검증 코드/이미지에 연결된 패키지를 준비한다. 이 문서만으로 기존 패키지를 설치하지 않는다.
2. Codex가 갱신 패키지 오프라인 검사를 재실행한다. metadata 일치 이후에도 이미지 실행 증거와 source hash 검증이 별도로 필요하다.
3. 운영자가 `deploy/lan/Enable-Workspace.ps1`을 승인 범위에서 실행한다. 실패 시 해당 installer의 `-Rollback` 절차로 복구하며 identity/journal 보존을 확인한다. 설치 수령증의 awaiting-server-mtls-verification을 인수 완료로 취급하지 않는다.
4. Codex/운영자가 준비된 상태의 `tools/check_remote_workspace.py`로 실제 mTLS·profile·작업 증거를 검증한다. 그 뒤에만 실제 장비 인수를 평가한다. 현재 원격 설치/접속 재시도는 수행하지 않았다.

## Docker 및 다음 행동

- daemon 응답 정상이라는 사용자 실측을 수신했다. image lane 이전1 passed/7 failed/exit1은 유지한다. 자원 누적은 원인 후보이며 아직 인과 판정 전이다.
- 170개 종료 시험 컨테이너 ID를 고정한 `.work/docker-cleanup-proposal.json`과 미사용 build cache만 정리 대상으로 제시했다. 실행 중4개/created7개/기타종료6개/이미지/볼륨 보존. 사용자 명시 지시에 따라 삭제 승인 대기; 아직 삭제나 정리 후 image 재실행 없음.
- 다음 Codex: 승인 도착 시 ID·상태 재검사 → 승인 범위 정리 → 동일 digest image lane 재실행 → 기존 실패와 비교 기록. CI billing은 사용자 조치 대기로만 기록한다.
- 구현/로컬 검증은 위 범위 확보. 새 오프라인 도구 독립 검토는 Claude 대기, CI 및 운영 인수 미완. Evidence는 `Evidence/vf-acceptance-preparation/`에 보존한다.

## 착지와 동기화 수령증

- integration/all-agents-unified: dc5b1e3(Claude 병합), f3d602f(독립 검토·오프라인 준비), 원격 push exit0. main/운영 배포 변경 없음.
- f3d602f 대상 Obsidian 선택 동기화: 73파일 관리/45파일 갱신/충돌0, 사후 pending0 및73해시일치. Evidence/vf-acceptance-preparation/sync.json.
- 문서 검사505문서와 ontology 검사 exit0. CI 통과 미확인, 기존 billing 사용자 조치 대기 유지.
