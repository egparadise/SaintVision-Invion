---
doc_id: "HIST-2026-09-22-REAL-VERIFICATION-BOUNDARIES-AND-TOOL-EXPANSION"
title: "화면별 실제 브라우저(Chrome 153) 대 실제 백엔드(Uvicorn) 실측 경계 및 검속 도구 확장 가이드"
version: "1.0.0"
status: "completed"
author: "Gemini"
reviewer: "Codex"
created: "2026-09-22T04:54:00+09:00"
updated: "2026-09-22T04:54:00+09:00"
source_of_truth: "Git"
branch: "integration/all-agents-unified"
---

# 화면별 실제 브라우저(Chrome 153) 대 실제 백엔드(Uvicorn) 실측 경계 및 검속 도구 확장 가이드

## 1. 개요 및 목적

오늘 밤 Gemini 레인에서는 jsdom 가상 환경의 한계(CSS 붕괴 미감지, WebCrypto 생략, 네이티브 I/O 부재 등)를 벗겨내고 **실제 Google Chrome 153 (공식 빌드, Blink 엔진)** 브라우저 실측을 도입했으며, 더 나아가 모의 네트워크 계층을 걷어내고 **실제 Uvicorn 0.52.4 ASGI 서버**와 연결하는 종단간 실측까지 진입했다.

그러나 **"실제 브라우저에서 보았다"는 사실이 "실제 백엔드와 데이터베이스까지 전부 연결되어 검증되었다"는 뜻은 아니다.**
화면마다 대역을 벗겨낸 깊이가 다르며, 대부분의 화면은 실제 Chrome 브라우저에서 렌더링되었으나 네트워크 응답은 정본 스키마 기반의 모의(Mock) 응답으로 채워졌다.

다음 작업자가 "실제 브라우저 검증 완료"라는 단편적 문구만 보고 시스템 전체가 단단하다고 착각하는 병(초록의 착각)을 원천 차단하기 위해, **어디가 단단한 실제이고 어디가 아직 모의에 머물러 있는 무른 층인지** 그 경계를 정직하게 기록한다.

---

## 2. 화면별 실측 경계 매트릭스 (Real Browser vs Real Backend)

| 화면 / 기능 영역 | 컴포넌트 | 실제 브라우저 (Chrome 153) 실측 | 실제 백엔드 (Uvicorn / DB) 실측 | 현재 상태의 성격 (단단함 / 무름) | 실측 증거 및 한계 |
|---|---|:---:|:---:|---|---|
| **산출물 다운로드 및 WebCrypto 전송 무결성** | `DeveloperStudio.tsx` (Step 4) | **YES** | **YES** (Uvicorn wire) | **가장 단단함** (실제 서버 소켓 ↔ 실제 브라우저 WebCrypto ↔ 실제 디스크 파일) | • Uvicorn `GET /artifacts/content` 200 OK 및 wire `X-Content-SHA256` 헤더 수신<br>• Chrome 네이티브 50B 다운로드 이벤트 실측<br>• ⚠️ 단, DB 영수증 및 IdP 세션은 로컬 인메모리 바인딩 |
| **노드 인벤토리** | `NodeList.tsx`<br>`NodeDetail.tsx` | **YES** | **NO** (모의 응답) | **중간** (실제 Blink 렌더링 / 네트워크는 모의) | • Chrome 153에서 `ACTIVE (활성 · 헬스 미결정)` 청록 뱃지, 정책 배너, `LOST (단절)` 적색 뱃지 표출 실측 (`real_chrome_active_node_status.png`)<br>• ⚠️ 백엔드 노드 데몬 및 DB Heartbeat 미연결 (모의 주입) |
| **작업공간 목록** | `WorkspaceList.tsx` | **YES** | **NO** (모의 응답) | **중간** (실제 Blink 렌더링 / 네트워크는 모의) | • Chrome 153에서 5대 계약 상태(`ready` 초록, `provisioning`, `suspended`, `deleting`, `deleted`) 및 미지의 상태 정직 표출 실측 (`real_chrome_workspace_list_5states.png`)<br>• ⚠️ 실제 컨테이너 오케스트레이션 미연결 |
| **승인 센터** | `ApprovalReviewPanel.tsx`<br>`ApprovalDetail.tsx` | **YES** | **NO** (모의 응답) | **중간** (실제 Blink 렌더링 / wire는 모의) | • Chrome 153에서 정본 fixture 기반 작업 스냅샷, 정책 다이제스트 렌더링, 승인 확정 버튼 활성화 실측 (`real_chrome_approval_review_snapshot.png`)<br>• ⚠️ 실제 Two-Person DB 원장 및 Keycloak 미연결 |
| **가상 패브릭 / 디스커버리** | `ResourceExplorer.tsx` | **YES** | **NO** (모의 응답) | **중간** (실제 Blink 렌더링 / 네트워크는 모의) | • Chrome 153에서 후보 노드 카드 렌더링 및 `승인 & 토큰 발급` 모달 렌더링 실측 (`real_chrome_discovery_candidates.png`)<br>• ⚠️ 실제 LAN 브로드캐스트 미연결 |
| **파일 탐색기 / 에디터** | `InvFileExplorer.tsx`<br>`MonacoWorkspaceEditor.tsx` | **YES** | **NO** (모의 응답) | **중간** (실제 Blink WebCrypto / 서버 파일 미연결) | • Chrome 153에서 WebCrypto SHA-256 `검증 통과 (VERIFIED)` 및 Base64 손상 파일 `검증 오류 (ERROR)` 차단 실측 (`real_chrome_workspace_edit_observation.png`)<br>• ⚠️ 실제 디스크 체크아웃/저장 미연결 |
| **불변 증거 뷰어** | `EvidenceViewer.tsx` | **NO** (미실측) | **NO** (미실측) | **무름** (단위/계약 시험만 통과, 화면 실측 미수행) | • 파이썬 계약 시험(`test_route_coverage.py`) 및 Vitest DOM 가드(`evidence-viewer-integrity-guard.test.tsx`) 3/3 통과로 논리는 닫힘<br>• ⚠️ 실제 Chrome 브라우저 스크린샷 미확보 |
| **실행 상세 / 샤드 원장** | `RunDetail.tsx`<br>`ShardObservation` | **NO** (미실측) | **NO** (미실측) | **무름** (Vitest DOM 시험만 통과) | • Vitest 19개 단위 테스트로만 검증됨<br>• ⚠️ 실제 Chrome 브라우저 및 백엔드 미연결 |

---

## 3. 정식 검속 도구 (`tools/run_real_browser_acceptance.py`) 현재 역량

현재 승격된 CLI 검속 도구는 가장 깊은 층(실제 백엔드 wire ↔ 실제 브라우저)까지 검증하도록 구축되어 있다.

### 3.1 현재 검속 범위
- **검증 대상**: `DeveloperStudio.tsx`의 Step 4 산출물 다운로드 및 WebCrypto 전송 무결성.
- **구동 환경**:
  - 백엔드: `uvicorn 0.52.4` (`FastAPI` 앱 기동, 포트 8080).
  - 프런트엔드: `Vite` 개발 서버 (`apps/web`, 포트 3005).
  - 브라우저: `Google Chrome 153` (Blink 엔진).
- **3대 시나리오 검속**:
  1. `--scenario verified`: 정상 wire 헤더 일치 ➔ Chrome 다운로드 성공 (50B 저장) ➔ `[전송 확인 완료]` 배너 표출.
  2. `--scenario mismatch`: 바이트 변조 주입 ➔ Chrome 다운로드 0건 차단 ➔ `[전송 불일치 · 저장 차단]` 배너 표출.
  3. `--scenario missing-header`: `X-Content-SHA256` 헤더 제거 ➔ 다운그레이드 방지 차단 ➔ `[전송 헤더 누락 · 저장 차단]` 배너 표출.

---

## 4. 새 화면을 검속 도구에 편입하기 위한 확장 가이드

다음 작업자가 `NodeList`, `WorkspaceList`, `ApprovalCenter`, `EvidenceViewer` 등을 이 도구에 붙여 **"실제 백엔드 ↔ 실제 브라우저"**로 승격시키고자 할 때 수행해야 하는 구체적 절차:

### 단계 1: Uvicorn 테스트 서버에 라우트 마운트
`tools/run_real_browser_acceptance.py` 내부의 `create_test_app()` 함수에 대상 화면이 호출하는 백엔드 API를 마운트한다:
```python
# 예: 노드 인벤토리 확장의 경우
@app.get("/v1/nodes")
async def get_nodes():
    # 정본 NodeResponse 계약에 맞춘 DB 조회 또는 fixture 반환
    return {
        "items": [
            {"nodeId": "nod_01", "hostname": "worker-01", "status": "active", ...}
        ]
    }
```

### 단계 2: Playwright 브라우저 시나리오 핸들러 추가
도구에 신규 시나리오 플래그(예: `--scenario nodes` 또는 `--scenario evidence`)를 추가하고, 실제 Chrome 페이지에서 해당 탭을 클릭하여 요소를 단언하는 로직을 작성한다:
```python
def verify_node_inventory(page):
    # 1. 탭 네비게이션
    page.click("button:has-text('Nodes 인벤토리')")
    # 2. 필수 계약 요소 단언 (가짜 초록 방지)
    badge = page.wait_for_selector("[data-testid='node-status-badge-nod_01']")
    assert "ACTIVE" in badge.inner_text()
    # 3. 안전한 스크린샷 증거 캡처 (scratch/에 보존)
    page.screenshot(path="scratch/real_chrome_nodes_acceptance.png")
```

### 단계 3: 돌연변이 사살(Mutation Killing) 실증
도구가 초록인데 아무것도 지키지 않는 "거짓 통과"가 되지 않도록, 의도적으로 백엔드 상태를 깨뜨렸을 때 도구가 `exit code 1`로 즉시 사살하는지 실측한다.

---

## 5. 결론 및 원칙

1. **실제 브라우저(Blink)와 실제 백엔드(Wire)는 별개의 검증 층이다.**
2. 화면 렌더링을 실측했다고 해서 백엔드 계약까지 실제 연결된 것으로 과장하지 않는다.
3. 현재 SaintVision-Invion 프런트엔드에서 두 층이 모두 실제 서버 소켓과 브라우저 네이티브 I/O로 완결된 곳은 **산출물 다운로드(GM-01/VF-GM-01)** 영역이며, 나머지 화면들은 실제 Chrome 렌더링에 모의 계약 응답이 결합된 상태이다.
4. 다음 작업자는 위 매트릭스의 '무른 층'부터 차례대로 실제 Uvicorn 라우트에 결속해 나가야 한다.
