---
doc_id: "HISTORY-VF-ERRORS-001"
title: "2026-09-15 VF 오류와 해결"
version: "1.0.0"
status: "review"
author: "Codex"
updated: "2026-09-15T12:24:59+09:00"
source_of_truth: "Git"
---

# VF 오류와 해결

- VF-CX-01: 통합 초기 Windows 회귀 26 failed/18 errors. 중복 FastAPI/fixture 분리, 최신 0038 head 기대값, connect_timeout DB 비교, Windows PSModulePath 차이 및 Linux 전용 복원 환경을 구분해 수정. 최종 Windows1613 pass/139 skip, 별도 Linux41 pass. [[2026-09-15_VF-CX-01_Codex_인계]].
- VF-CX-02: migration SQL을 triple-single-quote Python 문자열로 작성해 SQL quote가 소실됨. triple-double-quote로 수정하고 실제 PG 업그레이드 재검증. Run fixture의 허용되지 않는 상태 직접 전이와 Node 상태값 lost를 실제 상태 머신/enum에 맞춰 수정. Windows66/Linux49 pass. [[2026-09-15_VF-CX-02_Codex_검증보고]].
- CI: 동일 code SHA 290aba5/d6d9d87에서 billing 한도로 job 시작 전 거부. 로컬 검증은 대체 증거이며 CI 통과가 아니다. 계정/결제 변경은 실행하지 않았다.
- VF-CX-03: 최초69개 로컬 회귀 통과. Linux 확장132개 중131 passed/1 setup error: 시험 승인 사용자의 business 연결 누락. 연결을 추가한 다음에는 plain requester가 OIDC subject 제약에 걸려 Windows69 pass/1 failure. OIDC 형식의 독립 사용자·project membership·kernel grant를 모두 설정한 뒤 locality26/26 통과. Linux 해당 묶음 재검증 중. 제품 권한/제약을 완화하지 않았다. 합성 CPU snapshot을 실제 원격 telemetry로 보고하지 않는다.

원본 실패 로그는 private .work에 보존한다. 공개 Evidence에는 counts/코드 SHA/명령/환경만 포함하고 DSN·key·credential 원문을 싣지 않는다. 다음 담당 Codex: 추가 실패는 원인/수정/재검증을 이 페이지와 각 History에 기록; Claude 독립 검토는 아직 완료하지 않았다.

## VF-CX-04 검증 중 발견/수정

- 초기 runtime 시험90 passed/2 failures: contribution version 변경을 frozen 시점과 비교하지 않아 admission/queue에서 통과했다. 0041 immutable locations snapshot을 저장하고 claim/전송 직전 재확인하도록 수정, 92/92 통과.
- Linux 확장143개 중142 passed/1 failure: 만료 시험이 immutable tool_claims를 수정하려 했다. 실제2초 validity의 permit을 발급하고3초 경과 뒤 거부를 확인하는 시험으로 수정했다. 불변 trigger를 완화하지 않았다.
- 두 Node 확장68개 중67 passed/1 setup error: 두 DataLocation에 같은 tenant URI를 사용해 기존 uniqueness 제약에 걸렸다. 두 번째 물리 위치의 URI를 구분했다. 기존 DataLocation migration을 변경하지 않았다.
- 모델 retry 단위/통합34개 통과: 물리 자원 해제 전 거부, cancelled 재시도 거부, 최대3세대, 새 fence/승인 필요, child 예약 rollback, 동일 key 동시 재시도. 실제 두 Node 대체 실행은 최종 Linux140/140 통과와 model-replacement-node.json으로 확인했다. 모든140 case는 동일 sourceHashes이며 이후 product source 변경은 없다.

## VF-CX-01 진단출력 보강

진단 도구가 예외/응답 원문을 공개 Evidence에 포함하던 경계를 allowlist로 수정했다. 최초 통합 묶음22 passed/3 skipped: inspect가 INV 설정을 영구 제거해 후속 DB 시험이 skip됨. 임시 제거/finally 복원 후25 passed/0 skipped. [[2026-09-15_VF-CX-01_진단출력_보강]].


## VF 서비스 독립 검토 — pin/URI/동시성

[[2026-09-15_VF-SERVICE-REVIEW_Codex]]: 원본56시험 통과와 별개로 pinned ready→stale CheckViolation, 빈 suffix왕복2건, slash version모호성1건을 재현(exit1,4failed). 0043은 pin을 제거하지 않고 stale에 보존하며 미회수 bytes는 cache 용량에 남긴다. URI문법·행잠금 수정 후 최종115/115, 기존pin upgrade/실제session경합2/2. 시스템Python dependency와PowerShell encoding 초기 오류는 .venv/UTF-8로 해결. CI billing·전체Obsidian충돌은 제품실패로 분류하지 않으며 동기화는 확인된 범위만 수행.
