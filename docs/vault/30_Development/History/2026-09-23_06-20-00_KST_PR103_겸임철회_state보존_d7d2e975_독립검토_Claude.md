---
doc_id: "CLAUDE-REVIEW-PR103-COLOCATION-REVOKE-D7D2E975-001"
title: "PR #103 'fix(lan): preserve revoked colocated node state'(head d7d2e975, impl 791c7332, base 76f4528d; PR #95 관찰 O1/O2/O4/O5 반영) 독립 검토 — 판정: 승인(관찰 3, 비차단) — 철회는 겸임 Node의 disabled 표기·flag false·해당 channel revoke(version CAS+audit)만 하고 다른 Node/키/파일/DB row를 건드리지 않음, README O2/O4/O5 문구 코드와 일치, 기존 node1 3-Node state 하위 호환(읽기 전용 status), focused 47 passed, 되살림(삭제로 변경) KILLED"
version: "1.0.0"
status: "review"
author: "Claude (reviewer)"
reviewer: "Codex (owner)"
updated: "2026-09-23T06:20:00+09:00"
timezone: "Asia/Seoul"
source_of_truth: "Git"
base_sha: "d7d2e975"
impl_sha: "(문서 전용 — 코드 변경 없음)"
tags: ["independent-review", "codex", "LAN-pilot", "ADR-100", "co-location", "revocation", "fail-closed", "claude"]
---

# PR #103 `d7d2e975` 독립 검토 (2026-09-23, 06:20 KST)

대상: `tools/lan_pilot.py`(+81: `revoke-server-node-colocation` 서브커맨드, `disabled`/`disabledReason` 검증, `active_configured_nodes`, bundle/serve/enroll/artifact 경로를 active 집합으로) · `deploy/lan/worker_config.py`(+3: `schemaVersion==3` 선행 검사) · `prepare-worker.sh`(+1: BLOCKED 메시지) · `README.md`(+33) · 시험 +4(`test_lan_pilot_multinode` 2, `test_lan_worker_config` 2) · Codex History `2026-09-23_08-10-00_KST_LAN-PILOT-CP-관찰보강_Codex`(PR 브랜치에만 존재). 측정 트리 `D:\Project\sv-measure-claude`를 **`d7d2e975`에 고정**(porcelain 0). 커널·계약 diff **0**. 메모리 경보에 따라 pytest는 단일 호출(2파일)만, 실제 등록·철회 명령은 미실행.

## 1. 판정: **승인** (관찰 3, 비차단 — §6)

## 2. O1 — 철회가 보존하는 것·건드리는 것 (코드 정독)

| 대상 | 변경 범위 | 근거 |
|---|---|---|
| private-state.json | `nodeIP == serverIP`인 Node에만 `disabled=true`·`disabledReason='server-node-colocation-revoked'`, 최상위 `serverNodeColocationAllowed=false`. `save`는 `normalized_state(dict(state))`로 전체를 다시 쓰지만 다른 키(adminDSN·epoch·nodes[i] 필드)는 복사 보존, `0600`·tmp→`os.replace` 원자 교체(기존 init/bundle과 동일) | `revoked_colocation_state`(:177~189), `save`(:134~140) |
| 다른 Node | 무변경 — 루프가 서버 주소 일치 Node만 표기. `configured_nodes`는 disabled를 **겸임 Node + 정확한 사유**에만 허용(`'Only a revoked co-located Node may be disabled'`), active Node에 사유가 붙으면 거부 | :91~101 |
| 파일(키·journal·public 번들) | 삭제·회전 코드 없음 | diff 전체 |
| DB | 해당 Node의 `inv.node_channels` 한 행 `enabled=false, version+1` + `node_channel_audit` 'revoke' 1행(`revoke_channel`, `inv.nodes`/`node_channels` 해당 행 `FOR UPDATE`, version CAS 불일치 시 `NODE-0034` 409). Node row·다른 Node 무변경. 미등록이면 `not-enrolled`, 이미 disabled면 `already-disabled` 기록 | node_channels.py:205~226, lan_pilot.py:507~540 |
| 순서 | **state 저장 → DB revoke** — DB 단계가 실패해도 재실행 시 serve/enroll/bundle이 이미 fail-closed(주석·시험 `…persists_fail_closed_state_before_channel_revoke`가 순서 고정) | ✔ |
| 출력 | `statePreserved=true`, disabledNodes(nodeId·IP·사유), channels 상태 — DSN·키 없음 | ✔ |

active 집합 전환: `bundle`(active 없으면 `'No active Node remains'`로 명시 실패), `serve` allowlist, `artifact_for_client`(disabled Node 주소는 403), `node_by_id`(CSR/enroll 거부), `status`는 disabled 포함 표시, `observe` 예외 경로는 사유로 표기 ✔. `normalized_state`의 primary=nodes[0] 규칙은 disabled Node가 nodes[0]일 때도 identity 필드 정합만 검사하므로 무해.

## 3. O2/O4/O5 — README 문구 ↔ 코드

- **O2** "v3 스크립트를 v2 디렉터리에서 실행하면 fail-closed": `validate_topology`가 **첫 줄**에서 `schemaVersion != 3 → ValueError('Bundle schema v3 is required…')`, `prepare-worker.sh`·`finish-worker.sh`가 credential/container 전에 호출 ✔. "Ubuntu 3대 재설치 불필요": 설치된 v2 디렉터리에는 v2 스크립트가 있고(topology 호출 없음) 어떤 경로도 재실행을 강제하지 않으며 `same_identity`는 IDENTITY 6키만 비교 → 정확 ✔.
- **O4** loopback/WSL NAT source → 403이 정상, allowlist/portproxy 확대 금지, 겸임 Node는 Windows에서 `Start-Worker.ps1`이 cert 다운로드: `artifact_for_client`의 정확 일치 규칙과 일치 ✔.
- **O5** `BLOCKED: Docker API 1.45 or newer is required (Docker Engine 25+ / Docker Desktop 4.27+)` — 문자열을 시험이 고정, README 동일 문구. API 1.45 = Engine 25.0 = Docker Desktop 4.27 매핑 정확 ✔. 우회 금지 문구 유지 ✔.

## 4. 기존 node1 state 하위 호환 · 재현

- 코드: 기존 Node에 `disabled` 없음 → `.get(…, False)`, `disabledReason` 없음 → None → 검사 통과. `serverNodeColocationAllowed` 없음 → False.
- **읽기 전용 `status` 1회**(PR 코드, `status()`는 load→rows→print, `save` 없음): `database: ready`, `.143` online·observed, `.222` offline, `.210` online·observed, 세 Node 모두 `coLocatedWithControlPlane:false`·`disabled:false`·`disabledReason:null`. state 파일 무변경.
- focused PG-free **47 passed**(단일 pytest 호출, 3.7s). `bash -n` 2 스크립트·`py_compile` 2파일 exit 0, `--help`에 `revoke-server-node-colocation` 노출.

## 5. 되살림 · 게이트

- **되살림**: `revoked_colocation_state`를 "표기" 대신 **겸임 Node 삭제**로 바꿈 → `test_colocation_revoke_preserves_identity_but_removes_operational_access`·`test_revoke_cli_persists_fail_closed_state_before_channel_revoke` **2 failed** / 13 passed — KILLED. 원복, porcelain 0.
- 게이트(d7d2e975 정확 트리): `check_docs` **871** · `check_contract_bindings` 54/19/14 · `check_response_freshness` · `check_doc_single_source --ratchet` 18 · `check_ontology` · `check_frontend_integrity` 9/0 · `export_schemas --check` 58/58 · `git diff --check 76f4528d d7d2e975` — 전부 exit 0. 계약·커널 diff 0.

## 6. 관찰(비차단)

- **O-1 재활성 경로 없음**: 철회 뒤 `init --allow-server-node-colocation --node-ip <serverIP>`를 다시 실행해도 `add_requested_nodes`가 기존 주소를 건너뛰어 Node는 `disabled=true`로 남고 channel은 revoke 상태다(재등록도 `node_by_id`가 active만 보므로 거부). "보존하되 비활성"이라는 설계와 일치하지만 README에 "철회는 CLI로 되돌릴 수 없고 재활성은 별도 결정·명령"임을 명시 권장.
- **O-2** CLI 시험은 `psycopg.connect`·`revoke_channel`을 mock하므로 DB 경로(CAS·audit)는 커널 `node_channels` 시험에 의존한다 — PG-free 범위로 타당하나 실제 철회 실행 시 History에 `channelVersion` 증가·audit 행을 증거로 남길 것(Codex 다음 행동에 이미 있음).
- **O-3** 겸임 Node가 없을 때 철회를 실행하면 `disabledNodes=[]`이지만 `serverNodeColocationAllowed=false`로 저장된다 — 무해(다음 opt-in은 플래그로 다시 승격).

## 판정
**승인** — 철회는 겸임 Node 표기·flag·해당 channel revoke(CAS+audit)로 범위가 닫히고 다른 Node/키/파일/DB row를 건드리지 않으며, state 저장이 DB보다 먼저라 부분 실패에도 fail-closed다. README O2/O4/O5 문구가 코드와 일치하고 기존 3-Node state는 코드·47 시험·읽기 전용 status에서 호환이며, 되살림 KILLED·게이트 전부 0. PR #103 merge 가능. 실제 등록·철회 실행과 5 Node·S05/S07 주장은 이 PR에 없다.
