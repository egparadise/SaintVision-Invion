---
doc_id: "HIST-CLAUDE-DECISION7-CLOCK-SKEW-ALARM-001"
title: "결정 #7 A 구현 — ERR-DESIGN-007 v2.0.0 채택 + Node 시각 스큐 P2 알람 활성화 + 실 PG 7케이스 실측"
version: "1.0.0"
status: "active"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-22T17:30:00+09:00"
timezone: "Asia/Seoul"
source_of_truth: "Git"
tags: ["decision-7", "clock-skew", "alarm", "GOV-ALERT-001", "ERR-DESIGN-007", "real-postgres", "implementation"]
---

# 결정 #7 A 구현 — 시각 스큐 알람 활성화

**결정**: 2026-09-22 코디네이터(사용자 위임) — 선택지 **A** = [[2026-09-22_노드_시각_스큐_알람_ERR-DESIGN-007_규격개정안_Gemini]] 채택 + `tools/alarm_check.py`의 `Node 시각 스큐 한도 초과` P2 알람 즉시 활성화, **자동 조치 없음**.
**브랜치** `agent/claude/decision-7-clock-skew-alarm` (base: PR #38 head `26e12e25` — 개정안 문서가 그 브랜치에만 있어 그 위에 얹음; #38이 먼저 병합되면 이 PR은 순수 delta가 된다). integration 직접 push·병합은 동결 중이라 PR로만 올린다.

## 1. 무엇을 바꿨나 (파일 단위)

| 파일 | 변경 | 왜 |
|---|---|---|
| `docs/vault/40_Governance/Errors/ERR-DESIGN-007 시각 동기화 요구 부재.md` | v1.0.0 open → **v2.0.0 accepted**. 「개정 규격」 제1~4조를 정본으로 추가, 원문 제안 중 미채택(NTP 하드 전제·Node monotonic TTL) 명시. Codex의 채택-전 판정은 이력으로 유지 | 규격이 있어야 알람이 gated에서 풀린다(브리프 §7 선행조건) |
| `docs/vault/40_Governance/알람 라우팅과 대응 주체.md` | v1.1.0. "governanceGated로 표시" 문단 → 활성화 문단(P2·인프라·기록 채널·자동 조치 없음·래치 없음) | GOV-ALERT-001이 gated라고 말하면서 도구가 발화하면 문서-도구 불일치 |
| `docs/vault/30_Development/2026-09-22_노드_시각_스큐_알람_ERR-DESIGN-007_규격개정안_Gemini.md` | status proposed → **active**, v1.1.0, 채택 callout(정본은 ERR-DESIGN-007로 이동, §6.2 psql 절차는 계획이고 실측은 pytest) | 코디네이터 지시 "규격 개정 문서 확정" |
| `docs/vault/00_Index/사용자_결정대기_브리프_2026-09-22.md` | §7 ✅ 결정됨(A), 요약표 갱신 | 결정 대기 목록에서 제거 |
| `tools/alarm_check.py` | `GOVERNANCE_GATED = ()`(버킷은 유지해 "0 gated"로 **선언**), `CLOCK_SKEW_LIMIT_SECONDS = Decimal(5)`, 순수 함수 `skew_outside_limit`(커널 술어 미러: None/비유한/abs>5) + `clock_skew_alarm(online_nodes)`, `evaluate()`가 `status='online'` 노드의 `(node_id, clock_skew_seconds)`를 읽어 Python에서 판정 | 측정은 SQL, 판정은 한 함수 — 경계(5.0 포함)가 한 곳에만 산다. 커널 가드와 알람이 어긋날 수 없게 |
| `tests/test_alarm_check.py` | gated 시험 → "더는 gated 아님" 시험; 순수 양방향 시험 8(경계 5.0 quiet·5.0001 fire·None·NaN·Inf·음수·offender 이름·online 0대); **실 PG 7케이스**(TC-SKEW-01~07) — 일회용 DB를 실제 Alembic head로 만들고 `evaluate()`를 그대로 호출 | 되살림 가능: `skew_outside_limit`의 `>`를 `>=`로 바꾸면 경계 시험이, `abs`를 빼면 음수 시험이, SQL의 `status='online'`을 빼면 TC-06이 깨진다 |

**커널·마이그레이션·API 변경 0** — 커널 가드(`inv/scheduler.py` `_eligible`·`leases`·`dispatch`·`containment`·`tooling`의 `abs(clock_skew_seconds)<=5`)는 그대로. 알람은 그 필터를 **보이게** 할 뿐이다.

## 2. 실측 (실제 수행한 것만)

Provenance: 트리 `.worktrees/claude-d7`(base `26e12e25` + 이 변경), 인터프리터 `.venv/Scripts/python.exe`(py3.14.7, pytest 9.1.1), `PYTHONUTF8=1`. 실 PostgreSQL 16 = `.env` `INV_TEST_ADMIN_DSN`(127.0.0.1:55432, 컨테이너 `saintvision-invion-dev-pg`). 코디네이터 지시대로 **PowerShell `Start-Process`로 분리 실행 후 로그 폴링**(하네스 reaper 회피), 단일 파일만.

| 실행 | 명령 | 결과 |
|---|---|---|
| PG-free | `pytest tests/test_alarm_check.py -q` (DSN 없음) | **28 passed / 7 skipped**(skip = DSN 부재 사유 정직 표기) |
| 실 PG | 같은 명령 + `INV_TEST_ADMIN_DSN` (분리 프로세스, `alarm-pg.xml`) | **35 passed / 0 skipped / 0 failed**, 8.87s. TC-01 7.08s(일회용 DB Alembic head 생성 포함), 나머지 0.16~0.41s |
| CLI(읽기 전용) | `python tools/alarm_check.py --dsn <INV_TEST_DATABASE_URL>` (dev DB, 노드 0대) | exit 0. 새 줄 `ok Node 시각 스큐 한도 초과: 0 online node(s) … of 0 online`; 요약 `7 evaluated data families; 11 not evaluated; 0 governance-gated` |

7케이스 결과(개정안 §6.1 그대로):

| TC | 삽입 | 기대 | 관측 |
|---|---|---|---|
| 01 | online, 0.0 | quiet | quiet ✔ |
| 02 | online, 4.9 | quiet | quiet ✔ |
| 03 | online, 6.2 | firing P2/인프라 | firing ✔ (`…=6.2s`) |
| 04 | online, −5.5 | firing | firing ✔ |
| 05 | online, NULL | firing(미측정) | firing ✔ (`…=unmeasured`) |
| 06 | offline, 10.0 | quiet | quiet ✔ |
| 07 | 03 노드를 0.2로 갱신 | firing → quiet(래치 없음) | firing → quiet ✔ |

**실측 중 잡힌 것 2건(둘 다 시험 하네스, 제품 아님)**: (a) `evaluate()`에 psycopg 키워드 conninfo를 넘겨 SQLAlchemy URL 파싱 실패 → 픽스처가 URL(`postgresql+psycopg://`)과 conninfo를 둘 다 들고 각자에게 맞는 것을 준다. (b) 케이스 정리에서 `DELETE FROM inv.nodes`가 `inv.node_controls` FK(0022 트리거가 노드 삽입 시 자동 생성)에 막혀 이후 케이스가 앞 케이스의 online 노드를 봄(TC-04~07 오염) → 정리를 `status='offline'`으로 중립화(TC-06이 offline은 범위 밖임을 증명하므로 누수 불가).

## 3. 게이트 (exit code)

- `python tools/check_docs.py` — 아래 커밋 시점에 exit 0 확인(문서 수·링크는 커밋 메시지/PR에 기록).
- `pytest tests/test_alarm_check.py` — 위 표.
- backend CI: 이 PR은 `tests/`·`tools/`·docs만 바꾸므로 backend/core/docs가 뜬다(frontend 경로 없음). Codex의 backend skip-ratchet(`2aa80899`)은 사유별 정확 카운트라 **이 시험이 CI에서 DSN 없이 skip되면 ratchet이 깨진다** — backend job은 DSN이 있으므로 skip 0이 기대값이고, 그것이 곧 실 PG 실행 증거다.

## 4. 인계 · 다음

- **Codex(reviewer)**: 독립 검토 — 특히 (1) 술어 미러가 커널과 정확히 같은가(`scheduler._eligible` ↔ `skew_outside_limit`), (2) 도구 GOVERNANCE_GATED 빈 튜플 유지 판단, (3) PR base가 #38 위인 점(병합 순서).
- **Gemini**: 없음(화면 변경 없음). 대시보드 알람 로그가 이 알람을 표시하는지는 기록 채널 결정(GOV-ALERT-001 §3 `unknown`) 뒤.
- **코디네이터**: PR 병합 순서(#38 → 이 PR). 통지 채널·수신자 실제 값은 여전히 미결(이 결정 범위 밖).
- **미검증(정직)**: 파일럿 5대의 실제 heartbeat로 `clock_skew_seconds`가 채워지는 경로(Go `inv-node` → `inv/observation.py`)는 이 PR에서 안 돌렸다 — 실장비 인수 시 처음 관측된다. ±5초 보정도 그때.
