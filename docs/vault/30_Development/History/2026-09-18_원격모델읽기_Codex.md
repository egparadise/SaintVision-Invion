---
doc_id: "HIST-REMOTE-MODEL-READ-001"
title: "원격 모델 읽기와 Claude 재검토"
version: "1.0.0"
status: "review"
author: "Codex"
reviewer: "Claude"
updated: "2026-09-18T12:28:09+09:00"
source_of_truth: "Git"
---

# 원격 모델 읽기와 Claude 재검토

시작 2026-09-18T12:28:09+09:00, base28684ad, branch agent/codex/model-registry-binding, VF-CX-02/03 owner Codex/reviewer Claude(대기). agent-delivery1.1.0/core-reliability1.0.0, GUIDE-0011.1.0, ADR-INDEX-0011.35.15, INDEX-VIRTUAL-COMPUTER-001/ROADMAP-VIRTUAL-COMPUTER-001/GOV-CONTINUOUS-0011.0.0, 현재 공통/개인 진행판 확인.

범위: 운영자 고정 NodeTLSClient/ChannelProof와 정확한 LocationSnapshot에 대한 제한된 원격 모델 bytes 읽기. Docker/실장비 접속 없이 대역 transport 시험. 기존 실행 verifier에 자동 연결하거나 과거 binding을 실행 permit으로 승격하지 않는다. 원격 provider의 읽기 부분과 DB현재권한 재검사/Run연결을 구분한다.

## Claude b5f770a 재검토

Evidence/docker-host-review/r3-recheck-b5f770a.json: 소스+mocked subprocess 재현, 실제 Docker 실행 없음.

- R2-01: exited만 열거, Created 30분 기준, 비강제 rm 확인. 이전 force-delete blocker는 소스상 해소. 동시 running 생존 실제 시험은 Claude/사용자 보고로 수신, Codex가 재실행했다고 표시하지 않는다.
- R2-02: 사전 prune 전체호출 예외 처리 확인. 해당 사전 prune blocker는 해소. 별도로 run_vf_security_tests finally의 inspect/rm은 TimeoutExpired/OSError를 잡지 않아 proof JSON 기록을 건너뛸 수 있으므로 추가 보강 필요.
- R3-01 P1 유지: docker_diag.is_host_process_init_failure가 모든 error-severity NTSTATUS 및 -9를 초기화 실패로 분류한다. mock에서 0xC0000005 후 docker run 호출2회 재현. 실제0xC0000142 진단 성공은 인정하지만 다른 코드에서 명령이 시작되지 않았다는 보증이 없다. Claude 수정 조건: 플랫폼/정확한 종료코드 분리, 초기화 실패와 실행 중 crash/signal 구분, 변경명령의 재시도 안전성 및 시도별 결과 보존 시험. POSIX음수signal을 Windows초기화실패로 표시하지 않는다.
- R2-03 유지: URL user:password@만 가리고 libpq password=SYNTHETIC_SECRET는 그대로 남는다. 실제 자격증명은 사용/출력하지 않았다. Claude 수정 조건: libpq/인용문자열/URL 비밀값 마스킹 회귀와 원본오류 유지, finally 실패에도 비밀없는 결과 JSON을 기록.
- b5f770a의 근본원인 확정 및 심각도 하향은 인수 판정으로 채택하지 않는다. 호스트압박 인과는 미확정이고 잘못된DB role 거부는 미검증이다. image lane 중단 유지.

착지 판정: 위 잔여 blocker로 전체 branch 보류. 개별 prune 부분을 즉석 추출해 별도 fork하지 않고 Claude의 일관된 수정본을 재검토한다. 다음 Claude는 R3-01/R2-03 및 finally 결과보존 수정, Codex는 소스/mock 재검토. 운영자는 환경조치 후 고정 harness 인수실행 조건 마련.

## 구현 및 검증 결과

ConfiguredRemoteModelReader는 운영자 NodeTLSClient, 정확한 tenant/recovery epoch/ChannelProof와 LocationSnapshot을 받아 bytes만 반환한다. manifest 총32KiB/replica8개 제한, 각 TLS호출 기존40초이내 deadline, 재시도 없음. 모든 입력을 첫 네트워크 호출 전에 검사하며 caller URI/relative_path로 주소를 만들지 않는다. nonce·scope·길이·canonical Base64·각 replica의 신뢰 manifest SHA256·전체 모델 hash 검증 뒤 불변 RemoteModelBytes(원본채널/Location/manifestHash/shards)를 반환한다. 반환물은 실행permit이 아니며 DB최신권한/정책/채널 재검사를 대체하지 않는다.

`python -m pytest tests/core/test_model_remote.py tests/core/test_node_chunk.py tests/core/test_model_manifest.py tests/core/test_node_tls.py tests/test_migration_prerequisite.py -q --junitxml=.work/remote-model-offline.xml` exit0, 106 passed / 0 failed / 0 skipped. 신규 remote27건: 잘못된tenant/epoch/Node/channel버전/pin/endpoint·중복Location/channel·후반shard불일치의 사전차단, nonce replay/자체일관 위조bytes/wholehash 오류/timeout무재시도/모든replica확인. 합성 인증서 loopback 실제mTLS 정상읽기·pin거부·redirect거부 포함. 실장비 또는 실제 운영인증서 검증은 아니다. Evidence/model-registry-binding/remote-model-offline.json 참조.

원격 reader를 ModelRuntimeStore/ConfiguredModelVerifier에 자동연결하지 않았다. 이 컴포넌트를 호출할 trusted worker는 네트워크 전 권한있는 snapshot을 얻고 네트워크 후 같은 transaction에서 현재 policy·registry lifecycle·channel/Location·프로젝트권한·fence를 재확인해야 한다. 이 DB 통합과 승인 digest/Run 결속은 여전히 후속 구현이며 외부권한 차단으로만 분류하지 않는다. 운영DB/migration/HTTP API 변경 없음.

### 완료와 인계

작성자 오프라인 검증 완료; CI billing 대기(재실행 없음), Claude 독립검토 대기, VF 운영인수 미완료. image lane 추가 실행 없음, business-kernel-role 거부 단언 미검증 유지. 다음 Codex: 권한 snapshot/재검사와 frozen workload 결속 설계·구현; 다음 Claude: R3-01/R2-03 수정과 새 remote reader 독립검토. 잔여율/운영인수율 재산정 없음.

재검토 정확한 위치(b5f770a): tools/docker_diag.py:27 광범위 NTSTATUS 판정, :33 libpq 미마스킹, :55~60 변경명령 재시도. 정상0xC0000142 분류 실측과 이 false-positive/비밀출력 결함은 동시에 성립한다.
