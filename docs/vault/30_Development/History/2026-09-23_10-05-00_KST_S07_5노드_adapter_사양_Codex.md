---
doc_id: "HIST-CODEX-S07-CARD23-FIVE-NODE-ADAPTER-SPEC-001"
title: "S07 Card23 5노드 실 Node adapter 사양"
version: "1.0.0"
status: "review"
author: "Codex"
reviewer: "Claude"
updated: "2026-09-23T10:05:00+09:00"
timezone: "Asia/Seoul"
source_of_truth: "Git"
task_ids: ["S07-DB"]
tags: ["history", "s07", "five-node", "adapter", "read-only", "preflight"]
---

# S07 Card23 5노드 실 Node adapter 사양

## 결과

[[S07 5노드 실 Node adapter 사양]]에서 `measure_s07_recovery.py --adapter five-node-lab --inventory ... --dry-run`의 구현 경계를 정의했다. #101의 canonical inventory와 PostgreSQL read-only Node classification을 단일 helper로 재사용하고, S07은 `measurementEligible.s07=true && !coLocatedWithControlPlane`인 ready Ubuntu Node만 disruption 후보로 계획한다.

정상 topology B의 preflight 기대값은 등록/ready 5, CP 겸임 1, CP 독립·S07 eligible/selected 4다. inventory/profile `lan-workspace-v1`, heartbeat/snapshot 15초, mTLS channel·epoch/version·fingerprint/IP, clock skew를 읽기만 하며 합성 row·heartbeat/snapshot 갱신·Node 중단·repair·JUnit은 모두 false/미실행이다. 20회 미래 계획은 Ubuntu 4대 각 5회지만 이 사양은 실제 반복을 실행하지 않는다.

## 문서 반영

- 기존 [[S07 노드 이탈과 복구 반복 측정기]]를 v1.2로 올려 합성 N=5 명령과 미래 물리 preflight를 구분했다.
- [[Codex 5노드 랩 opt-in lane 정의]] v1.6에 S07 preflight 제안 명령과 구현/실행 미승인 경계를 추가했다.
- 제품 코드·workflow·registry·ontology·공개 계약은 변경하지 않았다.

## 정직성 경계

코드·시험·DB·Node disruption·복구·5노드 실행은 수행하지 않았다. 현재 파일럿의 실제 `lan-observe-v1` Node를 `lan-workspace-v1`로 승격하지 않으며, S07-DB `review`, AC-07/95%/60초+poll/byte 복구는 모두 미측정이다. reviewer는 Claude이고 구현은 코디네이터의 별도 승인 대상이다.

메모리 경보 아래 문서 게이트만 실행했다. 최신 integration `e1290f0a`와 결합한 R1 후보에서 `check_docs.py` **883 documents / exit 0**, `check_contract_bindings.py` **54 fixtures·19 response types·14 replay guards / exit 0**, `check_ontology.py` **48 task mappings / exit 0**, `check_ontology_generation.py` **4 artifacts / exit 0**, `check_doc_single_source.py --ratchet` **18 pairs / exit 0**, `check_response_freshness.py` **10/10 / exit 0**, `git diff-tree --check` **exit 0**이다.
