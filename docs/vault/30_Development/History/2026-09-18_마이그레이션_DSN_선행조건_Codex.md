---
doc_id: "HIST-MIGRATION-PREREQUISITE-001"
title: "마이그레이션 DSN 선행조건 오분류 수정"
version: "1.0.0"
status: "review"
author: "Codex"
reviewer: "Claude"
updated: "2026-09-18T11:39:03+09:00"
source_of_truth: "Git"
---

# 마이그레이션 DSN 선행조건 오분류 수정

## Finding MIGRATION-PREREQUISITE-001 — 수용

- 사용자 독립 clean worktree f3d602f: 1069 passed/488 skipped/1 failed 보고. 실패는 test_published_migration_heads_upgrade_without_rewriting에서 DSN 부재 KeyError가 일반 migration 실패로 변환된 것. 제품 migration 손상의 증거가 아닌 검증 도구/시험 선행조건 분류 결함이다. 이전 ModuleNotFoundError 진단 억제와 같은 오분류 유형의 재발로 판단한다.
- owner Codex, reviewer Claude(수정본 검토 대기). base74e6437, branch agent/codex/migration-prerequisite, 착수 2026-09-18T11:39:03+09:00. 공통/개인 진행판과 agent-delivery1.1.0/core-reliability1.0.0 확인. 범위는 검증 CLI와 시험 가드이며 migration 파일 변경 없음.

## 수정과 합격 기준

- CLI가 DSN 없음/빈 문자열을 먼저 분류해 exit2 및 고정 메시지 `Migration validation not run: INV_TEST_ADMIN_DSN is absent; disposable PostgreSQL is required.`를 출력한다. 성공exit0/실제 검증 실패exit1을 구분하며 비밀 억제는 유지한다.
- 기존 conftest의 DSN 가드를 session fixture `test_admin_dsn`으로 추출해 database_url과 migration subprocess 시험이 공유한다. 로컬 DSN 없음은 skip, CI DSN 없음은 실패. subprocess 시험을 위해 불필요한 DB를 추가 생성하지 않는다.
- 신규 회귀 시험: CLI DSN 미설정/빈 값, 설정된 잘못된 DSN의 exit1 및 비밀 비출력, 실제 pytest subprocess에서 로컬 skip/CI failure. 5 passed/exit0.
- 검증 중: 전체 비-integration 무DSN 회귀와 격리 PostgreSQL16 account/migration 시험. 사용자 결과와 별도 집계하며 skip은 합격으로 계산하지 않는다.

## 다음 행동

- Codex: 두 환경 결과를 Evidence로 고정, 문서/온톨로지 검증·push·선택 Obsidian 동기화. Claude: 수정된 CLI 상태 분류/공통 fixture 독립 검토.
- CI billing은 사용자 조치 대기. Docker 잔재 삭제 승인은 여전히 미수신이며 이번 finding 전달을 삭제 승인으로 해석하지 않는다.

## 최종 로컬 검증

- 무DSN: `python -m pytest -q tests --ignore=tests/integration --junitxml=.work/migration-prerequisite-no-dsn.xml`, INV/CX01/VF 및 CI 환경 변수 제거, **1074 passed/489 skipped/0 failed/0 errors, exit0**. 사용자 기준의 실패1건이 skip으로 전환됐고 신규5회귀가 통과했다.
- 실제 DB: `VF_EVIDENCE_PREFIX=migration-prerequisite-pg python tools/run_vf_security_tests.py tests/test_account_integration.py`, 격리 PostgreSQL16, **10 passed/0 skipped, exit0**. 전체 공개29 시작점의 upgrade/replay/보존 검증을 포함한 원래 migration subprocess 시험이 통과했다. 격리 컨테이너 제거 확인, 운영 DB 미사용.
- Evidence/migration-prerequisite의 classification.json/no-dsn.json/postgres.json에 비밀 없는 명령·환경·exit code·집계를 보존한다. raw private log/DSN은 게시하지 않는다.
- 문서 검사506문서와 ontology 검사 exit0. 수정 구현/로컬 검증 완료, Claude 독립검토/CI는 미완료. 사용자 기존 finding은 수용했고 새 수정본 검토 완료로 대신 표시하지 않는다.

## 착지 및 인계

- 수정 a591f4f; 동시 integration dc8e73a(Gemini UI)를 보존해665c0d8로 병합·공유 원격 push exit0. 충돌은 공통판 updated 시각뿐이며 양쪽 본문을 보존했다. 이번 backend 시험 파일/도구는 재변경되지 않았다. Gemini UI 독립 검증을 대신 주장하지 않는다.
- 665c0d8 선택 Obsidian 동기화:8파일 갱신, 충돌0,사후pending0/8해시일치. Evidence/migration-prerequisite/sync.json. 병합 후 문서507개·ontology exit0.
- 다음 Claude: a591f4f의 CLI 분류/공통fixture 독립검토. Codex: Docker 정리 명시 승인 수신 후 기존 image lane 판별을 이어간다. CI billing 및 원격 운영 인수 미완료 유지.

- 사용자 독립 clean worktree 재검증 수신: a591f4f CLI exit2·공통skip,5passed/1skipped; tip90c07c6 전체1074passed/489skipped/0failed. 사용자 검토 완료. Claude 수정본 검토 여부와는 별도다.
