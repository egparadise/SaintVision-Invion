---
doc_id: "REGRESSION-JUNIT-549e697-CLAUDE-001"
title: "마무리 회귀 junit 기반 — HEAD 549e697. 6배치 산술 합·커버리지 대조. migration-head 타임아웃 별도 항목"
version: "1.0.0"
status: "review"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-21T13:00:00+09:00"
head_commit: "549e697"
source_of_truth: "Git"
tags: ["saintvision", "regression", "junit", "batch-sum", "coverage-reconciliation", "docker-leak"]
---

# 마무리 회귀 junit 기반 — HEAD 549e697

지난 전체 회귀(40e921b) 이후 많은 변경(UI 세 화면 수정·새 DOM 시험·sync 도구 4차례 수정·skip 가드 전수 적용·recovery 전제 분리)이 들어와, 서로 간섭하지 않는지 확인했다. 아래 수치는 **6배치의 산술 합이며 단일 완결 실행이 아니다.**

## 실행 정본
- **인터프리터**: `C:/Project/SaintVision-Invion/.venv/Scripts/python.exe` (시스템 python 아님)
- **HEAD**: `549e697` (84f26ca 포함) · **KST**: 2026-09-21 11:59~12:45
- **환경**: 일회용 PG16(값 마스킹) + `CX01_CONTAINER` 설정 → **recovery drill 실제 backup/restore 실행**
- **산출물**: `Evidence/regression-549e697-20260921-115914/` (junit 6 + manifest 7, DSN 마스킹)

## 배치별 (junit testsuite 권위 수치)
| 배치 | tests | passed | failed | errors | skipped |
|---|---|---|---|---|---|
| 1 core | 631 | 628 | 0 | 0 | 3 |
| 2a rootA1 | 132 | 117 | **1** | 0 | 14 |
| 2b rootA2 | 368 | 368 | 0 | 0 | 0 |
| 3 rootB | 684 | 670 | 0 | 0 | 14 |
| 4 intA | 428 | 255 | 0 | 0 | 173 |
| 5 intB | 398 | 179 | 0 | 0 | 219 |
| **산술 합(6배치)** | **2641** | **2217** | **1** | **0** | **423** |

## 커버리지 대조
- `pytest tests/ --collect-only` = **2641 collected**. 배치 산술 합 tests = **2641 정확 일치** → 중복 0·누락 0. 전체엔 있으나 배치 union에 없는 파일 **0**. (배치는 core 폴더 / root `*.py` 반분 / integration `*.py` 반분으로 구조적 비중첩. rootA는 시간 초과로 2a+2b 재분할.)

## 유일 실패 = migration-head 타임아웃 (별도 항목, 코드 회귀 아님)
`tests/test_account_integration.py::test_published_migration_heads_upgrade_without_rewriting` — 배치 실패 메시지 **TimeoutExpired(180s)**. **격리 재실행도 181.47s로 실패**(지난 40e921b에선 격리 통과 <180s였음).
- **원인 판별**: 마이그레이션 60개 = 40e921b와 동일(추가 0), `check_migration_upgrade.py` 40e921b 이후 **불변**. 즉 도구 workload 불변 → **순수 호스트 타이밍**(오늘 코드 회귀 아님).
- **성격**: `subprocess.run(..., timeout=180)` **하드코딩·여유 없음**. 도구 런타임(~181s)이 호스트 속도/부하에 따라 180s를 넘나든다. 지난번 사용자 지적("부하에 따라 갈리는 고정 타임아웃")이 이번엔 격리에서도 넘어 **재현·악화**.
- **권고(별도 항목)**: 180s 여유가 사라졌다. 타임아웃을 늘리거나(예 300s) 호스트-상대적으로 하거나 도구를 빠르게 해야 한다. 소유 판단은 Codex.

## Node
vitest **33 files / 330 passed**(exit 0) — 새 `resource-explorer-dom.test.tsx`(8, Gemini의 DOM effect lane, happy-dom) 포함.

## Docker 자원 (누수 역추적)
| 시점 | 컨테이너 | 볼륨 | 네트워크 |
|---|---|---|---|
| 회귀 前(기준) | 48 | 79 | 11 |
| 배치간(내 PG 포함) | 49 | 80 | 11 |
| 최종(정리 후) | **48** | **79** | **11** |
**Δ0/Δ0/Δ0 — 누수 0.** recovery drill이 실제 실행됐으나 sv-rpo 자원을 스스로 정리(배치간 49/80/11 고정). 익명 69 보존·보호 4 미접촉·svcx01/sv-rpo 잔존 0.

## 종합
오늘 들어온 변경들이 **함께 돌아도 깨끗**하다. **오늘 작업 귀속 실패 0** — 유일 실패는 pre-existing 고정 타임아웃(호스트 타이밍)이며 별도 항목으로 올린다. 합계는 **6배치 산술 합**이고 커버리지 대조로 전체를 정확히 덮음을 확인했다.
