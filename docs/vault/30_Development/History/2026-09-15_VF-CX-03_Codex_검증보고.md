---
doc_id: "HISTORY-VF-CX-03-VERIFY-001"
title: "2026-09-15 VF-CX-03 Codex 검증보고"
version: "1.0.0"
status: "review"
author: "Codex"
updated: "2026-09-15T12:30:34+09:00"
source_of_truth: "Git"
---

# VF-CX-03 검증 및 인계

owner Codex, reviewer Claude (인계 대상, 수신/검토 미확인). branch agent/codex/vf-cx-03, base ddd04974c810800ae099d29b19a937c5abba3261. 원격 integration/all-agents-unified 재조회 b9752a8e31573ee92b5bbb89724224923459f295. [[모델 locality 예약과 실행 입력 경계]].

## 작업한 것

실제 ReadRoot bytes를 현재 Catalog/Node/project scope와 결속한 ModelLocalityStore, 기존 PlacementStore의 deterministic Explain/Lease 원자 예약, 0040 immutable tenant-scoped model_run_inputs를 구현했다. 기존 ModelManifest commit의 Catalog 검사를 공통 함수로 추출했다. 새 snapshot source와 RLS는 추가 migration만 사용하며 기존 Lease/fence/idempotency authority를 유지한다.

관측 실패한 replica는 가용량에 포함하지 않는다. 전송률 없는 Node는 모델을 원격에서 가져올 수 있다고 가정하지 않는다. 실행 입력 commit 실패 시 Lease와 ledger/outbox가 함께 rollback된다. 기존 ToolGateway는 모델 입력을 무시할 수 없으며 실제 adapter가 준비되기 전 MODEL-0006을 반환한다. 모델의 다중 GPU 실행/복제 전송/운영 인수는 이번 구현 결과가 아니다.

## 확인한 것

- Windows Python3.14 / 격리 tmpfs PostgreSQL16: `python tools/run_vf_security_tests.py tests/integration/test_model_locality.py tests/integration/test_model_commit.py tests/core/test_model_manifest.py tests/core/test_workspace_api_boundary.py`, 최초66, 추가69 passed exit0. Gateway 시험 추가 후69 passed/1 fixture failure exit1; 실제 OIDC/business 권한 fixture 수정 후 locality26/26 exit0. 이전 통과 시험을 중복 가산하지 않는다.
- Docker Linux Python3.12 / PostgreSQL16: `python tools/check_kernel_docker.py --go <Go1.27.1> --postgres-image postgres:16 --tests tests/integration/test_model_locality.py tests/integration/test_model_commit.py tests/integration/test_placement.py tests/integration/test_tool_admission.py tests/integration/test_definer_audit.py tests/test_account_integration.py`, 131 passed/1 fixture error exit1. 여기에는 실제 Node placement/기존 admission과 migration upgrade/definer 감사가 포함된다.
- fixture만 수정 후 `--tests tests/integration/test_model_locality.py`: 26/26 passed, exit0, cleanup 모두 true. 최초 확장 시험의 실패를 숨기거나 전체132 단일 실행 pass로 쓰지 않는다. 제품 source는 같고 변경된 시험 묶음을 재검증했다.
- corrupt/truncated/missing bytes, byte budget, 관측 만료/미래시각, root/location version, grant/membership/Node 이탈, tenant RLS, immutable input, 중복 예약, 원자 rollback, 6개 동시 예약의 기존 ceiling, 모델 없는 더 큰 CPU Node 배제, 실제 ToolGateway 거부를 확인했다.
- `python tools/check_docs.py`, `python tools/check_ontology.py`, `git diff --check`: exit0. 문서 검증은 runtime/운영 인수와 별도다.
- Evidence는 `docs/vault/30_Development/Evidence/vf-cx-03/`. Linux sourceHashes는 base+dirty 작업 입력을 고정한다. private 로그/credential 원문은 공개하지 않는다. [[2026-09-15_VF_오류와_해결]].

## 이어서

Codex: commit/push/동일 SHA CI 조회/Obsidian scoped sync 후 VF-CX-04 실제 모델 입력 freeze와 기존 permit 경로 adapter를 진행한다. 현재 로컬 계약은 준비됐지만 CI/독립 검토/실장비 인수는 미완료. Claude: 0040 RLS/FK와 rollback·동시성·입력 무시 거부를 독립 검토한다. Gemini: modelInput 관측/재검증 필요 상태와 미지원 모드를 실제 계약에 맞춰 표시한다. 승인/수신을 대신 기록하지 않는다.

기존57.81% 유지. 새 Codex VF 운영 인수0/5, 구현/로컬 확인을 운영 완료로 세지 않는다.
