---
doc_id: "HIST-20260922-CLAUDE-CODEX-4COMMITS-INDEPENDENT-REVIEW"
title: "Claude 독립 검토: Codex origin/integration 착지 4건 (881f2911, 1312e295, eceac8cf, dcf2b947)"
version: "1.0.0"
status: "completed"
author: "Claude"
reviewer: "Codex"
created: "2026-09-22T16:55:00+09:00"
updated: "2026-09-22T16:55:00+09:00"
source_of_truth: "Git"
---

# Claude 독립 검토: Codex origin/integration 착지 4건

## 1. 개요

- **검토 대상 커밋 (4건)**:
  1. `881f2911157b636338078052f8b83590b10776d1`: `fix(ci): make first hosted runs attributable and reproducible`
  2. `1312e295fc511de40e6af4d79c2a089770879219`: `test(kernel): pin EvidenceEnvelope weight at five PG sites`
  3. `eceac8cfa9e20834ec0cbb6a6c469bf023ac9a50`: `feat(contracts): adopt resource usage and download canons`
  4. `dcf2b947d802b86febdcb4acb0677c6640adb0ee`: `feat(verification): report fixture producer reachability`
- **검토자**: Claude (독립 검토 레인)
- **검토 환경**: Windows (새 PC 192.168.45.74), `.venv\Scripts\python.exe` (Python 3.14.7), 실 PostgreSQL 16 (`127.0.0.1:55432/invdev`).
- **원칙**: R6/GOV-AGENT-001 준수, 실제 수행한 실측 증거만 기록 (Zero-Mock Provenance).

---

## 2. 커밋별 정밀 분석 및 실측 검증

### 1) 커밋 `881f2911`: Hosted CI 귀속·재현성 보강 (`fix(ci)`)
- **변경 분석**:
  - `tests/integration/test_credential_backend.py`: Linux 러너에서 `os.chown` 실패 시 `docker exec $HOSTNAME`에 의존하던 결함을 제거하고, 독립형 일회용 컨테이너(`docker run --rm --volume ...`)로 격리된 synthetic 파일(uid 1)을 생성하도록 교정.
  - `tools/provision_credentials.py`: `CredentialDenied`, `JSONDecodeError`, `UnicodeDecodeError` 발생 시 `ProvisioningDenied` 및 `EXIT_REFUSED` (exit code 2)로 안전하게 격리하고 비밀 값 누출 방지.
  - `tools/run_vf_security_tests.py`: 파라미터나 비공개 세부정보를 노출하지 않고 실패한 테스트 클래스(`failedTestClasses`)만 안전하게 집계하도록 기능 확장.
  - `tests/test_vf_evidence.py`: `test_safe_failure_summary_exposes_only_owning_class` 신설.
  - 스키마 메타데이터 보강: `pool-list-response.schema.json`, `pool-list-item-response.schema.json`.
- **실측 검증**:
  - 명령: `pytest tests/test_vf_evidence.py`
  - 결과: **22 passed in 3.69s** (exit code 0).
  - 명령: `.venv\Scripts\python.exe -m pytest tests/core/test_credential_provision_cli.py`
  - 결과: **12 passed in 0.72s** (exit code 0).
- **판정**: **SOUND (합격)**.
  - *비고*: `inv.state`가 `StrEnum`을 사용하므로 Python >= 3.11 환경 필수 (저장소 기본 가상환경 `.venv` Python 3.14.7에서 100% 정상 작동).

---

### 2) 커밋 `1312e295`: EvidenceEnvelope 5개 실 PG 사이트 무게 고정 (`test(kernel)`)
- **변경 분석**:
  - `EvidenceEnvelope`의 계약 검증이 단순 모의 시험에 그치지 않고, 실제 PostgreSQL 트랜잭션 경계 내부에서 계약 거부 무게(weight)를 온전히 가지는지 5대 실 사이트에서 검증:
    1. `test_results_rejects_invalid_EvidenceEnvelope_before_persistence` (`inv.result_commitments` 미기록 단언)
    2. `test_runs_rejects_invalid_EvidenceEnvelope_before_persistence` (`inv.evidence` 미기록 단언)
    3. `test_shard_completion_rejects_invalid_EvidenceEnvelope_atomically` (`inv.shard_completions`, `inv.evidence` 원자적 롤백 단언)
    4. `test_storage_commit_rejects_invalid_EvidenceEnvelope_atomically` (PG 트랜잭션 롤백 단언)
    5. `test_storage_view_rejects_invalid_stored_EvidenceEnvelope` (영속화된 봉투 변조 시 VERIFY-0032 거부 단언)
- **실측 검증**:
  - 실 PostgreSQL DSN: `postgresql://invowner:***@127.0.0.1:55432/invdev`
  - 명령 1: `.venv\Scripts\python.exe -m pytest tests/integration/test_postgres.py -k "EvidenceEnvelope"`
  - 결과 1: **3 passed in 9.20s** (exit code 0).
  - 명령 2: `.venv\Scripts\python.exe -m pytest tests/integration/test_storage_commit.py -k "EvidenceEnvelope" tests/integration/test_storage_view.py -k "EvidenceEnvelope"`
  - 결과 2: **2 passed in 7.47s** (exit code 0).
- **판정**: **SOUND (합격)**.
  - 5개 실 PG 영속 사이트 전수에서 비정상 계약 봉투 유입 시 완벽한 예외 포착 및 원자적 DB 롤백이 실증됨.

---

### 3) 커밋 `eceac8cf`: 노드 자원 사용량 및 다운로드 정본 계약 채택 (`feat(contracts)`)
- **변경 분석**:
  - 결정 #2 Option A 채택:
    - `contracts/v1alpha1/core.schema.json`에 `NodeResourceUsageResponse` 및 `ResourceUsageMeasurement` 스키마 추가.
    - 미측정(`measured === false`) 시 `reserved: null`, `spare: null`, `observedAt: null` 강제 `allOf` 제약.
    - `kind`(`cpu`, `memory`, `gpu`, `storage`, `network`), `unit`(`millicores`, `bytes`, `devices`, `bitsPerSecond`) 제약.
    - TypeScript, Go, Python 모델(`models.py`) 일괄 동기화.
  - 결정 #5 Option A 채택:
    - `ArtifactDownloadMetadata` 스키마 및 고정 헤더(`X-Content-SHA256`, `application/octet-stream`, `no-store`, `nosniff`, `attachment; filename="artifact.bin"`) 추가.
  - 정본 fixture 2종 추가: `node-resource-usage-response.json`, `artifact-download-metadata.json`.
- **실측 검증**:
  - 명령: `.venv\Scripts\python.exe -m pytest tests/core/test_decided_resource_and_download_contracts.py`
  - 결과: **9 passed in 0.77s** (exit code 0).
- **판정**: **SOUND (합격)**.
  - 스키마 검증, Pydantic 모델 roundtrip, 변이 필드 거부(unit="percent", reserved=-1 등) 전수 완벽 검증.

---

### 4) 커밋 `dcf2b947`: Fixture Producer Reachability 정적 프록시 도구 구축 (`feat(verification)`)
- **변경 분석**:
  - `tools/check_fixture_reachability.py` 신설:
    - 50개 fixture를 `runtime-witness`, `circular-witness`, `schema-only`, `unreferenced` 4개 범주로 자동 분류하는 정적 분석기.
  - `tests/test_check_fixture_reachability.py`: 도구 자체의 파싱 및 분류 로직 단위 테스트.
  - `.github/workflows/docs.yml` 및 `docs/vault/40_Governance/검증검사도구_목록.md`에 report-only 검사로 편입.
- **실측 검증**:
  - 명령 1: `.venv\Scripts\python.exe tools/check_fixture_reachability.py`
  - 결과 1: exit code 0.
    - 총 50개 fixture 중:
      - `runtime-witness`: 10건
      - `circular-witness`: 29건
      - `schema-only`: 11건
      - `unreferenced`: **0건**
  - 명령 2: `.venv\Scripts\python.exe -m pytest tests/test_check_fixture_reachability.py`
  - 결과 2: **2 passed in 0.12s** (exit code 0).
- **판정**: **SOUND (합격)**.

---

## 3. 종합 검토 결론

오늘 Codex가 `origin/integration/all-agents-unified`에 착지시킨 4개 커밋(`881f2911`, `1312e295`, `eceac8cf`, `dcf2b947`)은 모두 엄밀한 계약, 완벽한 단위/통합 테스트, 실 PostgreSQL 트랜잭션 롤백 무결성을 충족하고 있습니다. 결함이나 회귀는 발견되지 않았으며, 전 커밋 **SOUND (합격)**으로 판정합니다.
