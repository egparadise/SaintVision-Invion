---
doc_id: "CODEX-RECOVER-LOST-GUARDS-001"
title: "미병합 Claude 회귀 가드 5건 재평가 및 필요한 시험 회수"
version: "1.0.0"
status: "active"
author: "Codex"
reviewer: "pending"
updated: "2026-09-21"
source_of_truth: "Git"
tags: ["regression-tests", "recovery", "definer", "role-guard", "fixed-sha"]
---

# 미병합 Claude 회귀 가드 5건 재평가 및 필요한 시험 회수

작업 입력: `0979fe6`의 유실 점검과 Claude 보고서 [[2026-09-21_미병합브랜치_44커밋_유실점검_Claude]]. 기준 integration SHA `938ea1b1d797e7c2ed4a81e9920cdab0cb9f620b`; owner Codex, reviewer pending. 요청 범위는 기존 속성에 대한 다섯 가드 시험이며 `alarm_check.py` 기능 구현은 포함하지 않는다.

## 다섯 항목 판정

| 잃어버린 시험 | 현재 통합의 보호 속성 | 처리 |
|---|---|---|
| `test_definer_rules.py` (`dee31e51`) | 이전 `_definer_rules.py` 소스 문법 helper는 없다. 현재는 `tools/check_definer_functions.py`가 live catalog 전체 함수 정의 digest·권한·role·migration revision을 정책과 비교한다. `tests/integration/test_definer_audit.py`는 정의 변경, 주석/미사용 setting, 예상 밖 함수, grant, role 및 빈 catalog를 실제 DB 변형으로 검사한다. | 이전 helper 시험은 이식하지 않았다. 현재 통합 경계 시험이 더 직접적인 대체다. 실제 DB에서 그것을 돌리자 정책 파일이 0044에 고정되고 0045의 `consume_discovery_issue_budget(uuid)` 항목도 빠진 것을 발견했다. `tools/definer-policy.json`을 0045 revision과 catalog에서 측정한 함수 digest/execute role로 갱신했다. |
| `test_recovery_capability.py` (`4b09dfc`) | 동명 `tests/core/test_recovery_capability.py`가 이미 존재한다. 현재 규칙은 archiver 설정만으로 전달 보장 RPO를 주장하지 않는 것으로 발전했다. | 구 브랜치 시험은 이식하지 않았다. 현 의미에 맞게 이미 갱신된 핵심 시험을 유지했다. 이전의 “archive timeout이면 bound가 있다” 기대는 현재 의도와 반대라 복사하지 않았다. |
| `test_recovery_drill_record.py` (`8a8f3b4`) | 통합 `tests/integration/test_recovery_drill.py`가 실제 drill 기록, backup ledger 검증/재검증, 변경된 backup 거부 및 두 기록의 rollback을 검사한다. 과거 `off_site=True` 시험은 현재 도구가 local backup을 off-site 증거로 승격하지 않고 항상 `off_site=False`로 기록하므로 유효한 현재 기대가 아니다. | 현재 구현에서 빠진 실패 무결성 경로를 새 실제-DB 시험 `test_failed_integrity_drill_is_recorded_without_measurements`로 재작성했다. 무결성 실패는 ledger outcome `failed`이고 RPO/RTO 측정은 NULL이어야 한다. |
| `test_role_shape.py` (`a5dd83c`) | 과거 `DESIGNED_ROLE_SHAPE` helper는 없다. 대신 `tests/test_migration_role_guard.py`가 소유한 격리 PostgreSQL에서 2개 role 각각의 LOGIN/SUPERUSER/BYPASSRLS/CREATEDB/CREATEROLE/REPLICATION 선행 조건을 거부하고, 빈 DB upgrade/replay 뒤 실제 역할 모양을 확인한다. | 이전 helper 시험은 이식하지 않았다. 기존 DB 경계 시험에서 head를 `0037_storage_sample_commit`으로 고정한 오래된 기대를 발견해 Alembic의 현재 head에서 유도하도록 고쳤다. |
| `test_alarm_check.py` (알람 도구 커밋 `00b1159`) | 통합에는 `tools/alarm_check.py`와 호출 가능한 alarm evaluator가 없다. | evaluator를 import하는 시험만 옮기면 수집 실패가 생기며 기존 동작의 regression guard가 되지 않는다. 이 항목은 guard 회수 불필요 판정이 아니라 **기능 부재로 미적용**이다. GOV-ALERT-001 evaluator 구현은 별도 기능 카드다. |

요약: 다섯 파일을 현재 통합과 대조했다. verbatim cherry-pick은 0건, 기존의 진화한 시험으로 대체되어 이식하지 않은 항목은 3건, 현재 속성의 빠진 부분을 재작성해 추가한 시험은 1건, 대상 기능 부재로 적용하지 않은 항목은 1건이다. Definer 정책과 role migration 기대값의 현재 통합 불일치는 별도 실결함으로 바로잡았다.

## 실행·되살림 근거

- 최종 테스트 트리 SHA `7779d8cf8126f6a17ba41e34bf9ef97282ab3d68` (implementation `7956355`와 integration `0979fe6` 병합), branch `agent/codex/recover-guard-tests`, clean tree. KST 2026-09-21 23:16–23:18. Interpreter `C:/Project/SaintVision-Invion/.venv/Scripts/python.exe` 3.14.6.
- Command: `C:/Project/SaintVision-Invion/.venv/Scripts/python.exe -m pytest -q --tb=no tests/core/test_recovery_capability.py tests/core/test_recovery_verdict.py tests/core/test_definer_audit_cli.py tests/integration/test_definer_audit.py tests/integration/test_recovery_drill.py::test_failed_integrity_drill_is_recorded_without_measurements tests/test_migration_role_guard.py` — exit 0, **89 passed, 0 skipped**, 1 Alembic `path_separator` deprecation warning, 28.47s. PostgreSQL-dependent tests used a newly created, uniquely named PostgreSQL 16 container with Codex owner/task labels and explicit `INV_TEST_ADMIN_DSN`/`CX01_CONTAINER`; role-shape tests created their own opt-in isolated PostgreSQL container. No ambient/operational DB was used.
- The first live run exposed stale policy revision/function coverage and the stale migration-head literal: 21 definer cases could not establish a clean baseline and the safe-upgrade role case expected 0037 while the database correctly reached 0045. No product pass was claimed from that run. After updating the policy and deriving the head, the exact-tip run above passed. The initial failure trace rendered the one-run disposable DB DSN in an assertion representation; no credential was copied to this document, and the uniquely owned PostgreSQL container was removed and its absence verified.
- **Red/green resurrection proof:** temporarily weakened `tools/recovery_drill.py` so it recorded RPO/RTO whenever numbers were present, even when integrity was false. The new test failed (`1 failed`, exit 1); source was restored. The exact merged-tree rerun then passed all 89 selected tests. This demonstrates the recovered assertion carries weight.
- **Live DB mutation coverage:** current definer integration cases exercise real catalog mutations and restoration; role-guard cases create deliberately unsafe roles and require migration refusal. These guard properties are directly exercised by the passing selected run. CI was not run; browser, physical-node and live application acceptance are outside this task.
- Disposable test PostgreSQL was removed only after checking its name and owner labels; label-filtered inventory reported zero task residue.

## 남은 범위

- GOV-ALERT-001은 규격만 있고 evaluator 도구가 없다. `test_alarm_check.py`를 초록색으로 복구했다고 보고하지 않는다. 다음 구현 담당은 알람 evaluator와 그 DB-free/DB-backed 검증 계획을 별도 카드로 만든다.
- Definer audit는 now-current 0045 policy와 실 DB audit로 검사된다. Hosted CI에서 실행되는 독립 결과는 아직 없다.
- 이 회수 작업의 독립 reviewer는 pending이며 Codex 작성 결과를 Claude 승인으로 표시하지 않는다.
