---
doc_id: "REGRESSION-JUNIT-40e921b-CLAUDE-001"
title: "전체 회귀 junit 산출물 기반 — HEAD 40e921b. 5배치 산술 합·커버리지 대조. 값 미기록"
version: "1.0.0"
status: "review"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-20T13:00:00+09:00"
head_commit: "40e921b"
source_of_truth: "Git"
tags: ["saintvision", "regression", "junit", "artifact-backed", "batch-sum", "coverage-reconciliation"]
---

# 전체 회귀 junit 산출물 기반 — HEAD 40e921b

앞선 종합(`2026-09-19_통합_53f81ba_전체회귀_종합`)의 `2180 passed`는 **터미널 요약 말고 산출물이 없었다.** Codex가 432 집계를 철회한 이유가 정확히 그것(산출물 없는 배치 집계)이었으므로 같은 기준을 내 숫자에도 적용해, **`--junitxml`로 보존 가능한 산출물을 남기고 다시 돌렸다.** 배경 27분 실행이 호스트 부하로 반복 kill되어 **5개 배치로 나눠 각각 junit을 남겼다.** 아래 수치는 **5배치의 산술 합이며 단일 완결 실행이 아니다.**

## 실행 정본 (정확 기록)
- **인터프리터(절대경로)**: `C:/Project/SaintVision-Invion/.venv/Scripts/python.exe` (시스템 `C:\Python314` 아님)
- **HEAD**: `40e921b` (배치 실행 중 통합 병합으로 이동. 앞선 터미널-only 2180은 `53f81ba` — **다른 커밋**이다. 아래 델타 참조)
- **환경**: `INV_TEST_ADMIN_DSN`=일회용 PG16(값 마스킹), `CX01_CONTAINER` 미설정(= 원래 2180 조건)
- **KST**: 2026-09-19 17:50~18:10 (배치 5개 순차)
- **배치별 명령 형식**: `<interpreter> -m pytest <배치 파일집합> -p no:cacheprovider -q --junitxml=<경로>`
- **산출물(마스킹 커밋본)**: `docs/vault/30_Development/Evidence/regression-40e921b-20260919-1755/junit-40e921b-20260919-1755-{1-core,2-rootA,3-rootB,4-intA,5-intB}.xml` + 배치별 `manifest-*.txt`(각 배치가 돌린 파일 목록) + `manifest-0-full-collect-only.txt`.

## 배치별 결과 (junit testsuite 속성 = 권위 수치)
| 배치 | 파일집합 | tests | passed | failed | errors | skipped | 시간 |
|---|---|---|---|---|---|---|---|
| 1 core | `tests/core/*.py`(40) | 631 | 628 | 0 | 0 | 3 | 2m15s |
| 2 rootA | `tests/*.py` 앞 39 | 504 | 489 | **1** | 0 | 14 | 9m16s |
| 3 rootB | `tests/*.py` 뒤 39 | 667 | 653 | 0 | 0 | 14 | 7m42s |
| 4 intA | `tests/integration/*.py` 앞 35 | 428 | 255 | 0 | 0 | 173 | 4m02s |
| 5 intB | `tests/integration/*.py` 뒤 34 | 398 | 167 | 0 | 0 | 231 | 1m50s |
| **산술 합(5배치)** | | **2628** | **2192** | **1** | **0** | **435** | |

## 커버리지 대조 (겹침·누락 확인 — 합계가 전체를 덮는다는 근거)
- **전체 대상** `pytest tests/ --collect-only` = **2628 collected items** (177 test-보유 경로).
- **배치 산술 합 tests = 2628** → **collect-only와 정확히 일치.** 겹치면 2628 초과, 빠지면 미만이므로 **중복 계수 0·누락 0**.
- **파일 union 대조**: 배치들이 돌린 파일 union에 대해 "전체엔 있으나 union에 없는 파일 = **0**"(구조적으로 core=하위폴더, rootA/B=root `*.py`의 disjoint 반, intA/B=integration `*.py`의 disjoint 반). manifest 파일로 검증 가능.
- 즉 **이 합계는 전체 대상을 정확히 한 번씩 덮는다.** (단일 실행처럼 인용 금지 — 5배치 산술 합.)

## 유일 실패 1건 = 환경 (오늘 작업 귀속 아님)
`tests/test_account_integration.py::test_published_migration_heads_upgrade_without_rewriting` — `subprocess.run(check_migration_upgrade.py, timeout=180)`가 부하 중 **`TimeoutExpired`**. **격리 실행 시 도구는 exit 0·전 revision PASS**(definer-policy 10개 정상). 부하 타임아웃 여유 문제이지 제품 결함 아님.

## 앞선 53f81ba 터미널 수치(2180/2/18/417)와의 델타 — 근거 있는 차이
1. **다른 커밋**: 40e921b는 53f81ba 이후 커밋 포함.
2. **recovery 18 errors → 이유보이는 skip**: Codex의 `recovery_drill_prerequisites`(40e921b 유입)가 `test_recovery_drill.py`를 컨테이너 전제 미비 시 **error가 아니라 reason-visible skip**으로 전환. junit에서 recovery 백업/복원 테스트가 `<skipped>` 태그(예: `test_real_backup_restore...` skipped). → errors 18→0, skip +18.
3. **factory_route 통과**: 이번엔 `.work` 디렉터리를 미리 만들어 `.work/vf-route-gap.json` 쓰기가 성공(앞선 run의 FileNotFoundError 해소). → failed 2→1.
결과적으로 40e921b 산술 합은 `2192 passed / 1 failed / 0 errors / 435 skipped`. 앞선 2180은 터미널-only·다른 커밋이므로 이 junit 수치로 대체한다.

## Docker 자원 (배치 사이 기록 + 전후)
| 시점 | 컨테이너 | 볼륨 | 네트워크 |
|---|---|---|---|
| 회귀 前(정본) | 48 | 79 | 11 |
| 배치1-3 후(내 PG 포함) | 49 | 80 | 11 |
| int_A 후 | 49 | 80 | 11 |
| int_B 후 | 49 | 80 | 11 |
| 내 자원 정리 후 | **48** | **79** | **11** |
내 PG(1컨테이너+1익명볼륨)를 빼면 전 구간 48/79/11 고정. **누수 0.** 어느 배치도 자원을 늘리지 않았다(int 배치는 recovery가 전제-skip이라 sv-rpo 컨테이너를 만들지 않음). 익명 69 보존·보호 4 미접촉·svcx01/sv-rpo 잔존 0.

## Node (재확인 불필요 — 앞 종합대로)
vitest 31 files/307 passed, `reproduce_handoff_review.mjs` 정상0·self-test1.

## backstop 갭 재규정 (사소함 아님 — Codex 인계)
`cleanup_owned_docker.py`의 인정 소유 라벨 7개(`ai.saintvision.acceptance/bridge-test/developer-studio/kernel-test/remote-test/test/upgrade-test`)에 **recovery 자원 라벨 `ai.saintvision.rpo-test`가 없다.** 이것은 사소한 갭이 아니다: 1차 정리는 fixture teardown이지만 **teardown이 못 도는 상황(run 중단·크래시)이 바로 backstop이 필요한 순간**인데, 그 라벨이 인정 목록에 없으면 `cleanup_owned_docker.py`가 orphan `sv-rpo-*`를 **"ownership label absent"로 건너뛴다.** 즉 **가장 필요한 순간에 없는 안전망**이다. 오늘 내가 중단시킨 recovery run의 sv-rpo orphan을 도구가 아니라 **내가 손으로 지운 것**이 그 증거다. 권고: `rpo-test`(및 recovery가 쓰는 다른 라벨)를 `OWNERSHIP_LABELS`에 추가해 backstop이 실제 필요 상황을 덮게 하라. (도구 소유 Codex.)

## 방법·제약 준수
`.venv` 인터프리터, 일회용 PG16(고유명 `svcx01-*`, 내 라벨), 종료 후 **내 자원만 제거·잔존 0**. 보호 컨테이너 4·익명 69 미접촉. junit은 DSN 값 마스킹 후 커밋(원본은 `.work` 로컬, 세션 종료 시 제거). 합계는 **5배치 산술 합**이며 커버리지 대조로 전체를 정확히 덮음을 확인.
