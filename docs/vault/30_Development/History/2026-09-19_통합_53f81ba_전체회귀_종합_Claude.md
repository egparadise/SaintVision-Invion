---
doc_id: "REGRESSION-INTEGRATION-53f81ba-CLAUDE-001"
title: "통합 tip 53f81ba 전체 회귀 종합 — 실 PG + Node. 실행-통과/실행-실패/미실행 분리"
version: "1.0.0"
status: "review"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-20T11:00:00+09:00"
target_commit: "53f81ba"
source_of_truth: "Git"
tags: ["saintvision", "regression", "integration", "postgres", "docker-leak", "node"]
---

# 통합 tip 53f81ba 전체 회귀 종합

오늘 통합에 50+ 커밋이 들어갔고 개별 검증은 됐으나 **전체가 함께 돈 확인은 없었다.** 통합 tip `53f81ba`에서 전체 회귀를 돌렸다. 조건 준수: **반드시 `.venv/Scripts/python.exe -m pytest`**(시스템 python 함정 회피), **일회용 PostgreSQL 16 기동해 `INV_TEST_ADMIN_DSN` 필요 시험까지 실제 실행**, **Node(vitest+mjs)** 포함. 생성 자원은 내 소유 라벨을 붙이고 정리했으며, `saintvision-lan-db*`·`saintview-orthanc*`·익명볼륨 69개는 건드리지 않았다.

## 결론
**오늘 작업에 귀속되는 실패 0.** 전체 회귀 `2180 passed / 2 failed / 18 errors / 417 skipped`(27분)에서 실패·에러는 **전부 환경**(setup 전제/부하 타임아웃/플랫폼)이고 제품 회귀가 아니다. **Docker 누수 0**(전후 48/79/11 동일). Node green.

## ① 실행되어 통과 (2180)
core 전체 + PG-게이트 통합시험(오늘 내가 붙인 funnel `match` 편집이 스위트 안에서 재통과) + registry/credential/provisioning 등. 실 PG(일회용 PG16, 127.0.0.1:포트)로 DSN 의존 시험이 실제 실행됐다.

## ② 실행되어 실패 (2 failed + 18 errors) — 오늘 귀속 0, 전부 환경
| 항목 | 원인 | 오늘 회귀? | 판별 근거 |
|---|---|---|---|
| **18 errors** `test_recovery_drill.py` | `args` fixture가 `CX01_CONTAINER`(또는 DSN host=컨테이너명)로 `docker inspect` + `ai.saintvision.cx01` 라벨을 요구. 내 초기 setup은 host=`127.0.0.1`·라벨 불일치 → 전제(L33 "Disposable PostgreSQL container unavailable")에서 error | **아니오(pre-existing)** | `git show 77c5311:...`에 **동일 fixture**. proper setup(CX01_CONTAINER + `ai.saintvision.cx01` 라벨)으로 재실행하니 **18 error 소멸·시험 실행·통과**(격리, 1→2 순서, cleanup 로직 8건 전부 PASS) |
| **Failure #1** `test_vf_canonical.py::test_factory_route_measurement_is_not_fixture_union` | `.work/vf-route-gap.json` 쓰기 시 `.work` 디렉터리 부재 → `FileNotFoundError`(테스트가 부모 dir 미생성) | **아니오** | 77c5311에도 존재. fresh worktree엔 `.work` 없음(메인 체크아웃엔 잔존). 환경 |
| **Failure #2** `test_account_integration.py::test_published_migration_heads_upgrade_without_rewriting` | `subprocess.run(check_migration_upgrade.py, timeout=180)`가 부하 중 **`TimeoutExpired`**(로그 `180.02s call`, `raise TimeoutExpired(...,180)`) | **아니오** | 도구를 격리 실행하면 **exit 0·전 revision PASS**(definer-policy 10개 정상). 부하 타임아웃 여유 문제 |

**판별법**: 오늘 이전 `77c5311`과 대조(fixture 동일=pre-existing), 실패 항목 격리 재실행(통과=환경). 오늘 들어온 코드로 깨진 것은 없다.

## ③ 실행되지 못함 (417 skipped) — 각 사유
전부 **이유 보이는 skip**이며 PG 미설정 때문이 아니다(PG 설정돼 PG 시험은 실행). 사유별:
- **플랫폼(win32, Linux 전용)**: "Actual Linux file backend", "Linux PTY", "Linux private storage", "Linux first/business Workspace execution", "Linux publication contract", "Linux scoped handles" 등.
- **opt-in CI 전용 Docker**: "Real Linux Docker runtime explicitly enabled only in isolated CI"(node runtime/storage/placement/shard 등).
- **이미지 env 필요**: "Explicit candidate image and disposable container DB address required"(image lane).
- 2 deselected(설정).
즉 미실행은 **플랫폼·opt-in·이미지 env** 전제이지 오늘 작업의 미검증이 아니다. (recovery 18건은 위 ②대로 setup 보강 시 실행·통과 확인.)

## Node
- **vitest**: `31 files / 307 tests passed`(통합 tip apps/web = 메인 체크아웃, 0줄 차이 확인).
- **`reproduce_handoff_review.mjs`**: `<folder> <typescript.js> --output <scratchpad>`로 정상 **exit 0**(6 PASS/0 FAIL/6 UNVERIFIED), `--self-test` **exit 1**(주입 mismatch 감지 = 비공허). `--output`으로 저장소 clean.
- `verify_two_pc_distributed_execution.mjs`·`reconcile_receipts_evidence.mjs`: `node --check` OK.

## 오늘 많이 바뀐 Docker 영역 (사용자 최우선 관심)
- **cleanup 로직 8 시험 전부 PASS**: `test_docker_volume_provenance.py`(신규 — PITR ephemeral / workspace·pilot named volume), `test_cleanup_owned_docker.py`(5, `test_symmetric_container_flag_loss_is_detected_by_daemon_count` 포함), recovery cleanup-소유권 시험. **상호 간섭 없음**(1→2 순서 통과).
- **내 이전 검토 주 finding이 반영됨**: `cleanup_owned_docker.py`에 `_engine_counts()`("daemon-owned counts, **independent of any `docker * ls` flags**")가 추가돼, 균일 플래그 손실을 daemon count로 잡는다 — 내가 권고한 진짜 독립 소스. `test_symmetric_container_flag_loss_is_detected_by_daemon_count`가 검증.

## Docker 자원 전후 (누수 역추적)
| | 컨테이너 | 볼륨 | 네트워크 |
|---|---|---|---|
| 회귀 前(정본) | 48 | 79 | 11 |
| 최종(내 자원 정리 후) | **48** | **79** | **11** |
**Δ0/Δ0/Δ0 — 누수 없음.** 볼륨은 전 구간 79 무변동(익명 69 보존). 중간에 관측된 +3 컨테이너/+3 네트워크는 **내가 killed/timeout으로 중단시킨 recovery 재실행의 `sv-rpo-*` orphan**(중단 시 pytest teardown 미실행)이며 내가 정리했다 — 제품 cleanup 결함이 아니라 내 run 중단의 산물(완주한 격리 run은 무누수). **사소한 관찰**: recovery 자원 라벨 `ai.saintvision.rpo-test`는 `cleanup_owned_docker.py`의 7개 인정 라벨에 없어 그 backstop 도구로는 안 쓸린다(1차 정리는 fixture teardown) — Codex가 판단할 minor 갭.

## 방법·제약 준수
`.venv` 인터프리터 사용, 일회용 PG16(고유명 `svcx01-*`, 내 라벨), 종료 후 **내 자원만 제거·잔존 0 확인**. 보호 컨테이너 4건·익명볼륨 69개 미접촉. 실패 판별은 77c5311 대조 + 격리 재실행. 값 미기록.
