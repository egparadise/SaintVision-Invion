---
doc_id: "HISTORY-VF-CX-04-VERIFY-001"
title: "2026-09-15 VF-CX-04 Codex 검증보고"
version: "1.0.0"
status: "review"
author: "Codex"
updated: "2026-09-15T12:59:40+09:00"
source_of_truth: "Git"
---

# VF-CX-04 검증 및 인계

owner Codex, reviewer Claude 미수신/미검토. branch agent/codex/vf-cx-04, base4761a8ccc916c9f829fc7338618fa936de68a84c. [[모델 실행 입력과 대체 Node 복구 계약]].

## 작업한 것

- ModelExecutionRef를 기존 WorkloadSpec에 추가하고 Python/TS/Go/Node validation schema를 생성했다. workspace start/resume/terminal과 모델 입력을 동시에 섞지 않는다.
- 실제 bytes를 검증하여 bounded snapshot으로 고정하고 원본 Catalog snapshot을 함께 저장한다. 기존 ApprovalStore/ToolGateway/DeliveryQueue에서 동일 workload/requester/현재 권한/Node/Lease/저장소 버전/bytes hash를 검증한다. 기존 signed initialized Workspace와 Node durable inbox/결과 수집 경로를 재사용한다.
- ModelRetryStore는 실패한 Run의 물리 Lease 해제 후 같은 모델 버전으로 새 Run을 만든다. 새 승인·새 fence를 요구하고 cancelled와4번째 세대를 거부한다. child/placement/Lease/model input/lineage/outbox를 같은 transaction에 commit한다.
- migration0041/0042는 forward-only, FORCE RLS/SELECT INSERT/immutable trigger/FK를 사용한다. 이전 migration, Lease/fencing/idempotency, definer hash와 grant를 변경하지 않는다. 임시 always-deny helper를 제거하고 실제 model reference 검사를 정본으로 사용한다.
- supported: CPU python-files/single-node, 기존32KiB content/64KiB snapshot, 대체 Node의 새 승인 실행. GPU/collective/offload/대용량 remote prefetch는 미지원이며 명시적으로 거부한다. 실제5대/운영 SSO/공개 배포를 주장하지 않는다.

## 확인한 것

- Windows Python3.14 / 격리 tmpfs PG16: `tools/run_vf_security_tests.py`로 runtime/model/locality/core 선택92/92, retry/runtime/migration 경계34/34 통과(exit0). 초기90/2 실패는 frozen contribution version 비교 누락을 수정한 기록이며 보존했다.
- Linux Python3.12 / Go1.27.1 / PG16 / 독립 Go Node keys/journals/mTLS endpoints: `tools/check_kernel_docker.py --go <Go1.27.1> --postgres-image postgres:16 --tests tests/integration/test_model_node.py tests/integration/test_model_retry.py tests/integration/test_model_runtime.py tests/integration/test_model_locality.py tests/integration/test_tool_admission.py tests/integration/test_definer_audit.py tests/test_account_integration.py`: **140/140 passed, exit0**, cleanup 모두 true. sourceHashes와 현재 모든 검사 대상 bytes가 같음을 확인했다.
- 6개 model Evidence: single(계산7), output-recovery(재실행 없이 완료), cancel/stale/node-loss(processStarted=false), replacement-node(서로 다른 peer2개, 시도2회, 성공1회). 모든 경우 잔여 Lease0. 한 Docker host의 격리 시험이며 실제 두 PC가 아니다.
- 초기 Linux143 중142 passed/1 fixture failure(immutable claim 수정), 이후68 중67 passed/1 fixture error(URI중복)도 보존. [[2026-09-15_VF_오류와_해결]]. 최종140 통과로 수정 확인.
- TypeScript strict compile exit0; Go contracts compile exit0(no test files); 19 schema export·문서·ontology·diff 검사 exit0.
- backend candidate `saintvision-backend-candidate:vf-cx-04`, image sha256:9ca325b1f980bcbafc3ab6de26e7ff195ac115f18f38b1966340ebdd35340ef8 build exit0. 이 이미지를 지정한 전체 Windows 회귀는 진행 중이며 결과를 이어 기록한다. Linux 전용 recovery_drill은 이 Windows 실행에서 제외하고 이전 별도 Linux 증거를 유지한다.
- Evidence: `docs/vault/30_Development/Evidence/vf-cx-04/`. private 로그/DSN/key는 게시하지 않았다.

## 이어서 할 첫 행동

Codex: commit/push/같은 SHA CI/Obsidian 인계 후 전체 Windows 회귀 결과를 보강한다. VF-CX-05는 실제5대·W2~W4 인수 선행조건을 읽기 전용으로 점검하고 미확인/외부 blocker를 기록한다. CI·독립 검토·운영 인수는 미완료이며 done이 아니다.

Claude: 0041/0042 RLS와 retry lock order/원자성, Catalog 재검증, 승인 입력 무시/교체 방지를 독립 검토하고 서비스 API를 정본 계약에 연결한다. Gemini: 모델 지원 한도·승인 상태·대체 Run lineage와 실제 API를 화면에 연결한다. 다른 Agent의 수신/검토/완료를 대신 표시하지 않는다.

기존2775/4800=57.81% 유지. 새 Codex VF 운영 인수0/5. 구현·로컬 성공으로 운영 인수를 올리지 않는다.

## 전달 checkpoint

- code a42178ab32bf72fc4b9ca83cdc9979690265250b push exit0. CI Backend34927086173/Core34927086146/Docs34927086105는 billing 한도로 job 시작 전 거부. 로컬 성공과 CI 성공을 구분한다.
- 전체 Windows 회귀는 같은 product source와 새 backend image로 진행 중. Linux140 및6개 model Evidence는 sourceHashes 일치 확인. 독립 reviewer Claude는 아직 수신/검토 미확인.
