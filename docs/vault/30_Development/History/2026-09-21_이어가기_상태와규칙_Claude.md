---
doc_id: "CONTINUE-STATE-AND-RULES-CLAUDE-001"
title: "이어가기 — 2026-09-21 정본 (오늘 바뀐 것·규칙·미검증·결정대기·다음·무엇이 틀림을 잡았나)"
version: "2.0.0"
status: "active"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-21"
timezone: "Asia/Seoul"
source_of_truth: "Git"
tags: ["handoff", "continue", "canonical", "rules", "what-caught-it"]
---

# 이어가기 — 2026-09-21 정본

새 컨텍스트는 **CLAUDE.md → 이 문서**만 읽고 「## 5. 다음 첫 행동」부터 시작. 세부는 각 History/Governance 문서를 가리킨다(여기 다시 쓰지 않음 = 규칙5). 검사 보고엔 provenance(어느 SHA/트리/인터프리터). 인터프리터 `.venv/Scripts/python.exe`. 트리는 세션 중 계속 이동하므로 **고정 SHA로 못 박고** 커밋은 개인 index로([[공유워크트리_개인index_커밋규칙]]).

## 1. 오늘 바뀐 것
- **계약 결속(내 레인, 커널)**: RunResultView·RunArtifactList·RunLogView·RunAttemptList·ModelCommitObservation·ShardObservation·WorkspaceEditView·StorageObservationView. fixture+Python 계약시험+양방향 Ajv. (Codex: run/approval 페이지·pool/placement/distributed·ProblemDetails 앵커·NodeStopReceiptView 개명.) 프런트 배선은 Gemini.
- **서빙 앵커 시험**: 위 커널 응답이 "앵커가 있다"에서 "앵커를 지우면 시험이 깨진다"로. `tests/core/test_serving_anchors.py`(7), 버킷2 7→0. [[2026-09-21_서빙앵커_감사_3갈래_Claude]].
- **검사 도구 2**: `tools/check_contract_bindings.py`(fixture 커버리지+서빙앵커 커버리지+죽은-계약 탐지). 규칙을 검사로: [[계약검증_자동화대판단_검사목록]].
- **보안 경계 2(Codex, 내가 독립검토)**: discovery announce 테넌트 강제([[2026-09-21_discovery_tenant_enforcement_독립검토_Claude]]) + 운영자 발급 자격증명([[2026-09-21_운영자자격증명발급_독립검토_Claude]]) — 익명 온보딩 닫고 운영자 경로로 다시 엶.
- **화면(Gemini)**: types.ts 생성타입 수렴, RunLog/RunResult/VF-GM 배선, VF-GM-03/04/05. (가짜값·합성 정리는 진행 중: [[2026-09-21_계약이_못잡는_가짜값_목록_Claude]].)

## 2. 오늘 세운 규칙·축 (한 질문의 변형: "있다 ≠ 작동한다")
- **규칙**: (1) 외부 사실 3-갈래[측정/외부조회/미확인추정] (2) 빈 결과는 사실 아님(대조군·exit·stderr로 계측 확정) (3) 정정은 읽히는 자리를 다 고쳐야 끝(유효 지시 한 곳) (4) 개인 index+rev-range 커밋([[공유워크트리_개인index_커밋규칙]]) (5) 유효한 것은 한 곳에서 읽히게(이 문서가 그 자기적용).
- **축 6층**: 초록≠도달 / 배선≠통과 / 통과했으나 다른 이유 / 방어가 엉뚱한 것 / 계약 있으나 서빙 미강제(앵커 없음) / 앵커 있으나 무게 없음. 자동/판단 분리: [[계약검증_자동화대판단_검사목록]].
- 상세 규칙 근거: `memory:` knowledge-cutoff / empty-output / passing-but-never-reached / integration-branch-moves-midsession.

## 3. 미검증으로 남은 것 (한 목록으로 단일화 — 이중 기록 금지)
- **실-인프라 통합 목록**([[2026-09-21_운영자자격증명발급_독립검토_Claude]]의 "미검증 통합 목록"): ① discovery 자격증명 실 PG integration ② Go inv-discover 바이너리(컴파일·main.run·announce 주입방지 분기=Go지도 T1/T2) ③ 교차언어 e2e(Go 클라↔Python 서버=T3) ④ 물리 Node 흐름·보호 전달 채널. 소관 Codex+운영자, 시점 PG/Go/CI/test-서버 가용 후. Go 지도([[2026-09-21_Go검증밀도지도_미검증표면_Claude]])가 이 목록을 가리킴.
- **언어별 검증 밀도 차이**: Python/프런트 깊음(결속·앵커·되살림), Go 0(소스만). 못 돌리는 PG/Go 시험은 만들지 않음.

## 4. 사용자 결정 대기
- **CI 결제/개방**: 열리면 Go 컴파일·node 단위·검사 CI 배선이 해소(위 미검증 상당수). 현재 미개방.
- **공개 이력의 DSN 회전**: 과거 커밋에 노출된 DSN 회전 여부·시점 결정.
- **LegacyProjectCatalog 처리**: bound인데 서빙 엔드포인트 없음(죽은 계약). 예정 배선 vs 계약·fixture·시험 제거 — Codex/saintvision. (내가 안 지움.)
- (참고) node 리스트/상세: 소비되나 response_model 없음 → Codex 데스크톱 레인 결속 대상.

## 5. 다음 첫 행동
- Codex 운영자 CLI가 integration에 올라오면 **그 SHA로 재검토**(지금은 브랜치 8b79382에서 봄; DB-층 SET ROLE·회전쓰기는 실 PG 대기). 발급 한도(10/24h) 착지 시 그 시험 확인.
- 겹치지 않는 것: 검사 CI 배선(층2)·자리표시자 린트·fixture-schema 게이트(자동화 목록에서). 방향은 사용자 지시 대기.
- 트리·커밋 규칙 준수(개인 index, dirty 공유파일 미접촉, rev-range 검증).

## 6. 오늘 틀린 것 — 누가가 아니라 무엇이 잡았나 (다음에 무엇에 의존할지)
- **되살림(앵커/가드 제거→시험 깨짐)**이 잡음: 내 결속 6개가 버킷2(앵커는 있으나 시험이 서빙 미행사); 발급 권한 가드가 실제로 거부·비밀차단을 만든다는 것(제거하니 비밀 유출); 테넌트 가드 403.
- **독립 검토(고정 SHA·내 손 실행)**가 잡음: ShardObservation 서빙 앵커 부재(→Codex 보강); discovery 부트스트랩 갭(순환 아님·runbook 문제); ProblemDetails 비정형 코드 취약(라이브 트리거 0).
- **대조군·실측(추정 금지)**이 잡음: go 1.27.1 오판(외부조회); RunAttemptList "라이브 소비" 오판(호출경로); NodeStopReceipt·죽은-계약 검사의 오탐(스크립트=리드, 실제 코드 대조·**양쪽 검증**); LegacyProjectCatalog(전수 grep으로 추정→측정).
- **양방향 확인**이 잡음: 수기타입 드리프트(Python+Ajv 둘 다 거부); fixture 실수(objectId·checkoutId=uuid — validate 실행이 포착).
- **stage·rev-range 검증**이 잡음: 공유 index 사고 양방향(Gemini 파일이 내 stage에 / 내 파일이 Gemini 커밋에).
- **가장 쓸모 있는 교훈**: 가장 위험한 오류(LegacyProjectCatalog, 내 버킷2, 가드 제거 시 비밀 유출, 내 검사의 오탐)는 **오늘의 축을 우리 자신의 산출물에 되돌려** 잡혔다. "있다"를 증거로 읽지 말고 "작동한다"를 별도로 확인하라 — 남의 것에도, 내 것에도.

## 인계
reviewer: Codex. 프런트 배선: Gemini. 오늘 커밋은 전부 개인 index로 무사고 착지(rev-range 검증). 새 컨텍스트는 「5. 다음 첫 행동」부터.
