---
doc_id: "CONTINUE-STATE-AND-RULES-CLAUDE-001"
title: "이어가기 — 오늘 상태·남은 것·다음 첫 행동 + 오늘 세운 규칙 5 (한 곳)"
version: "1.0.0"
status: "active"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-21T23:00:00+09:00"
timezone: "Asia/Seoul"
integration_tip_at_write: "5914f04"
source_of_truth: "Git"
tags: ["handoff", "continue", "rules", "contract-map", "claude-memory"]
---

# 이어가기 — 새 컨텍스트가 이 문서만 읽고 바로 시작

이 문서가 곧 기억이다. 새 컨텍스트는 **CLAUDE.md → 이 문서** 순으로 읽고 「## 다음 첫 행동」부터 시작한다. 매 검사 보고는 `python tools/provenance.py -- <검사>`로(실행 시점 환경·tip 자동 기록). 인터프리터는 항상 `.venv/Scripts/python.exe`.

## 오늘 한 것 (2026-09-21, 전부 통합 반영됨, tip 5914f04)
1. **CI 사전 점검**(`docs/vault/40_Governance/Errors/CI-PREFLIGHT-CONFIG-CLAUDE-001.md`, v1.3.1): 결제 열기 전 설정-원인 빨간불 제거. **①** image-opt-in 3파일(test_web_container/test_workspace_upgrade/test_lan_storage_install)이 backend·core no-skipped 단언을 깸(junit 22/22 skip, before-red/after-green 실증) → **Codex가 core.yml에서 분리 적용 완료**. **②** go 1.27.1은 철회(출시됨). **③** 하드코딩 개수(browser 6/docker-host 2)를 여정 이름 집합·케이스 이름 집합 대조로 유도(패치문안 그 문서에). ①③ **Codex 인계 완료**(사용자가 직접 전달).
2. **funnel 전량**(PG-only, `docs/vault/30_Development/History/2026-09-21_funnel잔여_PG전용처리_Claude.md` v1.3.0): binding·runtime·model_commit·model_locality + control_api·approvals·containment·dispatch_queue·registry_revalidation·lan_bootstrap. bare `pytest.raises(DomainError)`에 **ZZPROBE 실측 per-case match**(혼합) / 단일은 실측 확인 후 uniform / 양방향 돌연변이 판정. **남은 것: containment `test_control_between_start_reservation`[kill/drain](L399) 하나** — 실측 SKIP("Real Linux Docker runtime … isolated CI" = **node runtime 필요**, PG-only 아님)이라 추측 없이 bare 유지, node-runtime 레인에서 처리.
3. **run result/artifacts 계약 결속**(`docs/vault/30_Development/History/2026-09-21_run결과_artifacts_계약결속_Claude.md`): 공유 fixture `contracts/fixtures/run-result-view.json`·`run-artifact-list.json` + `tests/core/test_run_result_contract.py`(fixture↔커널 계약 `validate_contract` 결속 + 필수필드 음성대조, 4 passed, PG 불필요). 변형 양방향(Python `validate_contract`·프런트 Ajv 둘 다 필수필드 제거 시 실패). **프런트 배선은 Gemini 인계**(계약 시험·types 결속·FB-03 모의 대체 — 사용자가 Gemini에 직접 전달).
4. 그 외 오늘: provenance 도구(`tools/provenance.py`) 제작·env_gates as-of·wrap 상대경로 수정·integration_sync; 주 체크아웃을 통합 tip에 detached 동기; DSN 마스킹 독립검증; migration-head 타임아웃 누수 회수; Linux-게이트 감사(CI-fail 계층).

## 오늘 이어서 (2026-09-21 18시대, tip 7ab955b 위)
5. **Codex run/approval 페이지 계약 독립 검토 완료**(고정 SHA `3052038`, 실질 코드 `7a9b500`) — **sound, 병합 무방**. 4개 wire 사본 바이트 동일·생성 타입 정합·Python 7 passed·프런트 Ajv 6 passed·schema-level nextCursor 제거 양쪽 DomainError 실행 확증. 상세·트리 못 박기: [[2026-09-21_run-approval-page-contract_독립검토_Claude]].
6. **run-logs(RunLogView) 계약 결속 완료** — `contracts/fixtures/run-log-view.json` + `tests/core/test_run_log_contract.py`(**3 passed**: fixture↔커널계약·7필드 load-bearing·source const). 변형 양방향(Python VAL-0002 + 일회용 Ajv). **실측 드리프트**: 프런트 수기 `RunLogView`(types.ts:563)가 계약보다 느슨(`source` `\|string`으로 const 무력화, `truncated`/`absentReason` optional인데 계약은 required). **프런트 배선(어댑터 신설+타입 정합+Ajv 시험)은 Gemini 인계**: [[2026-09-21_run-logs_계약결속_Gemini인계_Claude]]. (사전확인: logs 소비 어댑터는 현재 부재 = run-result와 동일하게 Gemini가 소비 배선.)

## 남은 것
- **containment L399**(node runtime 필요) — funnel 유일 잔여.
- **계약 지도 미결 항목들** — 묶임: projects-list(Codex)·run-result/artifacts(Claude)·run/approval 페이지(Codex, 병합 대기)·run-logs(Claude)·run-attempts(Claude). 남은 내 레인 접근은 개별 결속이 아니라 **수기 타입 전수 드리프트 훑기**로 전환(다음 첫 행동). replica/model/shard/node 관측은 Codex 레인(겹침 주의).

## 오늘 이어서 (계속, tip d438db8 위)
7. **run-attempts(RunAttemptList) 계약 결속 완료** — `contracts/fixtures/run-attempt-list.json` + `tests/core/test_run_attempt_contract.py`(**4 passed**: fixture↔커널계약 · envelope 5필드 load-bearing · **attempt 8필드 load-bearing** · source const). 양방향(Python VAL-0002 + Ajv). **드리프트 4종 실측**(상세 [[2026-09-21_run-attempts_계약결속_Gemini인계_Claude]]): (a)const 무력화(`source |string`) (b)required→optional 6필드 (c)필드누락 없음 (d)**nullability 좁힘**(startedAt/nodeId를 계약은 null허용인데 타입은 non-null — 역방향·가장 위험). 프런트 배선 Gemini 인계.
   - **정정**: 지난 턴 "agentEngine/recoveryEngine 라이브 소비"는 **오판**(주석의 단어 'attempts' 오탐). 실측 결과 RunAttemptList/RunAttemptItem은 **정의만·소비처 없음**(RunLogView와 동일 dormant). 소비처 없음도 실측으로 확정.

## 오늘 이어서 (계속) — 수기 타입 드리프트 전수 훑기 **완료**
8. **드리프트 전수 훑기 완료**(고정 SHA `d416fe5`) — 상세·검증된 지도: [[2026-09-21_수기타입_계약_드리프트_전수훑기_Claude]]. 범위: types.ts 인터페이스 48 중 $def 동명 11 대조. **검증 결과**: RunResultView(소비O·결속됨, (b)+(d) 다수)·ProblemDetails(소비O·**미결속**, causeRef/evidenceId (b)+(d))·RunArtifactList·RunLogView·RunAttemptList에 드리프트. **NodeStopReceipt 11건은 오탐(이름충돌)**로 제외 — 스크립트 출력을 실제 대조로 검증(초록≠도달). 사각: 동명만 봄(RunAttemptObservation↔RunAttemptItem 같은 이름 다른 대응은 수동). **근본 권고: 수기 타입을 생성 타입(`packages/contracts-ts`) re-export로 대체**(Codex의 kernel-observation.ts 방식) — 드리프트 클래스 근원 제거.

9. **ModelCommitObservation 계약 결속 완료**(사용자 선택 2: 다른 `_checked`/`validate_contract` 응답) — `contracts/fixtures/model-commit-observation-response.json` + `tests/core/test_model_commit_observation_contract.py`(**3 passed**: fixture↔커널계약 · 15필드 load-bearing · classification enum). 양방향(Python VAL-0002 + Ajv). **앵커 코드 확인**: `model_view.py:56 validate_contract("ModelCommitObservation", result)`(서빙 응답). **드리프트 없음** — 프런트가 생성 타입(contracts-ts) import(fabricObservation.ts fetch). 생성-타입 패턴 모범. 상세: [[2026-09-21_model-commit-observation_계약결속_Claude]]. (ProblemDetails는 사용자가 Codex로, 생성-타입 전환은 사용자가 Gemini로 이미 넘김 — 내가 붙들지 않음.)

10. **ShardObservation 계약 결속 완료**(shard 우선, VF-GM-04 언블록) — `contracts/fixtures/shard-observation-response.json` + `tests/core/test_shard_observation_contract.py`(**5 passed**: fixture↔계약 · envelope 13 + ShardObservedMember 6 + ShardResultMember 6 load-bearing · member.state RunState enum). 양방향(Python VAL-0002 + Ajv 3레벨). 엄격 소비 호출경로 확인(RunDetail.tsx:93,150 실 호출). **앵커 갭**: `ShardRuntime._status`가 응답을 validate_contract 안 함 → Codex flag(shards.py는 Codex 복구·동시성 레인이라 미수정). VF-GM-04 기준: 관측 shape 확정(복제본/healthy 합성 금지, evidenceId null=미관측). fixture objectId=uuid(검증이 내 실수 포착·정정). 상세: [[2026-09-21_shard-observation_계약결속_Claude]].

11. **무결성·복제본 백엔드 능력 지도 완료 + WorkspaceEditView 결속**(사용자 재배정: VF-GM-03/04 근거). 지도(3갈래, 범위 명시, 두 백엔드 트리): [[2026-09-21_무결성_복제본_백엔드능력지도_Claude]]. **①HTTP노출**: 커널 verify_content/artifact·ModelCommitObservation·ShardObservation·**WorkspaceEditView(실 바이트 sha256, VF-GM-03 — 이 커밋 결속)**·StorageObservationView(미소비); saintvision ReplicaObservationResponse(Codex 소유, replica 카운트+unknown). **②내부·엔드포인트없음**: model_manifest.verify, verify_content/sample, reclaim_unclaimed/require_recovery_admission(복구 결과 반환 API 없음=VF-GM-03 합성 원인). **③없음**: 복제본 healthy 판정(백엔드가 의도적으로 unknown만), 온디맨드 복구 콜백. WorkspaceEditView: `test_workspace_edit_view_contract.py` **5 passed** + Ajv 양방향(fixture 검증이 checkoutId=uuid 실수 포착). Gemini 인계: 합성 금지→WorkspaceEditView.sha256 소비/미검증 표시, ReplicaObservation 카운트 그대로, 복구 없음→사용불가.
12. **StorageObservationView 결속 + 모델verify/lineage 권고**(내 레인 ① 나머지 둘). StorageObservationView(스토리지 샘플 무결성): `test_storage_observation_contract.py` **5 passed**(envelope 11 + RecordedStorageObservation 10 load-bearing + **반-합성 const 가드**: currentHealth "unknown"·operationalAcceptanceAssessed false·integrityVerified true) + Ajv 양방향. 앵커 storage_view.py:101. **모델 lineage는 결속 대상 아님으로 정정**: 커널 앵커·서빙 없음, 프런트가 하드코딩 INITIAL_LINEAGES 합성, 실 데이터는 saintvision(Codex). **모델 verify 노출 권고**(읽기-관측 추천, 온디맨드 재검증 신중; 엔드포인트=Codex, 결속=Claude) — Codex 인계 재료: [[2026-09-21_모델verify노출_lineage_권고_Codex인계_Claude]].

13. **Codex 계약 작업 독립 검토 완료** — 상세: [[2026-09-21_Codex계약결속_독립검토_Claude]]. 트리 못 박기: 조사 시작 dff6223(ProblemDetails/NodeStopReceiptView 미병합, 브랜치 3451880에만) → 검토 중 **ac78bf8로 이동해 3451880·PTY(9728556) 병합됨**(재확인으로 잡음, 3451880 동일 SHA라 검토 유효). **ProblemDetails 심층**(직접): 단일 funnel `problem()`+`validate_contract`, 직접 돌연변이 거부·커버리지 시험 도달 확인(3 passed), detail maxLength 1000=`[:1000]` 정합, HTTPException code "HTTP-0001" 정형. **NodeStopReceiptView**: 내가 flag한 이름충돌 개명으로 해소 확인. **pool/placement/distributed/approval**: 33 passed·mutation 무게·6 fixture 고아 없음. **판정 sound, 결함 0**. 저심각 하드닝 1: `problem()`이 비정형 코드 시 오류 직렬화 자체가 throw(라이브 트리거 없음—코드 92개 전수 정형; validate 전 code fallback 권고). PTY/terminal은 범위 밖(사용자 제외). run-approval은 7a9b500 심층검토(별도) 수용.

14. **계약이 못 잡는 가짜-값 부류 목록 완료** — 상세·위험순: [[2026-09-21_계약이_못잡는_가짜값_목록_Claude]]. 범위: apps/web/src 프로덕션+tools+saintvision 비-test(테스트 fixture 제외), 3기준(하드코딩 식별자·기본값 대체 필수값·형식만 맞춘 값), 값을 요청까지 추적·위험분류·실측/추정 구분. **최고**: PTY commandId(WebTerminal:16/TerminalSessionView:389/App.tsx:504) → /terminal-tickets, 백엔드 `_current` DB대조가 fail-closed로 잡음(=보안구멍 아님, PTY가 항상 가짜라 실동작 안 함을 계약통과가 가림); discovery tenantId '00..01'(ResourceExplorer:420) → X-Inv-Tenant 헤더(saintvision enforcement 미확인=Codex). **높음**: projectId 'prj_01JABCDE' fallback(EvidenceViewer/App.tsx)→요청, 백엔드 fail-closed. **중간**: 상태 기본값(무-선택 fallback), 클라이언트 합성엔진(mlops/deployment/security — 표시로만이나 **아무것도 안 잡음**=사용자 오인, 합성 부류). **낮음/정당**: git null SHA·해시 genesis·mock 도구. **결론: 계약은 절대 못 잡고, 백엔드 런타임이 fail-closed로 잡거나(안전하나 기능 조용히 깨짐) 아무것도 안 잡음(합성=다음 우선). 이 목록이 다음 만들 것을 규정.**

15. **discovery tenant enforcement(fb2ba31) 독립 검토 완료** — 상세: [[2026-09-21_discovery_tenant_enforcement_독립검토_Claude]]. 내 가짜-값 최고위험 finding(discovery tenantId)이 촉발한 Codex 보안 태세 변경(문서화 설계 뒤집음). 트리: origin/integration `e69f2b8`, fb2ba31 병합됨. **① 가드 무게=직접 확증**: boundary 시험 2 passed(교차테넌트 403·미인증 401·`writes==[]` DB전 거부), **가드 4줄 제거 되살림→교차테넌트 시험 `500≠403` 실패**(가드가 403·DB차단 생성 증명), 복원·clean. **② 부트스트랩 갭 확정(핵심)**: 토큰 없는 새 기계는 공지 불가(inv-discover가 `INV_DISCOVERY_BEARER_TOKEN` 없으면 거부+서버 401/403); 그 토큰은 **외부 IdP OIDC principal 토큰**(oidc.py 검증만·mint 안 함), `tokens.py issue_bootstrap_token`은 별개 node-등록용, **배포 자료에 설정 0건** → 발급·주입 절차 부재(문서상 pending)=**절차 생기기 전 새 노드 온보딩 불가**(보안↑/기능정지, 명시 필요). **③ Go**: 소스상 방어적 정합, **컴파일러 부재로 빌드 미검증(명시)**. 판정: 가드 sound, 부트스트랩 갭은 미완 운영 선행작업(Codex+운영자), Go 소스만.

16. **서빙 앵커 감사(3갈래) 완료** — 상세: [[2026-09-21_서빙앵커_감사_3갈래_Claude]]. 묶인 응답 28개 전부의 서빙 경로가 실제 validate_contract를 부르는지 감사. **①앵커+무게확인**: ShardObservation·ProblemDetails·ControlRunPage·ApprovalPage(시험이 서빙 함수 행사). **②앵커는 있으나 무게 미확인**: RunResultView·RunArtifactList·RunLogView·RunAttemptList·ModelCommitObservation·WorkspaceEditView·StorageObservationView — **전부 내 결속**(서빙 코드에 `_checked`/`validate_contract(result)` 있으나 시험이 `validate_contract(fixture)`만 → 앵커 제거해도 안 깨짐). saintvision은 FastAPI response_model로 프레임워크 앵커(견고, 버킷2이나 저위험). **③앵커 없음: 없음**(마지막 갭 ShardObservation을 Codex가 닫음). "앵커 호출 존재 ≠ 제거 시 시험 깨짐"을 내 작업에 적용하니 7개 전부 버킷2 — 계약 존재가 증거처럼 읽히는 함정.

17. **서빙-앵커 시험 추가 완료 (버킷2: 7 → 0)** — `tests/core/test_serving_anchors.py` 신설, 7개 전부 서빙 함수를 스텁으로 앵커까지 몰아 응답 계약명이 서빙 경로에서 검증되는지 단언, **7 passed**. **되살림(작동 확인) 4/7**[측정]: RunResultView·WorkspaceEditView·ModelCommitObservation·StorageObservationView — 서빙 앵커 제거 시 해당 시험 실패·복원(3 메커니즘 전부 커버). **만듦·되살림 표본밖 3**: RunArtifactList·RunLogView·RunAttemptList(RunResultView와 동일 `_checked` 메커니즘, 구조상 동일이나 "작동 확인"으로 적지 않음). 버킷1로 이동(기준점 버킷2=7). 남은 후보: ApprovalReviewView/Challenge·saintvision(response_model, 저위험) 범위 밖. 상세: [[2026-09-21_서빙앵커_감사_3갈래_Claude]] 갱신.

18. **규칙→검사 전환 + 공유-index 거버넌스**. (a) 시험 파일 상태 확인: 내 `test_serving_anchors.py`가 Gemini 커밋 `e7cbc51`에 섞여 올라갔으나 **7개 최종본**(중간본 아님)이고 origin tip 판=내 최종본 동일 — 정리 불요. (b) 거버넌스 규칙 신설(셋 다 준수): [[공유워크트리_개인index_커밋규칙]] — 개인 `GIT_INDEX_FILE`로 origin tip에 내 파일만 `commit-tree`+push, rev-range 검증, dirty 공유파일 blob 주입, 명령까지 수록. (c) 규칙→검사: `tools/check_contract_bindings.py` 제작 — fixture 커버리지 + 서빙-앵커 커버리지(bound∩커널앵커 11개 전부 서빙-앵커 시험 보유 확인, 회귀 포착 음성대조 확인). 자동/판단 분리 + 향후 검사 목록: [[계약검증_자동화대판단_검사목록]]. 내 검사 자체의 갭(parametrize 변수명 놓침)도 찾아 고쳐 enforced 7→11로 정정.

## 다음 첫 행동 (검사 게이트 확보 — 방향 대기)
- **버킷2 커널 7개에 서빙-앵커 시험 추가**(Codex의 `test_shard_runtime_status_is_anchored_before_serving`형: 서빙 함수 호출 + 앵커 제거 시 실패). 소비·판단 순: WorkspaceEditView·StorageObservationView(무결성)·RunResultView/RunArtifactList(verify_content)·ModelCommitObservation·RunLogView·RunAttemptList. 이러면 버킷2→버킷1. **커널=내 레인.** 방향 확정 대기 없이 착수 가능(승인된 트랙·내 결속 강화).
- ApprovalReviewView/Challenge 서빙-행사 시험 유무 확인(표본 미실시). saintvision 엔드포인트 행사 시험은 Codex 레이어.
- (참고) 가짜-값 목록(항목14)·discovery 부트스트랩(항목15)은 각각 프런트 실-id 배선(Gemini)·토큰 runbook(Codex+운영자) 대기.
- **가짜-값 목록 완료**(위 14)가 다음 작업을 규정한다. 우선순위: (가) 프런트가 실 식별자 배선(PTY commandId=활성 attempt의 command, tenantId=세션, projectId=선택 — 기능이 fail-closed로 안 깨지고 실제 동작하게; 프런트=Gemini), (나) **클라이언트 합성엔진(mlops/deployment/security)이 실 데이터처럼 표시하는 것**(백엔드 관측 없으면 "데이터 없음" — 사용자 오인 최고 위험, lineage와 동류), (다) discovery tenantId 헤더 enforcement 확인(Codex). 방향은 사용자 지시 대기.
- **독립 검토 완료**(위 13). 후보: Gemini 배선 지원 / 모델 verify 읽기-관측 엔드포인트(Codex 신설 시 결속).
- **내 레인 ① 무결성 view 응답 결속 완료**(WorkspaceEditView·StorageObservationView). 모델 lineage/verify 노출은 Codex 인계(위 12). ShardObservation 앵커도 Codex(terminal 후, 사용자가 넘김).
- 남은 내 레인 후보는 대부분 미소비(StorageObservationView처럼 앵커 있으나 프런트 미배선)이거나 앵커 없음(node 리스트=Codex 데스크톱 소비)·fire-and-forget(workspace prepare). 새 결속보다 **Gemini가 이미 결속된 계약(WorkspaceEditView/StorageObservationView/ShardObservation/Model/Run*)에 배선**하는 게 가치 큰 단계 — 방향은 사용자 지시 대기.
- **워크스페이스 무결성 후속**: WorkspaceEditView 결속됨 → Gemini가 File Explorer를 이 계약에 배선(합성 제거). 필요 시 관련 워크스페이스 view(WorkspaceStartView 등)도 같은 3점 확인 후 결속(단 대부분 fire-and-forget/미fetch로 저우선 — 능력지도 ②③ 참조).
- **node**: 서빙 node 응답(control.nodes 리스트, /v1/nodes/{id})은 **앵커 없음**·소비처가 Codex VF-CX 데스크톱(fabricControlApi) → 결속 시 겹침 확인. 앵커 있는 node 이름들은 인입/보안(Codex).
- **3점 확인 필수**: (1) `_checked`/`validate_contract` 앵커가 **서빙 응답**에 실제로(NodeStopReceipt=이름충돌·ModelManifest/verify=검증워커·인입 앵커 교훈 — 이름·인접 아님), (2) 소비 **실 호출경로**(import/주석/fire-and-forget 아님 — RunAttemptList 오판 재발 방지), (3) Codex worktree dirty 활성 겹침. 앵커 없으면 결속하되 앵커 추가는 owner(Codex)에 flag.
- **두 계약 시스템 구분**: 커널(`core.schema.json`+`validate_contract`, 내 결속 대상) vs saintvision(`contracts/*.schema.json`+pydantic, Codex contract-map 활성). replica/storage-location/pool/placement는 saintvision=Codex.
- **트리·커밋 규칙**: 고정 SHA(계속 이동). 공유 worktree index를 Gemini 자동화가 동시 구동 → `GIT_INDEX_FILE` 개인 index로 origin tip에 내 파일만 `commit-tree`+push, rev-range 검증. 공유 진행판은 blob 주입(working tree 미접촉) append. 상세 `integration-branch-moves-midsession`.
- **드리프트 훑기 방법 메모**(완료·재사용): types.ts vs $def 동명 대조, 4갈래 분류, 소비 실측, 스크립트=리드(재확인), "없음" 범위 명시. [[2026-09-21_수기타입_계약_드리프트_전수훑기_Claude]].

## 오늘 세운 규칙 5 (한 곳에서 다 읽히게 — 규칙 5의 자기적용)
1. **외부 사실 3-갈래**: 외부 사실(버전/출시/가용성) 판단은 **[측정]**(저장소 실측)·**[외부조회]**(go.dev·PyPI·릴리스 목록)·**[미확인추정]**(내 지식)로 나눠 적고, 근거로는 (측정)·(외부조회)만 쓴다. (미확인추정)은 조회 대상으로만. *내 지식 시점을 세계 상태로 착각한 go 1.27.1 오류에서.* 상세: CI-PREFLIGHT v1.1.0 · `memory:knowledge-cutoff-is-not-world-state`.
2. **빈 결과는 사실이 아니다**: 아무것도 안 나옴 = "없다" 또는 "명령이 조용히 실패했다". **반드시 무언가를 찾아야 하는 대조군에서 같은 명령이 도는지 + 종료코드·stderr**로 계측을 먼저 확정한다(파이프가 exit 가림·MSYS 경로변환이 인자 깨뜨림 같은 것). 상세: CI-PREFLIGHT 규칙4 · `memory:empty-output-is-not-evidence`.
3. **정정은 읽히는 자리를 전부 고쳐야 끝난다**: 정정문만 쓰면 갱신 안 된 사본이 남고 **행동하는 쪽은 요약(인계 목록)을 읽는다**. **유효 지시는 한 곳에만 두고 나머지는 그곳을 가리키게** 한다. 상세: CI-PREFLIGHT 규칙5.
4. **경계·귀속 미끄러짐 — 세 축** (대리를 목표로 착각. 처방: 대리가 아니라 진실의 원천·아티팩트를 한 층 더 본다):
   - **초록 ≠ 도달**: 시험이 초록인데 단언에 도달 못 함(passing-but-never-reached). `memory:passing-but-never-reached-trap`.
   - **배선 ≠ 통과**: 워크플로/검사가 존재한다 ≠ 그것이 돌아 통과했다(CI 미실행). before-red/after-green로 실제 실패까지 보여야 증거.
   - **통과했는데 다른 이유로 통과함**: bare `raises(DomainError)`가 인가·리스·멤버십·모델 거부를 뭉개면 한 원인의 회귀가 **다른 원인으로 위장해 성공(초록)**한다. 실패가 아니라 성공으로 위장해 특히 고약. per-case match/assert가 닫음. 상세: funnel 문서 v1.3.0.
   - (부가) **지식경계 ≠ 세계경계**(규칙1)는 이 축들과 달리 저장소를 읽어도 못 잡고 **밖을 조회**해야 잡힌다 — 처방이 다름.

**증거는 무엇에 대한 증거인지 못 박는다**: 돌연변이 표본은 표본이라 밝히고(전수 아님), 확증 대상을 명시한다. 검사 보고는 provenance(어느 tip/트리/인터프리터/실행시점 환경)를 붙인다 — `tools/provenance.py`, `memory:pass-report-provenance-rule`.

## 인계
reviewer: Codex. 프런트 배선 owner: Gemini(run-result/artifacts). 진행판(`docs/vault/00_Index/전체 개발 진행 현황.md`)에 run-result/artifacts=Claude 담당 기록됨. 새 컨텍스트는 이 문서 「다음 첫 행동」부터.
