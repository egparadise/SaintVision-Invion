---
doc_id: "HIST-CODEX-FIRST-HOSTED-CI-001"
title: "최초 호스티드 CI 실패 교정"
version: "1.1.0"
status: "review"
author: "Codex"
updated: "2026-09-22T17:20:00+09:00"
source_of_truth: "Git"
---

# 최초 호스티드 CI 실패 교정

## 작업 카드

- Task: `THREAD-2026-09-22-FIRST-HOSTED-CI-RECOVERY`
- Owner: Codex
- Reviewer: Claude pending
- Branch: `agent/codex/continuation-20260922`
- Initial base: `d01c931acf2127377a7cdc665ef3eee5f0402fe9`
- Refreshed integration base: `dcf2b947d802b86febdcb4acb0677c6640adb0ee`
- R1 landing base: `2aa80899392201c35f01f19569f8ff4b0b3a2fef`
- Integration landing: `36d3ee9b08774b382fff22d7b54cf10859cf598a`
- 역할: 공통 계약·보안 경계·분산 상태의 fail-closed 무결성을 유지하고, 첫 CI에서 드러난 실제 이식성 회귀를 교정한다.

## 최초 동일 SHA CI 판독

`d01c931a` push에서 Documentation Build는 통과했고, Backend·Core·Desktop Browser가 실패했다.

- Documentation Build `35688813795`: success
- Backend Build `35688813798`: schema export drift 2건
- Core Build `35688813794`: `rdflib` 누락, credential 거부 분류, 비이식적 `docker exec $HOSTNAME`, malformed JSON exit 계약 실패
- Auth and Desktop HTTP Browser Acceptance `35688813838`: 3 passed / 3 failed

## 교정 내용

1. `rdflib==7.1.4`를 core 테스트 의존성에 고정했고 pool list schema 2개를 정본 모델에서 재생성했다.
2. Credential provisioning의 순수 입력 검증 실패와 `CredentialDenied`를 보안 거부(exit 2)로 분류했다. 읽기·UTF-8·JSON 입력 실패도 고정된 비밀 비노출 거부 응답으로 매핑했다.
3. GitHub-hosted Linux runner를 Docker container로 가정하던 wrong-owner fixture를 폐기했다. 일회성·networkless container에 단일 임시 디렉터리만 mount해 uid 1 파일을 만든다.
4. 대규모 File Explorer UI 교체에서 빠진 실제 read-only storage catalogue surface를 복구했다. 수정 후 owner scope, revoke 반영, forged token 거부가 실 HTTP/Chromium에서 작동한다.
5. Studio 브라우저 인수는 kernel-only `/v1/projects` 환경이 아니라, 제한된 `inv_app` DB role·business project membership·issuer-qualified subject를 갖춘 실제 business surface를 사용하도록 고정했다.
6. Model availability 인수 문구를 `unknown` + `requiresExecutionRevalidation: true` 정본 계약으로 갱신했다.
7. 통합에서 누락됐던 `origin/codex/ontology-regeneration` 커밋 9개를 merge `23235bbb`로 복원했다. EvidenceEnvelope 서빙 거부와 WorkloadSpec 입력 앵커 증거가 다시 통합 계보에 포함됐다.

## 작성자 검증

- Python 3.14 focused core: 30 passed; 최신 integration 병합 후 41 passed
- 격리 Linux Python 3.12 + PostgreSQL 16 credential suite: 61 passed
- 실 PostgreSQL storage commit suite: 24 passed / 0 skipped
- 실 Edge/Chromium + Vite + configured HTTP + 격리 PostgreSQL: 6 passed / 0 failed / 0 skipped
- Frontend: 75 files / 655 tests passed, contract type check 16 passed, `tsc -b` + Vite build passed
- Schema: 58/58, contract bindings 50 fixtures / 14 response types / 12 replay guards
- Docs: 763 versioned documents passed; ontology/SHACL passed
- 보호 대상 기존 Docker volume 69개와 기존 container는 삭제하지 않았다. 로컬 검증용으로 만든 고유 label PostgreSQL/container/network만 소유권을 확인한 뒤 제거했다.

## R1 개인 index 착지와 정확 SHA 게이트

- 통합 tip `c5042322`를 부모로 만든 최초 후보 `8dc73ff9`는 push 직전 origin이 `2aa80899`로 이동한 것을 확인해 중단했다. 새 tip의 backend workflow 변경은 보존하고 같은 Codex 작성 경로만 다시 얹었다. force push는 사용하지 않았다.
- 최종 `36d3ee9b`의 부모는 `2aa80899`이며 24개 경로만 변경한다. `apps/web/**`, Claude 작업판·History와 Gemini 문서는 포함하지 않았다. 공유 진행판은 최신 Claude 항목을 보존하고 Codex 항목만 추가했다.
- 정확 후보 SHA clean worktree에서 `check_docs.py` exit 0(764 versioned documents), `check_contract_bindings.py` exit 0(50 fixtures, 14 response types, 17 anchor sites, 12 replay guards), `check_ontology.py` exit 0(48 task mappings)을 확인했다.
- core 단일 파일 시험은 credential CLI 14, EvidenceEnvelope serving 2, serving anchors 8, WorkloadSpec input anchors 5로 합계 29 passed / exit 0이다.
- integration 단일 파일 수집은 credential backend 48, shard parent 5, storage commit 24, desktop browser 2, studio browser 2가 각 exit 0이었지만, 이 Windows 검증에서는 Linux·PostgreSQL·browser opt-in 전제 부재로 명시적 skip이다. 위의 별도 Linux/실 PostgreSQL/실 browser 결과를 이 skip 실행과 합산하지 않는다.
- push 직전 세 게이트 exit 0과 origin tip 불변을 같은 PowerShell 조건으로 묶었다. push exit 0 후 origin tip, 부모, 24개 rev-range를 재확인했다.

## 남은 게이트

- integration landing은 완료했다. 최종 integration SHA의 hosted CI와 Claude 독립 검토는 pending이다. Documentation·Backend 3.12/3.14·Core·Frontend·Desktop Browser를 같은 SHA에서 재실행한다.
- Go 1.27.1 경쟁 테스트, Node binary/image lane, LAN installer는 로컬 Go toolchain 부재로 hosted Core CI에서 확인한다.
- 실 IdP/CA/DNS·물리 Node 5대·운영 storage product 선택은 이 카드로 닫지 않는다.

## 다음 행동

1. 최종 문서 게이트와 Obsidian export를 동기화한다.
2. integration head SHA의 모든 hosted workflow 완료를 확인하고 실패가 있으면 소유 lane에서 교정한다.
3. 최종 CI 결과와 Claude reviewer 인계를 이 문서에 추가한다.
