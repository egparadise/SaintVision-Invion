---
doc_id: "HIST-VERIFICATION-AUDIT-COVERAGE-001"
title: "검증 경계 미감사 범위와 다음 라운드 우선순위"
version: "1.0.0"
status: "review"
author: "Codex"
reviewer: "Claude"
updated: "2026-09-18T14:20:16+09:00"
source_of_truth: "Git"
---

# 검증 경계 미감사 범위와 다음 라운드 우선순위

base54ae7c4, task 표본감사 잔여 범위 정리, owner Codex, reviewer Claude(미검토), branch agent/codex/model-registry-binding. agent-delivery1.1.0/core-reliability1.0.0 적용. 공통판/자기 작업판 최신 수신 기록 확인. 이번 작업은 목록·위험 기반 계획이며 추가 제품 결함 발견이나 전수 검증을 주장하지 않는다.

## 독립 검증 수신 범위 정정

사용자가 clean worktree/54ae7c4에서 합성 FastAPI 두 앱으로 VB-AUDIT-02를 독립 실행했다: 무토큰401/위조Bearer RuntimeError는 exit1, 양쪽401은 exit0. 따라서 VB-AUDIT-01의 앞선3경로와 VB-AUDIT-02의 이번2경로는 **작성자 시험 외 사용자 독립 실행도 확인**됐다. '두 건 모두 작성자 시험만 확인'이라는 상태는 이제 맞지 않는다. Claude 소스 검토는 계속 별도 대기다. 503/404/200/403 등 이번 사용자 보고 범위 밖은 기존 작성자88시험 범위로 남긴다. 사용자 P3 동의는 판단 수용이며 별도 실행 증거로 집계하지 않는다. 모든 건수는 중복 합산하지 않는다.

## 감사 깊이와 남은 범위

지난 감사는 tools51/tests179 Python 파일의 패턴·AST 검색이었다. 그중 History 표에 명시한 후보만 의미 검토했고 deployment_surface와 빈 route만 합성 CLI를 실행했다. 230파일 의미 검토 완료, 무결점230파일, 감사완료율100% 어느 것도 뜻하지 않는다.

이번 파일 목록 확인: tools에는 Python 이외 .mjs4/.ps1 1이 있고, apps/web/tests에는 .ts26/.tsx8/.html2, .github/workflows에는 yml5가 있다. deploy에는 .ps1 13/.sh4/.py8이 있다. 이 개수는 파일 목록이며 시험 개수나 검증 완료율이 아니다. 앱 구현·package scripts·원격 장비 상태도 지난 Python 감사 범위 밖이다. 기존 다른 작업에서 검토/시험한 증거가 있을 수 있으므로 '이번 감사 제외'를 '한 번도 검증 안 됨'으로 바꾸지 않는다.

## 다음 라운드 우선순위

우선순위는 검증 주장의 영향과 아직 덮지 못한 경계를 기준으로 한 추정이다. 발견된 finding의 심각도와 다르다.

| 순서 / 영역 | 구체 대상 | 아직 확인하지 못한 것·위험 가설 | 다음 반증 시험 / 담당 |
|---|---|---|---|
| 1 / 브라우저·실장비 인수 집계 | tools/verify_two_pc_distributed_execution.mjs, reconcile_receipts_evidence.mjs, run_browser_smoke.mjs, reproduce_handoff_review.mjs | 지난 Python 검색에서 제외. 실제 HTTP/장비 관측과 합성 계약 검사가 어떻게 분리되는지, 실행0·요청거부·fixture fallback·catch 후에도 성공 집계 가능한지 의미 감사 안 함 | 먼저 정적 call graph와 각 PASS 데이터 출처 추적. 네트워크 호출은 대체 adapter/합성 실패로 주입해 nonzero·unverified 보존 여부 확인. 실제 호스트 실행 금지. Codex 검증경계, Gemini 구현 owner와 인계 |
| 2 / 실행→증거→CI 성공 경계 | tools/run_vf_security_tests.py, kernel_test_entry.py, node_dependent_tests.py, .github/workflows의5파일 | 일부 cleanup/exit 경계는 검토됐지만 stale XML·잘못된 SHA·잘못된 선택/0수집·부분결과를 조합한 전체 집계 감사는 안 함. shell pipeline의 최종 명령 코드가 원래 실패를 가릴 가능성 | 임시 JUnit/가짜 subprocess로 exit1+이전성공XML, XML없음/빈XML/all-skip/수집오류/timeout을 각각 주입. sourceSHA/실행ID/필수case와 최종 상태 연결 확인. Codex. CI 실제 재실행은 인증/결제 복구 후 |
| 3 / DB fixture 및 예외 경계 | tests/conftest.py, tests/integration/conftest.py, tests/db_login.py, credential_conformance.py와 integration 부정시험 | CAS 사례 밖의 모든 fixture setup/teardown 및 pytest.raises 내부 범위를 의미 검토하지 않음. setup 오류와 목표 오류가 뒤섞이거나 skip이 본문 실패를 가릴 가능성 | 목표 단언 전에 도달 표식을 관측하고, setup/본문/cleanup에 서로 다른 실패를 주입. infrastructure 부재·업무거부·권한거부 구분 확인. 먼저 offline fixture 경계, 필요 DB 시험은 파일 단위. Codex/Claude 각 owner |
| 4 / readiness·복원·스토리지 의미 | tools/operational_readiness.py, storage_check.py, rehearse_independent_restore.py, rehearse_lan_upgrade.py 및 관련 tests | 과거 별도 시험 존재. 이번 감사에서는 전체 evidence 생성/저장/조회 경로, 양쪽 빈값·동일오류 비교, freshness·실제 bytes 접근을 전부 검토하지 않음 | 원본만 손상/복원본만 손상, 양쪽조회실패, 오래된 증거, 샘플0, 목표 직전/직후를 대조. 합성 acceptance 입력을 운영 증거로 올리지 않는지 추적. Codex 경계검토, Claude PITR 수정과 중복 구현 금지 |
| 5 / 플랫폼 launcher·배포 판정 | tools/deploy_intranet.ps1, deploy의 ps1/sh/python launcher | 지난 검색 밖. native exitcode 전달, 후속 copy/log 명령의 성공으로 실패가 가려지는지, 실패 후 오래된 패키지를 사용해 성공 처리하는지 미감사 | 외부 명령을 실패 stub으로 바꿔 shell exit/status/receipt 확인. 실제 배포·삭제·서비스 재시작 없이 격리 경로. Codex/각 구현 owner |
| 6 / frontend fixture와 실제 backend 경계 | apps/web/tests, ResourceExplorer와 route adapters | a9294bc fallback 제거는 사용자 관측 수신/기존 통합 변경. 이번 감사의 독립 UI 검증 아님. 다른 화면의 오류·빈 데이터·미연결이 정상 표본으로 보이는지 미감사 | fetch reject/401/403/5xx/invalid JSON/응답없음을 넣고 UI에 가짜 rows·합격 badge가 생기지 않는지 확인. fixture browser 검증과 live backend 검증 명칭 분리. Gemini owner, Codex 경계 review |

core.yml의 'Require executed integration evidence'에는 testcase 존재 및 failure/error/skipped 거부와 Docker host2건 수 검사가 이미 있다. 따라서 'CI가 all-skip을 무조건 통과시킨다'는 finding은 없다. 다음 감사는 이런 기존 방어가 모든 관련 artifact/선택 경계에 적용되는지를 확인하는 것이며 아직 결론 내리지 않는다.

## 실행 계획과 중단 조건

다음 라운드의 첫 bounded 작업은 우선순위1 네 .mjs 파일의 각 PASS 입력 출처와 종료 코드 경로 표 작성이다. 준비물은 Git 소스뿐이며 Docker/운영 권한 없이 가능하다. 실제로 의심 경로가 나오면 최소한의 실패 대조군으로 재현하고, 없다면 검토한 경로와 반증 조건만 기록한다. 전체 프론트엔드 재구현·실장비 재실행을 암묵적으로 시작하지 않는다.

이번 요청은 미감사 영역과 우선순위 정리이므로 해당 2차 감사 자체는 실행하지 않았다. 새 finding0건. 이미 있는 독립검토/운영 인수 대기를 해소했다고 하지 않는다. 사용자 지정 image 재시도 조건 변화 없음.

## 확인 명령·인계

rg --files 및 pathlib 확장자 집계, 관련 스크립트 header와 core workflow gate 텍스트 확인 exit0. git fetch/log exit0, Claude tip1ff4324이며 PITR 수정본 미도착. 이번 제품 코드 변경0, 테스트/DB/Docker/CI/원격 실행0. 다음 Codex: 위1순위 후속 감사 또는 도착한 Claude PITR 수정본 검토. Claude: VB-AUDIT-01/02 독립 소스 검토와 PITR R1 수정. 운영인수0/5 유지.

## 후속 상태 위치

이 문서의 미착수 표는 작성시점03f1f7b의 계획이다. 현재1~5 첫표본 결과와6 미착수 및 영역별 잔여는 [[검증 경계 감사 종합과 잔여 범위]]를 따른다. 과거 계획을 전수검증 완료로 변경하지 않는다.
