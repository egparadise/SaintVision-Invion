---
doc_id: "INDEX-PROGRESS-001"
title: "전체 개발 진행 현황"
version: "1.0.256"
status: "review"
author: "Gemini"
updated: "2026-09-23T02:25:00+09:00"
source_of_truth: "Git"
---

# 전체 개발 진행 현황

2026-09-23 **S05-DB 5노드 lane 실행 계획** = **Codex**: 카드 7 opt-in lane에 legacy/candidate 20→50동시 × 3회, schema 1.3 hold·limit-row wait·retry/SQLSTATE artifact, 5대 node·NTP·cert 준비 대응표를 추가했다. inventory-bound adapter와 workflow는 미구현이고 물리 실행·AC-05 판정은 없으며, 카드 18/F-S05-03 후속 결정 SHA와 20동시 canary 없이는 50동시를 시작하지 않는다. [[2026-09-23_02-06-00_KST_S05_5노드_lane_실행계획_Codex]]

2026-09-23 **S05 옵션 1 기본 off 구현 · F-S05-03 경합 재배치** = **Codex**: short-commit 후보의 실 PG 불변식 10건과 model-retry outer transaction 1건을 통과했다. 20동시 legacy/candidate 각 3회는 모두 20/20·exit 0이고 hold P95 중앙값은 1399.883→122.126ms였지만 요청 P95 개선은 약 4.2%에 그쳤으며 내부 timeout은 0/0/0→limit-row `55P03` 14/10/11로 늘었다. timeout 감소 조건 미충족으로 flag off·S05-DB `review`를 유지하고 50동시/5노드 승격 없이 Claude 카드 18에 인계한다. [[2026-09-23_01-40-00_KST_S05_short-commit_구현과_F-S05-03_Codex]]

2026-09-23 **S08-DB recovery·PITR opt-in dry-run** = **Codex**: 결정 B(Tier-A 유예)를 유지하고 read-only readiness+retention 계획 통합 하네스와 외부 volume 3단계·AC-12 드릴 초안을 `50d5ebc4`로 착지했다. 실 PG는 여전히 `absent`이고 landed SHA focused 23 passed이며 restart/settings/compose/apply·PITR/AC-12 인수는 모두 false다. 실제 WAL/restore/RPO/RTO/off-device/5노드는 미측정이므로 S08-DB `review`를 유지하고 Claude 독립 검토로 넘긴다. [[2026-09-23_01-38-00_KST_S08-DB_PITR-opt-in-dry-run_Codex]]

2026-09-23 **F-S05-02 57014 원인 분리** = **Codex**: a1470435 실 PG disposable DB에서 timeout 설정·runtime role·tenant GUC 적용을 확인했다. project row Lock 대기는 `Lock/transactionid`에서 557.919ms 후 `55P03`, 비-Lock 서버 대기는 `Timeout/PgSleep`에서 2,092.747ms 후 `57014`였고 둘 다 `RES-0007`/503/retryable이다. 과거 20동시 57014는 wait-event 미수집 때문에 project Lock으로 귀속할 수 없고 정확 원인은 미확정이다. 제품·계약 수정과 AC-05 판정 없이 reviewer Claude에게 인계한다. [[2026-09-23_01-05-00_KST_F-S05-02_57014_원인분리_Codex]]
2026-09-23 **S02-FE 실제 API Chrome 로그인·Node 0대·401/403 수용 실측 완결 (Codex S1 반영 4 PASS)** = **Gemini**: 실제 Uvicorn 8080 제어평면, Dev IdP 8090, Vite 3005 개발서버 환경에서 Headless Chrome 153 실브라우저 4대 유저 저니(로그인 성공, Node 0대 정직 표기, 미인가 프로젝트 403 AUTH-0030, 토큰 만료 401 AUTH-0050 및 재로그인)를 100% 무목(Zero Mock) 실측 통과했다. Codex S1 보안 검토에 따라 비동기 mutation(승인/반려/취소/Workspace생성) 전후 세션/스코프 세대 가드를 적용하여 만료/로그아웃 지연 응답 누출 및 고스트 배너를 차단하고 지연 응답 회귀 시험 2종을 통과시켰다. SHA `ae4d9003` 증거 JSON을 확정하고 Codex/Claude 최종 승인을 요청한다. [[2026-09-23_S02-FE_실제API_Chrome_로그인_Node0대_401_403_수용실측_Gemini]]

2026-09-23 **S04-FE 만료·취소·중복·SSE 재연결 시나리오 매트릭스 v1.1.0 개정 (docs-only)** = **Gemini**: OUT-04 / AC-04 완결을 위한 만료(EXP-00~03: 401 AUTH-0050 토큰 만료 추가·403 AUTH-0031 시효 만료 반영), 취소(CNC-01~03: 409 GRAPH-0002 단말 거부·strict RunCancelInput 422 방어·resourceReleasePending), 중복(DUP-01~03: ApprovalDetail 더블클릭 방어·200 replay·409 IDEM-0001 vs 403 AUTH-0034 분리·isSelfApprovalBlocked), SSE(SSE-01~03: {epoch}:{runId}:{sequence} 커서·409 STREAM-0001·sequence 단조 증가) 4대 축 13개 시나리오 매트릭스와 프로덕션 심볼 돌연변이 사살 계획(v1.1.0)으로 Claude 및 Codex 독립 검토를 전면 반영했다. [[2026-09-23_S04-FE_만료_취소_중복_SSE재연결_시나리오_매트릭스_Gemini]]

2026-09-22 **S02-DB API↔PG 인증·RLS 경계 Evidence collector** = **Claude**: `tools/collect_rls_evidence.py`가 일회용 head DB에서 역할 5종의 테이블 142개 권한·RLS·정책·GUC 4상태 가시 행 수와 DEFINER 12 grant를 실측해 JSON+Markdown Evidence로 남긴다(비밀 0건, 트랜잭션 롤백, exit code). 자기시험 16 실 PG(부정 대조군 포함). 발견 1: `public.audit_events`가 inv_app에 RLS 없이 읽힘 → Codex 검토. registry 불변. [[2026-09-22_S02-DB_RLS_인증경계_Evidence_collector_Claude]]

2026-09-22 **S06-DB snapshot reader 제품 결속** = **Codex**: strict restore/checkout 계약·fixture·생성 타입과 project/run-scoped 제품 route를 `a4bf2cee`로 착지했다. 실 PG 1 passed에서 실제 object read, replay 권한 회수 403, 타 project/tenant 404, `inv_app` 직접 kernel table 거부·tenant 미설정 `inv_kernel` 0행을 확인했다. 원격 WS/PTY·실 Git·CP/Node 재시작 복원은 미측정이며 S06-DB는 Claude 독립 검토 전 `review`를 유지한다. [[2026-09-22_23-47-00_KST_S06-DB_snapshot-reader_결속_Codex_구현]]

2026-09-23 **F-S05-01 timeout 실측·결정 초안 v1.1 — 코디네이터 C 조건부 보류** = **Codex**: 단일 project lock 주입은 `55P03` → 기존 `RES-0007`/503/retryable, 20동시 한 라운드는 17/20 성공·성공 P95 2,417.912ms·실패 3건 모두 `57014` statement timeout → `RES-0007`이었다. 이로써 freshness→`RES-0003` 및 lock timeout 미매핑 전제를 함께 정정했다. 옵션 1은 active fit 재계산·stale winner 재계획·Node/Resource 병목·중복 project lock·admission 순서와 lock hold 시간 계측을 Claude가 재검토한 뒤 재결정한다. 50동시·물리 5노드·peak는 미측정이다. [[2026-09-23_00-18-00_KST_S05_timeout_실측과_결정초안_v1_1_Codex]]
2026-09-22 **F-S05-01 설계 결정 초안 — project 임계구역 입도 완화 검토 권고** = **Codex**: 유효한 근거는 개발 PC 합성 Node의 3동시 P95 861.651ms·10동시 1,738.762ms이며 각 두 라운드 전부 성공했고 50동시는 미측정이다. 코드상 project 직렬 구간/freshness 위험에 대해 speculative read+짧은 commit/full revalidation을 권고하고, batch API는 계약 대안으로 분리했으며 freshness 확대는 기각했다. 구현·AC 판정은 Claude 검토와 코디네이터 결정 뒤다. [[2026-09-22_23-30-00_KST_S05_배치잠금_결정초안_Codex]]
2026-09-22 **S03-DB 제한 컨테이너 출력·거부·lease 회수 Evidence collector** = **Claude**: `tools/collect_container_evidence.py`가 커널 run 원장(lease 회수 근거·stop receipt·commitment↔object hash·evidence·completion)과 sandbox plan 제약 하의 컨테이너 프로브(출력 sha256·rootfs/네트워크 거부·uid/CapEff)를 JSON+Markdown Evidence로 남긴다. 자기시험 20(실 PG·Docker). 이 PC에서는 실 run 원장이 미측정(Linux Node provider 부재)임을 evidence에 명시. registry 불변. [[2026-09-22_S03-DB_컨테이너_출력_거부_lease회수_Evidence_collector_Claude]]


2026-09-22 **S07-DB 노드 이탈·복구 반복 측정 — F-S07-03 발견** = **Codex**: 실 PostgreSQL에서 합성 measured-node N대의 한 node만 이탈시키고 실제 lost·replica stale·repair plan·15초 fresh candidate·health를 R회 재현해 JSON 분포와 JUnit을 내는 opt-in 하네스를 `d2471226`로 착지했다. N=3/R=3 축약 예비값은 감지 p95 `1.112639s`, 합성 shard proxy 3/3이나 물리 5-node/bytes 복구 합격은 아니다. 실제 60초 경계는 `60.066591s`로 red라 strict cutoff의 여유 부재를 F-S07-03으로 보존했고, Claude 독립 검토와 물리 lab 재실행 전 `review`를 유지한다. [[2026-09-22_23-00-04_KST_S07-DB_노드이탈_복구반복측정_Codex]]

2026-09-22 **S05-DB benchmark 교정 — 3·10동시 측정, 50동시 미측정** = **Codex**: 최초 landing의 snapshot 계약 위반과 report TypeError 때문에 기존 50동시 `RES-0003` 주장을 철회했다. 교정 `7569c418` 뒤 3/3×2 P95 861.651ms, 10/10×2 P95 1,738.762ms, 오류 0과 불변식 4종 true를 확인했다. 후속 20동시 실측이 현 실패를 `57014` statement timeout으로 고정했으며 50동시는 여전히 미측정이다. [[2026-09-22_22-38-00_KST_S05-DB_50동시배치_예비측정_F-S05-01_Codex]]

2026-09-22 **VF-CL-02(e) node-agent wire 계약 소비 + resolver M1 보강** = **Codex**: PR #64의 reader-scope 교집합 M1을 실 PG에서 KILL하고, strict `ModelExecutionManifestObservation`을 node-agent embedded schema·생성 Go 타입·project/model/version/hash·비권한 플래그 경계에 묶었다. 구현 `9c519774`에서 Go build/vet/test, focused 45 + 실 PG 1, bindings/route/freshness/docs/ontology/ratchet exit 0이고 동일 SHA hosted 5 workflow가 최종 success다. Claude 독립 검토 전 `review` 유지. [[2026-09-22_21-58-06_KST_VF-CL-02E_NODE-WIRE_Codex_구현]]

2026-09-22 **review→done 차단 지도 — review 18·in_progress 2의 우리 몫/U1~U6/물리 자원 분리** = **Codex**: 현재 착수 가능한 planned 카드가 없는 원인을 선행 base 카드 연쇄로 확인하고, 20개 카드의 실제 done 차단과 History 근거를 한 표에 고정했다. 다음 배정 후보는 Codex S05-DB/S07-DB 측정 도구, Claude S02/S03-DB Evidence collector, Gemini S09-FE eval 및 S02/S04-FE browser matrix다. registry·ontology 변경 없음. [[2026-09-22_21-55-00_KST_review_done_차단지도_Codex]]

2026-09-22 **S01-BE·S01-ST 증거 기록 갱신 — 상태 불변, 사용자 입력 대기 명시** = **Codex**: PR #62의 R1~R6 인계를 대조해 migration head를 `0046_model_manifest_readiness`로 바로잡고 계약/hosted CI·peer review·인벤토리 표 양식 증거를 연결했다. 두 task는 `in_progress`를 유지하며 registry handoff만 BE `U2/U3/U4`, ST `U1/U5/U6` 사용자 입력 대기로 바꿨다. [[2026-09-22_21-31-00_KST_S01_증거기록갱신_Codex]]

2026-09-22 **VF-CL-02(d) inv URI resolver 운영 바인딩** = **Codex**: project-scoped `/models/resolve?uri=inv://...`를 strict `ModelExecutionManifestObservation`과 실제 `resolve_model`에 결속했다. restricted `inv_app`이 kernel 관측의 location/version/ready node를 RLS로 재확인하고 교집합만 반환한다. `b06fc199`에서 focused 53 + 실 PG 1 passed, 직접 kernel table 거부·tenant 미설정 0행·타 project 403·미존재 404·stale false/empty·권한 폐기 replay 403, bindings 21 sites와 관련 게이트 exit 0이다. Claude 독립 검토 전 `review` 유지. [[2026-09-22_21-18-16_KST_VF-CL-02D_INV-URI-RESOLVER_Codex_구현]]

2026-09-22 **S06-DB·S08-DB owner 판정 — review 진입, done 차단 유지** = **Codex**: Workspace 재시작/체크포인트와 권한·감사·복원 DB 증거를 실제 PG/Linux 및 hosted Core JUnit에 대조해 두 카드만 `planned` → `review`로 전환했다. S05/S07 선행, 실제 원격 Workspace reader/PTY/Git 재시작, 물리 GPU·운영 승인·off-device/PITR/독립 역할 복원은 미완료로 남겨 outcome/acceptance와 진행률을 올리지 않았다. [[2026-09-22_21-13-00_KST_S06-DB_S08-DB_Codex_owner판정]]
2026-09-22 **S01 증거 기록 갱신 Codex 인계(1쪽): 갱신 항목 6·AC-01 증거별 현재 증거(SHA·run)·부족분(U1~U6)** = **Claude**: 판정 아님, registry status 불변. [[2026-09-22_S01_증거기록갱신_Codex인계_Claude]]



2026-09-22 **Web Desktop UI 불변식 9종 실브라우저 수용 재실측 완결 — 변이 M1/M2 전수 살해, DOM getComputedStyle 실측, 8 PASS 1 PARTIAL** = **Gemini**: 실제 Google Chrome Official Build + Uvicorn 8080 + Vite 3005 환경에서 Claude 독립 검토(f5ed5a61)의 지적을 전수 조치했다. M1 변이 살해(상수 RGB 제거 ➔ DOM getComputedStyle로 상단바 17.26:1, 활성 타이틀 14.05:1, 비활성 타이틀 6.99:1 실측), M2 변이 살해(활성 창 독 아이콘 클릭 시 토글 최소화 언마운트 실단언, 활성 점 스타일 실측), INV-07 형상(x=120, y=90, 1000x640) 직렬화/복원 실측, INV-09 DOM 코어 수치(16, 12, 4.8) 및 0<=allocatable<=total 실단언, INV-06 Escape 모달 닫힘 PASS/포커스 복원 미구현(PARTIAL) 정직 고지. 동적 관측치 정본 증거 `Evidence/desktop_ui_invariants.json` 커밋, boundary 3 passed, exit 0. [[2026-09-22_WebDesktop_UI_불변식_9종_실브라우저_수용실측_Gemini]]

2026-09-22 **Web Desktop UI 불변식 9종 수용 계획 수립 (Claude 독립검토 F1~F4 전수 반영 v1.2.0)** = **Gemini**: 19:40 4대 불변식 실측(unverified 4→0) 기반 위에, Claude의 코드 대조 지적(F1 실제 셀렉터 14건 대조, F2 calc(100%-104px)·언마운트·Escape 한계 등 실제 동작 정합, F3 4대↔9대 대응표 및 WCAG AA >= 4.5:1 정본 정합, F4 Git 영구 증거 경로 Evidence/ 지정)을 전수 반영한 v1.2.0 계획서를 확립했다. [[2026-09-22_WebDesktop_UI_불변식_9종_수용계획_Gemini]]

2026-09-22 **RunDetail 모델 재시도(model-retries, 결정 #6 6a) UI 구현 및 계약 결속 완결** = **Gemini**: Codex 커널 계약(`563c54ce`) 서빙 라우트 `POST /v1/projects/{project}/runs/{parent}/model-retries`를 `RunDetail` 컴포넌트에 실배선 완료했다. failed 종단 상태 가드, Idempotency-Key 발급, `requiresFrozenInputAndApproval: true` 정직 고지 배너(`role="status"`), Child Run ID 및 계보(Lineage) 표출, RFC 9457 Problem Details 에러 대응(`role="alert"`)을 전수 구현했다. Vitest 8/8 tests 통과, `tsc -b` 0, route_coverage 38 passed, check_frontend_integrity PASS, Vite 프로덕션 빌드 클린 생성. [[2026-09-22_RunDetail_모델재시도_model-retries_UI구현_Gemini]]

2026-09-22 **RunDetail Model Retry(결정 #6 6a) UI 설계 완결** = **Gemini**: Codex가 `563c54ce`에 착지한 `POST /v1/projects/{project}/runs/{parent}/model-retries` 계약에 따른 RunDetail 재시도 액션 UI 설계를 완결했다. 실패(`failed`) 종단 상태에서만 버튼 노출, Idempotency-Key 발급 및 중복 클릭 차단, `requiresFrozenInputAndApproval: true` 정직 고지(자동 승인 없음·S04 승인 센터 연계), 자식 Run/Lineage 표출, 409/403/503 오류 처리, Vitest 8대 회귀 시험 명세를 확립했다. 메모리 경보 해제 후 구현 예정. [[2026-09-22_RunDetail_재시도액션_model-retries_UI설계_Gemini]]

2026-09-22 **Web Desktop 4대 UI 불변식 및 A11y 실측 검증 (unverified 4 ➔ 0)** = **Gemini**: 실제 Google Chrome Official Build + Uvicorn 8080 백엔드 + Vite 3005 개발 서버 환경에서 Track 15 4대 UI 불변식(양방향 전환기 Desktop <-> Portal, 창 관리자 트래픽 라이트/동적 z-index 승격 23 > 22 > 21, 키보드 A11y Alt+Tab 순환 및 Escape 닫기, 레이아웃 영속성 localStorage 직렬화 및 언마운트/리마운트 복원) 및 WCAG AA 대비율(17.06:1, 13.98:1)을 전수 실측 완료했다. `tools/run_browser_smoke.mjs` 상의 미검증 잔여 수를 4건에서 0건으로 해소하고, `scratch/desktop_ui_invariants.json` 및 스크린샷 7종(7개)을 확보했다. 오늘 Gemini 관련 PR #39·#40·#44·#36(`c6094d71`, tip `8f3c80c8`) 4건 전수 병합 완료. [[2026-09-22_WebDesktop_4대UI불변식_및_A11y실측검증_Gemini]]

2026-09-22 **VF-CL-02(c) 실행 Manifest 관측 + model-retry F1 보정** = **Codex**: 타 tenant retry를 `403 AUTH-0030`으로 바로잡고 project-scoped strict manifest/shard/location 관측 계약을 `4473c7f1`에 착지했다. missing mapping은 `409 MODEL-0001`, stale/no-ready는 명시적 빈 `readyNodes`와 false이며 기존 commitment API는 불변이다. focused 68 + 실 PG 2 passed, 생성 drift 0, 계약·문서 게이트 exit 0. 후속 policy hotfix `57f3afb9`의 Docs/Frontend/Desktop/Core는 success(Backend는 후속 push로 cancelled), Claude PR #58 finding 0 승인이다. [[2026-09-22_20-19-51_KST_VF-CL-02C_MODEL-RETRY-F1_Codex_구현]]
2026-09-22 **S01 물리 노드 5대 인벤토리 표 양식(미확인 값·출처 열, 값 미기재)** = **Claude**: AC-01 '미확인 값 명시'용 양식, 등록 후 capability/skew 대조로 ☑→☐. [[S01_물리노드_인벤토리_표양식_2026-09-22]]
2026-09-22 **PITR 보존 7일 정리 도구 실측(dev PG 실제 라벨·WAL 이름; dry-run 0 삭제, +8일 계획, 사본 apply 정확, 9 passed) — 실 아카이브 부재라 도구 검증까지** = **Claude**: [[2026-09-22_PITR_보존7일_정리_dry-run_실측_dev_PG_Claude]]





2026-09-22 **Codex 커널·계약·CI 레인 후속 착지** = CI 첫 실행 red를 소유별 귀속하고 Codex 몫을 교정했다. EvidenceEnvelope 실PG 5-site 앵커를 모두 rejection-tested로 올렸고, docs 게이트·fixture reachability report-only·결정 #2/#5 계약·결정 #6a model-retry HTTP 계약을 착지했다. `563c54ce` 6a 검증은 focused 68 + 실PG HTTP 1, bindings 51 fixtures/16 types/19 sites, app anchor 3/0, Go·TS·문서 게이트 exit 0. 최종 `f2aa2b14`에서 Backend 35714785554, Core 35714470445, Browser 35712413553, Docs 35712413561, Frontend 35712428159가 동일 SHA success했다. S02-DB/S03-DB는 미충족 경계를 유지한 채 `review`로만 승격(`3882496d`). [[2026-09-22_18-37-00_KST_CODEX-KERNEL-CI-CONTRACT_Codex_최종보고]] · [[2026-09-22_18-27-37_KST_S02-DB_S03-DB_Codex_독립검토]]
2026-09-22 **결정 #6 6a model-retries 통합 검증(실 PG+실 HTTP) 8 passed — 6c 배치 예약 연결 확인, finding 1(타 tenant 503→4xx 필요, Codex)** = **Claude**: [[2026-09-22_결정6a_model-retries_통합검증_실PG_실HTTP_Claude]]

2026-09-22 **CX-09 PITR Tier-A 활성 여부 결정 준비(A/B 비교 1쪽, 권고 A·전제 2)** = **Claude**: 실측 근거(absent·리허설 매트릭스·7일)로 비교, 설정≠RPO 증거 유지, 코디네이터 결정 요청. [[2026-09-22_CX-09_PITR_Tier-A_활성여부_결정준비_Claude]]
2026-09-22 **hosted Core 첫 완주 skip 58 분류표 + 로컬 142 대조** = **Claude**: CX01 19·opt-in 23·Windows 11·브라우저 1·도구 4(물리 노드 0); Linux 96↔Windows 11 상보, 어디서도 안 도는 skip 31건; core.yml 이미지 변수 3개면 23건 실행 전환(Codex). [[2026-09-22_hosted_Core_첫완주_skip58_분류표_Claude]]





2026-09-22 **route_coverage CLI를 docs.yml에 report-only로 배선(승격 조건 명시)** = **Claude**: exit 기록·job 미실패·step-summary·artifact; 검증검사도구_목록 v1.4.0. 워크플로 소유 Codex 검토 요청(PR). [[2026-09-22_route_coverage_CLI_docs.yml_report-only_배선_Claude]]
2026-09-22 **결정 #7 실 운영 증거: 정본 tip에서 alarm_check를 dev PG에 실행, P2 시각 스큐 알람 평가 대상으로 동작(quiet = 노드 0대)** = **Claude**: exit 0·7 family ok·gated 0, 채널 없음 그대로. [[2026-09-22_결정7_시각스큐알람_실운영증거_dev_PG_Claude]]

2026-09-22 **S02 착수 선행 입력 체크리스트(사용자용 1쪽) 작성** = **Claude**: IdP·CA/mTLS·DNS·물리 PC 5대·Storage·DB를 항목별 '어디에 넣는가·성공 신호'로, 코드의 실제 env/파일 이름과 절차서 §4에 정합. [[S02_선행입력_체크리스트_2026-09-22]]
2026-09-22 **S01-BE·S01-ST 잔여 합격 조건 표(1쪽)** = **Claude**: AC-01 증거별 충족/미충족/외부 대기(U1~U6)를 S02 체크리스트에 연결, 사용자 결정 맨 위, 우리 몫 3건 명시. [[2026-09-22_S01-BE_S01-ST_잔여_합격조건표_Claude]]

2026-09-22 **route_coverage CLI를 docs.yml에 report-only로 배선(승격 조건 명시)** = **Claude**: exit 기록·job 미실패·step-summary·artifact; 검증검사도구_목록 v1.4.0. 워크플로 소유 Codex 검토 요청(PR). [[2026-09-22_route_coverage_CLI_docs.yml_report-only_배선_Claude]]




2026-09-22 **route_coverage 스캐너 사각 수정 — 템플릿 hole 안 함수 호출이 SPA 경로 13개를 지워 30/0이 거짓 초록** = **Claude**: 평탄화 워커로 수정, 회귀 5개(원본 FAILED→KILLED), tip 재측정 43/0, PR #36 트리는 unserved 2(resource-usage)로 F1을 도구가 잡음. [[2026-09-22_route_coverage_템플릿hole_사각_수정_Claude]]

2026-09-22 **PR #36(노드 자원 사용량 UI) 독립 검토 — 게이트 GREEN·Zero-Mock 무게 확인, 그러나 서빙 라우트 0·App 배선 0·스캐너 사각** = **Claude**: 어댑터가 부르는 `/v1/nodes/{n}/resource-usage`를 아무 트리도 서빙 안 함(결정 #2는 계약만), 부르는 화면도 없음, route_coverage는 변수 조립 URL을 못 봐 30/0 초록(거짓). 조건부 병합(미병합 작업 동승 정리·'연결 완결' 표현 정정). 후속: 라우트 Claude·배선 Gemini·스캐너 Claude. [[2026-09-22_PR36_NodeResourceUsage_UI_독립검토_Claude]]

2026-09-22 **새 PC 실 PG 3건: 전수 실행(2523 passed / 142 skipped / 2 failed, skip 1011→142) · 인수 증거 6세트 762 passed / 0 failed · PITR runbook 실측(readiness absent + 물리 리허설 6회 재현)** = **Claude**: SHA d01c931a, Claude 전용 측정 워크트리 `D:\Project\sv-measure-claude`(clean). 전수는 3덩어리 순차(첫 단일 시도는 메모리 reaper 종료 → 코디네이터 승인 분할). 남은 skip 142는 전부 이전 절차서 §4 환경 게이트. 실패 2건 귀속 Codex: F1 migration-head(동시 실행 경합, 단독 통과, 진단 은닉) · F2 vf_canonical `.work` 미생성(51d53b7f로 이미 수정). 다른 Claude 세션 c5042322 측정과 실패 이름·판정 일치. S02/S03/S09/S10/S04-05/S07 증거 재실행 762/88/0(skip=Linux 백엔드·CX01, 늘어난 skip 0). PITR: dev-pg readiness absent(비파괴, disposable DB), owned probe 물리 리허설 정상 PASS·결함 5 FAIL·잔재 0, 보관 7일(파일럿, 코디네이터 결정). hosted CI backend는 export_schemas pool 스키마 stale(Codex)로 pytest 미도달. 검증상태지도 §5·§6. [[2026-09-22_17-02-56_KST_REALPG-FULLRUN_Claude_실측]] · [[2026-09-22_17-12-01_KST_REALPG-EVIDENCE_Claude_인수증거]] · [[2026-09-22_17-14-47_KST_PITR-RUNBOOK_Claude_실측]]

2026-09-22 **새 PC 첫날: CI 첫 실행 triage 지도(원인 7·Claude 레인 0) + 이전 후 전수 검증 완료** = **Claude**: d01c931a에서 docs 성공, backend는 pool-list 계약 drift(B1, Codex `9425062c` 재생성 누락)가 pytest 전체를 가림, core 2964 중 6 실패=rdflib 미설치(B2)·docker exec 경로·자격증명 오류매핑(A) — Codex가 881f2911·51d53b7f로 착지; 2차 실행에서 브라우저 3=desktop/studio 여정(Gemini), 신규 C6=vitest 시간포맷 locale 의존(B2, Gemini), C7=backend no-skip 게이트(Codex 2aa80899 ratchet). **backend 첫 완주 2645 passed/45 skip/0 fail(3.14)**. machinery 인계: cancel-in-progress로 backend·core가 integration에서 한 번도 안 끝남(내 문서 push도 취소 유발). **Go가 CI에서 처음 컴파일·시험됨**(옛 '0 컴파일' 종료), PG-in-CI로 skip 1039→58. 새 PC clean 트리 전수: 게이트·프런트 기준선 동일, 실PG pytest 2549 passed/408 skip(Linux 전용). 환경 차이: check_frontend_integrity cp949 출력 crash(Gemini). [[2026-09-22_새PC_첫날_CI첫실행_triage_및_이전후_전수검증_Claude]]
2026-09-22 **결정 #7 A 구현: ERR-DESIGN-007 v2.0.0 채택 + Node 시각 스큐 P2 알람 활성화(자동 조치 없음)** = **Claude**: 커널 가드 술어를 한 함수로 미러해 알람이 가드와 어긋날 수 없게, 실 PG 7케이스(경계·양음수 초과·미측정·offline 제외·복구 해제) 35 passed/0 skip 실측. 커널·API 변경 0. PR(base #38), reviewer Codex. [[2026-09-22_결정7_시각스큐알람_활성화_구현_Claude]]

2026-09-22 **새 PC 첫날: CI 첫 실행 triage 지도(원인 6·Claude 레인 0) + 이전 후 전수 검증 완료** = **Claude**: d01c931a에서 docs 성공, backend는 pool-list 계약 drift(B1, Codex `9425062c` 재생성 누락)가 pytest 전체를 가림, core 2964 중 6 실패=rdflib 미설치(B2)·docker exec 경로·자격증명 오류매핑(A) — Codex가 881f2911·51d53b7f로 착지; 2차 실행에서 브라우저 3=desktop/studio 여정(Gemini), 신규 C6=vitest 시간포맷 locale 의존(B2, Gemini). **Go가 CI에서 처음 컴파일·시험됨**(옛 '0 컴파일' 종료), PG-in-CI로 skip 1039→58. 새 PC clean 트리 전수: 게이트·프런트 기준선 동일, 실PG pytest 2549 passed/408 skip(Linux 전용). 환경 차이: check_frontend_integrity cp949 출력 crash(Gemini). [[2026-09-22_새PC_첫날_CI첫실행_triage_및_이전후_전수검증_Claude]]

2026-09-22 **최초 hosted CI 실패 교정 통합 착지** = **Codex**: `d01c931a` 최초 push의 docs success / backend·core·desktop-browser failure를 실행 증거로 분해하고 schema drift, core `rdflib`, credential fail-closed 분류·Linux owner fixture, File Explorer catalogue 배선 탈락, Studio business identity fixture, model availability 문구를 교정했다. 누락된 Codex ontology guard도 복원했다. R1 개인 index로 이동 중인 통합 tip을 재기준화해 `36d3ee9b`에 24개 Codex 작성 경로만 착지했으며 force push는 없었다. 정확 SHA에서 docs·contract bindings·ontology exit 0, core 단일 파일 29 passed를 확인했다. hosted 동일-SHA CI·Claude 독립 검토·운영 인수는 별도 pending이다. [[2026-09-22_최초_호스티드_CI_실패_교정_Codex]]

2026-09-22 **뿌리: 공유 문서의 게이트-미적용 예시 교정 + R2-b 예시(bash·PowerShell) 추가** = **Claude**: Codex도 세미콜론 체인으로 검사·push를 이어 써 exit가 push를 안 막던 것(우연히 초록) — 셋이 같은 뿌리에서 배웠을 가능성. 내 것 고침: 이전 절차서 §1-4의 `| tail -1`(exit를 tail이 삼킴) → exit 함께 기록형; R2-b 옆에 올바른 예시 2셸(bash if/&&·PowerShell $LASTEXITCODE, 파이프 금지) 추가해 복사해 쓰게. 짚어 넘김(소유자 Codex/공유): GOV-GIT-001 '현재 사용 가능한 명령' 블록과 AGENTS.md 검증 절이 check·--check→--apply·push를 게이트 없이 나열 → R2-b 참조·exit 게이트 문구 필요. [[공유워크트리_개인index_커밋규칙]]

2026-09-22 **커밋 규칙 R2-b 추가(공유 규칙) — push는 게이트 exit로 막는다** = **Claude**: 오늘 check_docs-red를 두 번 push한 부류(게이트 돌리고 exit 안 보고 tip 비교로만 게이트)를 개인 메모리에서 공유 커밋규칙으로 승격. R2-a(drift 출력만·미halt)와 형제로 묶고 부류에 이름 「검사 ≠ 준수」(검사는 했으나 결과가 행동을 안 바꿈) 부여. 실례 1d413155→d1c6992c·44022523→2c83aa21. Codex·Gemini도 push하므로 공유 필요(통지는 사용자). [[공유워크트리_개인index_커밋규칙]]

2026-09-22 **이어가기 §0 마감 갱신 + Codex 집계 대조** = **Claude**: 새 PC Claude가 이어갈 §0을 최종 정합 — 지금상태(done2)·열린갈래(앵커무게/WorkloadSpec/다운로드정본)·새PC 첫 할일(gh·Go)·오늘vs어제 도구규칙·기준선SHA 이후 착지多 경고. **답 아는 자리 대조**: check_contract_bindings 자리수를 Codex 집계에 대봄 — 응답계약 EvidenceEnvelope 5=5 일치, 입력·식별자(ProjectId/RunId/WorkloadSpec)는 내 도구가 RESPONSE_ARGS로 의도 제외라 0(Codex는 셈) = 범위 차이 확인(버그 아님). [[개발환경_이전_절차서]]

2026-09-22 **check_contract_bindings가 세는 단위(타입/자리)를 출력에 선언** = **Claude**: 어젯밤 내가 만든 (2) 서빙앵커 게이트가 "14 커널 응답 각각 서빙앵커 시험 있음"을 **타입 단위**로 세어, 한 응답이 여러 자리서 앵커되는데 한 자리만 시험 있어도 참이면서 오해를 줬다. 게이트 로직은 그대로 두고 출력을 정직화 — **14 타입 / 17 앵커 자리**(inv 전체 스캔)·**다중자리 타입 목록**(EvidenceEnvelope 5자리: results/runs/shard_completion/storage_commit/storage_view 등)·"자리별 시험 매핑은 정적 불가→check_anchor_weight+변이"·"자리 보고는 회귀 아니라 정확해진 것" 주석. 답 아는 자리 대조: EvidenceEnvelope 5 = Codex 확인값 일치. 두 도구 보완. [[2026-09-22_앵커무게도구_전수적용_결과_Claude]]

2026-09-22 **check_anchor_weight가 자기 범위를 출력에 선언** = **Claude**: 답을 아는 자리(replay12) 검증에서 도구가 거짓으로 신뢰받을 뻔한 것을 교정. 이제 출력이 **범위를 먼저** 말한다 — 스캔한 7 서빙/뷰 모듈·**타입 단위라 fresh/replay 분기 못 가름**·도구가 스스로 센 **범위 밖 replay 앵커**(2b가 권위)·in-scope replay 타입엔 별표 주석(ControlRunView*). 숫자가 실제보다 넓게 안 읽히게. 범위는 **의도적으로 좁게 유지**(프런트 대면 서빙 앵커 거부만; replay는 check_contract_bindings 2b+negative-prior 소관, 넓히면 중복·오분류)—이유를 docstring에 기록, --modules로 opt-in 확장 가능. fresh/replay 정적 분리는 불가라 명시하고 2b로 넘김. 자기시험 4 passed. 두 도구=대체 아닌 보완 명문화. [[2026-09-22_앵커무게도구_전수적용_결과_Claude]]

2026-09-22 **check_anchor_weight 전수 적용 + 답을 아는 자리 검증** = **Claude**: 도구를 tip에 실전 실행(7 rejection-tested/7 called-only/1 no-serving-test). 세 갈래: **진짜 구멍 = EvidenceEnvelope**(5 서빙경로 앵커·시험 0·기존 게이트도 안 잡음 → Codex 인계); **괜찮음 = called-only 7**(계약 거부 시험 있음·앵커 정당, 서빙거부 시험만 없음); **애매 = 그 서빙거부 시험 필요 여부**(변이 몫). **replay 12에 대보니 도구가 못 봄**: control 2만 scope 내(fresh로 판정·replay 분기 구분 못 함), 나머지 10은 scope 밖 → replay 무게는 Codex 2b+negative-prior가 봄. 도구 두 사각(scope·분기 granularity) 드러남. self-fix 없음. [[2026-09-22_앵커무게도구_전수적용_결과_Claude]]

2026-09-22 **앵커 무게 발견을 구조로 — `tools/check_anchor_weight.py` + 자기시험 + 변이 절차서** = **Claude**: bbc1eb2f 스펙트럼 발견을 도구화. 서빙 앵커를 거부-커버리지 tier(rejection-tested/called-only/no-serving-test)로 **정적** 분류(report-only, check_contract_bindings 보완). 저장소에서 변이 ground truth와 **정확히 일치**(7/7/1) 교차검증. 과잉경보 방지: 함수 수준(파일 수준 거짓양성 2건 발견·수정), called-only=정죄 아닌 tier. 자기시험 `tests/test_check_anchor_weight.py`(병 심기: rejection→recorder 시 잡힘) 3 passed. 못 재는 런타임 무게는 격리 변이 절차 [[앵커_무게_검증_절차]]로 문서화. [[앵커_무게_검증_절차]]

2026-09-22 **이전 기준선 측정(규칙8 전수) + check_ontology RED 발견** = **Claude**: 새 PC 대조용 기준선을 clean 워크트리 `47b0d2de`에서 측정해 이전 절차서에 기록 — CI-스코프 pytest 1650 passed/1039 skip·vitest 655/75·contracts:check(16)·tsc·build 통과. **RED 1건 = check_ontology**: registry가 S01-DB·S01-FE를 done으로 닫았으나 `ontology/example.ttl` 미재생성(in_progress/review로 stale). `python tools/generate_ontology.py`가 정확히 2줄만 done으로 고쳐 exit 0 됨을 격리 검증(revert). **CI 개방 상태라 다음 CI서 실패 — 소유자 Codex 재생성·착지 필요.** not_run=Go·PG통합·브라우저·hosted CI. [[개발환경_이전_절차서]]

2026-09-22 **새 PC Claude 첫 메시지 작성(복붙용)** = **Claude**: 운영 문서. 새 PC에서 Claude Code 처음 띄울 때 붙일 첫 입력 한 덩어리 — 읽을 순서(CLAUDE.md→AGENTS.md→이어가기§0→이전절차서; 마지막이 새 PC 추가=환경 먼저 확인)·registry 실측 상태(done1·review12·in_progress2·planned33; S01-DB 첫 done, S01-FE는 review로 Codex 인계)·이 PC서 처음 가능(Go 첫 컴파일·worker 5대→옛 Linux-skip 채움)·오늘 새 규칙 포인터(7·8·조용한강등·R2-a)·하지말것(보호컨테이너 증거 지어내기 금지·소유확인 없이 삭제 금지). [[새PC_Claude_첫메시지]]

2026-09-22 **개발 환경 이전 절차서 작성(새 PC 192.168.45.74)** = **Claude**: 운영 문서. 경로 3곳(source-manifest.json·.git/obsidian-sync-state.json·AGENTS.md45; JSON은 이중백슬래시·md는 단일; egpar→inviz·OneDrive-Inviz→(1))·환경변수 전수(사용자 8종 외 INV_OIDC 4·INV_ENV·INV_TENANT_ID·다수 이미지/토글 추가; 값 미기재 재발급)·clone(폴더복사 금지, venv/node_modules 새로)·Go 첫 컴파일·규칙8 전수검증 비교. **이동전 실측 손실위험**: 미커밋 워크트리 3(main2·codex-write-response-contract12·C:/vw4)·origin앞선 브랜치 3(+7/+24/+32)·stash 10 — 소유자가 push/폐기해야 clone에 옴. 보호컨테이너 4 잔류. [[개발환경_이전_절차서]]

2026-09-22 **서빙 앵커 무게 전수 변이 — replay만이 아니다(스펙트럼)+자동화 제안** = **Claude**: 커널 서빙 앵커 15타입/18call-site를 격리 워크트리 변이로 전수 측정(대조군이 죽어 방법 유효 확인). **층1 완전 vacuous**: replay 앵커 12 + EvidenceEnvelope(5경로 검증하나 계약시험 0). **층2 (a)호출만 검증·거부 미검증**: RunResultView/ArtifactList/LogView/AttemptList·WorkspaceEditView·ModelCommitObservation·StorageObservationView — 유일 서빙시험 test_serving_anchors.py가 validator를 레코더로 대체해 "불렸나"만 봄(validator 중화해도 7개 전부 초록). **층3 완전 무게**: control·shard·terminal·problem·artifact·workspace start/resume(서빙 경로 거부 실측). check_contract_bindings가 스스로 인정한 갭(bears-weight 미검사)을 실측. **자동화 제안**: 앵커별 검증-중화 후 ≥1시험 실패 요구+대조군 자기검증(mutation testing). 자리 짚음, 수정은 Codex. [[2026-09-22_서빙앵커_무게_전수변이_부류확장_Claude]]

2026-09-22 **독립 검토 — Codex 커널 쓰기면 감사(a2dada9a)** = **Claude**: (1) 28 쓰기 라우트 전수 일치·누락 0 확인. (2) 프런트가 node control·runs·resume·terminal·checkouts/files를 실제 직접 호출함을 실측(위험 판정 옳음; 단 28이 다 브라우저 대면은 아니라 위험 이질성 주석). (3) **발견** — 격리 워크트리 변이 실측: fresh 앵커는 무게 있음(create 앵커 제거 시 test KILLED), 그러나 **replay 분기 앵커 12개는 vacuous**(전부 제거해도 44 passed 불변). 무효 prior를 replay에 심고 거부를 확인하는 시험이 없다. 하필 resume/replay 자리라 어젯밤 재생-우회와 동류. Codex 실PG 실행도 같은 호스트면 이 갭 못 닫음(무효-prior replay 시험 필요). [[2026-09-22_Codex_커널쓰기면감사_독립검토_Claude]]

2026-09-22 **S01-FE 최종 검토 완료** = **Codex**: 최신 integration `5180a737`에서 Vitest 75 files/655 tests, 계약·정적·route/build 게이트를 재현하고 RunItem 수기 API 사용 잔여 0건을 확인했다. S01-FE를 `done`으로 판정했다. 실제 Chrome 보고는 Gemini 증거를 검토한 것이며 Codex의 브라우저 재실행을 뜻하지 않는다. [[2026-09-22_S01-FE_Codex_최종판정]]

2026-09-22 **PlacementSimulator 정본 PoolList 연동, 용량 분리 및 디스커버리 후보 신고스펙 정직화와 정직성 스캐너 Rule 9 확장** = **Gemini**(완료): Codex의 `/v1/pools` 정본 착지 및 사용자 지시(풀 목록과 신선도-민감 용량 분리, 후보 신고스펙과 측정 가용량 구별, 허위 online 합성 차단)에 따라 1) PlacementSimulator 정본 PoolList 및 용량 분리 조회: 수기 `PoolItem` 인터페이스 및 raw apiClient 호출을 제거하고 `getPoolList` 어댑터에 결속했으며, 신선도 유지를 위해 용량 정보는 `getPoolCapacity` 어댑터로 온디맨드 분리 조회하여 실시간 가용 코어/메모리/GPU 및 활성 멤버 수를 정직 표출했다. 2) Discovery 후보 허위 합성 박멸: `getDiscoveryCandidates` 어댑터 및 정본 `DiscoveryCandidateResponse`를 결속하고, 미등록 후보의 자체 보고 스펙(`claimedCpuCores`, `claimedRamBytes`, `claimedGpuCount`)을 가용량으로 둔갑시키지 않고 "신고 스펙 (Claimed · 실측 가용량 아님)"으로 고지했으며, 부재한 `gpuName` 미제공 고지 및 허위 `online` 합성을 차단하여 `CANDIDATE (미검증)` 뱃지로 표출했다. 3) 정직성 스캐너 Rule 9 확장: `/v1/pools` 및 `/v1/discovery/candidates` direct raw apiClient 호출을 차단하고 음성 대조군(Test 12) 사살 실측. 4) 단위 테스트 및 게이트 통과: `placement-simulator.test.tsx` 2종 추가(75개 파일 **655/655 passed 100%**, 순증 +2 passed), `npx tsc -b` exit 0, `npm run build` exit 0 (3.24s), `check_frontend_integrity.py` 82개 파일 All 9 rules satisfied (0 violations), `pytest tests/test_route_coverage.py` 30 passed in 0.90s, `check_contract_bindings.py` 48 fixtures / 14 anchors PASS, `check_doc_single_source.py --ratchet` PASS. [[2026-09-22_PlacementSimulator_정본PoolList_용량분리_및_디스커버리후보_신고스펙정직화_Gemini]]
2026-09-22 **독립 검토 — Codex 저위험 쓰기 계약 7건: 기록 대 코드 대조** = **Claude**: f2d86db5가 문서 전용으로 완료 기록한 7건(heartbeat·liveness-sweep·discovery announce/decline·member removal·user/project status 응답)이 지금 tip 코드에 실제로 살아 있는지 대조했다. **7건 전부 live·기록==코드** — 모델 import·정확한 route response_model 결속·fixture·회귀시험 19 passed. 무게: 7건 앵커 각각 response_model 떼면 KILLED, 복원 성립. 누락 0(나쁜 가짜초록 없음). 방법: 문서 전용 커밋의 완료 기록은 코드 위치를 대조. [[2026-09-22_Codex_저위험쓰기계약7_코드대조_독립검토_Claude]]
2026-09-22 **Placement preview 정본 어댑터 결속, EvidenceViewer RunResultView 정본 전환 및 정직성 스캐너 규칙 9 신설** = **Gemini**(완료): Codex의 프런트엔드 와이어 계약 감사 지적 2건을 즉시 수용·완결했다. 1) PlacementSimulator 어댑터 결속: raw apiClient 인라인 제네릭 호출을 정본 어댑터 `getPoolPlacementPreview`로 전면 결속하고, TS2367 타입 불일치로 드러난 죽은 분기(`c.eligible !== false` 비교)를 치유했다. 2) EvidenceViewer 정본 계약 전환: `apiClient<any>`를 정본 생성 타입인 `RunResultView`로 전환하고, `any` 뒤에 숨어 있던 가짜 식별자(`evi_${runId}`) 합성 및 미실재 필드 접근을 영구 소거했으며 미봉인 시 '미발급 (출력 미봉인)'으로 정직하게 고지했다. 3) 정직성 스캐너 규칙 9(Canonical Wire Contract & Adapter Invariant) 신설: 프로덕션 전역의 `apiClient<any>` 사용 및 `placement-preview` 어댑터 우회 호출을 원천 차단하고 음성 대조군(Test 12) 사살을 실측했다. 4) 게이트 통과: `npx tsc -b` exit 0, `npm run build` exit 0 (3.92s), Vitest 75개 파일 653/653 passed 100%, `check_frontend_integrity.py` 82개 파일 All 9 rules satisfied (0 violations), `pytest tests/test_route_coverage.py` 30 passed in 0.78s, `check_contract_bindings.py` 47 fixtures / 14 anchors PASS, `check_docs.py` 747 docs PASS. [[2026-09-22_PlacementPreview어댑터결속_및_EvidenceViewer_RunResultView정본전환_Gemini]]
2026-09-22 **독립 검토 — Codex 부재주장 회귀가드(2679f0c7): sound·non-vacuous** = **Claude**: 닫힌 도메인 가드(capability kind·node OS·placement strategy)와 retry-terminal 가드를 통합 tip에서 실행(29+47 passed)하고 non-vacuity를 직접 확인했다 — 미지값 거부의 ValidationError loc가 전부 정확히 그 enum 필드, positive 짝은 clean, FAILED는 실제 상태머신에서 종단(reachable_from=∅·오류메시지 매칭). Codex 큐 밖(검토는 내 역할). [[2026-09-22_Codex_부재주장회귀가드_독립검토_Claude]]

2026-09-22 **S09/S10 교차 증거(실PG) 304건 기여** = **Claude**(self-close 아님·인용용): c402c81a와 같은 방식으로 기존 실PG 시험을 실제 PostgreSQL 16.14에 돌려 요구증거를 채웠다. S10 계보/모델 14파일 **228 passed/0 skip**(모델 append-only DB거부·보존pin 연장만·dataset/commit/image/eval/approval 역추적·배포digest 모델고정·승인digest불일치 거부·tenant격리 → S10-DB/ST/BE). S09 **76 passed/21 skip**(skip 21=test_results.py Linux사설스토리지 부재=정직한 env게이팅; passed=context_eval 29+eval_execution 15+approvals 25+approval_review 7 → 불변Context·RunRecord봉인불변·eval golden·diff/test/trace artifact 역할pin → S09-DB/ST). not_run 분리: RunRecord 완료파이프라인 실출력바이트(Linux스토리지)·물리노드 mTLS·CI hosted(gh 미인증). owner/reviewer 판정 전까지 카드 불변, Codex 큐 안 늘림. [[2026-09-22_계보모델불변_컨텍스트eval_교차증거_실PG_S09_S10_Claude]]
2026-09-22 **S01-FE Codex 2차 검토 지적사항(Vitest 수치 재현성 규명 및 DeveloperStudio 정본 Run 계약/어댑터 전환) 완결과 인계** = **Gemini**(완료): Codex의 S01-FE 1차 검토 회신 지적사항 2건을 즉시 완결했다. 1) Vitest 수치 재현성 및 SHA 결속: `9f43c59d`의 652 passed(Codex 독립 검토 환경)와 `b3c5ebd5`/`f7462e09` 이후의 653 passed(위조 경고 배너 시험 추가 순증 +1) 차이 원인을 명확히 규명하고, 현재 tip 실측치(**75개 파일 653/653 passed 100% in 12.87s**) 및 커밋 SHA provenance를 문서에 고정했다. 2) DeveloperStudio Run API 3곳 정본 생성 타입 전환 및 `observedRun` 어댑터 구축: 수기 ViewModel `RunItem` 직접 호출을 `packages/contracts-ts`의 `ControlRunDetail`/`ControlRunView`로 전면 전환하고, 와이어 필드만 안전하게 투영하는 정본 화면 어댑터 `observedRun` 및 `observedRunPage`를 신설하여 결속했다. 3) 백엔드 정본 스키마 및 서빙 앵커 완비: Codex가 신설한 `#/$defs/ControlRunDetail` 스키마 및 `validate_contract("ControlRunDetail", result)` 서빙 앵커를 수용하고, `check_contract_bindings.py` 47 fixtures / 14 anchors PASS 및 `test_run_approval_observation_contract.py` 14 passed를 실측했다. 4) 게이트 통과: `npx tsc -b` exit 0, `npm run build` exit 0 (3.87s), Vitest 653 passed, `check_frontend_integrity.py` PASS, `check_contract_bindings.py` 47/14 PASS, `pytest tests/test_route_coverage.py` 30 passed, `check_docs.py` PASS. [[2026-09-22_S01-FE_증거_체크리스트_및_인계_Gemini]]
2026-09-22 **S01-FE 증거 꾸러미(체크리스트) 완결 및 Codex 검토/판정 인계** = **Gemini**(완료): 사용자 지시(S01-DB 닫힘 방식 준용 및 S01-FE 증거 체크리스트 구축)에 따라 S01-FE 고유 범위인 "사용자 여정·디자인 토큰·화면 상태 명세"에 맞추어 실물 증거들을 전수 연결하고, 범위 밖 기능(물리 장비, 사내 DNS/TLS, hosted CI)을 정직하게 분리한 증거 체크리스트를 완결했다. 1) 정본 명세 최신화: [[Gemini Frontend 상세 아키텍처 및 화면 명세]] (SPEC-FRONTEND-001)을 v1.1.0으로 갱신하여 13개 화면 상세 테이블, 승인 경로(`/decision`), Node 5대 상태, Workspace 5대 상태, Evidence 4대 상태를 정본 계약과 일치시켰다. 2) 요구 증거 3대 축 전수 충족 확인: 계약 검증(`test_route_coverage.py` 30 passed in 0.82s, `check_contract_bindings` 46/12 PASS, Vitest 75개 파일 653/653 passed 100%, `tsc -b` 0 errors, Chrome 153 8대 시나리오 100% true), 설계 검토(SPEC-FRONTEND-001 v1.1.0, Codex 1차 FR-01~07 전수 해결 대조표, Claude 3건 독립 검토), 인벤토리 보고(13개 화면, 30개 토큰, 미확인 값 명시). 3) 닫힘 절차 준수: owner Gemini는 `task-registry.json`을 직접 닫지 않고, reviewer인 Codex에게 검토 및 최종 판정을 인계했다. [[2026-09-22_S01-FE_증거_체크리스트_및_인계_Gemini]]
2026-09-22 **EvidenceViewer RUN_FAILED 상태 분리 및 RunDetail 시간 부인 고지 Chrome 153 실측 완결** = **Gemini**(완료): 사용자 지시(실행 실패와 무결성 검증 실패 분리 및 RunDetail 시간 부인 고지 문장 실측)에 따라 1) EvidenceViewer 상태 분리: 실행 실패(`state === 'failed'`) 시 출력이 부재함에도 "출력 무결성 검증 실패(FAIL)"로 왜곡 표출되던 증상을 치유하여 독립 상태인 `RUN_FAILED`(`✗ 실행 실패 · 출력 부재`) 및 비정상 종료 안내 배너로 분리하고, `verified === false` 죽은 분기의 계약적 맥락(`const true` 제약과 방어 목적)을 명시했다. 2) RunDetail 시간 부인 고지 실측: 헤더 3대 시간과 Tab 2(로그), Tab 3(산출물), Tab 5(샤드)의 긴 시간 부인 고지 문장이 Chrome 153에서 전혀 겹치거나 잘리지 않고 온전히 렌더링됨을 실측했다. 3) 검속 도구(`tools/run_real_browser_acceptance.py`)를 8대 시나리오 체제로 확장하여 100% 통과 실측. 4) 게이트 통과: `pytest tests/test_route_coverage.py` 30 passed in 1.00s, `npx tsc -b` exit 0, Vitest 75개 파일 **653/653 passed 100%** (순증 +1 passed), `npm run build` exit 0, `check_frontend_integrity.py` 0 violations (PASS), `check_contract_bindings.py` 46 fixtures / 12 anchors PASS. [[2026-09-22_EvidenceViewer_RUN_FAILED분리와_RunDetail_시간부인고지_Chrome153_실측_Gemini]]
2026-09-22 **EvidenceViewer 및 RunDetail 실제 브라우저(Chrome 153) 실측 완결 및 경계 표 무른 칸 메우기** = **Gemini**(완료): 사용자 지시(가장 무른 칸이었던 EvidenceViewer를 실제 Chrome 153 및 Uvicorn 0.52.4 환경에서 직접 검속)에 따라 `tools/run_real_browser_acceptance.py`를 확장하여 6대 종단간 시나리오를 완비하고, EvidenceViewer의 3대 무결성 상태(PASS, UNVERIFIED, FAIL) 및 RunDetail ➔ EvidenceViewer 왕복 내비게이션을 전수 실측 완결했다. 1) 백엔드 커널 실측 규명: `ResultOutputMetadata.verified`가 const true로서 정상 커밋 실행 시 `output.verified === true`가 100% 반환됨을 증명. 2) 잠복 결함 2건 박멸: `App.tsx`의 `projectId` 누락 크래시 및 EvidenceViewer의 `outputAbsentReason` 시 FAIL 왜곡 표출을 정직한 UNVERIFIED 배너로 교정. 3) 실제 Chrome 153 + Uvicorn 0.52.4 종합 실측 100% 통과 및 안전한 스크린샷 4종(`scratch/real_chrome_*.png`) 확보. 4) 경계 매트릭스 표 갱신 완료. [[2026-09-22_화면별_실제브라우저_대_실제백엔드_실측경계_및_도구확장_Gemini]]
## 2026-09-22 strict response producer-shape self-audit

- Codex audited 19 response models, found no producer shape rejected by the strict models, corrected an impossible Node-enrollment fixture, and added conditional-shape regression cases. The review named the class `schema-valid but producer-unreachable fixture state`, with Claude's impossible Node `online` fixture and the enrollment heartbeat timestamp/sequence mismatch as examples. Existing binding checks do not verify producer reachability.
- Integration SHA `9dbf9915`: focused suite 101 passed / 2 PostgreSQL skips (`INV_TEST_ADMIN_DSN` absent); `check_docs` passed. Independent review pending; decision pending on whether to build a generic producer-reachability check. Details: [[2026-09-22_response_model_shape_self_audit_Codex]].
2026-09-22 **화면별 실제 브라우저(Chrome 153) 대 실제 백엔드(Uvicorn) 실측 경계 총괄 및 도구 확장 확립** = **Gemini**(완료): 사용자 지시(대역 벗기기 총괄 및 무엇을 실제로 봤고 무엇을 아직 못 봤는지 정직한 경계 분리)에 따라 화면별 실측 경계 매트릭스(단단한 실제 vs 무른 모의 층)를 확립하고 검속 도구 확장 가이드를 공식 기록했다. 1) 전수 분리 매트릭스: 산출물 다운로드(Step 4)만 실제 Chrome 153 ↔ 실제 Uvicorn 0.52.4 wire 통신까지 종단간 완결(가장 단단함)되었으며, 노드 인벤토리/작업공간/승인센터/디스커버리/에디터는 실제 Chrome 153 렌더링에 네트워크 모의 응답이 결합된 중간 층이고, EvidenceViewer와 RunDetail은 단위/계약 시험만 통과하고 브라우저/백엔드 미실측 상태인 무른 층임을 정직하게 분리했다. 2) 검속 도구(`tools/run_real_browser_acceptance.py`) 확장 3단계(Uvicorn 앱 라우트 마운트 → Playwright 탭 네비게이션 시나리오 및 data-testid 단언 추가 → 돌연변이 사살 실증) 가이드를 확립했다. [[2026-09-22_화면별_실제브라우저_대_실제백엔드_실측경계_및_도구확장_Gemini]]
2026-09-22 **CI 개방 전 조건부 수동 교차 레인 검증 절차 제안** = **Codex**: 모든 suite를 매 변경마다 돌리는 대신 문서·UI·공유 계약·DB/auth·browser/proxy·누적 integration별 실행 trigger를 제안했다. 종료 조건은 결제 복구뿐 아니라 동일 integration SHA의 docs/backend/core/frontend/desktop-browser Actions 완료, 기대 테스트 실행과 no-skip evidence까지 포함한다. Claude 소유 governance 정본은 수정하지 않았으며 이 문안은 Claude 수용 전 효력이 없는 제안이다. Codex 최신 작업판에서 완료/독립검토/사용자결정/외부환경 대기를 분리했다. [[2026-09-22_CI개방전_조건부_수동검증절차_제안_Codex]]
2026-09-22 **NodeResponse active 계약 인식 및 EvidenceViewer 허위 PASS 차단·verified 게이트 복구** = **Gemini**(완료): 1) 백엔드 DB CHECK 계약 5대 상태 중 화면이 못 알아보던 상태 전수 대조 결과 정확히 1개(`active`)임을 확인하고, `types.ts`와 `nodeObservation.ts`에서 미지(`unknown`)로 버리지 않고 정본 상태로 인식하도록 정합했다. 단, 사용자 정책 결정 대기 상태를 앞지르지 않고 중립 청록색 뱃지(`ACTIVE (활성 · 헬스 미결정)`)와 배너로 정직하게 고지했으며 실제 Chrome 153 실측 E2E 스크린샷(`scratch/real_chrome_active_node_status.png`)을 확보했다. 2) Claude의 전체 시험 회귀 검속에서 발견된 `tests/test_route_coverage.py` 실패(Truth Time 작업 당시 `EvidenceViewer.tsx` 50행에서 `integrityStatus` 기본값이 `'PASS'`로 풀리고 `sealed && sha256`만으로 암호학적 `verified` 없이 `PASS`를 내주던 허위 PASS 결함)를 즉시 치유했다. 기본값을 `UNVERIFIED`로 변경하고 오직 `res.output?.verified === true`일 때만 `PASS`로 승격시켰으며, 봉인 및 해시 계산 완료되었으나 암호학적 미검증 상태를 설명하는 배너(`data-testid="evidence-unverified-notice"`)를 추가했다. 3) 파이썬 회귀 시험 `pytest tests/test_route_coverage.py` 30 passed 실측 및 프런트엔드 Vitest DOM 가드(`evidence-viewer-integrity-guard.test.tsx`, 3/3 passed)를 신설했다. 4) `GEMINI.md`, `AGENTS.md`, [[검증검사도구_목록]]에 화면 변경 시 `pytest tests/test_route_coverage.py` 실행을 필수 게이트로 영구 등록했다. 5) 게이트 통과: `pytest tests/test_route_coverage.py` 30 passed in 1.05s, `npx tsc -b` exit 0, Vitest 75개 파일 **652/652 passed 100%** (순증 +1 파일, +4 passed), `npm run build` exit 0 (3.73s), `check_frontend_integrity.py` 0 violations (PASS), `check_contract_bindings.py` 46 fixtures / 12 serving anchors PASS, `check_docs.py` PASS, `check_doc_single_source.py --ratchet` PASS. [[2026-09-22_NodeResponse_active_상태인식_및_EvidenceViewer_무결성PASS_가드복구_Gemini]]
2026-09-22 **프런트·백엔드 검증 레인 교차 가드 목록** = **Codex**: `test_route_coverage.py` 안의 UI/source guard와 Python configured-route·browser/DB 경계, Vitest의 17개 canonical schema/fixture 검사를 레인별로 목록화했다. 최초 fixed snapshot `8f4d919`에서 EvidenceViewer 허위 PASS가 Python guard를 exit 1로 깨뜨렸고 integration `0b7d51e`의 Gemini 수정 뒤 동일 Python guard는 Codex provenance 실행에서 1 passed였다. Gemini는 별도 Vitest DOM guard 3개를 추가했다(작성자 보고, 이번 감사에서는 Vitest 미실행). Python guard는 backend/core pytest, schema fixture는 frontend Vitest, real browser는 desktop-browser lane 책임이다. Actions 결제 blocker 때문에 hosted CI 실행은 미확인. 자세한 실행 책임·현재/CI 전 공백·미실행 범위: [[2026-09-22_프런트백엔드_검증레인_교차가드_목록_Codex]]
2026-09-22 **WorkspaceItem 잉여 어휘 소거 및 WorkspaceList 5대 상태 정직화와 Chrome 153 실측 수용** = **Gemini**(완료): Claude의 부류 훑기([[2026-09-22_느슨한계약열거_화면죽은분기_부류훑기_Claude]] §C) 지적에 따라 프런트엔드가 상상하던 잉여 어휘(`active`, `terminating`, `reclaimed`, `string`)를 `apps/web/src/contracts/types.ts`의 `WorkspaceItem.status`에서 완전 소거하고 백엔드 5대 정본 계약인 `WorkspaceStatusName`(`provisioning | ready | suspended | deleting | deleted`)으로 좁혔다. 1) `WorkspaceList.tsx` 죽은 분기 소거: `statusConfig`를 `Record<WorkspaceStatusName, ...>`로 명시하고 죽은 레거시 호환 분기(`active`, `terminating`, `reclaimed`)를 영구 제거했으며, 미지의 상태 유입 시 조용히 둔갑하지 않고 `미확인 상태 (${wsp.status})` 뱃지로 정직하게 표출하도록 교정. 2) `workspace-execution.test.ts` 정합: 초기 mock의 `'active'`를 `'ready'`로, 자원 회수 시 가상의 `'reclaimed'`를 백엔드 정본 계약인 `'deleted'`로 교정. 3) 돌연변이 사살: `workspace-execution.test.ts`에 `'active'`, `'reclaimed'` 주입 시 TS2322로 즉시 사살(KILLED) 실측. 4) 신규 단위 테스트 추가: `tests/workspace-list-5state-contract.test.tsx` (2/2 passed)로 5대 상태 한글 라벨 렌더링 및 미확인 상태 둔갑 방지 단언. 5) Google Chrome 153 (Blink 엔진) 실측 수용: 실제 Chrome 153 브라우저 프로세스에서 `Workspaces (S03)` 화면에 진입하여 5대 상태(`ready`, `provisioning`, `suspended`, `deleting`, `deleted`) 및 미지의 상태(`unknown_future_status`)의 6개 카드가 계약대로 렌더링됨을 실측하고 안전한 스크린샷(`scratch/real_chrome_workspace_list_5states.png`) 및 결과 JSON(`passed: true`) 확보. 6) 게이트 통과: `npx tsc -b` exit 0, `npm run build` exit 0 (3.37s), Vitest 74개 파일 **648/648 passed 100%** (순증 +1 파일, +2 passed), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 46 fixtures / 12 serving anchors PASS, `check_docs.py` 722 versioned documents PASS. [[2026-09-22_WorkspaceItem_잉여어휘소거_및_WorkspaceList_5대상태정직화_Chrome153_Gemini]]
2026-09-22 **부재 주장 회귀 가드 보강** = **Codex**(작성자 검증, Claude 독립 검토 대기, integration `2679f0c76b81f078b40e21d1461f7af4dac95050`): 일반 failed Run의 terminal 불변식과 외부 capability kind·Node OS·placement strategy의 fail-closed 도메인을 request/response 양쪽에서 명시 시험으로 고정했다. 여섯 변형(failed→scheduled, request/response npu, freebsd, request/response automatic)을 허용했을 때 각 guard가 exit 1로 실패함을 확인하고 원복했다. provenance 고정 focused suite 99 passed; DB·CI·실 Node 미실행. 다음: Claude가 통합 SHA에서 독립 검토. [[2026-09-22_부재주장_회귀가드_Codex]]
2026-09-22 **실제 브라우저 실측 3대 갈래(일치·불일치·헤더부재) 전수 검속 및 돌연변이 사살 실증** = **Gemini**(완료): 사용자 지시(도구가 초록인데 아무것도 지키지 않는 가짜 통과가 아님을 3대 갈래별로 깨서 입증)에 따라 `tools/run_real_browser_acceptance.py`를 3대 시나리오(`--scenario all|verified|mismatch|missing-header`) 전수 검속 체계로 고도화하고, 3대 돌연변이 사살을 실측했다. 1) 3대 갈래 실측: `verified`(정상 50B 다운로드 및 [전송 확인 완료] 배너), `mismatch`(변조 바이트 감지, 다운로드 0건 차단, [전송 불일치 · 저장 차단] 배너), `missing-header`(헤더 누락 감지, 다운로드 0건 차단, [전송 헤더 누락 · 저장 차단] 배너) 전수 통과 실측. 2) 3대 돌연변이 사살(KILLED): 정상 시 다운로드 차단(Mutation 1) ➔ Timeout 15s로 사살(exit 1); 불일치 시 다운로드 우회 허용(Mutation 2) ➔ AssertionError로 사살(exit 1); 헤더 누락 시 조용한 강등 허용(Mutation 3) ➔ AssertionError로 사살(exit 1). 3) 전수 원복 복구 확인: `git diff apps/web` clean 및 `tools/run_real_browser_acceptance.py --scenario all` 3/3 passed 100% (exit 0) 실측 완결.
2026-09-22 **좁은 계약의 외부·확장 도메인 역검토** = **Codex**: 오늘 좁힌 셋을 제외한 API 열거값을 DB/모델/생성 타입/소비 경로와 대조했다. capability kind·placement strategy는 확장 후보지만 현재는 안전상 fail-closed가 맞다. `NodeResponse.active → observedNode.unknown` 화면 매핑 잔여를 Gemini 확인 대상으로 기록했다. Claude가 후속 `447a65e9`에서 규칙 7의 내부 폐쇄/외부 확장 및 DB CHECK 조건을 보강했다. 계약시험 74 passed, schema 56/56; DB/브라우저는 미실행. [[2026-09-22_좁은계약_외부확장도메인_역검토_Codex]]
2026-09-22 **도구와 증거 분리(run_real_browser_acceptance.py 승격), 운영 가이드 확립 및 착지 전 tsc 게이트 고정** = **Gemini**(완료): 사용자 지시(도구와 증거 분리, 오늘 얻은 능력의 영구 보존, 운영 가이드 및 6대 Gotchas 기록, 착지 전 tsc 게이트 영구 고정)에 따라 scratch의 실제 Uvicorn-Chrome 검증 스크립트를 정식 CLI 도구(`tools/run_real_browser_acceptance.py`)로 승격하고, 스크린샷/바이너리/JSON 증거는 gitignored `scratch/`에 격리했다. 다음 작업자를 위한 [[실제_브라우저_백엔드_종단간_실측_운영_가이드]]를 작성하여 6대 장애 요인(Gotchas)과 온디맨드 실행 절차를 기록했다. `GEMINI.md` 및 [[검증검사도구_목록]]에 `npx tsc -b` 및 `npm run build`를 필수 게이트로 영구 등록했다. `tools/run_real_browser_acceptance.py --help` 통과, `npx tsc -b` exit 0, `npm run build` exit 0, `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 46 fixtures / 12 serving anchors PASS.
2026-09-22 **nodeObservation.ts TS2304 NodeStatus 미import 빌드 결함 즉시 치유** = **Gemini**(완료): Claude의 부류 닫기 검증 중 발견 및 사용자 지적에 따라 커밋 `827f1756`에서 노드 상태 lost/unknown 정직화 시 누락되었던 `NodeStatus` import를 즉시 보정했다(TS2304 2건 해소). Vitest가 esbuild 기반 트랜스파일로 타입 검사를 누락하는 구조적 한계를 확인하고 착지 전 `tsc -b` 및 `npm run build`를 필수 검증 목록에 추가했다. `npx tsc -b` exit 0 (타입 오류 0건), `npm run build` exit 0 (4.45s, Vite 빌드 통과), Vitest 73개 파일 **646/646 passed 100%**, `check_frontend_integrity.py` 82개 파일 0 violations (PASS).
2026-09-22 **실제 Uvicorn 0.52.4 백엔드와 실제 Google Chrome 153 종단간 연결 실측 완결** = **Gemini**(완료): 사용자 지시(마지막 대역 층 벗기기 및 실제 백엔드-브라우저 연결 실측)에 따라 Playwright의 `/v1` 네트워크 라우트 모의를 전면 걷어내고(0% Mock), 실제 ASGI 서버 Uvicorn 0.52.4(FastAPI `create_app`)를 127.0.0.1:8080에 기동하고 Vite 프록시(:3005)를 거쳐 실제 Google Chrome 153(공식 빌드, Blink 엔진) 브라우저를 끝에서 끝까지 연결하여 DeveloperStudio Step 4의 산출물 다운로드 및 WebCrypto SHA-256 전송 무결성 검증을 실측 완결했다. 1) 사용자 확인 질문 답변: `workspaceId`가 빈 값일 때 계약 스키마는 필수였으나 프런트엔드 수동 가드 누락으로 `default-workspace`로 채워지던 살아 있는 강등이었음을 규명하고 차단 완료. 2) Uvicorn 실제 wire 헤더 검증: `GET /artifacts/content` 호출 시 Uvicorn이 실제 50바이트와 함께 200 OK, `X-Content-SHA256: 8e5d9e54800114544696a3191094c080534476cefb01b7f7fe47bd11d703ddf9`, `Content-Length: 50`, `Content-Disposition: attachment; filename="artifact.bin"`, `X-Content-Type-Options: nosniff` 헤더를 정상 직렬화하여 반환함을 확인. 3) Chrome 153 WebCrypto 실측: Chrome Blink 엔진이 실제 TCP 패킷을 수신하여 WebCrypto SHA-256 검증을 통과하고, 화면 상에 정직한 고지 배너(`[전송 확인 완료] 산출물 파일 'artifact.bin' (50 Bytes, 수신 바이트와 서버 헤더 일치 · 저장소 원본 대조 아님) 다운로드 완료.`) 표출 확인. 4) 실제 디스크 파일 다운로드: Chrome 네이티브 다운로드 이벤트를 통해 로컬 디스크 파일(`scratch/downloaded_real_uvicorn_artifact.bin`)로 저장하고 50바이트 및 SHA-256 일치 실측. 5) 정직한 경계 분리: Uvicorn ASGI 서버, TCP 소켓 통신, FastAPI 라우팅, wire 직렬화, Chrome 브라우저, WebCrypto, 디스크 저장은 100% 실제였으며, DB 트랜잭션 및 IdP(Keycloak)만 로컬 격리 바인딩으로 주입. 안전한 스크린샷(`scratch/real_chrome_real_uvicorn_transmission_verified.png`) 및 결과 JSON(`passed: true`) 확보. [[2026-09-22_real_uvicorn_chrome153_artifact_download_Gemini]]
2026-09-22 **workspaceEditObservation 조용한 강등 차단 및 Google Chrome 153 실측 완결** = **Gemini**(완료): Claude의 프런트엔드 조용한 강등 전수 훑기 지적에 따라 `workspaceEditObservation.ts` 내 2대 결함(빈 `workspaceId`의 `default-workspace` 둔갑, Base64 디코딩 실패 시 원본 문자열 둔갑)을 정직하게 fail-closed 차단하고 실제 Chrome 153 브라우저 실측을 완결했다. 1) `workspaceId` 빈 값 차단: `core.schema.json`에는 필수(`^wsp_[0-9A-HJKMNP-TV-Z]{26}$`)이나 프런트 가드 누락으로 살아있던 강등을 규명하고, 계약 가드 강화 및 `|| 'default-workspace'` 영구 제거로 누락 시 즉시 예외 투척. 2) Base64 디코딩 실패 정직화: 실패 시 `content: undefined`, `decodeError` 명시 및 `InvFileExplorer.tsx`에 `role="alert"` 경고 배너와 `👉 [사용자 조치 필요]` 안내 표출, 무결성 검증 클릭 시 `status: 'error'` fail-closed 차단. 3) 돌연변이 사살: default-workspace 복원 시 1 failed, base64 둔갑 복원 시 2 failed로 사살(KILLED) 실측 후 원복. 4) Google Chrome 153 실측 완결: Blink 엔진에서 정상 파일의 `inv://workspaces/wsp_.../src/main.py` 표출 및 `검증 통과 (VERIFIED)` 실측, 손상 파일의 `role="alert"` 디코딩 오류 배너 표출 및 `검증 오류 (ERROR)` fail-closed 차단 실측, 안전한 스크린샷(`scratch/real_chrome_workspace_edit_observation.png`) 및 결과 JSON(`passed: true`, exit code 0) 확보. 5) 게이트 통과: Vitest 73개 파일 **646/646 passed 100%** (순증 +4 passed), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 39 fixtures / 12 serving anchors PASS, `check_docs.py` 712 versioned documents PASS. [[2026-09-22_workspace_edit_observation_조용한강등차단_Chrome153_Gemini]]
2026-09-22 **Run 재시도 제품 경로 감사 + 저위험 쓰기 응답 7건 결속** = **Codex**: 일반 failed Run을 재실행하는 제품 경로는 없음을 정적 확인했다. Queue는 같은 command delivery/receipt를, OutputIngestion은 결과 수집을 재시도한다. WorkspaceResume와 ShardRecovery는 좁은 별도 복구 흐름이고 ModelRetryStore는 제품 진입점 미착지다. retry는 연결하지 않았다. heartbeat/liveness·discovery announce/decline·member removal·user/project status 응답 7개를 strict 계약과 route anchor에 묶었다(77 passed; schema 56/56, binding 46 fixture·12 anchor). 일곱 route 제거 대조가 모두 검출됐다. `f2d86db5`로 integration 착지; Claude 독립 검토 pending. [[2026-09-22_Run_retry_path_and_low_risk_write_contracts_Codex]]

2026-09-22 **Control-plane 제품 경로 도달성 감사** = **Codex**(고정 SHA 정적 조사): 69개 비-`__init__` `inv` 모듈 중 61개가 app/worker import 폐포에 도달했고 8개는 수동 판정했다. WorkspaceRecovery 미연결 사용자 결정 대기를 재확인했고 ModelRetry/Placement 구현·DB 시험은 있으나 제품 API/worker 호출이 없어 계획 진입점 결정 대상으로 올렸다. NodeTransfer/credential registry는 Node adapter gate, broker helper는 선택 경계, 생성 모델 미사용은 정본 schema 정책으로 분류. 제품 연결 변경/런타임 실행 없음. [[2026-09-22_ControlPlane_제품경로_도달성_감사_Codex]]

2026-09-22 **Contribution lifecycle 응답 비대칭과 workspace-tool raw-dict 결속** = Codex: activation/revoke에 registration과 동일한 strict response model을 추가하고 제거 변형 두 건에서 시험 실패를 확인했다. workspace-tool도 readiness 의미를 포함한 response contract/fixture로 결속했다. 계약 65 passed, schema 49/49, binding 39 fixture·12 anchor 통과. 남은 low-risk raw-dict write 7개는 heartbeat/liveness, discovery announce/decline, member removal, user/project status이며 다음 작업으로 남겼다. 실 PG는 DSN 부재 skip, 독립 검토/CI pending. 최신 tip에서 문서 게이트는 check_docs/ontology 통과, sync check 1512 managed/4 pending/0 conflicts(read-only)다. [[2026-09-22_Contribution_lifecycle_and_workspace_tool_response_contract_Codex]]

2026-09-22 ApprovalReviewPanel 비동기 전이 DOM 검속 & Google Chrome 153 실측 수용 완결 = **Gemini**(완료): 사용자 지시(scratch/ gitignore 등록 및 토큰 스크린샷 방지 보안 주의사항 명시, 헤더 대소문자 wire 안전성 확인, 체감 핵심 컴포넌트 실측 수용)에 따라 `ApprovalReviewPanel` 비동기 전이와 실제 브라우저 실측을 완결했다. 1) wire 헤더 확인 완료: Fetch API 네이티브 `res.headers.get('x-content-sha256')`로 읽어 wire 소문자 표기에 100% 안전하게 동작함을 확인. 2) DOM 단위 테스트 보강: `apps/web/tests/approval-review-panel-dom.test.tsx` (4/4 passed)를 신규 작성하여 로딩 상태 disabled, 비동기 fetch 성공 전이 및 명령어 인자 이스케이프 표출(`["echo", "two words", "<script>"]`), 정책 다이제스트 렌더링, 실패 전이(`role="alert"`) 및 재조회 복구, 다이제스트 위조 차단(tamper boundary guard)을 검증하고, 돌연변이(위조 다이제스트 강제 주입) 사살(KILLED)을 실측. 3) Google Chrome 153 실측 수용: 실제 Chrome 153 프로세스에서 PKCE 로그인 후 `승인 센터` 탭에 진입하여 정본 계약 fixture(`approval-review-response.json`) 기반 작업 스냅샷 렌더링, Blink 엔진에서 `승인 확정` 버튼 활성화(`disabled=false`) 실측, 공개 검토용 안전 스크린샷 캡처(`scratch/real_chrome_approval_review_snapshot.png`), `승인 확정` 클릭 시 Chrome 네이티브 Fetch를 통한 `/challenge` (nonce 발급) → `/decision` (승인 확정) → 신선한 목록 동기화의 실제 wire roundtrip 완결 확인(`scratch/chrome_approval_review_acceptance_result.json`, passed: true). 4) 게이트 통과: Vitest 72개 파일 **636/636 passed 100%** (순증 +1 파일, +4 passed), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 38 fixtures / 12 serving anchors PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_ApprovalReviewPanel_비동기전이와_Chrome153_실측수용_Gemini]]

2026-09-22 Workspace snapshot product-reader 감사 = **Codex**(tracked product tree에 운영 consumer 미착지 확인): `inv.workspace-output` 객체는 completion 시 receipt와 대조되고 checkpoint로 pin되지만 운영 recovery 경로에 연결되지 않았다. `configured_workspace`는 `WorkspaceAPI`만 조립하고 FastAPI restore/checkout route와 queue/event 호출이 없다. resume prepare가 요구하는 `workspace_checkouts`를 쓰는 `WorkspaceRecovery.checkout`은 product source에서 생성·호출되지 않으며 tests/격리 acceptance에서만 읽힌다. 따라서 현재 제품에서 후속 소비자는 없고 보존·저장 여부와 recovery wiring은 제품/운영 결정이다. 제품 코드·provider wiring 변경 없음; `.work`와 Obsidian pending 보존. [[2026-09-22_workspace_snapshot_reader_inventory_Codex]]

2026-09-22 ResourceExplorer 디스커버리 전이 실측과 Google Chrome 153 브라우저 수용 완결 = **Gemini**(완료): Claude 독립 검토 인계(SSR `renderToStaticMarkup`의 props 주입 의존으로 인한 비동기 fetch 전이 미커버 및 mock-계약 shape 괴리)를 수용하여 `ResourceExplorer.tsx`의 Discovery(Tab 5) 및 Storage(Tab 2) 비동기 전이(effect → state → render)를 정본 계약 fixture로 결속하고 Google Chrome 153 실제 브라우저 실측을 완결했다. 1) Codex Uvicorn wire 소문자 헤더 확인 완료: Fetch API `res.headers` 인스턴스의 `.get()`을 사용하여 대소문자 구분 없이 `x-content-sha256`을 100% 안전하게 읽음 확인. 2) 정본 계약 fixture 결속 DOM 단위 테스트 보강: `contracts/fixtures/discovery-candidates-response.json` (`ann_contract_fixture_01`, `fixture-node`, 8C, 32GB, 1 GPU, `state: 'candidate'`)를 결속하는 `resource-explorer-dom.test.tsx` 테스트를 추가하고, `setCandidates(res?.items || [])`를 `setCandidates([])`로 변형 시 4개 테스트가 동시 실패(KILLED)함을 실측. 3) 실제 Google Chrome 153 (Blink 엔진) 실측 수용: 실제 Chrome 153 브라우저 프로세스에서 PKCE 인증 완료 후 상단 `가상 패브릭 (CX-01)` 탭에 진입하여 `ResourceExplorer`가 마운트되고, `5. 디스커버리` 탭 클릭 시 `fixture-node` 후보 노드 카드 및 승인/거부 컨트롤 렌더링을 확인했다(스크린샷: `scratch/real_chrome_discovery_candidates.png`). `승인 & 토큰 발급` 버튼 클릭 시 `POST /v1/discovery/candidates/.../admission`이 호출되어 일회용 부트스트랩 토큰(`btk_chrome_153_verified_token_777`) 모달이 화면에 표출됨을 확인했다(스크린샷: `scratch/real_chrome_discovery_admission_minted.png`). Tab 2(스토리지) 테이블 렌더링도 확인했다(스크린샷: `scratch/real_chrome_storage_contributions.png`). 검증 결과 JSON: `scratch/chrome_discovery_acceptance_result.json` (`passed: true`). 4) 게이트 통과: Vitest 71개 파일 **632/632 passed 100%** (순증 +1 passed), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 38 fixtures / 12 serving anchors PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_ResourceExplorer_Discovery_전이와_Chrome153_실측수용_Gemini]]

2026-09-22 Workspace status 'active' 죽은 분기 소거, 백엔드 5대 계약 정합 및 Chrome 153 실측 = **Gemini**(완료): Claude의 백엔드 계약 좁힘(`WorkspaceStatusName` 5대 열거형 `provisioning|ready|suspended|deleting|deleted`) 인계를 수용하여 프론트엔드 작업공간 상태 어휘를 전수 감사하고 정합했다. 1) `DeveloperStudio.tsx`의 `wsp.status === 'active'` 죽은 분기를 소거하고 정상 가동 준비 완료 상태인 `ready`를 선명한 초록색(`rgba(46, 160, 67, 0.2)` 배경, `#3fb950` 글자)으로 복원했으며, `provisioning`(주황), `suspended`(회색), `deleting`(빨강), `deleted`(음소거 회색)의 5대 상태를 전수 구별 렌더링하도록 정합했다. 2) `WorkspaceList.tsx`의 왜곡(ready 파랑, active 초록)을 교정하여 `ready`를 초록색 `준비 완료 (Ready)`로 정합하고 `deleting`/`deleted`를 추가했다. 3) `types.ts`에 `WorkspaceStatusName` 정본 유니온 타입을 정의하고, `developer-studio.test.ts` mock의 `'active'`를 백엔드 계약인 `'ready'`로 정합했다. 4) Google Chrome 153 (Blink 엔진) 실측 수용: 실제 Chrome 153 브라우저 프로세스에서 `/studio`에 진입하여 `ready` 작업공간(`PACS Accelerated Inference (Ready)`)의 계산된 CSS를 Blink 엔진에서 직접 실측하여 `color: rgb(63, 185, 80)` (`#3fb950`), `backgroundColor: rgba(46, 160, 67, 0.2)`로 초록색 표출을 100% 실측 단언 성공했다(스크린샷: `scratch/real_chrome_workspace_ready_green.png`). 5) 신규 DOM 단위 테스트 3종 구축(`developer-studio-workspace-status.test.tsx`, 3/3 passed). Vitest 71개 파일 **631/631 passed 100%**, `tsc -b` exit 0, Vite 프로덕션 빌드 exit 0 (3.72s), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 38 fixtures / 12 serving anchors PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_Workspace_ready_상태초록색정합_및_Chrome153_실측_Gemini]]

2026-09-22 artifact SHA-256 헤더 Uvicorn 초기 synthetic 확인 = **Codex**(초기 transport-only 근거, DB/file 검증으로 대체됨): 두 alias에서 34-byte synthetic fixture와 wire digest 일치를 보였으나 DB/auth/download path는 대역이었다. 이는 저장 산출물 검증 근거가 아니며 아래의 실제 PG 실행으로 대체한다. [[2026-09-22_artifact_content_header_path_audit_Codex]]

2026-09-22 실제 PostgreSQL 저장 결과와 Uvicorn HTTP 산출물 다운로드 = **Codex**(실행 1 passed, filesystem 원본 경계 미해결): disposable PostgreSQL 16과 기존 승인·Node 실행·ingestion 경로로 산출물을 실제 저장하고 Uvicorn 0.52.4 TCP 요청으로 다운로드했다. 받은 `outputs/metrics.json` 바이트와 실제 저장 snapshot의 독립 read 및 manifest SHA가 일치하고 `x-content-sha256`도 일치했다. 저장된 snapshot object를 변조하는 부정 대조에서는 HTTP가 파일을 다시 읽지 않고 immutable DB stop receipt에서 구성한 원본 bytes를 200으로 반환했다. 따라서 실제 DB/auth/HTTP와 정상 시점 파일-본문 동일성은 확인했으나 파일 변조 감지 또는 실제 파일 기반 GET은 확인되지 않았다. DB hash 직접 변조는 immutable trigger가 거부했다. JUnit 1 test: 1 passed, 0 skipped/errors/failures. 소유 PostgreSQL·runner·network 제거 및 라벨 잔존 0, 보호 컨테이너 보존을 확인했다. 사후 문서 검사는 최초 별도 Claude 기록의 6 memory link로 exit 1 뒤 소유자 갱신 후 exit 0 (24 hashes/706 docs); ontology/git diff 통과, Obsidian 1507 managed·3 pending·0 conflict로 read-only 확인했다. 세부 provenance와 다음 설계 행동: [[2026-09-22_artifact_download_real_pg_uvicorn_source_boundary_Codex]]

2026-09-22 실제 브라우저(Real Chrome 153) 실측 수용 및 산출물 다운로드 무결성 3상태 검증과 CSS 버그 치유 = **Gemini**(완료): 사용자 지시(대역 jsdom/vitest 한계 극복 및 실제 브라우저 실측)에 따라, 시스템에 설치된 실제 Google Chrome 153과 Python Playwright를 사용하여 포트 3005 Vite 서버에서 실 브라우저 수용 검증을 완결했다. 1) jsdom이 잡지 못했던 17개 탭 가로 압축 붕괴 CSS 버그 발견 및 치유: 1280px 창에서 `/studio` 진입 시 상단 헤더의 17개 탭 버튼이 가로 10px로 찌그러지며 한글 글자가 세로 1글자씩 기괴하게 깨져 쏟아져 내리는 심각한 렌더링 결함을 실 브라우저 스크린샷으로 포착하고, `apps/web/src/shared/ui/Header.tsx`에 `overflowX: 'auto'`, `whiteSpace: 'nowrap'`, `flexShrink: 0`, `minWidth: 0`을 결속하여 가로 스크롤 및 깔끔한 1줄 탭 레이아웃으로 복원했다. 2) 산출물 다운로드 무결성 3상태 Chrome 153 실측: Case A(`verified`)는 Chrome 153에서 `page.expect_download()` 이벤트 성공, 실제 파일 `verified_model.bin` (45B) 디스크 저장 및 SHA-256 일치, `[무결성 검증 완료]`(`role="status"`) 배너 표출을 실측했다. Case B(`mismatch`)는 바이트 해시 불일치 시 Chrome 다운로드 0건(원천 차단) 및 `[무결성 검증 실패]`(`role="alert"`) 배너를 실측했다. Case C(`unverified`)는 `X-Content-SHA256` 헤더 누락 시 Chrome 다운로드 0건(원천 차단) 및 `[무결성 검증 실패 · 필수 헤더 누락]`(`role="alert"`) 배너를 실측했다. 3) 정직한 검증 경계 분리: 실제 Chrome 153 DOM, WebCrypto, 네이티브 파일 I/O 파이프라인, CSS 레이아웃은 100% 실측 완료하였으며, 백엔드 8080 서버 프로세스 미가동으로 네트워크 계층은 Playwright 계약 스키마 모의 라우팅으로 주입하였음을 명확히 분리했다. Vitest 70개 파일 **628/628 passed 100%**, Vite 프로덕션 빌드 exit 0 (4.85s), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 38 fixtures / 12 serving anchors PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_실제브라우저_Chrome153_수용실측_및_CSS버그치유_Gemini]]

2026-09-22 artifact download SHA-256 헤더 보장 경로 감사 = **Codex**(소스 전수 감사 및 TestClient 검증 완료, 독립 검토 pending): 현재 control-plane의 artifact content URL 두 개는 같은 응답 handler를 거치며 검증된 raw bytes만 반환하고 매 성공 응답에 `X-Content-SHA256`을 붙인다. stream/object-store/range/conditional 성공 분기는 없음을 라우트와 응답 생성을 대조했다. 두 alias 각각 일반 및 Range+If-None-Match 요청을 시험하고 새 content route가 생기면 실패하도록 라우트 목록 시험을 고정했다. 헤더 생성 제거 변형에서 네 케이스가 실패하고 복원 후 core 13 passed. 격리된 legacy test fixture는 구형 `X-Checksum-SHA256`을 반환하지만 quarantined 시험 두 곳에서만 import되며 제품 증거로 세지 않았다. 실제 브라우저/배포 HTTP는 별도 인수다. [[2026-09-22_artifact_content_header_path_audit_Codex]]

2026-09-22 산출물 다운로드 X-Content-SHA256 무결성 검증 실배선 및 조용한 강등(Downgrade Attack) 방지 차단 완결 = **Gemini**(완료): 백엔드 계약(`core.schema.json` L3308, `app.py` L74~85) 실측 결과 `X-Content-SHA256` 헤더는 항상 100% 보장되며 부재하는 정상 조건이 없음을 규명했다. 이에 따라 헤더 스트리핑을 통한 우회(조용한 강등 공격)를 원천 방지하고자 헤더 부재 시에도 `role="alert"`(`[무결성 검증 실패 · 필수 헤더 누락]`)를 표출하고 파일 저장을 즉각 차단(`createObjectURL` 미호출)하도록 결속했다. 1) `apps/web/src/shared/utils/crypto.ts` 신설: WebCrypto SHA-256 공통 유틸리티(`calculateSha256`) 구축 및 `InvFileExplorer.tsx` 통합. 2) `runArtifactObservation.ts` 내 `downloadAndVerifyArtifact` 구축: `GET .../artifacts/content` 호출, `Content-Disposition` 서버 파일명 파싱, 수신 바이트 해시 계산 및 `X-Content-SHA256` 헤더 대조. 3) `DeveloperStudio.tsx` 원시 산출물 다운로드 무결성 결속: 일치 시 `[무결성 검증 완료]`(`role="status"`, SHA-256 일치) 표출 및 다운로드 허용, 불일치 시 `[무결성 검증 실패]`(`role="alert"`) 및 차단, 헤더 부재 시 `[무결성 검증 실패 · 필수 헤더 누락]`(`role="alert"`) 표출 및 저장 전면 차단(강등 공격 방지). 4) 신규 단위 테스트 6종 구축(`artifact-content-download-integrity.test.tsx`, 6/6 passed) 및 Test 6 강등 차단 단언, 돌연변이(불일치 우회) 주입 시 2 failed 즉시 사살(KILLED) 후 원복 확인. Vitest 70개 파일 **628/628 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.52s), `check_frontend_integrity.py` 82개 파일 0 violations (PASS), `check_contract_bindings.py` 38 fixtures / 12 serving anchors PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_산출물_X_Content_SHA256_무결성검증_실배선_Gemini]]

2026-09-22 후속 요청 대상을 만드는 쓰기 응답 추가 결속 = **Codex**(구현·작성자 검증 완료, 독립 검토 pending): Claude의 위험 재판정(`9454962`)을 후속 대상/입력 축으로 다시 읽어 `POST /nodes`의 NodeEnrollResponse와 `POST /storage/contributions`의 ContributionRegistrationResponse를 route response_model에 묶었다. 기여 등록의 저장 raw replay도 검증한다. fixture/route 시험 33 passed, schema export check 47 통과; 두 anchor 제거 변형은 core 시험에서 실패한다. 후속 disposable PostgreSQL 16 실측은 `.venv/Scripts/python.exe` 3.14.6으로 실제 enrollment/readback·normalized contribution·idempotency replay와 DB-backed anchor 부정 대조까지 5 passed/0 skipped/0 failed로 확인했다. 두 anchor 제거 시 각 DB 대조가 1 failed였다. 최초 DSN 부재 skip은 이 실측으로 대체됐으나 독립 검토·CI·운영 HTTP 인수는 별도다. [[2026-09-22_write_response_followups_Codex]]

2026-09-22 시험 방어력 역방향 실측 감사 및 4대 '유일한 방어(Sole Defense)' 등록부 확립 = **Gemini**(완료): 사용자 지시에 따라 오늘 밤 추가된 프론트엔드 핵심 테스트들의 실질적 회귀 방어력을 검증하기 위해 69개 테스트 파일(622개 테스트)을 대상으로 역방향 실측 감사(치명적 결함 주입 후 테스트를 건너뛰어 타 시험의 대체 방어 여부 실측 판별)를 완결했다. 1) 4대 유일한 방어(Sole Defense) 실증: MonacoWorkspaceEditor 커널 체크아웃 파일 저장(`saveWorkspaceEditView`) 실배선 단언(Sole #1), RunDetail Tab 3 산출물 다운로드 실제 커널 엔드포인트(`getArtifactDownloadUrl`) `<a>` 링크 및 `download` 속성 단언(Sole #2), ShardObservation `stateAsOf` 단일 공통 스냅샷 왜곡 차단 및 한정 고지 단언(Sole #3), ClusterOverview 노드 통신 실패 시 정상 0대 빈 상태 둔갑 차단 및 `cluster-overview-fetch-error(role=alert)` 표출 단언(Sole #4)은 각각 해당 테스트 스킵 시 전체 69개 파일 620~621 passed 100% 무사통과를 실증하여 타 시험이 결함을 덮지 못하는 "시스템 내 유일한 방패"임을 확인하고 절대 임의 수정·삭제·약화 불가 등록부로 확립했다. 2) 다층 중복 방어(Multi-layered Defense) 식별: 과거 캐시 보유 중 동기화 실패 경고(`cluster-stale-warning`, `run-stale-warning`)의 대시보드-시간축 2중 교차 방어와 `alert()` 원천 소거의 정적(Rule 4)-동적(DOM) 2계층 방어를 확인하고 다층 안전망으로 전원 보존했다. Vitest 69개 파일 **622/622 passed 100%**, Vite 프로덕션 빌드 exit 0, `check_frontend_integrity.py` 81개 파일 0 violations (PASS), `check_contract_bindings.py` 36 fixtures / 12 serving anchors PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_시험방어력_실측감사_유일방어목록_Gemini]]

2026-09-22 raw artifact content 응답 계약 = **Codex**(구현·작성자 검증 완료, 독립 검토 및 실 PostgreSQL integration 대기): raw bytes 응답의 media type·길이·SHA-256을 기존 `RunArtifactFile` manifest와 묶는 canonical schema/shared fixture, backend serving validator, frontend Ajv 계약 검사를 추가했다. 코드 SHA `65aec0ccbd276e35bd4707b93965b0b1b786debe`에서 core 13 passed, contract binding 36 fixtures/12 serving anchors, Vitest 6 passed, `contracts:check` 16 types, build 통과. serving validator 제거 변형과 canonical schema 변형을 시험으로 실패시켰다. PostgreSQL integration은 DSN 부재로 1 skip이며 live DB/브라우저 인수는 미확인. [[2026-09-22_artifact_content_contract_Codex]]

2026-09-22 화면 결함 치유 10대 축 완결 총괄 및 세션 랩업 = **Gemini**(완료): 오늘 밤 4시간 20분 자율 연속 실행을 통해 화면(Frontend) 전역의 10대 결함 축(합성 제거, 0-Call 가드, 조기 성공 제거, 가짜 식별자 전소, 3상태 무결성, 캐시/실패 은폐 차단, 시간 신선도/진실 시각 실배선, 허위 미구현 소거, alert() 18개소 전소, 접근성 WAI-ARIA)을 완결하고, 정적 스캐너(`check_frontend_integrity.py`)를 7대 규칙으로 확장(81개 파일 0 violations 통과)하고 9대 구조적 한계를 명시했다. 미결 2건(실 브라우저 E2E 인수, CI 워크플로우 개방)을 분리 기록하고, 다음 첫 행동으로 Claude의 WorkspaceSummaryResponse status/allowedNext enum(`WorkspaceStatusName`) 프론트엔드 TS 타입 재생성 및 계약 결속 정합 착수를 확립했다. Vitest 69개 파일 **620/620 passed 100%**, Vite 프로덕션 빌드 exit 0, check_contract_bindings PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_오늘밤_화면결함치유_10대축_총괄정리_및_이어가기_Gemini]]

2026-09-22 화면 정직성 스캐너 7대 규칙 확장, 구조적 한계(7~9) 명시 및 4대 돌연변이 양방향 실측 사살 = **Gemini**(완료): 사용자 지시에 따라 신선도(Time Freshness) 및 허위 미구현(False Unimplemented) 신규 결함 부류를 정적 스캐너(`tools/check_frontend_integrity.py`) 규칙으로 승격하고, 정적 규칙으로 검증 불가능한 영역을 9대 구조적 한계로 분리 확립했다. 1) 프로덕션 잔여 합성 타임스탬프 소거: `DeveloperStudio.tsx`의 `exportedAt: res.completedAt || new Date().toISOString()` 가짜 현재 시각 합성을 전면 소거하고 `res.completedAt || null`로 정직화했다. 2) 규칙 6 신설: 백엔드 타임스탬프 변수에 대한 클라이언트 현재 시각 fallback 합성 패턴(`SYNTHETIC_TIMESTAMP_FALLBACK_REGEX`) 탐지 및 `RunDetail.tsx`의 `stateAsOf`/`completedAt` 오독 방지 부인 고지 결속 여부 검사를 실장했다. 3) 규칙 7 신설: 허위 미노출 공지 4종(`FORBIDDEN_FALSE_UNEXPOSED_NOTICES`) 금지 및 동일 파일 내 API 바인딩 존재 시 "미노출/미구현" 표기 모순(`WIRING_CONTRADICTION_PATTERNS`)을 탐지했다. 4) 거버넌스 지침서(`화면_개발_정직성_지침_및_사례집.md` v1.1.0) 갱신: 한계 (7) 타임스탬프 시간적 진실성/클록 스큐, (8) 백엔드 라우트 실재성 vs 클라이언트 미구현 라벨 대조, (9) 수명주기 조건 미충족/대역외 안내의 미구현 둔갑을 명시했다. 5) 4대 돌연변이 양방향 실측 사살: Mutation A(합성 타임스탬프 주입), Mutation B(부인 고지 누락 주입), Mutation C(허위 미노출 공지 주입), Mutation D(배선-미구현 모순 주입) 전수를 exit 1로 실측 사살(KILLED) 후 원복했다. `check_frontend_integrity.py` 81개 파일 **0 violations (All 7 integrity rules satisfied)**, `--test-negative` 통과, Vitest 69개 파일 **620/620 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.86s), check_contract_bindings PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_화면정직성스캐너_7대규칙확장_및_양방향실측_Gemini]]

2026-09-22 허위 미구현(Class 3) 표기 전수 재감사, 산출물 다운로드 실제 커널 엔드포인트 실배선 및 에디터 컨텍스트 정직화 = **Gemini**(완료): Claude의 백엔드 대조 감사 커밋(`52a3eea4`) 및 보고서에 따라 프론트엔드 내 허위 "미구현 / API 미노출" 표기를 전수 재감사하고 실제 백엔드 라우트 실배선 및 상태를 정직화했다. 1) RunDetail Tab 3 Fallback 아티팩트 다운로드 실배선: `(run as any).artifacts` fallback 항목의 다운로드 버튼을 실제 커널 다운로드 URL(`getArtifactDownloadUrl(run.projectId, run.id, art.name)`)을 가리키는 `<a>` 태그로 전면 실배선하고, `[모의 고지] ... (서버 아티팩트 파일 스트림 다운로드 API 미노출 상태)` 및 버튼 텍스트 `다운로드 (API 미노출)` 허위 문구를 완전 소거하여 `📥 다운로드` 버튼으로 통일했다. 2) MonacoWorkspaceEditor 에디터 저장 라벨 정직화: 백엔드 파일 저장 엔드포인트(`POST /v1/projects/.../checkouts/.../files`)가 이미 실재하므로 허위의 `(백엔드 저장 API 미노출)` 라벨을 완전 소거하고 `(체크아웃 컨텍스트 미연결)`로 정정하여 `runId/checkoutId` 컨텍스트 부재 시 브라우저 인메모리 버퍼 샌드박스로 동작함을 정직하게 고지했다. 3) ResourceExplorer의 Logical 점유율 `미제공 (API 미노출)`은 백엔드에 HTTP 읽기 엔드포인트가 없는 상태임이 감사로 확인되어 조작된 0 숫자 대신 정직한 미제공 상태를 유지하고 있음을 재확인했다. 4) 단위 테스트 정합: `tests/dashboard-runlist-freshness-wiring.test.tsx` 등 관련 테스트를 실배선 및 정직 고지에 맞게 동기화했다. Vitest 69개 파일 **620/620 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.92s), `check_frontend_integrity.py` 81개 파일 0 violations (PASS), check_contract_bindings PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_허위미구현소거_및_산출물다운로드_실배선_Gemini]]

2026-09-22 백엔드 내구성 시각 필드 3종(stateUpdatedAt, stateAsOf, completedAt) 정직한 UI 실배선 및 신선도 오독 차단 = **Gemini**(완료): Codex의 백엔드 시각 계약 정합 커밋(`f1d95466`)을 수용하여 3대 시각 필드를 출처와 의미에 맞게 고유 라벨로 실배선하고 없는 신선도를 지어내던 오독을 차단했다. 1) `Run.stateUpdatedAt`: `inv.runs.updated_at` 기반 DB 마지막 갱신 시각을 "실행 상태 갱신" 고유 라벨로 RunDetail 헤더(`data-testid="run-state-updated-at"`), RunList 각 행(`data-testid="run-state-updated-at-${run.id}"`), DeveloperStudio Step 4 헤더(`data-testid="studio-run-state-updated-at"`)에 실배선했다. 2) `Shard.stateAsOf`: parent/member Run들의 최신 `updated_at`을 Tab 5 샤드 원장에 "샤드 상태 기준" 라벨(`data-testid="shard-state-as-of"`, `data-testid="shard-state-as-of-card"`)로 표출하고 "단일 공통 스냅샷이나 조회 시각이 아닙니다"라는 한정된 의미를 정직 고지했다. 3) `completedAt`: `inv.result_completions.completed_at` 기반 실행 완료 시점을 Tab 2(로그) 및 Tab 3(산출물)에 "실행 완료 시각" 라벨(`data-testid="log-completed-at"`, `data-testid="artifact-completed-at"`)로 표출하고, "로그 캡처나 산출물 다운로드 시각이 아닙니다"를 명시하여 거짓 신선도 둔갑을 원천 차단했다. 4) DeveloperStudio 가짜 현재 시각 합성(`new Date().toISOString()`) 완전 소거 및 `shared/api/runArtifactObservation.ts` 신규 작성으로 실제 산출물 목록과 파일 다운로드 API를 실배선했다. 5) 신규 단위 테스트 7종 구축(`response-freshness-wiring.test.tsx`, 7/7 passed 100%). Vitest 69개 파일 **620/620 passed 100%** (순증 +7 passed), Vite 프로덕션 빌드 exit 0 (3.61s), `check_frontend_integrity.py` 81개 파일 0 violations (PASS), web contracts:check 16/16 PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_백엔드시각_3종_정직한UI실배선_Gemini]]

2026-09-22 브라우저 alert() 18개소 전소 및 3분류(오류·성공·미구현) 정직화 = **Gemini**(완료): 프론트엔드 전역(`apps/web/src`)의 브라우저 블로킹 다이얼로그 `alert()` 잔여 18개소를 전면 소거(Zero Alert Invariant)하고 WAI-ARIA `role="alert"` / `role="status"` 규격 및 기계적 치환을 금지한 3대 분류 판정으로 정직화했다. 1) Class 1 (오류 알림, 11개소): 승인 처리/반려/취소 실패, 프로젝트 부재, 산출물 수신/다운로드/API 실패 시 `app-global-action-error` 및 `studio-action-notice` (`role="alert"`) 인라인 에러 배너로 전환하여 스크린 리더 전달 및 단위 테스트 단언을 가능화했다. 2) Class 2 (성공/완료 알림, 3개소): 결과 매니페스트 및 원시 파일 바이트 다운로드 완료, 복구 단계 준비 완료 시 `studio-action-notice` (`role="status"`) 인라인 성공 피드백을 실장했다. 3) Class 3 (미구현 / 뒤가 없는 기능 / 동작 불가 가드, 4개소 전수 집계): 노드 0대 시 빈 상태의 허위 alert 팝업을 소거하고 인라인 부트스트랩 가이드(`node-agent-install-guide`, `role="status"`, CLI 명령어 및 복사 피드백)로 전환(`NodeList`), 실행 중 상태에서 결과/원시 아티팩트 다운로드 시도 시 버튼 사전 비활성화(`disabled`) 및 `title`에 "실행 완료 후 활성화" 사전 고지 명시(`DeveloperStudio`), NodeStopReceipt 영수증 미발행/서버 미보관 시 치명적 오류가 아닌 차분한 상태 안내(`role="status"`)로 전환했다. 4) 테스트 정합 및 사살: 신규 단위 테스트 6종 구축(`alert-elimination-and-unimplemented-audit.test.tsx`, M23/M24 사살, 6/6 passed), 기존 단위 테스트 8종 마이그레이션(`developer-studio-dom.test.tsx`, 18/18 passed). Vitest 68개 파일 **613/613 passed 100%** (순증 +6 passed), Vite 프로덕션 빌드 exit 0 (3.21s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_alert소거_및_미구현기능_3분류정직화_Gemini]]
2026-09-22 Run/Shard/artifact/log 응답의 저장 진실 시각 계약 = **Codex**(구현·작성자 검증 완료, 독립 검토 대기): 라이브 Run에는 `inv.runs.updated_at` 기반 `stateUpdatedAt`, shard 집계에는 표현된 parent/member Runs의 최대 `updated_at`인 `stateAsOf`, artifact/log에는 기존 `result_completions.completed_at` 기반 nullable `completedAt`을 노출한다. 조회 시각이나 공통 스냅샷인 양 오인하지 않도록 계약 설명에 의미·범위를 명시했다. 코드 SHA `f1d95466`의 clean worktree에서 PostgreSQL 16 시험 4 passed/0 skipped; 현 integration SHA `2158f030`에서 `appliedToKernel=true` parameter만 분리 재실행해 1 passed이며 실제 `inv.resources.offered`를 HTTP 결과와 대조했다. 전체 Vitest 607/607, core 18 passed, schema 46, freshness map 9/9, build/contracts:check, docs·ontology·sync 통과. 이는 Codex 작성자 실행이며 독립 검토/브라우저 인수가 아니다. History: [[2026-09-22_response_freshness_asof_Codex]].

2026-09-22 화면 결함 6대 부류 치유 트랙 8차: 백엔드 진실 시각(Truth Time) 실배선 및 화면 조회 시각(Query Time) 분리 = **Gemini**(완료): Claude 백엔드 관측시각 축 감사 수용(`34ef753e`)에 따라 화면의 조회 시점과 백엔드 진실 시각을 엄격히 분리했다. 1) 진실 시각 실배선: 물리 노드 `lastHeartbeatAt`(`ClusterOverview`, `ResourceExplorer`), 스토리지 관측 `createdAt` 및 표본 `observedAt`(`ResourceExplorer`), 종단 완료 시점 `completedAt`/`updatedAt`(`RunDetail`, `RunList`), 모델 커밋 `committedAt`(`ModelStudioView`), 실행 시도 `startedAt`(`RunDetail` Attempts 탭)을 실배선했다. 특히 복제본 관측 시 `currentHealth: "unknown"`, `operationalAcceptanceAssessed: false` 불변 계약에 따라 `storage-observation-health-disclaimer`("건강 상태 단언 유보 고지 · 실행 시점 재검증 필수")를 명시하여 "지금 건강" 오독을 원천 방어했다. 2) 진실 시각 미제공 대상의 조회 시점 명시: 실시간 로그(`RunLogView`), 산출물 목록(`RunArtifactList`), 분산 샤드 원장(`ShardObservation`), 라이브 실행 상태에 대해 백엔드가 `observedAt`를 주지 않으므로 `[화면 확인 기준]` 스냅샷임을 정직하게 못 박았다. 3) 사실(Fact) vs 판단(Judgement) 분리: 기준 없는 주관적 낙인("오래된 정보 주의", "신선도 저하 주의")을 전면 소거하고 "동기화 실패 사실"과 "화면 확인 시점 스냅샷 시각"을 건조하게 제시했다. 4) ModelStudioView/EvidenceViewer 가짜 현재 시각 fallback 합성 소거 및 `alert()` 제거: 클립보드 복사 피드백을 `copy-evidence-success`(`role="status"`) 인라인 피드백으로 정합했다. 5) 신규 DOM 단위 테스트 10종 구축 및 3대 돌연변이(M20~M22) 전수 실측 사살(KILLED). Vitest 67개 파일 **607/607 passed 100%** (순증 +10 passed), Vite 프로덕션 빌드 exit 0 (4.02s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_진실시각_실배선_및_조회시각_분리_Gemini]]

2026-09-22 대시보드(ClusterOverview) 및 Run 목록 신선도 지표화, 에러 은폐 차단 및 alert() 소거 = **Gemini**(완료): 화면 결함 6대 부류(시간 경과 묵인 및 신선도 은폐, 가짜 alert 다이얼로그, 실패 은폐) 치유 트랙 7차를 완결했다. 1) ClusterOverview 대시보드 장애 은폐 차단 & 신선도 지표화: 노드 동기화 실패 시 정상 0대 빈 상태로 둘러대던 결함을 치유하여 `nodesState === 'error'`일 때 `cluster-overview-fetch-error`(`role="alert"`, "정상 0대 아님" 명시 및 재시도 버튼) 전용 에러 뷰를 표출하고, 정상 0대일 때만 `cluster-overview-empty-state`(`role="status"`)를 표출하도록 엄격 분리했다. 상단 헤더에 `cluster-freshness-indicator`(`role="status"`, `🔄 자동 갱신 (5초 주기) · 최근 관측: HH:mm:ss`) 및 수동 `cluster-refresh-btn`을 실장하고, 폴링 실패 시 기존 캐시 노드가 있으면 `cluster-stale-warning`(`role="alert"`)을 표출했다. 2) RunList 작업 목록 에러 은폐 차단 & 신선도 지표화: Run 동기화 실패 시 "해당 상태의 Run이 없습니다" 정상 0건 빈 상태로 둔갑하던 결함을 치유하여 `runsState === 'error'`일 때 테이블 내부에 `run-fetch-error-state`(`role="alert"`, "작업 0건(정상 0건 아님)" 명시 및 재시도 버튼)를 표출했다. 상단 헤더에 `run-list-freshness-indicator`(`role="status"`, `🔄 자동 갱신 (5초 주기) · 최근 동기화: HH:mm:ss`) 및 새로고침 버튼을 실장하고, 폴링 실패 시 `run-stale-warning`(`role="alert"`)을 표출했다. 3) RunDetail alert() 소거 및 정직한 DOM 피드백 배너: 브라우저 블로킹을 유발하고 스크린 리더에서 실종되던 `alert()` 호출을 전면 소거하고 상단에 `run-action-${type}-notice`(`role={type === 'error' ? 'alert' : 'status'}`) 배너를 신설하여 샤드 취소/영수증 조회 실패를 정직하게 표출했다. 아티팩트 다운로드 버튼의 허위 팝업을 소거하고 `다운로드 (API 미노출)`로 정직화하여 클릭 시 인라인 배너(`role="status"`)를 표출했으며, 취소 모달 내부에 `cancel-modal-error`(`role="alert"`) 인라인 에러 배너를 실장했다. 4) App.tsx 신선도 결속 및 Studio 전환 실배선: `lastRunsFetchedAt` 실배선 및 컴포넌트 결속, `onCreateRun`의 가짜 alert 창을 `handleOpenStudio({ step: 3 })`로 실배선하여 새 Run 버튼 클릭 시 Developer Studio의 코드 편집 & 실행 단계로 전환했다. 5) 신규 DOM 단위 테스트 9종 구축 및 4대 돌연변이(M16~M19) 전수 실측 사살(KILLED). Vitest 66개 파일 **597/597 passed 100%** (순증 +9 passed), Vite 프로덕션 빌드 exit 0 (3.55s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_대시보드_Run목록_신선도지표화_및_alert소거_Gemini]]

2026-09-22 화면 결함 6번째 부류 치유: 시간 경과 묵인 및 신선도 은폐 차단, 승인·노드 동기화 지표화 = **Gemini**(완료): 화면 결함 6번째 부류("시간 경과 묵인 및 신선도 은폐 / Stale Data Concealment & Silent Aging")를 치유했다. 1) ApprovalCenter 신선도 지표화 & Stale 캐시 은폐 차단: 상단에 `approval-freshness-indicator`(`role="status"`, `🔄 자동 갱신 (5초 주기) · 최근 동기화: HH:mm:ss`) 및 수동 `approval-refresh-btn`을 실장했다. 폴링 실패 시 과거 스냅샷을 최신인 양 침묵하지 않고 `approval-stale-warning`(`role="alert"`, 과거 스냅샷 시각 명시 및 처리 전 새로고침 안내)을 표출했다. 서버 장애 시 안건 0개일 때 허위 `EmptyState`("대기 중인 거버넌스 승인 안건 없음") 둔갑을 원천 차단하고 `approval-fetch-error-state`(`role="alert"`, `이는 '대기 안건 0건'(정상 0건 아님)이며, 미확인된 고위험 안건이 대기 중일 수 있습니다.` 고지 및 재시도 버튼) 전용 에러 뷰를 분리했다. 2) ResourceExplorer 노드 관측 시각 지표화 & 수동 탭 스냅샷 고지: 상단 헤더에 `node-freshness-notice`(`role="status"`, `🔄 클러스터 노드 동기화 (5초 주기) · 최종 관측: HH:mm:ss`)를 투명 표출했다. 노드 자동 폴링과 달리 1회만 조회되는 정적 스냅샷 탭인 Tab 1(풀 관리)에 `pool-tab-manual-refresh-notice`(`role="status"`, `[스냅샷 모드 · 수동 갱신]`) 및 새로고침 버튼을 실장하고, Tab 5(디스커버리)에 `discovery-tab-manual-refresh-notice`(`role="status"`, `[스냅샷 모드 · 수동 갱신]`) 및 새로고침 버튼을 실장했다. 3) 신규 DOM 단위 테스트 8종 구축 및 3대 돌연변이(M13: Stale 경고 배너 억제 회귀, M14: 에러 시 EmptyState 둔갑 회귀, M15: 수동 갱신 탭 스냅샷 고지 누락) 전수 실측 사살(KILLED). Vitest 65개 파일 **588/588 passed 100%** (순증 +8 passed), Vite 프로덕션 빌드 exit 0 (4.01s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_시간경과_신선도은폐차단_및_승인_노드_동기화표시_Gemini]]

2026-09-22 화면 결함 5대 부류 치유 5차: 쓰기 동작 실패 은폐 차단 및 비상정지·배포 모의 정직화 = **Gemini**(완료): 상태를 바꾸는 고위험 쓰기 동작 전수 감사를 통해 "실패 은폐", "가짜 식별자 합성", "허위 조기 성공" 결함을 정직하게 치유했다. 1) ResourceExplorer 실패 은폐 차단 및 모달 규격화: Tab 5 디스커버리 및 Tab 1 풀 관리에서 API 실패 시 화면에서 실종되던 결함을 치유하고, 상단 및 인라인 메시지 배너에 `role="alert"`(실패 시) 및 `role="status"`(성공 시)를 명시했으며, 일회용 토큰 모달에 `data-testid="admission-result-modal"`, `role="status"`를 부여했다. 2) AdminSecurityConsole 위조 actor 합성 차단 및 Kill Switch 모의 고지: 마운트 및 승인 우회 검증에서 `usr_security_auditor`, `usr_bypass_tester` 가짜 식별자 합성을 전면 제거하고 세션 부재 시 즉시 차단했다. 비상 정지(Kill Switch) 토글 버튼에 `disabled={!actor}` 가드를 집행하고, 활성 배너(`kill-switch-active-banner`, `role="alert"`) 및 모달(`kill-switch-mock-notice`, `role="status"`)에 제어 평면 비상 정지 API 미노출 상태임을 정직하게 고지했다. 3) ModelLineageView 배포 모의 시뮬레이션 정직 고지: 백엔드 서빙 배포 API 부재 상태의 허위 배포 축하 배너를 소거하고 `✔ [모의 시뮬레이션] [모델명] 로컬 배포 게이트 검증 완료 (백엔드 서빙 배포 API 미노출 상태로 실제 인프라 미반영)`으로 정직화했다. 4) 신규 DOM 단위 테스트 8종 구축 및 2대 돌연변이(M11: 승인 실패 에러 배너 억제 회귀, M12: 미인증 시 가짜 actor 합성 우회 회귀) 전수 실측 사살(KILLED). Vitest 64개 파일 **580/580 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.50s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_쓰기동작_실패은폐차단_및_비상정지_배포모의고지_Gemini]]

2026-09-22 화면 결함 5대 부류 치유 4차: IntranetDeploymentView 운영자 행위자 실배선/미인증 차단 및 ReleaseCandidateView 롤백 모의 고지 = **Gemini**(완료): `IntranetDeploymentView.tsx`와 `ReleaseCandidateView.tsx`에 남아있던 "가짜 식별자", "합성", "조기 성공 표시" 결함을 정직하게 치유했다. 1) 운영자 가짜 식별자 소거 및 세션 실배선: `IntranetDeploymentView.tsx` 내 `usr_operator_lead` 하드코딩 식별자를 전면 소거하고 `currentUser?.id`를 `operatorId`로 실배선했다. `App.tsx`에서 `currentUser={currentUser}`를 결속하고, 세션 부재(`!currentUser`) 시 `deployment-auth-required-notice`(`role="alert"`) 표출 및 서명 버튼 `disabled`/`aria-disabled="true"` 차단 가드를 집행했다. 상단에 `deployment-unexposed-notice`(`role="status"`, "백엔드 배포 API 미노출")를 배치하고 서명 완료 통지를 `[모의 시뮬레이션]` 규격으로 정직화했다. 2) 롤백 모의 고지 및 허위 축하 배너 소거: `ReleaseCandidateView.tsx` 상단에 `release-unexposed-notice`(`role="status"`)를 신설하고, 롤백 실행 시 과거의 허위 완료 배너(`... 캐시 무효화 및 무중단 상태가 확인되었습니다`)를 완전 소거하고 `✔ [모의 시뮬레이션] AC-11 롤백 절차 검증 완료 (백엔드 릴리스 제어 API 미노출 상태로 실제 인프라 및 CDN 캐시 미반영)`으로 정직 고지했다. 3) 신규 DOM 단위 테스트 3종 구축 및 2대 돌연변이(M9: 미인증 경고 억제 회귀, M10: 롤백 허위 축하 배너 회귀) 전수 실측 사살(KILLED). Vitest 63개 파일 **572/572 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.64s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_배포운영사인오프_실배선_및_롤백모의고지_Gemini]]

2026-09-22 화면 결함 5대 부류 치유 3차: MonacoWorkspaceEditor 커널 체크아웃 파일 저장 실배선, 인메모리 은폐 차단 및 PTY 모의 고지 = **Gemini**(완료): `MonacoWorkspaceEditor`의 파일 저장 및 내장 터미널 PTY에 남아있던 "캐시/로컬 은폐(False Persistence)" 및 "합성 & 조기 성공 표시(Synthetic PTY)" 결함을 정직하게 치유했다. 1) 커널 체크아웃 파일 저장 실배선: 백엔드 `POST /v1/projects/{project}/runs/{run_id}/checkouts/{checkout_id}/files` (`WorkspaceEditor.edit`) 엔드포인트와 정합한 프런트엔드 API 클라이언트(`saveWorkspaceEditView`)를 구축하고 실배선했다. `runId`와 `checkoutId`가 주어지면 실제 커널에 저장하고 Revision/SHA를 갱신하며 `editor-save-success-notice`(`role="status"`)를 표출하고, 실패 시 `editor-save-error-banner`(`role="alert"`)로 에러를 즉각 표출해 조기 성공 위장을 원천 차단했다. 2) 컨텍스트 부재 시 네트워크 0회 호출 가드 및 로컬 임시 보존 경고: 체크아웃 컨텍스트가 없는 단독 실행 환경에서는 네트워크 호출을 0회로 원천 차단하고 `editor-context-notice`(`role="alert"`, "서버에 영속 저장되지 않고 로컬 브라우저 샌드박스 메모리에만 임시 보존됨")를 표출하여 창 닫기 시 작업 증발 오판을 방지했다. 3) 터미널 로컬 에뮬레이션 모의 고지: 내장 터미널에 `[로컬 에뮬레이션 · 독립 PTY 미연결]`(`role="status"`, `editor-terminal-mock-notice`) 뱃지를 상시 표출했다. 4) 신규 DOM 단위 테스트 4종 및 2대 돌연변이(M7: 백엔드 저장 배선 무력화 회귀, M8: 미연결 경고 배너 억제 회귀) 전수 실측 사살(KILLED). Vitest 62개 파일 **569/569 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.62s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-22_에디터저장_백엔드실배선_및_터미널모의고지_Gemini]]

2026-09-22 Codex: HIGH 쓰기 응답 네 건과 재판정으로 상향된 workspace status 응답을 strict 모델에 결속했다. PG16 실제 HTTP/service/DB 검증은 admission·project create·member role·resource offer·workspace transition 5건 통과다. 제공량 경로는 kernel resource 미등록 분기까지 확인했고 실제 적용 성공 분기는 미검증이다. 커널 시각 스큐 5초 적격성 필터는 유지하며 라우팅 알람은 장비 보정·채널·담당 미결로 governance-gated 유지. [[2026-09-22_write_route_response_binding_Codex]] [[2026-09-22_write_response_real_pg_Codex]]

2026-09-21 화면 결함 5대 부류 치유 2차: 관리자 콘솔 행위자 실배선, 분산 복구 모의 고지, 에디터 샌드박스 고지 및 브라우저 여정 정본 고정값 감사 = **Gemini**(완료): 화면 결함 5대 부류 잔여 3개 항목(Priority 4~6)을 전면 완결하고 Codex 감사 지침에 따른 브라우저 여정 정본 고정값을 대조했다. 1) 관리자 행위자 실배선(Priority 4): `AdminSecurityConsole.tsx`에서 `usr_admin_01` 하드코딩 식별자를 전면 소거하고 `currentUser?.id`를 `actor`로 실배선했다. 세션 부재 시 `data-testid="admin-auth-required-notice"`(`role="alert"`) 표출 및 노드 격리(Drain)/비상 정지(Kill Switch)의 네트워크 0회 호출 가드를 집행했으며, Mutation 5(하드코딩 복원 회귀)를 실측 사살했다. 2) 분산 복구 모의 고지(Priority 5): `DistributedRecoveryView.tsx` 상단에 `recovery-unexposed-notice`(`role="status"`, 백엔드 복구 API 미노출 명시)를 신설하고 체크아웃 생성 시 `sim_chk_...`, `sim_ino_...`, `sim_sha256_mock_checkpoint`로 전환하여 `[모의 시뮬레이션]`으로 정직 고지했다. 3) 에디터 로컬 샌드박스 고지(Priority 6): `MonacoWorkspaceEditor.tsx` 상단에 `editor-unexposed-notice`(`role="status"`, 백엔드 저장 API 미노출 및 커널 `WorkspaceEditView` 계약 필요 명시)를 신설하고 저장 버튼을 `Save File (Local Sandbox)`로 변경했다. 4) 브라우저 여정 정본 독립 고정값 감사 수용: `.github/workflows/desktop-browser.yml:63-70`의 5대 canonical browser journey 목록이 시험 대상에서 유도되지 않고 계약 수용 범위 자체를 표현하는 "독립 정본 집합"으로 고정되어 있음을 확인하고, 시험 삭제 시 게이트가 함께 줄어드는 무력화 차단 실증 및 갱신 시 잔여 위험을 명시했다. 단위 테스트 `tests/defect-recovery-admin-recovery-editor.test.tsx` 5/5 passed, Vitest 61개 파일 **565/565 passed 100%**, Vite 프로덕션 빌드 exit 0 (5.76s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-21_화면결함_5대부류_치유_2차_관리자행위자_분산복구모의_에디터샌드박스_Gemini]]

2026-09-21 화면 결함 5대 부류 치유 1차: 노드 에러 은폐 차단, 작업공간 백엔드 실배선, 자연어 가상 KPI 합성 및 조기 성공 배너 치유 = **Gemini**(완료): 화면 내 남아있던 5대 부류 결함(합성, 죽은 방어, 캐시 은폐, 가짜 식별자, 조기 성공 표시) 중 위험도가 가장 높은 3개 우선 항목을 전면 치유했다. 1) 노드 API 실패 시 에러 은폐 차단: `App.tsx`에서 `fetchNodes` 실패 시 `setNodes([])`로만 처리해 클러스터가 0대 빈 상태(정상 빈 클러스터)로 오인되어 잘못된 장애 대응을 유발하던 결함을 치유하고, `nodesState`와 `nodeError`를 신설하여 논리 자원 카드에 `조회 실패` 명시 및 `nodes-fetch-error-banner`(`role="alert"`)를 표출함으로써 정상 0대 빈 상태와 엄격히 분리했다. 2) 작업공간 생성 가짜 ID 제거 및 백엔드 실배선: `App.tsx`의 `wsp_${Date.now()}` 가짜 ID 및 `status: 'active'` 조기 성공 클라이언트 합성을 전면 제거하고, `POST /v1/projects/{projectId}/workspaces` 백엔드 API를 실배선하여 `provisioning` 상태를 "프로비저닝 중 (Provisioning)"으로 정직 표기했다. 3) 자연어 뷰 가상 KPI 99% 달성 합성 및 조기 성공 배너 치유: `NaturalLanguageRunView.tsx` 상단에 `agent-unexposed-notice`(`role="status"`, "API 미노출")를 신설하고 골든 평가 지표 라벨을 `[AC-09 픽스처 (로컬 시뮬레이션)]`으로 정직 표기했으며, 코드 Diff 적용 축하 배너를 모의 적용 안내문으로 대체했다. 4) DOM 단위 테스트 `node-fetch-error-workspace-wiring.test.tsx` 8/8 passed 및 M1(노드 에러 은폐 회귀), M2(작업공간 provisioning 조기 성공 회귀), M3(자연어 코드 Diff 허위 축하 배너 회귀) 3대 돌연변이를 전수 실측 사살(KILLED)했다. Vitest 60개 파일 **559/559 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.71s), `check_frontend_integrity.py` 80개 파일 0 violations (PASS), check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-21_화면결함_5대부류_치유_노드에러은폐_작업공간실배선_자연어KPI_Gemini]]

2026-09-21 Codex 회귀 가드 회수: Claude의 미병합 guard 5건을 현 구현 기준으로 재평가했다. 기존의 현재형 보호로 대체된 3건은 이식하지 않고 recovery 실패기록 1건은 실제 PostgreSQL 가드로 재작성했다. evaluator가 없는 alarm guard 1건은 기능 미구현으로 분리했다. 감사 도중 발견된 definer policy의 0044/0045 드리프트와 role-guard head 0037 하드코딩을 고쳐 selected suite 89 passed/0 skipped와 red/green 대조를 확인했다. [[2026-09-21_회귀가드회수_Codex]]

2026-09-21 Codex 변경연동 하드코딩 감사: 기준 시점의 tests 220개·tools 66개·workflow 5개 파일에서 revision/cardinality/evidence-inventory 후보를 분류했다. ontology ownership 행 수를 task registry outcome mapping에서 유도하고 acceptance evidence 세 도구의 count-only 판정을 정확한 scenario 집합으로 강화했다. 고정 governance baseline·canonical browser journeys·historical migration priors 등은 독립 기대값으로 유지하고 갱신 trigger를 기록했다. 작성자 검증·착지 SHA `05c250e23c674a5a6ca72f83aaa848962323934f`: focused 4 passed, docs(668 docs)/ontology/py_compile/sync check exit 0; Windows 11, project Python 3.14.6, clean worktree, 2026-09-21 23:50:44 KST. 현재 integration은 후속 `5bad6327d5c5`까지 전진했다. Docker/Go/PG 및 hosted CI 미실행, 독립 검토 대기. [[2026-09-21_하드코딩_변경연동값_감사_Codex]]

2026-09-21 노드 정적 용량(Capacity) vs 동적 실시간 사용률(Utilization) 엄격 분리, 텔레메트리 부재 정직 반영 및 클러스터 생존성 보존 = **Gemini**(완료): 백엔드 HTTP 라우트 실측 검증(Codex/Claude/User 합의)에 따라, 1) 평면 텔레메트리 부재 시 `measuredNodes` 필터링으로 인해 클러스터 전체가 0대로 전락하던 결함을 치유(`App.tsx`에서 `nodes={nodes}` 전달)하여 클러스터 물리 노드 생존성을 보존했다. 2) 결측된 동적 점유량을 `0 Cores`나 `0 B`로 왜곡하여 100% 유휴 상태라는 착시를 주지 않고, `LogicalResourceSummary`를 nullable(`number | null`)로 전환하여 `미제공 (API 미노출)`으로 명확히 고지했다. 3) "0 (없음: 0대)" vs "모른다 (미확인: unobserved/NaN)" vs "미제공 (unprovided: HTTP API 미노출)"의 엄격한 3분할을 확립하고, `formatBytes`의 NaN 방어로 `'NaN undefined'` 표출을 원천 차단했으며, 물리 노드 카드에 `📊 자원 사용률: 미제공 (HTTP 읽기 경로 부재)`(`role="status"`) 배너를 배치했다. 4) Tab 4 Capabilities 표 상단에 정적 총용량과 실시간 동적 사용량의 HTTP 미제공 상태를 못 박는 안내 배너(`capabilities-static-capacity-notice`, `role="status"`)를 배치했다. 5) DOM 단위 테스트 `node-telemetry-capacity-distinction.test.tsx` 6/6 passed 및 M1/M2/M3 3대 돌연변이를 실측 사살(KILLED)했다. Vitest 59개 파일 551/551 passed 100%, Vite 빌드 exit 0, check_frontend_integrity 0 violations (PASS), 음성 대조 PASS, check_docs / ontology / sync_obsidian 전수 PASS. [[2026-09-21_노드_용량과사용률_구별고지_및_텔레메트리부재정직반영_Gemini]]

2026-09-21 Codex telemetry 계약 정밀화: 노드 상세 `capabilities`는 정적 선언이고, heartbeat 사용량 관측은 인증 수집·DB 저장 후 placement 내부에서 소비된다. 이를 UI가 읽는 HTTP read route는 현재 없다. 평면 동적 telemetry도 미구현이다. 현재 list/detail 계약은 유지하며, 향후 UI 사용량 노출 계약과 평면-vs-capability 모델 선택은 사용자 조정 항목으로 남긴다. [[2026-09-21_web_response_contract_map_workspace_Codex]]

2026-09-21 Codex: docs workflow에 계약/프런트 정직성 검사를 배선했고 issuer quota의 role 범위와 superuser/table-owner 예외를 ADR 및 migration에 기록했다. 12 동시 발급의 disposable PostgreSQL 16 시험은 10 성공·2 거부와 budget rollback을 확인했다. Node list/detail 응답을 strict 계약과 공유 fixture/양쪽 시험에 결속했다. 전체 결과와 provenance는 Codex workboard 및 History 참조. hosted CI 권한 실측과 브라우저/운영 인수는 별도 미완.

2026-09-21 integration migration-head 실패 수정 완료: `a1833e3` 원격 integration exact tip에서 Core 769 passed, 4 선행조건 skip, 0 failed/errors다. LegacyProjectCatalogResponse는 producer 부재로 제거했다. Node list/detail도 후속 Codex 계약 slice에서 strict producer/consumer 계약으로 결속했다. 상세 provenance와 red/green 근거는 Codex workboard 및 [[2026-09-21_web_response_contract_map_workspace_Codex]] 참조.

2026-09-21 화면 접근성 상태 역할 분리, 비색상 단서, 비활성 버튼 고지 및 DOM 검증 완결 = **Gemini**(완료): 화면 내 상태 표출(미검증·검증됨·불일치·차단·빈 상태 등)과 차단 버튼에 대해 보조공학기기(스크린 리더) 및 색각 이상 사용자를 위한 웹 접근성(WAI-ARIA 및 WCAG 2.1 AA)을 체계적으로 실장했다. 1) 스크린 리더 상태 역할 엄격 분리: 즉각적 주의가 필요한 차단·불일치·컨텍스트 누락 요소(`integrity-mismatch-banner`, `discovery-tenant-required-notice`, `storage-no-nodes-notice`, `checkout-context-warning`, `terminal-command-required-notice` 등)에 `role="alert"`/`aria-live="assertive"`를 적용하고, 일반 상태 전이 및 정상 대기 빈 상태(`verified`, `discovery-empty-state`, `preview-empty-state`, `terminal-connection-status` 등)에 `role="status"`/`aria-live="polite"`를 적용했다. 무결성 뱃지(`integrity-badge`)는 `unverified` 시 `role="status"`에서 해시 변조 시 `role="alert"`, 검증 통과 시 `role="status"`로 동적 전이하도록 구축했다. 2) 비색상 단서: 적록 색각 이상자를 위해 뱃지에 텍스트 라벨(`[저하]`/`[정상]`, `[VERIFIED]`, `[TAMPERED]`) 및 `aria-label`을 병행 제공했다. 3) 비활성 버튼 고지: 조건부 차단된 조작 버튼들에 `aria-disabled="true"`, `aria-describedby="<notice-id>"`, `title` 속성을 연동하여 차단 사유를 전달했다. 4) 정적 스캐너 한계(동적 역할 전이, `aria-describedby` DOM 결속, 색상 대비 계산, 발화 타이밍은 런타임/육안 감사 필수) 및 점검 범위(AdminSecurityConsole, Monaco 캔버스 제외)를 명시했다. Vitest 접근성 단위 테스트 16건 신설 및 전체 57개 파일 543/543 passed 100% (순증 +16 passed), Vite 프로덕션 빌드 exit 0 (3.84s, 96 modules), `check_frontend_integrity.py` 80개 파일 0 violations 및 음성 대조 PASS, check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-21_화면_접근성_상태역할_비색상단서_비활성버튼_고지_Gemini]]

2026-09-21 화면 차단·미검증·미노출 상태의 3대 해결경로 분류정합 완결 = **Gemini**(완료): 화면 내 모든 비활성화·차단·미검증 요소를 사용자가 "시스템 전체 고장"으로 오인하지 않도록 3대 해결경로(1. [사용자 조치 필요]: 로그인/테넌트 선택, 워크스페이스 선택, 승인 ID/Run ID/체크아웃 ID 입력, 슬라이더 변경 등 화면 내 직접 해결 경로 / 2. [운영자 조치 필요]: 노드 온보딩, 운영자 자격증명 발급 `saint operator issue-grant` 런북 연계, 리스 연장 `AUTH-0070` 등 문서화된 절차 안내 및 허위 셀프서비스 버튼 생성 금지 / 3. [제품 기능 미제공]: 복제본 온디맨드 수복 API, 샤드 온디맨드 복구 API, 모델 원격 검증 라우트, 계보/평가 HTTP 서빙 라우트 등 현재 백엔드 엔드포인트 부재를 투명하게 못 박고 재시도/일시적 장애 위장 배제)로 전수 분류·안내를 실장했다. `resource-explorer-dom.test.tsx` 내 명시적 태그 단언 추가, Vitest 56개 파일 527/527 passed 100%, TypeScript/Vite 프로덕션 빌드 exit 0 (5.30s, 96 modules), `check_frontend_integrity.py` 80개 파일 0 violations 및 음성 대조 PASS, check_contract_bindings / check_docs / check_ontology / sync_obsidian 전수 PASS. [[2026-09-21_화면_차단상태_3대해결경로_분류정합_Gemini]]
2026-09-21 ADR-097 사용자 결정 및 임시 발급 경로: 운영자 CLI 발급/폐기 구현, Claude 독립 검토, tenant별 rolling 24h 발급 10회 DB 제한이 integration `6dcd09dd69729e7651aae62b54bb6fd2d3858636`에 반영됐다. DB 전용 NOLOGIN issuer role, digest-only 저장, one-time TTY 출력, 15분 만료, 회전/명시/승인 자동 폐기와 감사 이벤트를 포함한다. 발급 제한은 권한 있는 발급자가 서로 다른 installation ID로 tenant별 500 후보 한도에 도달할 수 있는 burst/실수 위험을 줄이지만, 10회/일씩 장기간 누적하는 신뢰 발급자를 막지는 않는다. PostgreSQL 16 통합 파일 `tests/integration/test_discovery_machine_credentials.py`는 프로젝트 Python으로 4 passed/7 warnings, exit 0; 한도를 100으로 올린 되돌림 대조는 issue 11 거부 단언에서 1 failed/exit 1, 복원 후 4 passed다. 소유 라벨 임시 컨테이너 제거와 Docker count 48 복귀를 확인했다. History에 provenance를 기록했다. 최초 실패 진단의 일회용 합성 bearer 노출도 기록했다. Node runbook은 10/24h 한도를 안내한다. Go 바이너리/실물 Node와 승인 전달 채널 인수는 남고 장기 protected API는 미결정이다. Obsidian 1450 managed/0 pending/0 conflict. [[2026-09-21_Discovery_기계자격증명_최소권한_계약제안_Codex]]

2026-09-21 Codex 계약 서빙 앵커 감사: 기준 `6403611e1ffdb64e313b9ef5c9eabef56fcee11b`에서 Codex 범위의 수동 응답 앵커 5개와 FastAPI pool/placement 응답 모델 6개를 실제 invalid-response 경로와 앵커 제거 대조로 재분류했다. 미검증 Bucket 2는 Codex slice 11→0이며, `ControlRunPage`/`ApprovalPage`의 기존 Bucket 1 판정도 정상 fixture만으로는 앵커 제거를 잡지 못해 정정했다. focused regression 55 passed; live DB, CI, browser 인수는 포함하지 않는다. [[2026-09-21_Codex_계약서빙앵커_버킷감사]]
2026-09-21 디스커버리 3대 빈 상태 분리, 운영자 자격 규칙 고지 및 DOM 돌연변이 실측 사살 완결 = **Gemini**(완료): Codex의 디스커버리 테넌트 경계 강화로 인해 자격증명 없는 미인가 노드의 공지가 거부되어 후보 목록이 0대로 조회되는 현상에 대응하여, `ResourceExplorer.tsx` Tab 5에서 세 가지 서로 다른 비-후보 상태를 엄격히 분리(Tri-Partition)했다. 1) 정상 조회 빈 상태(State 1): `candidatesState === 'success' && candidates.length === 0`일 때 `data-testid="discovery-empty-state"`를 렌더링하고, 테넌트 격리 정책에 따라 운영자 CLI(`saint operator issue-grant`)로 일회용 자격증명을 부여받은 노드만 디스커버리 안내 방송이 승인되어 목록에 나타남을 정직하게 고지하며 에러 배너를 완전 배제했다. 2) 연결 실패(State 2): `candidatesState === 'error'`일 때 `role="alert"` 속성의 `data-testid="discovery-error-banner"` 및 `data-testid="discovery-retry-btn"`을 표출하고 잔여 후보 및 빈 상태를 완전 소거했다. 3) 세션 테넌트 미식별 차단(State 3): `!tenantId`일 때 `data-testid="discovery-tenant-required-notice"` 표출, 안내 방송 버튼 비활성화, 네트워크 0회 호출 가드를 집행했다. `tests/resource-explorer-dom.test.tsx`에 4종의 정밀 DOM 테스트를 신설하고, 3대 돌연변이(M1: 에러 배너 무력화 시 State 2 실패, M2: 운영자 자격 규칙 고지 문구 제거 시 State 1 실패, M3: 테넌트 부재 시 브로드캐스트 활성화 시 State 3 실패)를 전수 실측 사살(KILLED) 후 원복했다. Vitest 56개 파일 **527/527 passed 100%** (순증 +4 passed, `resource-explorer-dom.test.tsx` 24/24 passed), Vite 프로덕션 빌드 exit 0 (3.32s, 96 modules), Pytest discovery contract 8 passed, check_frontend_integrity(0 violations & negative control PASS), check_contract_bindings, check_docs, ontology, sync_obsidian 전수 PASS. [[2026-09-21_디스커버리_3대빈상태분리_운영자자격규칙고지_Gemini]]

2026-09-21 화면 정직성 스캐너 6대 한계 명시 및 5대 규칙 양방향 실측 사살 완결 = **Gemini**(완료): 사용자 피드백에 따라 정적 검사기 `tools/check_frontend_integrity.py` 상단 독스트링 및 거버넌스 지침서 `docs/vault/40_Governance/화면_개발_정직성_지침_및_사례집.md` 섹션 3.2에 스캐너가 구조적으로 잡지 못하는 6대 한계((1) 동적 변수 조립/계산식 가짜 값, (2) 오늘 목록에 없는 신규 형태 합성 식별자, (3) 특정 컴포넌트 타겟팅 규칙의 새 파일 미추적, (4) 소스 어휘 존재 vs 런타임 데이터 흐름, (5) 모양만 유효한 임의 식별자의 실존성, (6) 비-텍스트적/시각적 조기 성공 표출)를 명시하여 "검사 초록이 완벽한 안전을 뜻하지 않음"을 선언했다. 아울러 "통과만 보면 아무것도 안 잡는 검사도 통과한다"는 원칙에 따라, 실제 프로덕션 소스 3개 파일에 대해 5대 규칙 각각에 대한 6개 돌연변이(M1: 가짜 테넌트 UUID 주입, M2: 미인가 파일 commandId 자리표시자 주입, M3: 안내 방송 테넌트 0-call 가드 무력화, M4: 소켓 연결 전 초기 버퍼 Connected 주입, M5: 일회용 티켓 콘솔 로깅 주입, M6: else 블록에서 mismatch를 verified로 조작)를 주입해 스캐너가 exit 1과 해당 규칙 위반을 정확히 포착함을 실측 사살(KILLED)하고 원복했다. 실측 과정에서 발견된 함수 본문 스코프 가드 검사(M3) 및 else 분기 상태 검사(M6)를 규칙에 대폭 강화 반영했다. `check_frontend_integrity.py` PASS (0 violations), 5대 규칙 내장 음성 대조(`--test-negative`) PASS, Vitest 56개 파일 **523/523 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.73s, 96 modules), check_contract_bindings, check_docs, ontology, sync_obsidian 전수 PASS. [[2026-09-21_화면정직성_스캐너_한계명시_및_5대규칙_양방향실측_Gemini]]

2026-09-21 화면 개발 정직성 5대 원칙 수립, 자동 검사 도구(check_frontend_integrity) 구축 및 전체 감사 지도 확정 = **Gemini**(완료): 사용자 지시에 따라 오늘 프런트엔드에서 확립한 판단 기준을 5대 정직성 원칙(① 백엔드가 주지 않는 것을 만들지 않는다, ② 부를 근거가 없으면 부르지 않는다, ③ 일어나지 않은 일을 일어났다고 표시하지 않는다, ④ 검증 못 함과 검증 통과를 같게 표시하지 않는다, ⑤ 실패를 캐시나 이전 결과로 가리지 않는다)으로 집약하여 거버넌스 지침서 `docs/vault/40_Governance/화면_개발_정직성_지침_및_사례집.md`를 신설했다. 이를 구조적으로 영구 집행하기 위해 Claude의 계약 검사 도구와 정렬된 `tools/check_frontend_integrity.py`를 제작하여 `apps/web/src/` 80개 프로덕션 파일의 금지된 하드코딩 자리표시자, 소켓 연결 전 조기 성공 표출, 티켓/토큰 console 로깅 노출, 엄격한 3상태(`verified` | `mismatch` | `unverified`), 0-call 가드를 자동 검증하도록 구축하고 고의 돌연변이 사살(음성 대조)을 실증했다. `DeveloperStudio.tsx`의 fallback runId 및 영수증 0-call 가드, `RunDetail.tsx`의 가짜 SSE 스트림 제거(정직한 빈 상태 복원), `mlopsEngine.ts`의 테스트 fixture 격리(`apps/web/tests/fixtures/model-lineage.ts`)를 완결했다. 아울러 감사 완료 영역과 미감사 영역을 구분한 전체 감사 지도를 수립하고, Codex의 운영자 CLI(`saint operator issue-grant`) 연동 시 `discovery-empty-state`에서 실시간 후보 카드로 전환되는 연동 시퀀스를 확립했다. Vitest 56개 파일 **523/523 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.63s, 96 modules), check_frontend_integrity(0 violations & negative control PASS), check_contract_bindings, check_docs, ontology, sync_obsidian 전수 PASS. [[2026-09-21_화면정직성_5대원칙_자동검사도구_감사지도_Gemini]]

2026-09-21 WorkspaceEditView 실바이트 해시 검증 개통, StorageObservationView 스토리지 샘플 무결성 배선 및 엄격한 3갈래 불변식 확립 = **Gemini**(완료): Claude의 `WorkspaceEditView` 및 `StorageObservationView` 계약 결속에 발맞추어, 프런트엔드에서 실제 커널 체크아웃 바이트 기반의 진정한 무결성 검증(`verified` / PASS) 경로를 정식 개통했다. `DesktopShell`에서 `checkoutId` 주입 및 `InvFileExplorer`에 동적 체크아웃 입력/로드 바(`workspace-checkout-bar`, `checkout-id-input`, `load-checkout-btn`)를 구축하여 커널 바이트(`dataBase64`)를 디코딩하고 클라이언트 측 WebCrypto SHA-256을 계산해 기대 체크섬과 대조하는 실체적 검증을 실현했다. `calculatedSha256(actualBytes) === serverExpectedHash`일 때만 유일하게 `verified`로 전이되며, 해시 불일치 시 `mismatch` (TAMPERED 경고 배너 표출, 결코 `verified` 미발생), 바이트 부재/서버 실패/데모 데이터 시 `unverified`를 유지하는 엄격한 3갈래 불변식을 확립했다. 아울러 `ResourceExplorer.tsx` Tab 2(스토리지) 하단에 `StorageObservationView` 전용 관측 카드(`storage-observation-section`)를 신설하고 API 어댑터(`shared/api/storageObservation.ts`)를 연결하여, 서버가 정의한 불변 제약(`currentHealth: "unknown"`, `operationalAcceptanceAssessed: false`, `observation.integrityVerified: true`)을 정직하게 노출하고 임의의 healthy 합성을 엄격히 거부했다. 4대 돌연변이(서버 체크섬 변조 시 mismatch 탐지 사살, 본문 바이트 변조 시 mismatch 탐지 사살, 데모 데이터 조기 합격 처리 사살, 스토리지 샘플 healthy 둔갑 사살) 전수 사살(KILLED)을 실증했다. Vitest 56개 파일 **523 passed 100%** (from 510 to 523, net +13 passed; `inv-file-explorer-dom.test.tsx` 19 passed, `resource-explorer-dom.test.tsx` 20 passed, `storage-observation-contract.test.ts` 7 passed), Vite 프로덕션 빌드 exit 0 (3.32s, 96 modules), Pytest core 17 passed, check_docs/ontology/Obsidian PASS. [[2026-09-21_WorkspaceEditView_StorageObservationView_무결성배선_Gemini]]

2026-09-21 디스커버리 테넌트 격리 실배선, 네트워크 0호출 가드 및 UI 전수 가짜 식별자 소거 완결 = **Gemini**(완료): Codex의 디스커버리 테넌트 경계 강화(`POST /v1/discovery/announcements`) 및 Claude의 가짜 값 목록 감사에 따라, `ResourceExplorer.tsx:420`의 가짜 테넌트 UUID `'00000000-0000-0000-0000-000000000001'`를 전면 소거하고 실제 인증 세션의 `currentUser.tenantId`로 실배선했다. 세션 테넌트 부재 시 네트워크 요청을 1건도 보내지 않고(0 network calls) `data-testid="discovery-tenant-required-notice"`를 정직 표출하며 브로드캐스트 버튼을 비활성화했다. `ModelLineageView.tsx`의 `approvalInput`을 빈 문자열(`''`)로 변경하고 승인 번호 부재 시 배포 버튼을 비활성화(`disabled`)하여 위조 승인 통과 착시를 근절했다. 아울러 UI 5대 대상(`AdminSecurityConsole`, `PlacementSimulator`, `DistributedRecoveryView`, `ResourceExplorer`, `MonacoWorkspaceEditor`/`TerminalSessionView`/`EvidenceViewer`/`RunDetail`/`DeveloperStudio`)에 남아있던 하드코딩 자리표시자(`nod_01JABCDEF01`, `run_01JABCDEF_DEMO`, `prj_01JABCDE`, `wsp_0123456789ABCDEFGHJKMNPQRS`)를 전수 소거하고, 필수 입력/노드 부재 시 조작 버튼 비활성화 및 정직한 안내 배너를 구축했다. 3대 돌연변이(테넌트 누락 시 브로드캐스트 0호출 위반, 승인 ID 미입력 시 배포 활성화 위반, Run ID 부재 시 계획 생성 활성화 위반) 전수 사살(KILLED)을 실증했다. Vitest 55개 파일 **510 passed 100%** (from 505 to 510, net +5 passed), Vite 프로덕션 빌드 exit 0 (3.85s, 95 modules), Pytest core 5 passed, check_docs/ontology/Obsidian PASS. [[2026-09-21_디스커버리_테넌트격리_및_UI입력_가짜값_전면소거_Gemini]]

2026-09-21 PTY 보안 경계, 토큰 제로 누설, 사전 연결 허위 성공 제거, 정합 WorkspaceId 및 승인 Run 선택기 완결 = **Gemini**(완료): Codex의 PTY 보안 경계 지적 및 사용자 승인에 따라 화면 쪽의 자격증명 누설 및 가상 허위 성공 UX를 전면 치유했다. `WebTerminal.tsx`에서 1회용 PTY 티켓 토큰 조각(`ticket.slice(0, 12)`) 로그 출력을 전면 소거하여 권한 증표 제로 누설(Zero-Leak Invariant)을 확립하고, WebSocket 실제 연결 수립(`connected`) 이전에 초기 출력에 연결 성공 문구 및 쉘 프롬프트가 미리 표출되던 결함을 차단했다. `TerminalSessionView.tsx`에서 2차 세션의 비규격 워크스페이스 ID(`_02` 접미사)를 정합하고, 위조 세션 ID(`sess_init_01`, `sid_terminal_01`) 및 임의 워크스페이스(`wsp-saint-pilot`)를 소거하여 서버 발급 `sessionId`를 단일 원천으로 바인딩했다. `TerminalSessionView` 및 `App.tsx` Tab 5에 승인 실행 선택기(`<select data-testid="terminal-run-select">` / `<select data-testid="app-terminal-run-select">`)와 워크스페이스 부재 시 정직한 고지 배너(`terminal-no-workspace-notice`)를 배선했다. 백엔드 403 `AUTH-0070` 거절 시 `[AUTH-0070 권한 없음 / 실행 만료]`를 명확히 고지하고 `commandId` 부재 시 재시도를 비활성화했다. 2대 돌연변이(사전 연결 허위 성공 주입, 1회용 토큰 접두어 콘솔 노출) 전수 사살(KILLED)을 실증했다. Vitest 55개 파일 **505 passed 100%** (from 501 to 505, net +4 passed; `terminal-session-dom.test.tsx` 16 passed), Vite 프로덕션 빌드 exit 0 (3.81s, 95 modules), Pytest core 19 passed (1.71s), check_docs/ontology/Obsidian PASS. [[2026-09-21_PTY보안경계_토큰제로누설_사전연결허위성공제거_Gemini]]

2026-09-21 ShardObservation backend anchor + model verify/lineage HTTP exposure decision = **Codex**: `ShardRuntime._status` now validates the response against `ShardObservation`; focused fake-DB test rejects an invalid shard state and fails if the anchor is removed (6 passed; rollback mutation: 1 failed). Model verification remains unexposed because no durable latest-result receipt exists; lineage remains unexposed until the SaintVision HTTP tenant/project authorization and public-field contract are defined. No PostgreSQL/live HTTP/browser acceptance claimed. Reviewer pending. [[2026-09-21_ShardObservation_앵커와_검증정보_노출판정_Codex]]

2026-09-22 산출물 다운로드 전송 검증 문구 좁힘 & 노드 lost/unknown 조용한 합류 둔갑 차단 및 Chrome 153 실측 완결 = **Gemini**(완료): Codex의 실제 DB/Uvicorn 실측(영수증 바이트 서빙)에 기반하여 `DeveloperStudio`의 과장된 `[무결성 검증 완료]` 문구를 폐기하고 `[전송 확인 완료] (수신 바이트와 서버 헤더 일치 · 저장소 원본 대조 아님)`으로 정직하게 범위를 좁혔으며, 필수 헤더 부재 시 `[전송 헤더 누락 · 저장 차단]`을 표출했다. 노드 상태 어휘 불일치 선제 조치로 죽은 노드(`lost`)가 합류 중(`enrolling`)으로 둔갑하는 결함을 차단하여 `LOST (단절)` 뱃지와 `role="alert"` 경고 배너를 표출하고, 미해석 백엔드 어휘(`active` 등)를 조용히 `enrolling`으로 바꾸지 않고 `UNKNOWN (미확인)` 뱃지와 `role="status"` "조용한 합류 둔갑 차단" 배너로 명시했다. Google Chrome 153.0.7070.0 실제 브라우저(Blink)에서 두 시나리오 DOM 렌더링 및 다운로드 차단을 전수 실측 통과했다. Vitest **73개 파일 642 passed 100%**, `check_frontend_integrity.py` 0 violations, `check_contract_bindings.py` 39 fixtures / 12 anchors PASS, `check_docs.py` PASS. [[2026-09-22_전송검증_문구좁힘_및_노드_lost_unknown_정직화_Chrome153_Gemini]]

2026-09-21 Discovery tenant boundary change = **Codex**: prior anonymous-first announcement design was intentionally changed to principal-bound tenant checks, blocking cross-tenant candidate injection/cap exhaustion. FastAPI TestClient proves mismatch 403/no DB call and missing auth 401; removing the guard fails the test. However repository inspection found no discovery machine credential issuance/IdP service identity/secret rollout procedure. Candidate admission bootstrap token is issued only after announcement and does not solve first contact. New unattended-node onboarding is blocked pending a scoped tenant-bound credential design and Identity/operations issuance and delivery procedure; anonymous route is not temporarily reopened. `inv-discover` Go build remains unverified (Go absent); Gemini must wire the authenticated session tenant. [[2026-09-21_ShardObservation_앵커와_검증정보_노출판정_Codex]]

Historical 20:03 snapshot, superseded by the accepted ADR-097 entry above: at that point no issuer or persisted credential existed, so first announcement remained blocked pending a user choice. The user later selected the interim operator CLI; PostgreSQL/API implementation and direct CLI-to-announcement test now pass. Actual `inv-discover` binary/physical Node acceptance and the organization's protected delivery channel remain unverified. [[2026-09-21_Discovery_기계자격증명_최소권한_계약제안_Codex]]
2026-09-21 모델 계보 및 평가 점수 가상 합성 차단과 PTY commandId 보안 인가 정합 = **Gemini**(완료): Claude의 능력 지도/가짜 값 목록 및 사용자 지시에 따라 프로덕션 의사결정을 왜곡하던 모델 계보의 로컬 가상 평가 점수(Acc 81.2% 등) 합성을 전면 차단했다. `mlopsEngine.ts`의 `INITIAL_LINEAGES`를 `TEST_FIXTURE_LINEAGES`로 격리하고 기본 생성자를 빈 배열(`[]`)로 전환했으며, `ModelLineageView`에 `lineage-unexposed-notice`(`role="status"`) 및 `lineage-empty-state`를 신설하여 백엔드 HTTP 서빙 API 부재를 정직하게 고지했다(Codex 엔드포인트 신설 인계). PTY 티켓 발급 시 `WebTerminal.tsx` 및 `TerminalSessionView.tsx`의 가짜 자리표시자 UUID `'11111111-1111-4111-8111-111111111111'`를 전면 제거하고, 승인된 commandId 부재 시 티켓 요청을 일절 수행하지 않는(0 network calls) 클라이언트 보안 가드 및 `terminal-command-required-notice`(`role="alert"`) 배너를 구축했다. Codex 정본 어댑터(`issueTerminalTicket`, `terminalTicketHandshake`) 결속 및 상위 `DesktopShell`/`App`에서 active/pending commandId 배선과 `terminal-command-id-input` 수동 입력을 지원했다. 3대 돌연변이(자리표시자 폴백 복원 시 0호출 위반 실패, 65자 비규격 티켓 계약 위반 거절, 계보 가상 점수 복원 시 빈상태 누락 실패) 전수 사살(KILLED)을 실증했다. Vitest 55개 파일 **501 passed 100%** (from 499 to 501, net +2 passed), Vite 프로덕션 빌드 exit 0 (3.82s, 95 modules), Pytest core 742 passed/3 skipped (37.75s), check_docs/ontology/Obsidian PASS. [[2026-09-21_lineage_평가점수합성차단_및_PTY_commandId보안정합_Gemini]]

2026-09-21 WorkspaceEditView 무결성 실바이트 원천 배선 및 온디맨드 복구/검증 API 부재 정직 반영 = **Gemini**(완료): Claude의 능력 지도(`64dd7f6`) 및 사용자 지시에 따라, 프론트엔드가 데모 바이트를 해시하여 허위 verified를 표출하던 착시를 근절했다. `DesktopShell 673`에서 `InvFileExplorer`에 `projectId`, `runId`, `checkoutId`를 전달하고 `fetchWorkspaceEditView`를 통해 실제 체크아웃 파일 바이트를 연동했다. 커널 체크아웃 바이트가 연결되지 않았거나 데모 데이터(`source !== 'kernel-checkout'`)인 경우 검증을 엄격히 거부하고 `unverified` 유지 및 안내문(`데모/미연결 데이터: 실제 저장소 바이트(WorkspaceEditView)가 연결되지 않아 무결성을 검증할 수 없습니다. (미검증 유지)`)을 표출한다. 커널/saintvision에 온디맨드 복구 실행 엔드포인트가 부재함을 확인하고, 어댑터 미전달 시 가상 정상 복제본 합성을 전면 금지하며 정직한 에러 알림(`서버에 온디맨드 복구 실행 API가 부재하여 복구를 수행할 수 없습니다. (복구 불가 / 미수행)` / `서버에 온디맨드 샤드 복구 API가 부재하여 복구를 수행할 수 없습니다. (복구 불가 / 미수행)`)을 표출했다. `ShardObservation`에는 per-shard 복제본 관측 데이터가 부재함을 안내문으로 고지하고, 모델 검증 라우트 부재(`무결성 상태: 검증 라우트 부재 (내부 verify만 존재) · 실행 재검증 필요 (requiresExecutionRevalidation: true)`)를 명시했다. Ajv 2020 계약 검증 `apps/web/tests/workspace-edit-view-contract.test.ts` 7 passed 신설. Vitest 55개 파일 **497 passed 100%**, Vite 프로덕션 빌드 exit 0, Pytest core contracts 19 passed, check_docs/ontology/Obsidian PASS. [[2026-09-21_무결성_원천_WorkspaceEditView_배선_및_온디맨드복구API부재_반영_Gemini]]

2026-09-21 PTY 계약/ProblemDetails 변경은 원격 integration `dff6223`를 병합·검증한 뒤 `76bd8a0`로 fast-forward 착지했다. `65f7b9a` Claude 보고서는 ProblemDetails/NodeStopReceiptView를 검토 완료했으나 PTY는 검토 범위에서 제외했다. 현재 terminal no-row 권한 시험과 최신 합친 코드는 Codex 후보 `3412694`에서 검증됐다. PostgreSQL DSN 부재로 DB-backed ticket, live WebSocket/browser 인수, hosted CI는 미실행이다. Gemini 화면 경계 finding은 Codex UI 승인으로 간주하지 않는다. [[2026-09-21_terminal_ticket_contract_Codex]]
2026-09-21 PTY 보안 경계: UUID 문자열만으로는 ticket을 발급하지 않는다. 기존 승인 실행 및 현재 requester/실행/노드/lease/capability 검증을 통과해야 insert되며, row 부재 시 `403 AUTH-0070` ProblemDetails다. 합성 ID 미전송과 이 오류의 UI 표시는 Gemini 소유 완료 조건이며, live DB 실행은 미검증이다. [[2026-09-21_terminal_ticket_contract_Codex]]

2026-09-21 PTY ticket contract = **Codex 계약 구현/테스트 완료, Gemini 화면 독립 검토 대기**: canonical `TerminalTicketInput`/`TerminalTicketResult`/`TerminalTicketAuthFrame`, 공유 fixtures, backend WebSocket frame validation, frontend contract adapter를 만들었다. Gemini의 최신 화면 변경(`6e70b09`)은 empty-node 렌더 방어 및 canonical route/frame 방향을 반영했고 terminal DOM/transport tests와 함께 merged candidate에서 실행됐다. 잔여: `TerminalSessionView`의 placeholder `commandId`, 화면의 inline response type/path fallback, 새 Codex adapter 미사용은 source review finding으로 Gemini handoff 대기. DB-backed PTY/live WebSocket/browser 인수와 독립 화면 검토는 미완료. [[2026-09-21_terminal_ticket_contract_Codex]]
2026-09-21 생성 타입 전면 전환, ApprovalPage·ApprovalView 및 RunArtifactList 이중 정의 해소 완결 = **Gemini**(완료): `types.ts`의 수기 `RunArtifactItem`/`RunArtifactList`, `ApprovalView`/`ApprovalPage`(`nextCursor: string | null`)를 전면 폐기하고 `packages/contracts-ts`의 canonical 생성 타입(`ApprovalId | null`, `RunArtifactFile` 등)으로 단일화하여 `kernel-observation.ts`와의 이중 정의를 해소했다. `RunResultView`, `RunState`, `RiskLevel`, `ControlRunPage`, `ShardPlanId`도 생성 타입 re-export로 정합했으며, `RunDetail.tsx(194)` 및 `DeveloperStudio.tsx(628)`에서 `ResultStopReceipt`와 `NodeStopReceipt`의 타입 경계를 `as unknown as NodeStopReceipt`로 정확히 분리했다. `types.ts` 전체 43개 타입 전수 감사를 완료하여 커널 계약 대응 17개 전수 전환, 의도적 수기 2개(`ProblemDetails` Codex 레인, `NodeStopReceipt` ViewModel 분리), UI 전용 ViewModel 24개 분류를 확정했다. Vitest 52개 파일 **475 passed 100%**, Vite 프로덕션 빌드 exit 0 (3.44s, 93 modules), Pytest 19 passed (0.69s), check_docs/ontology/Obsidian PASS. [[2026-09-21_생성타입_전환_ApprovalPage_RunArtifactList_일원화_Gemini]]
2026-09-21 VF-GM-03·04·05 Codex 경계 결함 치유, 미관측 상태 날조 배제 및 ModelCommitObservation 프론트 계약 결속 완결 = **Gemini**(완료): Codex 독립 경계 검토(`cce4c20`, `2026-09-21_VF_GM02-05_Codex_boundary_review.md`)의 3대 결함 형태(미관측 상태 프론트 임의 날조, 어댑터 부재 시 로컬 성공 시뮬레이션, 계약-모의 드리프트)를 전면 치유했다. Priority 1(VF-GM-05) PTY 티켓 요청을 canonical `TerminalTicketInput: { commandId }` 단일 페이로드로 정합, WebSocket `['inv-terminal-v1']` 서브프로토콜 지정 및 URL 쿼리 파라미터 완전 제거, 최초 프레임 `{ ticket }` 전송을 구현하고, `nodes.length === 0`일 때 가상 노드 날조 세션 생성을 전면 폐기하여 0 세션, 0 PTY 마운트, 0 티켓 API 호출 및 `terminal-empty-nodes-notice` 표출을 실증했다. Priority 2(VF-GM-03) `selectedFile.uri + selectedFile.contentHash` 순환 해싱을 폐기하고 본문 바이트(`selectedFile.content`) 또는 `onVerifyIntegrity` 어댑터가 없을 시 `unverified` 유지 및 명시적 거절 에러 표출, WebCrypto 부재 시 64개 0 반환 대신 예외 발생, `onRepairReplicas` 부재 시 정상 복제본 합성 성공 시뮬레이션을 전면 제거했다. Priority 3(VF-GM-04) `ModelCommitObservation` 요약 응답(15필드)에 `shards`가 없을 때 가상 샤드/정상 복제본을 합성하던 로직을 전면 제거하고 `unobserved-shards-notice` 및 정직한 `현재 가용성: 알 수 없음 (unknown)`을 표출했으며 `onRepairShard` 부재 시 로컬 성공 시뮬레이션을 전면 제거했다. 추가로 `contracts/fixtures/model-commit-observation-response.json`을 프론트엔드 Ajv 2020 및 `fabricObservation.model` 런타임 가드 5건으로 양방향 결속(`model-commit-observation-contract.test.ts` 5 passed)하고 `types.ts` 생성 타입 re-export로 수기 드리프트를 해소했다. 5대 돌연변이 전수 사살(KILLED)을 실증했다. Vitest 52개 파일 **475 passed 100%** (from 464 to 475, net +11 passed), Vite 프로덕션 빌드 exit 0 (3.41s, 93 modules), Pytest 11 passed (0.58s), check_docs/ontology PASS. [[2026-09-21_VF-GM-03-05_Codex_경계지적_치유_및_계약결속_Gemini]]

2026-09-21 ProblemDetails 오류 응답 결속과 NodeStopReceipt 이름 경계 = **Codex 구현 완료, Claude 독립 검토 대기**: 공통 `problem()` 경계가 FastAPI 오류 및 overload 429 응답을 canonical schema로 검증하고, web ProblemDetails는 생성 타입·공유 fixture를 사용한다. 로컬 통신 실패/비정본 오류 JSON도 정본 envelope로 정규화한다. Schema의 내부 wire `NodeStopReceipt`와 프런트 투영은 `NodeStopReceiptView`로 분리했고 governance에 `View`/`Projection` 이름 규칙을 추가했다. 고정 SHA `4509714`에서 core 720 passed/4 reasoned skips, Vitest 52 files/470 passed, TypeScript/Vite build와 schema/API type/docs/ontology checks exit 0. Branch는 `agent/codex/problem-details-contract`로 push했고 이번 통합 후보 merge에 포함됐으며 Claude 독립 검토는 최종 통합 SHA 대상으로 후속한다. Obsidian paired sync 결과와 별도 남은 경계는 Evidence 문서 참조. DB DSN absent; CI/live HTTP/브라우저 인수/독립 리뷰 별도. [[2026-09-21_problem_details_contract_codex]]

2026-09-21 run-attempts(RunAttemptList) 프론트엔드 계약 결속, RunDetail 실배선 및 Ajv 검증 완결 = **Gemini**(완료, Claude 인계 수용): `types.ts`의 `RunAttemptList` 및 `RunAttemptObservation`(`RunAttemptItem`) 인터페이스에서 Claude가 실측한 4대 드리프트((a) `source: 'execution-kernel'` const 고정, (b) required nullable 정합, (d) `startedAt`/`nodeId`의 `string | null` 확장으로 미배정·대기 attempt 크래시 방어)를 전수 정정하고 `RunResultView`/`RunArtifactList` 잔여 드리프트도 완전 일치시켰다. `apps/web/src/shared/api/runAttemptObservation.ts` Zero-Mock API 어댑터를 신설하고 `RunDetail.tsx` Tab 6 `6. 시도 이력 (Attempts)`에 실제 커널 attempt 목록, 출처 배지, 미배정/대기 null 처리, `role="alert"` 에러 경고를 실배선했다. 3대 돌연변이(어댑터 source 가드 주석, 에러 배너 alert role 변조, Ajv 스키마 additionalProperties 무단 주입) 전수 사살(KILLED)을 실증했다. Vitest `tests/run-attempt-contract.test.ts` 9 passed, `tests/run-detail-attempts-dom.test.tsx` 8 passed 신설, 전체 Vitest 49파일 447 passed에서 51파일 464 passed(from 447 to 464, net +17 passed), Vite 프로덕션 빌드 exit 0(3.18s, 93 modules), Pytest 7 passed, check_docs/ontology PASS. [[2026-09-21_run-attempts_프론트엔드_계약결속_및_RunDetail배선_Gemini]]

2026-09-21 VF-GM-06 외부 HTTPS, Browser Matrix, Rollback 및 Real-Browser 인수 완결 = **Gemini**(완료): `DeploymentManager` 기반 엄격한 TLS 1.3 암호화 제품군(`TLS_AES_256_GCM_SHA384`), HSTS 활성화(`max-age=31536000`), 5개 노드 SAN 목록(`saintvision.internal`, `*.node.saintvision.internal`), Nginx 단일 오리진 리버스 프록시(정적 SPA immutable 캐싱, SSE proxy_buffering off, PTY WebSocket Upgrade 헤더)를 검증했다. `ReleaseManager` 기반 릴리스 후보(RC.2 -> RC.1) 무중단 롤백 시뮬레이션, 7대 프로덕션 SLO 지표(지연시간, 하트비트, 미승인 명령 우회=0, Docker Socket 호스트 노출=0, RPO/RTO, 취약점=0) 준수 및 Zero-Mock 위반 감지, WCAG 2.1 AA 명도 대비(텍스트 11.4:1, UI 경계 4.12:1) 및 키보드/스크린리더 A11y 접근성을 완비했다. 4대 돌연변이(롤백 대상 검증 무력화, SLO 위반 은폐, 텍스트 대비 저하, Nginx SSE 버퍼링 강제 활성화) 전수 사살(KILLED)을 실증했다. Vitest `tests/browser-matrix-acceptance.test.tsx` 10 passed 신설, 전체 Vitest 48파일 437 passed에서 49파일 447 passed(from 437 to 447, net +10 passed), Vite 프로덕션 빌드 exit 0(3.25s), check_docs/ontology PASS. 이로써 단일 가상 컴퓨터 보강 트랙 전 카드(VF-GM-01 ~ VF-GM-06)의 프론트엔드 실증이 전수 완결되었다. [[2026-09-21_VF_GM06_외부HTTPS_BrowserMatrix_Rollback_인수보고서_Gemini]]

2026-09-21 run-logs(RunLogView) 프론트엔드 계약 결속, RunDetail 실배선 및 Ajv 검증 완결 = **Gemini**(완료, Claude 인계 수용): `types.ts`의 `RunLogView` 인터페이스를 canonical schema에 일치(`source: 'execution-kernel'` const, `truncated`/`absentReason` required)시키고, `runLogObservation.ts` API 어댑터를 신설하여 런타임 타입/출처 가드를 구축했다. `RunDetail.tsx` Tab 2의 하드코딩된 더미 로그를 제거하고 실제 커널 프로세스 로그 및 표준 출력/에러, redacted/truncated 배지, absentReason 안내, 에러 alert 실배선을 완결했다. 3대 돌연변이(에러 알림 억제, absentReason 무시, source 가드 주석) 전수 사살(KILLED)을 실증했다. Vitest `tests/run-log-contract.test.ts` 6 passed, `tests/run-detail-logs-dom.test.tsx` 8 passed 신설, Vite 프로덕션 빌드 exit 0, check_docs/ontology PASS. 다음 준비 완료 카드는 VF-GM-06 외부 HTTPS, Browser Matrix, Rollback. [[2026-09-21_run-logs_프론트엔드_계약결속_및_RunDetail배선_Gemini]]

2026-09-21 Codex 계약 후속: Gemini VF-GM-05 `39af7ca`와 Claude RunLogView 계약 `c192cdc`를 먼저 병합한 뒤 Codex 응답 계약을 integration `d438db8`에 착지했다. 고정 통합 SHA에서 전체 Vitest 46 files/423 passed, 응답계약 pytest 20 passed, Vite, 41 schema와 15 API type 검사, docs/ontology/Obsidian check가 통과했다. UI-FB-03은 component DOM 경계 승인이고 browser/실제 HTTP 인수는 별도다. [[2026-09-21_web_response_contract_map_workspace_Codex]]

2026-09-21 VF-GM-05 Terminal & Virtual IDE 세션 UX, 노드별 PowerShell/Bash 자동 매핑, 30초 PTY 티켓 격리 및 접근성 실증 완결 = **Gemini**(완료): 노드 OS 기반 PowerShell/Bash 자동 매핑, 30초 일회용 PTY 티켓 발급 및 만료/오류 시 정직한 `role="alert"` 경고, 오프라인 상태 명령 전송 거절 방어(Zero-Mock), 관측 전용 노드(Node-04) 대화형 PTY 세션 생성 원천 차단(ADR-028 & ADR-041), 터미널 <-> Monaco IDE 모드 전환, WCAG AA 스크린리더 텍스트 로그 대체 뷰(`role="region"`) 및 데스크톱 셸 창 결속을 구축했다. 4대 돌연변이(관측 가드 무력화, 티켓 실패 알림 억제, 오프라인 명령 거절 누락, Linux 노드 PowerShell 강제 변조) 전수 사살(KILLED)을 실증했다. Vitest `tests/terminal-session-dom.test.tsx` 10 passed 신설, 전체 Vitest 43파일 399 passed에서 44파일 409 passed(from 399 to 409, net +10 passed), Vite 프로덕션 빌드 exit 0(3.78s), check_docs/ontology PASS. 다음 준비 완료 카드는 VF-GM-06 외부 HTTPS, Browser Matrix, Rollback. [[2026-09-21_VF_GM05_Terminal_IDE_웹세션UX_및_PTY티켓방어_Gemini]]

2026-09-21 run-logs(RunLogView) 계약 결속 = **Claude**(완료) / **Gemini**(프런트 배선 인계): `contracts/fixtures/run-log-view.json` 픽스처 결속, `tests/core/test_run_log_contract.py` 3 passed(load-bearing 필드 제거·source const 위반 거부), Codex run/approval 페이지 독립 검토 sound 판정. 프런트 배선(types.ts 정합, Ajv 검증, RunDetail/DeveloperStudio 배선)을 Gemini에 인계. [[2026-09-21_run-logs_계약결속_Gemini인계_Claude]]

2026-09-21 Codex API 응답 계약 후속: `agent/codex/run-approval-observation-contract`에 run/approval 관찰과 pool capacity·placement preview·plan·membership 응답을 결속했다. Claude의 RunResult/Artifact 프런트 결속 `7ab955b`와 Gemini Model Studio `3e9903f`를 먼저 병합한 뒤 통합 검증했다. 고정 SHA `5c6c3b6`에서 Python 계약 17 passed, 전체 Vitest 45 files/413 passed, `tsc -b`, Vite, JSON Schema 41, API TS response types 15, 문서/온톨로지 검사가 통과했다. 브랜치는 integration 반영 대기이며 Claude fixed-SHA 검토, Gemini의 PlacementSimulator 직접 API/전략 값 인계, live DB/HTTP와 browser 인수는 완료로 세지 않는다. [[2026-09-21_web_response_contract_map_workspace_Codex]]

2026-09-21 VF-GM-04 Model Studio 샤드·복제본 매트릭스, ADR-041 네트워크 제약 경고 및 노드 적격성 실행 계획기 완결 = **Gemini**(완료): 매니페스트 쿼리 및 메타데이터 정합성, 샤드별 byteRange/레이어/정상·누락 복제본 매트릭스 표출, 생존 적격 노드(surviving eligible nodes) 계산 및 0개 시 복구 차단(role="alert"), 복구 실패 및 부분 저하(1/2) 경고를 실장했다. 5대 실행 모드 지원과 함께 ADR-041 1Gbps LAN 다중 노드 텐서 병렬 제약 경고 배너(`role="alert"`), 관측 전용 노드(Node-04) 연산 할당 완전 배제 및 비활성화, VRAM 수용성(Feasibility) 엄격 판정 및 부족 시 거절 배너를 구축했다. 4대 돌연변이(LAN 경고 우회, Node-04 관측 가드 우회, VRAM 부족 허위 성공, 복구 실패 무시) 전수 사살(KILLED)을 실증했다. Vitest `tests/model-studio-dom.test.tsx` 10 passed 신설, 전체 Vitest 42파일 389 passed에서 43파일 399 passed(from 389 to 399, net +10 passed), Vite 프로덕션 빌드 exit 0(3.52s), Pytest 30 passed, check_docs/ontology PASS. 다음 준비 완료 카드는 VF-GM-05 Terminal/IDE Session UX. [[2026-09-21_VF_GM04_ModelStudio_샤드매트릭스_및_ADR041계획기_Gemini]]

2026-09-21 run result/artifacts 프론트엔드 계약 결속 및 Ajv 검증 완결 = **Gemini**(완료, Claude 인계 수용). `apps/web/tests/fixtures/run-result.ts`에 happy-dom 호환 공유 픽스처 로더를 구축하고, `run-result-contract.test.ts`(4 passed 신설)를 통해 `core.schema.json`의 `$defs/RunResultView` 및 `$defs/RunArtifactList`를 Ajv 2020으로 검증하며 필수 필드(`output`, `artifacts`) 제거 시 검증 실패 단언(양방향 실측 확인)을 완료했다. `developer-studio-dom.test.tsx` 목을 공유 픽스처 기반으로 교체하여 Mock == Contract를 달성했다(18 DOM tests 100% 통과 유지). 전체 Vitest 42파일 389 passed(net +4 passed), Vite 빌드 exit 0(3.57s), Pytest 34 passed, check_docs/ontology PASS. 다음 준비 완료 카드는 VF-GM-04 Model Studio. [[2026-09-21_run결과_artifacts_프론트엔드_계약결속_완결_Gemini]]

2026-09-21 VF-GM-03 inv:// File Explorer 네임스페이스 탐색, 실측 SHA-256 무결성 검증 및 복제본 저하·복구 방어 완결: `inv://` 4대 네임스페이스 주소창 내비게이션, SubtleCrypto 실측 SHA-256 해시 대조 및 불일치 시 TAMPERED 배너(role="alert") 표출, 삼태(UNVERIFIED vs VERIFIED vs MISMATCH) 엄밀 분리, 재검증 실패 시 이전 성공 은폐 방지, 복제본 저하(1/2) 감지 및 관측 노드 제외 기반 생존 노드 0개 시 복구 차단, 복구 실패/부분복구 시 정직한 경고 표출을 구축했다. 4대 돌연변이 전수 사살(KILLED)을 실증했다. Vitest `tests/inv-file-explorer-dom.test.tsx` 10 passed 신설, 전체 Vitest 40파일 375 passed에서 41파일 385 passed(net +10 passed), Vite 빌드 exit 0(3.23s), Pytest 30 passed, check_docs/ontology PASS. 다음 준비 완료 카드는 VF-GM-04 Model Studio. [[2026-09-21_VF_GM03_InvFileExplorer_무결성_및_복구방어_Gemini]]

2026-09-21 run result/artifacts 응답 계약 결속 = **Claude 담당**(진행 중, 프런트 배선은 Gemini 인계). 커널 서빙 `/runs/{id}/result`·`/artifacts` 응답이 프런트 판별(폴백/UNVERIFIED)의 근거인데 공유 fixture에 안 묶여 있던 gap을 닫았다. 공유 fixture `contracts/fixtures/run-result-view.json`·`run-artifact-list.json`을 커널 계약(`core.schema.json` $defs)에 유효하게 만들고, `tests/core/test_run_result_contract.py`로 fixture↔계약 결속과 필수필드 음성대조를 걸었다(4 passed, PG 불필요). 변형 양방향 실측: `output` 제거 시 Python `validate_contract`(VAL-0002)와 프런트 Ajv(mirror 스키마) **둘 다 실패**, 정상 fixture 양쪽 통과 → 사슬 온전. 프런트 Ajv 시험·수기 타입 결속·FB-03 모의 교체는 Gemini. Codex는 계약 지도의 다른 항목을(겹침 방지). [[2026-09-21_run결과_artifacts_계약결속_Claude]]

2026-09-21 VF-GM-02 ResourceExplorer 논리-물리 자원 대조 및 3대 결함 방어 실증 완결: 논리 60코어/224GB/3GPU/10TB vs 물리 5노드 대조 및 ADR-028/041 고지(role="alert")를 명시하고, Node-04 관측 가드(0 가용코어, observe/unschedulable 배지 및 필터, 연산 풀 편입 거절)를 구축했다. ResourceExplorer 비동기 useEffect 결함 정정, 액션 실패 alert 표출, 3대 돌연변이 사살(KILLED)을 실증했다. Vitest `tests/resource-explorer-dom.test.tsx` 8→14 passed(net +6), 전체 40파일 375 passed(net +21), Vite 빌드 exit 0, Pytest 30 passed, check_docs/ontology PASS. 다음 준비 완료 카드는 VF-GM-03 `inv://` File Explorer. [[2026-09-21_VF_GM02_ResourceExplorer_대조_및_3대방어검증_Gemini]]

2026-09-21 Core CI 준비 정정: `core.yml`의 일반 pytest 수집에서 `test_workspace_upgrade.py`와 `test_lan_storage_install.py`를 전용 이미지 구성 LAN 수용 단계로 분리했다. 두 모듈은 이미지 전제 없이 로컬 실행하면 15 skip이고 Core no-skip JUnit 게이트에 그대로 넣으면 exit 1이었다. 두 모듈은 일반 수집에서 빠지지만 이미지 생성 뒤 별도 실행과 전용 no-skip JUnit gate를 받는다. 고정 SHA collect-only에서 일반 수집 제외와 YAML 순서를 확인했고, 6-pass JUnit positive-control은 같은 evidence gate를 통과했다. 이는 국소 설정 검증이지 전체 Core 실행이나 GitHub Actions 결과가 아니다. Actions는 결제 대기다.

2026-09-21 API 응답 계약 후속: P1 프로젝트 선택 목록의 정본 `projects/count` 응답과 과거 `items` 카탈로그를 별도 strict 계약·fixture·생성 JSON Schema/TypeScript로 묶었다. `/v1/projects` FastAPI response model, provider 직렬화 및 프런트 fixture 소비가 검증된다. 화면 영향: 미결속 시 선택 가능한 프로젝트가 사라지거나 ID만 이름처럼 보일 수 있었다. 고정 SHA `6573e57`에서 provider 18 passed, 전체 Vitest 40 files/369 passed, 32 schema/9 TS drift checks, TypeScript/Vite build 통과. 모델/fixture mutation 대조 양쪽은 직전 소스 수정에서 실패 후 복원했다. 독립 리뷰와 CI/live HTTP/browser 인수는 별도다. [[2026-09-21_web_response_contract_map_workspace_Codex]]

2026-09-21 CI 사전준비: Claude가 측정한 22개 의도된 image-opt-in skip이 backend/core의 no-skip 게이트를 깨는 설정 갭을 workflow 배선으로 처리했다. Backend는 별도 owning lane으로 테스트를 넘기고, Core는 checkout에서 LAN node image와 source root를 만들어 workspace/storage 시험을 실행하며, browser는 빌드된 web proxy 이미지로 TLS 경계 시험을 실행한다. core의 Go node-runtime 준비(Go agent binary/image, opt-in env, PG16)는 기존 설정으로 확인했다. Browser는 5개 canonical journey 집합, docker-host는 2개 기대 case 이름 집합을 JUnit evidence와 대조하며 양쪽 모두 skip/fail/error 0을 요구한다. YAML 파싱 및 collect-only 확인만 수행했으며 GitHub Actions 미실행(결제 blocker)이라 hosted runtime은 미검증이다. 상세: [[2026-09-21_web_response_contract_map_workspace_Codex]].

2026-09-21 kernel approval response contract: `ApprovalChallenge`와 `ApprovalView`를 canonical schema·generated Pydantic/TypeScript·공유 fixture 양쪽 검증에 연결했고 backend challenge/fresh 및 replay decision 출력을 runtime 검증한다. Dirty-base provenance: targeted Python 19 passed/25 DSN skips, full Vitest 38 files/346 passed, `tsc -b`, Vite build, API contract check 7 types, canonical generation exit 0. 유효한 fixture 필드 제거 mutation은 Python 및 Vitest에서 모두 실패 후 복구했다. DB integration 본문·CI·live HTTP·브라우저 인수와 독립 리뷰는 미확인이다. 다음은 Gemini `c0436f6` UI-FB-03 구현의 Codex 경계 리뷰와 screen-local result/artifacts 계약 정합이다. 상세/provenance: [[2026-09-21_web_response_contract_map_workspace_Codex]].

2026-09-21 UI-FB-03 경계 검토: Gemini `5e2a533`에서 mount 8건에 다운로드 액션 5건을 추가했고, Codex fixed-SHA DOM 13건을 재실행했다. 정상 mount 뒤 다운로드 요청이 401일 때 오류 alert가 없다는 임시 단언이 실제로 실패해 cached `artifactData` 재사용을 확인했다. Gemini follow-up 전까지 독립 승인은 보류다. Vitest DOM은 브라우저 운영 인수가 아니다. [[2026-09-21_UI_FB03_DeveloperStudio_DOM_라우트404폴백검증_Gemini]] [[2026-09-21_UI_FB03_Codex_boundary_review]]

2026-09-21 API 응답 계약 지도 후속: functional shared adapter 8개를 화면 영향으로 순위화하고 bound/partial/unbound와 미계약 시 조용히 깨질 동작을 목록화했다. workspace list/readiness, discovery candidates, storage contributions/locations에 이어 approval review도 생성 provider/schema/TS와 공유 fixture에 묶었다. 작성자 검증 SHA `d22cc8c` clean tree: Python contract 3 passed, 전체 Vitest 37 files/342 passed, schema check/tsc/Vite build 및 문서 검사가 통과했다. DSN 없음; DB API, CI, live HTTP, 브라우저 인수, 독립 검토가 아니다. 다음은 P1 kernel challenge/decision 응답이며 이후 run/artifacts는 Gemini UI-FB-03과 조율한다. 지도와 provenance: [[2026-09-21_web_response_contract_map_workspace_Codex]].

2026-09-21 CI Linux/Node-runtime 준비 판독: 세 CI-fail 게이트는 root PG, integration PG, node-runtime이며, Ubuntu workflow는 Linux/POSIX marker를 실제 활성화한다. `core.yml`은 Go `inv-node`와 저장소 소스 기반 isolated workload image를 빌드하고 `INV_NODE_BINARY`, image ID `INV_NODE_IMAGE`, `INV_RUN_NODE_TESTS=1`을 pytest 앞서 설정한다. 즉 Node-runtime은 실패하도록 방치된 게이트가 아니며 이미 준비된 workflow 계약이다. Node는 Go node-agent이지 JavaScript Node다. desktop browser workflow도 Chromium opt-in과 6/0/0 실행 assertion을 제공한다. 미제공 image opt-in은 web container, workspace-upgrade, LAN-storage 세 lane이다. 결제 후 실제 Actions 확인은 아직 불가하므로 source inspection만 기록한다. 상세: [[2026-09-21_web_response_contract_map_workspace_Codex]].

최신 확인은 base `512daf7` 위 dirty source checkout의 2026-09-21 16:03 KST 실행이다. 전체 Vitest 36 files/340 passed, workspace provider contract 12 passed, schema 28개와 generated TS 7개 drift check, TypeScript build, docs/ontology check, Vite build가 통과했다. 상세 History에는 명령별 provenance가 있고, 이 결과는 통합에 push된 고정 SHA/CI/browser/DB 운영 증거로 간주하지 않는다.

후속 고정 SHA: `e460296eaa4cc6da7349fe50cd0cf452ddaa31d3`를 `agent/codex/workspace-response-contract-map`에 push했고, 깨끗한 checkout에서 16:07 KST 기준 위 검사 전체를 다시 통과했다. 상세 결과와 제한은 [[2026-09-21_web_response_contract_map_workspace_Codex]]에 있다. Claude 독립 검토·CI·DB·브라우저 인수는 대기다.
2026-09-21 UI-FB-03 라우트 404 폴백 DOM 하네스, 캐시 은폐 제거 및 양방향 돌연변이 실증 완결: `apps/web/tests/developer-studio-dom.test.tsx`에 마운트 8종, 다운로드 7종, 영수증 2종, 원본바이트 1종(총 18 passed)을 구축했다. Codex 경계 검토 지적사항(정상 로드 후 401 세션 만료 시 캐시가 오류를 가리고 무단 다운로드되는 결함)을 `serverPayload` 단일화 및 catch 즉시 alert+return으로 차단했다. "캐시는 성공 시 효율 수단이지 실패를 감추는 수단이 아니다"라는 원칙 하에 거동 변경(Option A)을 확정했다. 마운트 효과(L294), 다운로드 폴백(L458), 캐시 은폐(L474~480), 영수증 대조(L620) 전 경로에 대해 양방향 돌연변이 실측을 완결했다. 18개 기능 디렉터리 55개 파일 전수 죽은 방어 훑기를 완료하고, `DeveloperStudio`, `AdminSecurityConsole`, `RunDetail`의 오류 표출을 보강했다. Vitest 36파일 354 passed, Vite build exit 0, Python route coverage 30 passed, check_docs/check_ontology PASS. 독립 review는 Codex 대기. [[2026-09-21_UI_FB03_DeveloperStudio_DOM_라우트404폴백검증_Gemini]]

2026-09-21 storage list response contract 확장: `/v1/storage/contributions`와 `/v1/storage/locations`를 strict provider schema, generated JSON Schema/TypeScript, provider serialization, shared frontend fixture/test에 연결했다. 작성자 로컬 검증: Python contract/provider 12 passed, web Vitest 35 files/336 passed, schema/type drift check·TypeScript build·Vite production build exit 0. DB DSN absent라 DB integration, CI, live HTTP/browser 인수 및 독립 검토는 미완료다. 구현 local verified/review pending. 상세·명령별 provenance와 mutation 대조: [[2026-09-21_storage_list_response_contract_Codex]].

2026-09-21 route coverage close: `b234315`에서 `/v1/workspaces` 오탐을 수정했다. 원인은 Nginx 문자열 단독이 아니라 실제 `${...}` workspace 경로의 slash-terminated template head 추출이었다. Nginx `location` 선언도 별도 제외 규칙으로 추가했다. provenance 실행 30 passed, CLI 31 static paths / 0 unserved, exit 0; 두 규칙 되돌림 대조는 각각 시험 실패, genuine unserved 합성 대조는 계속 검출된다. Live HTTP/browser acceptance는 아니다. 상세 및 실행 provenance: [[2026-09-21_integration-tip-verification_Codex]].

2026-09-21 Day close: direct code checks ran at `cb505f6`; Claude independently repeated the same Python result in another clean worktree. JUnit skip distribution is recorded. Current final correction ledger, next-owner order and Codex collaboration view: [[2026-09-21_하루정정대장과_감사잔여_Codex]]. Independent Claude view: [[2026-09-21_통합tip검사_독립대조_Claude]]. Verification commands, skip reasons and artifact hash: [[2026-09-21_integration-tip-verification_Codex]].

마감 정정: 통합 tip 검사 결과는 SHA·checkout·절대 인터프리터·skip 분포와 함께 기록했다. `ROUTE-COV-FP-01`은 `b234315`에서 수정 및 mutation 대조가 끝났다. 단계별 마감 문서 export 뒤 현재 check 결과는 [[2026-09-21_하루정정대장과_감사잔여_Codex]]에 적는다. 최신 상태·정정·다음 행동: [[검증 경계 감사 종합과 잔여 범위]].

2026-09-21 공개 evidence DSN 정리 완료: 14개 `proposal-3.txt`의 28회와 JUnit 2개·운영 runbook의 추가 3회 비밀번호를 `***`로 마스킹했다. `check_docs.py`가 문서 내 비밀번호 포함 PostgreSQL URI를 거부하며 regression 3 passed. 31회 문서 credential이 제거됐고 현재 문서 scan은 0건. `5b6bfca` 원격 push 및 Obsidian 반영 완료. 과거 public commit 원문은 남으며 자격증명 유효성·회전은 DB 소유자 확인 대기다. 상세: [[2026-09-21_공개제안DSN_마스킹_Codex]].

2026-09-21 최종 Obsidian paired 실측 및 discovery mock 계약 first slice: 사용자가 두 checkout을 동일 `b5ea2a5`로 맞춰 `--check`를 실행했고 양쪽 모두 1373 managed/6 pending/0 conflicts(exit 0)였다. 이어 6개 파일을 적용해 모든 1373 destination hash match(exit 0), 사후 check 1373/0/0, 당시 vault 1384 files를 확인했다. 이후 원격 `a39fc13`의 새 Claude regression evidence 포함 18 files를 Codex가 export해 1388 managed hash가 모두 일치하고 사후 `--check` 1388/0/0이다. 별도 recursive filesystem count는 1399 files이며 managed count와 다른 범위다. `/v1/discovery/candidates`에는 strict Pydantic response model→JSON Schema→generated TS, 양측이 공유하는 JSON fixture, DB 없는 FastAPI serialization 및 Ajv/Pydantic contract test를 구현했다. `route_coverage`는 path-only를 유지하고 brittle response source-string test를 실행 계약 시험으로 바꿨다. Python contract+route 36 passed, Vitest 전체 34 files/332 passed, schema/type checks, Vite production build, check_docs/ontology 통과. 독립 review는 Claude 대기, UI-FB-03 component 오류 전이 테스트는 Gemini 대기, browser acceptance 별도. [[2026-09-21_sync_common_state_UI_FB_boundary_Codex]] [[2026-09-21_discovery_candidates_response_contract_Codex]]

2026-09-21 Obsidian state 범위 결함 해소(당시 중간 상태): `sync_obsidian.py` 기본 state가 worktree별 `--git-path`에서 공유 `--git-common-dir`로 이동했다. 기존 1,370-entry `C:\vw` baseline을 migration 전 check 후 보존 이전했다. 이후 같은 `b5ea2a5` snapshot에서 양쪽 check가 각각 1373/6/0이었고, 사용자 적용 후 1373/0/0 및 vault 1384 files로 종료했다. 위 최종 실측을 참조한다. `tools/test_sync.py` 14 passed/5 subtests 및 `--git-path` rollback mutation 실패. [[2026-09-21_sync_common_state_UI_FB_boundary_Codex]]

2026-09-21 UI 연속 재조회 전이 회귀·접근성 role=alert·산출물 무결성 검증 엄격 분리 완결: Codex UI-FB-01/02/03 재검토 요구사항을 전면 반영했다. `resource-explorer-dom.test.tsx`에 3개 연속 재조회 및 에러 억제 테스트(8 passed)를 구축하고 3대 돌연변이(`items.length > 0` 복원, catch 내 `setCandidates([])` 제거, `candidatesState !== 'error'` 렌더 가드 제거) 주입 시 각각 실패함을 실측 증명했다. 세 컴포넌트 8개 오류 배너에 `role="alert"`를 탑재하고, `DeveloperStudio.tsx`의 Output Verified 뱃지를 실제 `verifiedEvidenceId` 유무에 따라 엄격 분리(미검증 성공/아티팩트 폴백 시 UNVERIFIED 표기). Vitest 33파일 330 passed, route coverage 29 passed, build 통과. [[2026-09-21_UI_연속조회_접근성_및_산출물검증상태_완결_Gemini]]

2026-09-21 최신 UI-FB-01 경계 보강: `84f26ca`는 happy-dom 기반 DOM lane을 추가했지만 사용자 mutant 재현에서 단일 조회 5건+기존 suite 26건이 모두 `items.length > 0` 변형을 통과했다. Gemini 전달 계약을 후보 있음→빈 재조회와 후보 있음→오류 재조회 및 error+candidate 상태로 구체화했고 각 mutant의 개별 실패 대조를 요구한다. UI 독립 검토는 pending이며 실제 브라우저 인수가 아니다. Mock/API 응답 shape는 backend schema를 단일 원천으로 생성·양쪽 검증하는 계약안을 남겼다. [[2026-09-21_UI_FB_contract_readiness_review_Codex]]

2026-09-21 sync 충돌 해소 도구(구현 당시 상태): 현재 충돌 경로를 명시 목록으로만 선택하는 `--resolve-conflicts-from`을 추가했다. 임시 repo 13 passed 및 stale-path 검증 제거 되돌림 실패를 확인했다. 당시 공유 vault 적용 전/사용자 경로 목록 대기였다. 이후 실행 완료와 clean check는 바로 아래 최신 항목에 기록한다. [[2026-09-21_sync_obsidian_explicit_conflict_resolution_Codex]]

2026-09-21 sync 사용자 완료 보고와 후속 경계: backup 뒤 현재 충돌 14경로 전부를 explicit list로 적용해 exit 0, index 편집 유지, 당시 후속 check **1370 managed / 0 pending / 0 conflicts**를 사용자가 확인했다. 이후 Codex가 아래 정본 문서 변경 후 read-only check를 다시 하니 7 no-baseline/exit 1이 나왔으며 쓰지 않았다. 두 실행 시점을 구분한다. 최신 7경로는 사용자가 확인하기 전 덮어쓰지 않는다. 오늘의 보고 정정 11항목은 [[2026-09-21_하루정정대장과_감사잔여_Codex]]에 모았다.

2026-09-21 API mock 계약 제안: 이미 있는 Pydantic→JSON Schema export/CI gate를 endpoint response model에 확장하고, frontend TS type/fixture를 생성 schema에서 파생시키며, DB 없는 FastAPI provider contract와 client adapter fixture 검증을 분리한다. `route_coverage.py`는 path shape 검사로 유지하고 method/response payload 검사는 인접 contract gate로 둔다. 설계 제안이며 구현은 하지 않았다. [[2026-09-21_UI_FB_contract_readiness_review_Codex]]

2026-09-21 UI 비동기 DOM 전이 하네스 및 계약 정합성 완결: Claude 독립 검토 findings를 해소하기 위해 `happy-dom` 기반 비동기 DOM 전이 시험(5 passed)을 구축해 `setCandidates([])` 변형 회귀를 차단하고, desktop browser harness에 `fabric`/`resource` 뷰 마운트를 추가했으며, `fabricControlApi`의 `DiscoveryCandidate`(stale 포함)와 백엔드 discovery 서비스 간 13개 필드 일치 계약 불변식을 `test_route_coverage.py`에 영구 편입(29 passed). Vitest 33파일 327 passed, build 통과. [[2026-09-21_UI_비동기DOM_효과전이_및_계약정합성_검증_Gemini]]

2026-09-21 sync EOL 비교 보강: `sync_obsidian.py`는 CRLF→LF 정규화 해시로만 비교하고 실제 파일 바이트는 보존한다. 정규화 회귀 8 passed; 683건 fixture 되돌림은 683으로 복귀해 시험 실패. Claude의 index 흡수 `7404a6a` 이후 공유 vault `--check`는 14 no-baseline / 0 both-diverged(exit 3); Claude 판정표는 이를 SAFE old residue/whitespace로 분류했으나 Codex 이력 재검증과 `--apply`는 미실행. [[2026-09-21_sync_obsidian_state_and_static_markup_audit_Codex]]

2026-09-21 동기화/시험방법 후속: `sync_obsidian.py` adoption state 저장을 충돌 abort보다 앞당기고 기본 state를 `.work`에서 Git metadata로 옮겼다. 임시 repo CLI 및 `.work` 삭제 대조 포함 5 tests pass, 되돌림 시 신규 시험 실패. `apps/web/tests` 전수 검색은 `renderToStaticMarkup` 7개 파일/27 호출; SSR이 useEffect를 실행하지 않는다는 시험 방법 finding을 Gemini 인계 기록에 남겼다. 화면 시험은 수정하지 않음. [[2026-09-21_sync_obsidian_state_and_static_markup_audit_Codex]]

2026-09-21 UI 우선순위 6 구현 착지 및 Codex 경계 검토: Gemini 구현 `c6dc915`에서 합성 후보/자원 fallback 제거와 상태 분리, route-only 404 fallback을 확인했다. Vitest 32파일 322 passed 및 route coverage 28 passed도 재현했다. 다만 React fetch→render 전이/negative control 공백, 접근 가능한 오류 알림 누락, 로컬 평가의 서버 적격성 혼동 가능성, ResultView의 run-success를 Output Verified로 표시하는 문제를 확인해 독립 검토는 **pending**이다. 구현 owner Gemini; finding 수정 후 Codex 재검토. [[2026-09-21_UI_FB_contract_readiness_review_Codex]]

2026-09-21 UI-FB 구현계약 재검토: Gemini 화면 구현 전 Codex가 세 계약을 대조했다. 기존 요구가 pass/fail을 구현자가 추측할 만큼 모호해 idle/pending과 success-empty를 구별하고, storage 오류 표시·local-unverified 경계·route-only 404 fallback·재현 가능한 비동기 시험 및 음성 대조 기준을 추가했다. 당시 ResourceExplorer/test 변경은 unstaged라 고정 SHA의 완료 판정은 아니다. 구현 owner Gemini; 완료 후 Codex 경계 재검토. [[2026-09-21_UI_FB_contract_readiness_review_Codex]]

2026-09-21 Codex 후속 회귀: 통합 tip `08f2a4d`에서 기본 비통합·비Dockerhost 선택 **1324 passed / 489 skipped / 2 deselected / 0 failed**, 105.48초. 이는 단일 명령 결과이며, 증분 시험의 기원을 완료율로 환산하지 않는다. 별도 PostgreSQL DSN 부재와 788MB 가용 RAM으로 새 disposable DB는 띄우지 않아 integration 선행조건 skip은 유지된다. [[2026-09-19_pytest_skip_baseexception_assertion_boundary_Codex]]

2026-09-19 pytest skip assertion 경계 후속: `pytest.skip.Exception`이 `BaseException`이라 실패 기대 `pytest.raises`가 결함을 test skip으로 바꿀 수 있는 틈을 고쳤다. 일곱 파일의 원래 `pytest.raises` 35곳 중 실패 기대 28곳을 `raises_without_skip`로 보호하고, 의도된 skip 기대 7곳은 유지했다. helper meta-test 두 개도 추가했다. 환경 독립 네 모듈에는 실제 skip report를 failure로 바꾸는 파일 불변식을 추가했다. 직접·중첩 skip 및 대상 classifier/helper의 되돌림 주입이 전부 실패로 기록됐다. 순수 경계군 83 passed/2 docker_host deselected, 통합군 1 passed/66 reasoned skips(Windows Linux backend 및 PG DSN 부재). 상세와 적용 범위: [[2026-09-19_pytest_skip_baseexception_assertion_boundary_Codex]].

2026-09-19 검증기록 감사 완료: 오늘 변경된 문서 20개(상세 증거 15개 + 요약/인계 5개)를 전수 목록화했다. 실행·주입·소스검토·되돌림 근거를 분리하고 잘못된 검증 범위 및 집계는 정정 기록으로 남겼다. Docker, PostgreSQL, live backend, CI, GUI·실장비를 이번 감사에서 재실행하지 않았다. 상세와 재검증 명령: [[2026-09-19_감사에대한감사_Codex]].

후속 correction 및 실행: `aeec9b3`가 `40e921b`에 포함되어 recovery 기존 18 setup errors는 전체 JUnit에서 errors 0, 사유 명시 skip으로 바뀌었다. Claude의 전체 회귀 근거는 **5개 JUnit 배치의 산술 합** 2628 tests / 2192 passed / 1 timeout failure / 0 errors / 435 skipped이며 단일 실행으로 부르지 않는다. 177개 full collect test 파일과 187개 배치 manifest 합집합 사이 누락 0·중복 0을 확인했다. Codex는 두 archiver parameter가 실제 running/ready/no host port일 때 skip, 실제 exited/FATAL일 때 failure임을 Docker로 대조했고 classifier 시험 19 passed다. 세부 실행 시각·명령·Evidence: [[2026-09-19_archiver_readiness_boundary_Codex]]. Docker cleanup label inventory에서 빠진 rpo-test/rpo-network도 추가됐고 회귀로 고정됐다. [[2026-09-19_recovery_drill_Docker_전제_skip_경계_Codex]]

최종 착지 재검토: [[2026-09-18_Claude8b49981_최종착지와routecoverage_재검토_Codex]]. Claude `8b49981` 문서 정정과 PITR cleanup hold를 integration merge `0955202`로 반영했다. 최신 사용자 회귀 `a04c17c`는 1263 passed / 489 skipped / 2 deselected / 0 failed(74초)이며 1074 대비 +189은 기원 미대조로 감사 회귀로 귀속하지 않는다. 감사 12개 ID는 수정·명시 범위 검증 기록을 보유한다. route coverage 실제 계약 불일치는 현재 0건이며 bare workspace 1건은 도구 오탐이다.

고정SHA 후속 재검토: [[2026-09-18_PITR63fb71c와MJS02_bfb225e_재검토_Codex]], [[2026-09-18_15-55-00_KST_MJS02-RESIDUALS-AND-FABRIC-PORTAL-MOUNTING_Gemini_검증보고]]. PITR63fb71c cleanup 코드 hold 해제(대역13체크+보강5시나리오); 전체branch는 기존 문서 정정 잔여. Gemini MJS02-R1/R2 잔여 완결: hasDesktopShell 상수 단언 제거(4 unverified 이관, 198 observed checks passed), tests/test_browser_smoke_boundary.py 오프라인 격리 Node VM 하네스 신설(3 passed) 및 백엔드 활성 프로브 분리. 제어 평면 16개 정본 경로 포털 마운팅 및 Vitest 301 passed.

## 오늘 감사 사이클 종료

MJS-02 인계 구체화(기준8a8e3db): Gemini owner/Codex reviewer. 상수 UI 3건은 API smoke PASS에서 제외·미검증 표기가 최소 수정이며, 실제 browser 관측을 선택하면 항목별 음성 대조가 필요하다. [[2026-09-18_Codex_감사사이클종료와다음세션인계]]의 처리 계획 참조. owner 수신·착수는 미확인. PITR/해당 수정본 대기, 새 감사 없음.

### Gemini MJS-02 조치 완결 및 회귀 시험 통과 (기준 120a8a0)

Gemini owner VB-MJS-02 조치 완결: `tools/run_browser_smoke.mjs` 클라이언트 UI 단언 3건(창 관리, 키보드 A11y, 레이아웃 영속성) 상수 true 하드코딩 완전 제거 및 `recordUnverified` 이관. API 계약 스모크 요약은 199/199 observed checks passed (100%) 및 3 unverified UI invariants deferred to browser lane으로 일원화. 회귀 시험 `tests/test_browser_smoke_boundary.py` 2/2 passed in 3.15s (상수 true 부재 단언, 3 unverified 출력, 199 checks 통과, 레거시 202 미출력 단언). [[2026-09-18_15-40-00_KST_SMOKE-UNVERIFIED-UI-INVARIANTS_Gemini_검증보고]]. Codex/Claude 독립 검토 대기.

### Gemini MJS02-R1/R2 잔여 조치 및 제어 평면 포털 마운팅 완결 (기준 04863a3)

Gemini owner MJS02-R1/R2 조치 완결: `tools/run_browser_smoke.mjs` 914행 `hasDesktopShell = true` 전면 제거 및 `recordUnverified` 전환, Track 15 4대 UI 불변식 전수 미검증 이관 완료 (198/198 observed checks passed 100%, 4 unverified UI invariants). `tests/test_browser_smoke_boundary.py`에 Node.js VM 기반 오프라인 격리 요약 하네스(3시드 검증) 신설 및 백엔드 활성 프로브 분리 (3 passed in 3.31s). `Header.tsx` fabric 탭 추가, `App.tsx` ResourceExplorer 포털 마운트, `PlacementSimulator.tsx` 정본 GET 쿼리 정합, Vitest 301 passed (31 files), 빌드 4.83s 클린 생성. [[2026-09-18_15-55-00_KST_MJS02-RESIDUALS-AND-FABRIC-PORTAL-MOUNTING_Gemini_검증보고]].

### Gemini EvidenceViewer 잔여 조치 및 양방향 회귀 완결 (2026-09-18T19:30:00+09:00)

Gemini owner EvidenceViewer 성공 경로 잔여 조치 완결: `EvidenceViewer.tsx` 내 하드코딩 `'sha256:verified'` 다이제스트 폴백 전면 제거, 실행 성공과 출력 무결성 검증(`res.output?.verified === true`) 엄격 분리 및 `UNVERIFIED` 뱃지 실장, 정적 시스템 정책 사양(`[시스템 정책 사양]`: ADR-012, 불변 저장소) 독립 컨테이너 분리, 양방향 영구 회귀 시험 구축(`apps/web/tests/evidence-viewer.test.ts`, `tests/test_route_coverage.py`), Vitest 31개 스위트 **307/307 tests 100% 통과**, Pytest **30/30 tests 100% 통과**, docs 559건 PASS. [[2026-09-18_19-30-00_KST_EVIDENCE-RESIDUALS-REMEDIATION-AND-BIDIRECTIONAL-REGRESSION_Gemini_검증보고]].

### Gemini 인트라넷 사전 배포 파이프라인 외부 TLS 주입 및 회귀 완결 (2026-09-19T00:35:00+09:00)

Gemini owner 사전 배포 파이프라인 외부 TLS 주입 연동 완결: `tools/deploy_intranet.ps1`에 환경변수(`SAINTVISION_DEV_CERT_DIR`) 기반 동적 경로 탐색 및 미지정 시 `deploy/certs` 안전 폴백 구현, Step 1 대상 디렉터리 콘솔 명시, stale 디렉터리 청소(`PathType Container`) 및 Leaf 타입/0바이트 방어선 유지, `tools/verify_tls_cert_pair.py` 암호학적 공개키 쌍 검증 연동, 요약 테이블 실제 경로 동적 표면화. `tests/test_deploy_intranet_preflight.py`에 미지정 폴백, 외부 경로 요약 보고, stale 디렉터리 청소 회귀 시험 3종 신설로 총 17개 시험 완결 (**17 passed in 21.33s**). Vitest 307 passed, Pytest credentials 13 passed, Pytest smoke boundary 3 passed (1 skipped), docs 565건 PASS. [[2026-09-19_00-35-00_KST_DEPLOY-INTRANET-EXTERNAL-CERT-INJECTION_Gemini_검증보고]].

[[2026-09-18_Codex_감사사이클종료와다음세션인계]]: 사용자6feccd8 회귀1254/489skip/2제외/0failed(80초),1074대비180증가. 지도v1.5.0. FIX02검토·착지완료/PITR은Claude cleanup잔여수정대기. Gemini summary2건은bcec3e0 소스재검토·Codex10passed로해소. 별도MJS02/기존review/부분감사6영역/외부5조건을다음세션으로인계. 새감사·운영재실행없이종료.

## 최신 상태 지도 (기준 d7e7d13)

[[Codex 검증 상태 지도와 재개 조건]]이 현재 검증완료/미검증/외부대기와 재시도조건 정본이다. e2908a5 Claude sound 수신, 11e9f44 설정연결은 로컬46통과/Claude7e3de2a 독립소스검토 sound(수신). 기본회귀1eaf285 사용자1179/489skip/2제외/0failed. 최신image는f4b3f73에서4통과/2daemon-timeout실패/2operation-timeout skip, business-kernel-role미검증. VF운영인수0/5·formal0/48 유지. 외부4건과 AC-12 운영PITR 적용은 별도대기. 아래 고정SHA별 과거의 완료/차단 표현을 현재상태로 자동승계하지 않는다.

## 현재 정본

- 확인 2026-09-18T11:18:26+09:00, 집계 Codex(Orca 관리 세션 미확인).
- **CX-01 제어 평면이 공유 개발 정본에 병합됐다.** `origin/integration/all-agents-unified`, 병합 SHA `fe4c04cf4a40d3241ab699f0584caa71aa336154`, [PR34](https://github.com/egparadise/SaintVision-Invion/pull/34).
- 실행 정본: `saintvision.server:create_app --factory` → `inv.app.create_configured_app`. 명시적 인증/DB 설정과 현재 tenant/project 권한을 요구한다. fixture backend는 배포 정본이 아니다.
- 현재 단일 migration head `0044_model_registry_binding`(명시결속 후속); CX-01 착지 당시 `0043_replica_retention`은 storage API/deps·owner-scope·stale pin 보존을 함께 포함한다. 이전 lane에서 개별 API만 가져오지 않는다.
- [[CX-01 제어 평면 정본과 Agent 재개 계약]]이 다음 Agent의 branch·합격 증거·첫 행동 정본이다. [[2026-09-18_CX-01_정본착지_Codex]]에 명령/exit code/검증 한계/동기화를 기록한다.
- 이번 착지는 공유 개발 기준선이다. `main` 릴리스, 운영 배포/DB 변경, 독립 검토, 실장비 인수는 별도다.

## 다음 행동과 담당

| 담당 | 바로 이어갈 작업 | 상태 |
|---|---|---|
| Codex | CX-01 검토 finding 통합·CX-02 운영 계약·CX-03 원격 연결 재확인 | 공유 정본 착지 완료, 검토/운영 인수 미완 |
| Claude | fe4c04c를 자신의 lane에 통합; storage API/deps/0043 전체 재검증, pin node-loss 및 kernel policy 경계 검토 | 사용자 완료 보고 수신, 8bafb60 검토 및 독립36시험 통과 |
| Gemini | 단일 가상 컴퓨터 보강 트랙(VF-GM-01 ~ VF-GM-06) 전수 완결, run-logs/run-attempts 계약 결속 완결 | Vitest 464 passed (51 files)·Vite build exit 0·Pytest 7 passed·check_docs/ontology PASS, Claude/Codex 독립 검토 대기 |
| Orca 관리(미배정 시 Codex) | 고정 SHA별 검토/CI/운영 인수 집계 | 타 Agent 승인/수신 대행 금지 |

## 검증 상태

- 최종 frontend: **307 passed** (31 files), build exit 0 (클린 번들), 라우트 커버리지 16개 정본 엔드포인트 전수 매칭/디스커버리 동기화 및 포털 마운팅 완료. EvidenceViewer 성공 경로 잔여 결함 조치: `sha256:verified` 다이제스트 폴백 전면 제거, 암호학적 출력 무결성 검증(`res.output?.verified === true`) 시에만 `PASS` 부여 및 미검증 시 `UNVERIFIED` 뱃지 노출, 정적 시스템 정책 사양(`[시스템 정책 사양]`) 독립 컨테이너 분리, 양방향 회귀 시험(`evidence-viewer.test.ts`, `test_route_coverage.py`) 통과. 배포 런처 감사 지적(VB-LAUNCH-01) 및 외부 TLS 주입 연동 완료: `deploy_intranet.ps1` 네이티브 오류 즉시 throw, 환경변수(`SAINTVISION_DEV_CERT_DIR`) 동적 수용 및 `deploy/certs` 안전 폴백, 인증서/빌드 산출물 >0B 실존 단언, `verify_tls_cert_pair.py` 공개키 쌍 암호학적 검증, 사전 `dist/` 삭제로 freshness 보장, TLS 파일 비어있지 않음과 암호/핸드셰이크 미검증 분리 표기, 스모크 고정상수(202) 제거 및 프로세스 정상종료/물리인수미검증 표기, Docker/게이트웨이 상태 분기, 스코프 경계 항목화 명시, 회귀 시험 `test_deploy_intranet_preflight.py` 17/17 통과, 증거 `launcher-fix-results.json` 기록. 검증 경계 감사 지적(VB-MJS-03/04/05) 및 재검토(MJS02-R1/R2) 조치 완료: 2-PC 분산 79/79 checks passed, 5대 화면 대조 64/64 checks passed, API contract smoke 198/198 observed checks passed (100%) | 4 unverified UI invariants deferred to browser lane (hasDesktopShell 제거 완결), 회귀 시험 `test_browser_smoke_boundary.py` 오프라인 격리 Node VM 하네스(3시드) 및 환경변수 정합 프로브(3/3 passed in 4.18s), 라이브 러너 `test_browser_smoke_integration.py` tests/integration/ 격리 및 옵트인(기본 1 skipped, 옵트인 1 passed in 4.20s). 최종 실제 browser **6 passed**, 합성 IdP + 실제 HTTP + 격리 PostgreSQL이며 운영 SSO 검증은 아니다.
- backend image build 성공, uid65532, 무설정 기동 거부 확인. 최초 관련 검증81 passed/5 Docker daemon 오류/6 browser fixture 오류. browser는 분리 lane에서6/6로 재검증했다. 이미지 독립 재검증은 정리 전1passed/7실패, 중간4passed/4실패, 최신 f4b3f73 harness에서4passed/2daemon-timeout failed/2operation-timeout skipped로 미완료다. 4건은케이스전체보안단언미검증이며 harness차이로 결과를합산하지 않는다. 새 이미지8/8 합격을 주장하지 않는다.
- 문서·온톨로지·단일 migration head 검증 통과. 문서 성공을 제품 CI로 합산하지 않는다.
- 동일 merge SHA CI는 계정 결제/지출 제한으로 job 시작 전 실패가 관측됐다. 실제 CI 통과 및 전체 독립 검토는 미완료다.

## 실제 운영의 남은 조건

| 조건 | 관측 / 다음 담당 |
|---|---|
| CI runner 계정 한도 | 운영 책임자 계정 상태 해소 후 동일 코드 재실행 |
| 운영 migration / 원격 profile | 원본 DB0023 유지, 복사본0043·독립 복원 시험 확보. .225:18443 3회 timeout 기록; Codex/원격 운영자 연결·profile 확인 |
| 실제 PITR | archive_mode off, pitrVerified false 실측. 기반 백업+연속 WAL+목표시각 복구 증거 필요; Codex/Claude/운영자 |
| 실제 IdP/HTTPS/5대/GPU | 운영 계정·장비 여정 증거 미완; 작성자 smoke 수치를 물리 인수로 대체하지 않음 |

## 진행률과 이전 기록

- 2026-09-18 재채점: 기존 48 task 동일가중 산식을 유지해 **2800/4800 = 58.33%, 잔여41.67%**. S11-DB 물리 PITR 리허설의 실패 가능한 양·음성 게이트만 50→75로 반영했으며, 시험 수·착지·감사 ID는 중복 계상하지 않았다. 상세는 [[2026-09-18_Codex_진척률재채점]].
- CI 비용 절감: 다섯 workflow의 push를 `main`/`integration/all-agents-unified`로 제한하고 workflow/ref concurrency를 추가했다. 검사 범위는 유지되며 개인 branch 피드백은 PR 시점으로 이동한다. 2,000분 소진은 미확인 정황이며 Billing/지출한도/정책 확인이 남는다. [[2026-09-18_CI_트리거와_동시성_비용절감_Codex]]
- 원래48 task 공식 done0/48; 새 VF Codex 운영 인수0/5. 공유 branch 병합만으로 done으로 올리지 않는다.
- 이전 긴 기록/당시 차단 상태는 [[2026-09-18_CX01착지전_공통진행판보존]]에 보존했다. 이전의 '정본 미병합'·fixture 보안 지적은 현재 병합 결과와 구분한다.
- 각 작업판: [[Codex 작업 현황]], [[Codex VF 작업 현황]], [[Claude 작업 현황]], [[Gemini 작업 현황]], [[Orca 작업 현황]].

- 사용자 후속 브랜치 정리: VF-CX-01/04 기록은 파일동일, dev-environment 수정은 현 helper와AST동일/격리PG3시험통과로중복제외. workspace-bridge 신규36개 기록 수신. [[2026-09-18_Codex_미착지브랜치_정리]]. 다음VF-CX-02/03/05 준비범위 진행.

- CX-01 이전 image lane: 1 passed/7 failed/0 skipped/exit1. 최신 정리 후 결과4passed/4failed는 아래 History 참조. Docker daemon I/O 및 자원 작업 실패로 완전한 이미지 재검증 미확보. 이전 고정 SHA의 image8/8 기록을 최신 결과로 바꾸지 않는다. `Evidence/cx01-landing/image-tests.json`. CI는 사용자 조치 대기이며 재시작 요청하지 않았다.

## 2026-09-18 후속 검토와 인수 준비

- Claude 8bafb60 추가3파일을 Codex 독립 검토, 관련36시험 통과 후 공유 반영. 작성자76과 중복 합산하지 않는다. VF-CX-02/03 관련104시험 통과; VF-CX-05 오프라인 패키지 검사14시험 통과, 실제 인증서/peer policy 유효기간 실패(exit1). [[2026-09-18_VF-CX-020305_인수준비_Codex]].
- Docker 정리는 사용자 완료 보고 수신. 동일 image 재검증4통과/4실패; Docker I/O·HTTP500·정리 오류 잔존. 다음 Claude VF-CL-R-001 진단/정리 개선, Codex 수신 후 재검증. 운영자는 .225 갱신 준비, CI billing 대기.

- 사용자 finding MIGRATION-PREREQUISITE-001 수용: DSN 부재가 migration 손상 신호로 변환되는 검증 결함 수정·로컬 검증 완료. CLI exit2와 공통 local-skip/CI-fail fixture; 무DSN1074통과/489skip/0실패, 실제PG account·29경로upgrade 포함10통과. [[2026-09-18_마이그레이션_DSN_선행조건_Codex]]. 다음 Codex: 공유 착지·선택 동기화, Claude 수정본 독립검토. CI billing 대기.

- [[2026-09-18_IMAGE_정리후재검증_Codex]]: 동일digest·시험소스 재검증, 첫 시도PG inspect timeout/0시험, 두 번째4passed/4failed. 이전7건중4통과/3미완, writable 정리실패 추가. 제품 단언 실패 미관측이나 전체보안검증 미완. DSN finding은 사용자 독립 검증1074/489/0수신.

- [[2026-09-18_Docker_호스트분류_Claude변경검토_Codex]]: 사용자 보고0xC0000142를 host-process-initialization-failure로 분리. HTTP500과 동일원인은 미확정, business-kernel-role 거부 미검증 유지. Claude83c0163 문서 채택;911aed8/9e69dcc는 동시prune 강제삭제·timeout전파·DSN마스킹 findings로 보류, 다음 Claude 수정/Codex 재검토.

- [[2026-09-18_Workspace_아카이브무결성_Codex]]: VF-CX-05 오프라인 내부image archive SHA-256 검사 추가(합계8GiB한도),21시험통과·실제2archive일치. 인증서/정책만료로exit1 유지. OneDrive는 쓰기버스트상관으로 정정, 일정누수/NTSTATUS인과미확정. 다음Claude 독립검토/R2수정, Codex 수정수신 후검증.

- [[2026-09-18_모델레지스트리_명시결속_Codex]]: VF-CX-02 명시registryVersionId↔manifest/content/policy 불변결속·현재권한·동시성구현,0044head,격리PG100/100통과. 실행permit연결/원격provider는후속,운영DB미변경. 사용자image XML3pass/host-init3/timeout2 직접확인, business-kernel-role미검증유지. Claude07bae29는과도한NTSTATUS재시도/R2-03미해소로보류.

- 최신 image 독립판별(XML/JSON직접확인): 동일digest3회4/3/2pass,최신2pass/6fail(host-init5/timeout1). 재시도구제실패·전체반복인수미확보, 제품보안단언실패관측0≠미통과합격. business-kernel-role미검증유지. 환경조치후조용한조건까지image추가실행중단;모델결속100PG시험은이미완료. [[2026-09-18_모델레지스트리_명시결속_Codex]].

- 원격 읽기 기반: NodeTransfer의 bounded chunk 검증 공통화·공격 입력 회귀 포함 오프라인79통과, Docker/실장비 실행 없음. 사용자 독립 5b783d2 전체비integration1081/489/0·DSN exit2 유지 수신. provider/runtime 결속·PG통합재실행/Claude검토 미완료. [[2026-09-18_모델레지스트리_명시결속_Codex]].

- 원격모델 bounded reader 구현·합성loopback mTLS 포함106시험통과, runtime/DB현재권한 연결은 후속. Claude b5f770a 재검토: R2-01/사전prune R2-02 수정인정, R3-01 오분류·변경명령재시도/R2-03 libpq미마스킹 재현으로전체착지보류. [[2026-09-18_원격모델읽기_Codex]].

- **미착지 후보** 원격권한 snapshot 전후검사·frozen source hash·승인/delivery/claim 결속 구현.132passed/29PGskip(DSN없음), 실제DB검증전 integration5e4d6ae유지. Claude71fc9f5 timeout개선인정/R3-01·R2-03·OSError잔여로보류. [[2026-09-18_원격모델권한결속_Codex]].

- **DB검증 보류해제**: 사용자disposablePG16에서파일별79passed/0failed/0skip(원격6/runtime23/registry16/locality26/retry8). 최초3fixture CAS오류수정,제품코드변경0.8c347b7권한결속을integration반영판정. 기존489skip전체검증아님/image중단유지. Claude56aa7cb의기존재현해소인정, b809fbe timeout타입불일치R4-01은mock재현/전체브랜치보류. [[2026-09-18_원격모델권한결속_Codex]].

- 사용자e89a415 독립4파일/실PG75passed·0failed수신,착지판정유지. Claude816346c 오프라인15passed/실Docker2제외,R4-01해소. cleanup skip이합성본문실패를1skip/exit0로덮는R5-01(P1), 격리소스docker_diag누락R5-02(P2) 재현으로전체착지보류. [[2026-09-18_Claude816346c_재검토_Codex]].

- 후속사용자 clean816346c 두파일17passed/0failed(실Docker포함)수신,R4-01해소확인. 새R5-01/02는해당시험밖의경계로보류유지. Claude e89a415/8c347b7 소스검토배정수신·결과대기.

- VF-CX-02/03 registry 트랜잭션 재검사 구현(base f2297ae), 실제PG 신규7+기존16=23passed/0skip/0failed. 기존 결속만 허용하고 호출자 잠금 유지. frozen workload/승인 연결은 다음 Codex, 독립검토는 Claude 대기. [[2026-09-18_Registry_트랜잭션재검사_Codex]].

- VF-CX-02/03 registry frozen workload·승인/dispatch/delivery/claim 결속 구현(base1b39d40), 실제PG84+오프라인60=144passed/0failed/0skip. policy 제거·변경/retired 거부. 독립검토 Claude 대기, 운영 정책 구성 연결은 후속. 사용자 helper 독립23통과 수신, 외부대기4건 유지. [[2026-09-18_Registry_실행권한결속_Codex]].

- Claude d59b8a6 전체 착지 보류해제: 사용자독립24/작성자24, Codex오프라인22+실Docker2제외와 격리import통과(미합산). R2~R5 blocker 해소. image미검증6/business-kernel-role 미검증은 운영인수 항목으로 유지. e2908a5 registry 실행결속은 별도 독립검토대기. [[2026-09-18_Registry_실행권한결속_Codex]].

- 기본 Docker 의존 분리: 실제prune2건만 docker_host로 기본 deselect, mock22건 유지/Core CI 명시lane 추가. 사용자1157/489/0·62초 수신, 작성자 비integration1179passed/489skip/2deselected/0failed,66.23초. 기존 Compose2건 CLI부재가드 보강, 실제CLI2통과. e2908a5 registry 독립검토는 다음 Claude. [[2026-09-18_Docker_시험선택경계_Codex]].

- 사용자1eaf285 독립1179/489skip/2deselected/0failed(75초), Claude c9e6ddf의 e2908a5 sound 소스검토/부재22pass2skip 수신. registry 운영자 설정 연결 구현, offline34+실PG12=46passed. 최초 관측용DSN application_name 차이12setup오류는 원본DSN재실행으로 분리. 이번 설정 변경 독립검토 Claude 대기. [[2026-09-18_Registry_운영정책설정_Codex]].

- 호스트조치후 사용자image4pass/2fail/2skip(exit1),XML/JSON직접확인. host-init0회/OneDrive인과미확정,잔여timeout4건·business-kernel-role미검증유지. [[2026-09-18_IMAGE_OneDrive재시작후판별_Codex]].

## PITR 준비안 검토 수신

[[2026-09-18_PITR_준비안_검토_Codex]]: Claude 8c72fbf 검토 결과 R1-01~04 수정 전 착지 보류. 논리 복원을 PITR로 간주한 판정, same-host MinIO의 off-host 보장, archive 재시도·용량 설명을 수정해야 한다. 따라서 남은 사항이 모두 외부 조치인 것은 아니다. 다음 내부 담당 Claude: 준비안/격리 PITR 증거 보강; Codex: 수정본 재검토. Docker 실행/운영 적용 없음. 사용자 image 방법론 정정 수신, 동일 lane 반복 없음.

### PITR 독립 확인 수신·수정본 대기

사용자가 R1-01~03 소스 근거를 독립 확인했고 Claude 수정 중이라고 보고했다. 원격 1ff4324는 image 지도만 갱신, PITR 수정본 미도착. [[2026-09-18_PITR_준비안_검토_Codex]]에 수신·대기 조건 기록. 다음 Codex: R1-01~04 수정본/격리 증거 재검토. 현재 새로 착수할 Codex ready 작업 없음; 동일 lane 반복 없이 대기.

## 검증 경계 표본 감사

[[2026-09-18_검증경계_표본감사_Codex]]: tools51/tests179파일 패턴 검색, 합성 CLI9관측으로 deployment_surface의 factory 오류/attr 부재 성공 처리와 위조 Bearer probe 예외 누락 P2 두 건 확인. 빈 route 입력은 P3 보강 후보. 기존 offline73시험 통과가 이 미검증 경로를 대체하지 않음. 다음 Codex: VB-AUDIT-01/02 수정; Claude: 독립 검토/PITR 수정본. 이번 감사는 미수정 finding이며 Docker/DB 실행 없음.

## 검증 경계 finding 수정

[[2026-09-18_검증경계_오류분류수정_Codex]]: VB-AUDIT-01/02 로컬 수정·offline88시험 통과, Claude 독립검토 대기. factory 인자 결속과 본문 오류 분리, 위조 Bearer 검사 미완료 nonzero, 빈 route 입력 P3는 exit2 미판정으로 보강. 과거 감사의 미수정 표기는 당시 상태. 운영인수/CI·실장비 미완 상태 유지.

### 검증 도구 수정 독립 확인 수신

[[2026-09-18_검증경계_오류분류수정_Codex]]: 사용자 bb4f4cb VB-AUDIT-01 세 경로 독립 재현 수신. VB-AUDIT-02/P3는 작성자88시험 범위이며 사용자/Claude 독립검토 미수신. Claude tip1ff4324로 PITR 수정본 대기. 다음 Codex: 수정본 재검토; 동일 image lane 반복 없음.

## 검증 감사 잔여 범위와 독립 재현 수신

[[2026-09-18_검증경계_후속감사범위_Codex]]: VB-AUDIT-02 사용자 clean worktree 두 앱(RuntimeError→exit1,실제401→exit0) 독립 재현 수신. VB01/02는 작성자 시험+사용자 명시 경로 독립 실행 확인, Claude 소스검토 대기; P3는 사용자 판단 동의. 미감사 영역6종과 다음 실패 대조군 정리. 우선순위1 Python검색 밖 .mjs 인수 스크립트,2 실행/증거/CI 집계 경계. 새 finding/추가 운영실행 없음.

## MJS 빈집합 단언 수정

[[2026-09-18_MJS_빈집합단언_감사수정_Codex]]: shard2단언에 배열/고정2개 가드,실제문장 offline Node12시험 통과. .mjs4파일 every5곳 중 나머지3곳 개수가드 확인. 추가 VB-MJS-02 UI const true PASS3건(P2) 미수정, Gemini 실제 UI 검증 보강/Codex review 인계. 과거 full smoke 실입력은 미확인. Claude PITR a681da3 도착, 다음 재검토 대상.

## PITR a681da3 재검토

[[2026-09-18_PITR_a681da3_재검토_Codex]]: 물리복원 경로/MinIO 정정 확인, Claude 성공텍스트 수신(사용자·Codex 실PG 재실행 없음). R2-01 after INSERT 실패 후 PASS 합성재현(P1), R2-02 변경명령 무차별재시도/소유권없는cleanup(P2), 기존R1-04 설명미해소로 전체착지 보류. 다음 Claude 수정/증거, Codex 재검토. shard9251f18 수정은 별개 착지.

## MJS 나머지3도구 감사

[[2026-09-18_MJS_후속3도구_감사_Codex]]: 사용자 shard가드12회귀 독립통과 수신. 합성 fetch로2PC67/67·reconcile59/59 exit0(unrelated receipt/invalid digest), 물리정지false 대조군은각1failed/exit1. VB-MJS-03 실행결속/실장비주장P1,04 로컬상수hash를snapshot검증으로표시P2,05 실패시verified문구P2 미수정 인계. handoff재현도구는 scope명시된 관측JSON이며 이번소스검토에서 추가finding없음. 다음 Gemini수정/Codex검토, 실제장비실행0.

## MJS 수치 보증 범위 정정

[[2026-09-18_MJS_수치인용_정정_Codex]]: 지도v1.2.0에67/200·202/59 checks의 응답조건·로컬계산 범위와 실제장비/화면 미보증을 명시. 공통판202/202 문구 직접정정, VF01~05 인수조건에 실제bytes/identity/장비·UI 관측 요건 보강. 실제browser6은 별도범위 유지. 운영0/5 변경없음. PITR2468912 보류사유와 다음owner 명시.

## PITR09db057 gate 재검토

[[2026-09-18_PITR_09db057_재검토_Codex]]: 사용자 실제PG 정상0/after-insert음성1 수신+소스대조로R2-01해소. Codex는PG재실행없음. archive명령 순차4조건 정상,다른writer게시 interleaving은덮어쓰기 관측. 고정tmp논거는전용아카이브·단일writer 한정이며전역직렬성아님. R2-02/R1-04미해소로전체착지보류,다음Claude수정/Codex재검토.

## 실행증거 집계 감사·최신 관측

[[2026-09-18_실행증거집계_감사_Codex]]: VF staleXML의과거PASS귀속/불완전증거exit0 두P2 확인. 실제runner외부경계stub·원문CI gate로대조,실제CI/Docker0. Gemini API79/64는보고수신+소스일부확인,독립전체해소미선언. 사용자handle119773/RAM1665MB수신,실행직전handle관측조건강화. 다음Codex집계수정/Claude리뷰.

## 실행 증거 집계 수정과 검토 인계

[[2026-09-18_실행증거집계_오류수정_Codex]]: VB-AGG-01/02 고유 run namespace·subprocessExitCode/evidenceStatus 분리·빈/미생성/깨진 XML nonzero·collect-only 거부 구현. offline43passed/2 docker_host 제외, 실제Docker/PG/CI0. 사용자 기존4prefix 비오염 및 coord-business-retry 증거부재 exit1 수신(거짓성공 아님). 다음 Claude 독립검토; 필수suite/SHA provenance 전체보강·CI·운영인수는 별도 미완.

## Fixture 표본 감사 및 집계수정 독립 실행 수신

[[2026-09-18_Fixture_검증경계_감사_Codex]]: VB-AGG 사용자 collect-only exit2/고유디렉터리 독립실행 수신, Claude 검토대기. 실제fixture 합성경계11관측으로 VB-FIX-01 setup 부분할당 DB잔재(P2), VB-FIX-02 dispose실패시 role정리 생략(P3) 확인·미수정. 성공 위장은 아님. credential action별 grant회수는 기존명시계약이므로 오탐 제외. offline47passed/실DB3skip, Docker/PG/CI0. 다음Claude fixture수정/AGG검토, Codex재검토.

## Launcher와 운영 증거 경계 감사

[[2026-09-18_Launcher와운영증거_경계감사_Codex]]: 원본PS1+native대역5조건에서 인증서생성exit23→전체exit0/All Exit Codes 0 재현, VB-LAUNCH-01 P2 미수정/Gemini owner·Codex reviewer. 운영증거5관측: LAN실패exit2에이전JSON보존 위험후보, 독립복원기존output거부/빈storage false 정상. 기존시험5passed/DB30skip, 실제배포/Docker/PG0. Claude PITR/fixture구현 중복없음. 다음 수정본검토·잔여후보대조, 운영0/5유지.

## 감사1~5 종합과 누락 없는 상태표

[[검증 경계 감사 종합과 잔여 범위]]이 감사 결과별 owner·수정·검증·잔여 검토 정본이다. 후속8건/초기AUDIT포함10건/MJS01·02포함12개 ID의 분모를 구분. 요청10건 중 사용자 수정·독립검증 확인7/미수정3, 별도MJS01수정·독립실행/MJS02 UI상수 미수정. MJS03~05 최신사용자확인 수신과 Codex계약리뷰잔여는 분리. 우선순위4확정finding추가0, 위험후보보존; 1~5첫표본정리·전수완료아님·6 frontend미착수. 제품/운영 재실행0, 운영0/5유지.

## Fixture 수정본 재검토·registry policy 검토 수신

[[2026-09-18_Fixture_a5401a7_재검토_Codex]]: a5401a7 9시험 Codex독립통과(합성DB/engine,실PG0); 사용자의 DSN설정9통과도 실제PG시험은 아님. FIX01 원래경로해소, FIX02 DROP시도해소/동시dispose+DROP 오류누락 P3잔여로전체수정종결보류·미병합. 다음Claude FIX02-R1수정/Codex재검토. 11e9f44는Claude7e3de2a sound 독립소스검토 수신으로대기해소(CI/운영별도). PITR0da140e도착·검토대기.

## Claude fixture 해소·독립커밋 착지 / PITR 잔여

[[2026-09-18_Claude_c754933_부분착지검토_Codex]]: c754933 새2건은DROP단독/양쪽실패, 원본11passed·통합32passed(합성,실PG0). FIX01/02해소, a5401a7/c754933/7e3de2a 원본커밋을0a65313/f7a46da/495df5c로반영. PITR원본함수에서cleanup조회실패은폐/제거실패후재시도 재현,PID label잔여로전체브랜치보류. 후속8건중코드미수정LAUNCH01만맞지만별도MJS02·review·PITR잔여는유지. 다음Claude PITR보강/Gemini LAUNCH01·MJS02/Codex재검토.

최신원격입력: Gemini84a86f1 VB-LAUNCH-01수정이integration에선행착지해정상병합. 후속8건은모두수정본존재로갱신하되LAUNCH01 Codex재검토/기존독립검토잔여별도. 신규launcher시험미실행. [[2026-09-18_Claude_c754933_부분착지검토_Codex]] 참조.

## Launcher scope 재검토·PITR hold 인계

[[2026-09-18_Launcher_scope와PITR_hold_Codex]]: 사용자66bbcf0 16passed수신(실PG증거아님). LAUNCH원래nativeexit누락해소, Docker SKIPPED/gateway Optional분리확인. 원본PS1합성2조건에서invalid-nonempty cert도TLS1.3 VERIFIED·고정202 E2E문구출력→Gemini scope잔여. npmbuild exit검사는수정전부터존재. PITR query/remove실패·PIDlabel잔여와nonce/cleanup상태/재시도게이트/음성대조조건명시. git cherry로fixture2커밋·registry검토 patch동등착지확인,2ed3d6은상태문서추가만/PITR코드변경없음.

## Claude 수정대기 정정 / 사용자 방법론 정정 수신

[[2026-09-18_Claude_대기상태정정과수정인계_Codex]]:2ed3d65는문서1파일뿐/PITR코드변경0. FIX02재검토·내용착지완료, PITR은Codexreview대기가아니라Claude cleanup잔여수정대기. 내용차이8/동등patch3 대조,image workspace skip누락·실PG11오표기·1179실행주체등상태문서정정인계. bb4f4cb Claude sound 문서수신(독립소스검토,런타임별도). 사용자의npm검사시점/16시험합성범위정정수용수신. 동일주입/운영시험반복없음.

## 에이전트 계약결속 범위 명시 (2026-09-21, Claude 기록 — 겹침 방지)

응답 계약결속(공유 fixture + Python `validate_contract` 시험 + 양방향 Ajv, 프런트 배선은 Gemini) 담당 경계:
- **Claude**: node·shard·workspace 의 View/Result 응답. 완료: RunResultView·RunArtifactList(run-result/artifacts), RunLogView(run-logs), RunAttemptList(run-attempts), ModelCommitObservation, **ShardObservation**(VF-GM-04 관측/합성 구분 기준). 다음: node·workspace View/Result 중 앵커·미결속·소비O.
- **Codex**: terminal 응답(PTY 티켓=보안 경계, VF-GM-05 차단 해소). ProblemDetails 결속(백엔드 에러경로 계약검증 동반 필요). ShardObservation `_status` 앵커 추가(shards.py=복구·동시성 레인). 앵커/보안/분산복구 경계.
- **Gemini**: 프런트 수기 타입(types.ts) → 생성 타입(`packages/contracts-ts`) re-export 전환(드리프트 근원 제거), 소비O 타입 정합 및 프런트 Ajv 시험.

상세: [[2026-09-21_이어가기_상태와규칙_Claude]] · [[2026-09-21_수기타입_계약_드리프트_전수훑기_Claude]] · [[2026-09-21_shard-observation_계약결속_Claude]]
