---
doc_id: "GOV-VERIFICATION-CHECK-CATALOG-001"
title: "검증 검사 도구 목록 — 무엇을·등급(게이트/ratchet/report-only)·어떻게 실행·실패 시 대응"
version: "1.4.0"
status: "active"
author: "Claude"
reviewer: "Codex"
updated: "2026-09-22"
source_of_truth: "Git"
tags: ["governance", "ci", "checks", "catalog", "onboarding"]
---

# 검증 검사 도구 목록

새로 온 사람이 **여기 하나만** 보면 저장소의 검증 도구가 무엇이고 어떻게 돌리고 실패하면 뭘 하는지 안다. (AGENTS.md `## 검증`에서 이 문서를 가리킨다.) 등급 기준은 [[검사_게이트승격_기준과_5검사_분류_Claude]]: 백로그0·오탐낮음·대응명확 셋 다면 게이트, 아니면 ratchet/report-only.

## 문서·온톨로지 (기존)
| 도구 | 무엇 | 등급 | 실행 | 실패 시 |
|---|---|---|---|---|
| `tools/check_docs.py` | 원본해시·비밀DSN·위키링크·프런트매터·task-registry 무결성 | 게이트 | `python tools/check_docs.py` | 메시지가 어느 문서/필드/링크인지 지목 — 그것을 고침 |
| `tools/check_ontology.py` | RDF/SHACL·task 매핑(48행 registry 유도)·per-task realizes 집합 | 게이트 | `python tools/check_ontology.py`(requirements-docs 환경) | 어느 query/task가 불일치인지 지목 |
| `tools/sync_obsidian.py` | vault↔공유 미러 동기 | 확인 후 `--apply` | `--check` 후 권한 내 `--apply` | pending/conflict 목록 확인 |

## 계약·화면·신선도 (오늘 밤 신규 5)
| 도구 | 무엇 | 등급 | 실행 | 실패 시 |
|---|---|---|---|---|
| `tools/check_contract_bindings.py` | ①fixture-무-시험 ②bound 커널응답-무-서빙앵커시험 (③dead-contract=내부 report-only) | **게이트**(①②) | `python tools/check_contract_bindings.py`(docs.yml) | "fixture X에 시험 없음"→그 fixture 참조 시험 추가; "응답 X 서빙앵커 시험 없음"→X를 명명하고 `inv.<module>`를 import하는 시험 추가 |
| `tools/check_anchor_weight.py` | 서빙 앵커의 **거부-커버리지 tier**(rejection-tested/called-only/no-serving-test) 정적 분류 — check_contract_bindings(존재)의 보완(시험이 거부를 검증하나) | **report-only** | `python tools/check_anchor_weight.py`(opt-in `--strict`); 자기시험 `pytest tests/test_check_anchor_weight.py` | called-only=서빙 거부 시험 추가 또는 계약시험이 덮는지 확인; no-serving-test=사각지대. 런타임 무게 확증은 변이 절차([[앵커_무게_검증_절차]]) |
| `tools/check_frontend_integrity.py` | 프런트 무결성(가짜 id·조기성공·tri-state 등) | **게이트** | `python tools/check_frontend_integrity.py`(docs.yml) | 위반 파일·지문 목록 → 해당 코드 정정(Gemini 소관) |
| `tests/core/test_serving_anchors.py` | 커널 응답의 서빙 함수가 실제로 validate_contract 호출(앵커 무게) | **게이트**(pytest 스위트) | backend/core pytest가 수집 | 어느 응답 앵커가 안 불렸는지 → 서빙 경로에 앵커 복구 |
| `tools/check_doc_single_source.py` | 살아있는 문서 간 실질 라인 중복(rule5 분기면) | **ratchet**(`--ratchet`) / 기본 report-only | 게이트: `python tools/check_doc_single_source.py --ratchet`; 가시화: 기본 실행→`$GITHUB_STEP_SUMMARY` | 새 doc-pair→통합 or `tools/baselines/doc_single_source_pairs.txt`에 명명 줄 추가; stale→그 줄 삭제(하향). [[ratchet_적용_doc_single_source_Claude]] |
| `tools/check_response_freshness.py` | 신선도-중요 응답이 관측시각 필드를 담는가(현재 9검사 중 map은 Codex `f1d95466`이 durable run time 노출로 확장 중, 일부 스키마 노출 대기) | **report-only**(ratchet 후보) | `python tools/check_response_freshness.py`→`$GITHUB_STEP_SUMMARY` | MISSING=스키마+서빙에 시각필드 추가. (현 MISSING 메시지는 영향만 말하고 방법은 약함 → Codex 소관 파일이라 개선 권고) map 큐레이션은 사람. [[2026-09-22_시간축_자동화_가능한것과_사람몫_Claude]] |
| `tools/check_fixture_reachability.py` | fixture를 이름으로 참조하는 시험이 실제 HTTP/runtime producer를 비순환 방식으로 호출하는지 정적 분류(runtime/circular/schema-only/unreferenced). 값·조합의 실제 도달 가능성을 증명하지 않는 초기 proxy | **report-only**(`--strict`는 실험용) | `python tools/check_fixture_reachability.py`(docs.yml summary); 자기시험 `pytest tests/test_check_fixture_reachability.py` | circular/schema-only는 실 producer를 통과한 fixture witness 추가; unreferenced는 계약시험 추가. backlog·오탐 0 전에는 게이트 승격 금지 |
| `npx tsc -b` / `npm run build` | 프런트엔드 TypeScript 정적 타입 무결성 및 프로덕션 번들 빌드 검증 (Vitest의 타입 strip 한계 보완) | **게이트** | `cd apps/web && npx tsc -b && npm run build` | 타입 에러(TS2304 등) 및 번들링 실패 정정 (Gemini 소관) |
| `pytest tests/test_route_coverage.py` | 프런트엔드-백엔드 간 라우트 커버리지 및 EvidenceViewer 무결성 불변식(허위 PASS 차단·verified 게이트 등) 검증 | **게이트**(pytest 스위트) | `pytest tests/test_route_coverage.py` | 미서빙 라우트 정합 또는 EvidenceViewer 무결성 가드 위반 정정 (Gemini 소관) |
| `tools/route_coverage.py` (CLI) | SPA가 부르는 `/v1` 경로 집합 ↔ 서빙 트리(`src`·`services/control-plane/src`)의 라우트 선언 집합 대조, unserved 목록. 정적 모양만(메서드·인증·payload·live 미검증). 템플릿 hole 안 함수 호출 사각은 2026-09-22 수정([[2026-09-22_route_coverage_템플릿hole_사각_수정_Claude]]) | **report-only**(2026-09-22 배선; exit 기록·job 미실패) — **승격 조건**: docs.yml에서 **1주 연속 exit 0** ∧ unserved 백로그 0 ∧ 오탐(인공물 경로) 0이면 **ratchet**(baseline=unserved 이름 집합, 새 unserved만 실패) → 그 뒤 게이트 | docs.yml step `Report client route coverage` → `$GITHUB_STEP_SUMMARY` + artifact `route-coverage-report`(json/txt); 로컬 `python tools/route_coverage.py --served src --served services/control-plane/src --client apps/web/src` | unserved=그 경로를 서빙하는 라우트 카드 착수(백엔드 레인) 또는 화면이 죽은 경로를 지움(Gemini); exit 2(클라이언트 경로 0)=스캔 입력 오류 |
| `tools/run_real_browser_acceptance.py` | 실제 Uvicorn 0.52.4 백엔드 ↔ Vite 프록시 ↔ 실제 Google Chrome (Blink) 종단간 WebCrypto 무결성 및 산출물 다운로드 실측 | **온디맨드 실측**(수동/마일스톤) | `.venv\Scripts\python.exe tools/run_real_browser_acceptance.py` (Vite 기동 후) | [[실제_브라우저_백엔드_종단간_실측_운영_가이드]] 참조 (Gotchas 확인) |

- **report-only 가시성**: report-only는 로그에 묻히면 무시되므로 CI에서 `$GITHUB_STEP_SUMMARY`로 뽑는다([[검사도구_CI배선과_리포트가시성_Claude]]). 미배선 2개(doc_single_source·response_freshness) 배선 문안은 그 문서에.
- **배선 현황**(2026-09-22): check_docs/ontology/contract_bindings/frontend_integrity=docs.yml 게이트, fixture_reachability=docs.yml report-only summary, **route_coverage CLI=docs.yml report-only summary+artifact(2026-09-22, PR agent/claude/route-coverage-docs-gate)**, serving_anchors=pytest 스위트. doc_single_source·response_freshness=배선 대기(문안 Codex 인계, YAML 소관 Codex).

## 왜·판단 근거
검증 규칙·축(무엇을 왜 검사하나)은 [[검증규칙과_세축_canon]]. 게이트 승격 기준은 [[검사_게이트승격_기준과_5검사_분류_Claude]]. 자동/판단 분리는 [[계약검증_자동화대판단_검사목록]].
