# Backend Build dependencies.
#
# Composed from requirements-test.txt rather than restating it. Two lists for
# one runtime is what broke the build after the integration merge: Codex's
# tests arrived needing jsonschema, cryptography and PyJWT, this file did not
# have them, and the failure looked like a test bug rather than a packaging
# one. A dependency added for the core tests now reaches this build by
# construction.
-r requirements-test.txt

# Pinned separately because FastAPI's floor is looser than the version this
# suite is exercised against.
starlette==1.6.0
